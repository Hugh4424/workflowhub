# 审查报告 — wh-review-rebuild-r6-20260705T235019Z-7112ed (round 25)

- verdict: revise_required
- provenance: single-context

## Summary

先统一 3rd-review 的唯一调用接口，再补齐 D2 人工确认门的状态机与可验证产物，其余措辞和校验规则随后收口。

## Findings

- [blocking] 问题: 3rd-review 调用契约前后冲突 | 建议: 文档对 3rd-review 的输入定义不一致。§2 目标、§6 数据流写的是 `{mode, contract, materials}`，但 FR-THIRDREVIEW-001 方案A又要求 wh-review 先把合同和材料装配成一份“纯文本审查包”，且 3rd-review 不接收 stage/合同路由信息。实现方无法判断引擎到底接收三元结构，还是单一装配包；验收方也无法据此设计稳定测试。需要统一为一个权威接口，并同步修正所有章节和 AC。
- [blocking] 问题: D2 人工确认门缺少可执行合同 | 建议: FR-D2-001 只规定了“触发人工确认流程、挂起等待”，但没有定义具体状态机、落盘字段、等待对象、恢复命令或通过/拒绝后的状态迁移。当前 AC8-1/AC8-2 只能口头描述“停住/自动推进”，无法机器验证，也无法保证 5 个 stage 实现一致。需要补齐最小可执行协议，例如确认状态值、持久化路径、批准信号来源、恢复入口和失败处理。
- [minor] 问题: 报告结构来源表述自相矛盾 | 建议: FR-WHREVIEW-004 已把 6 章名称、顺序、语义定死，但 Known Gaps 又写“6章结构名称未在 decision-log 中明确列出，build-plan 阶段需在 agenthub 原实现中核实”。这会造成后续实现时到底以当前 spec 还是以 agenthub 原实现为准的不一致。应明确当前 spec 已是最终权威，或把待核实部分改成仅允许调整副标题文案。
- [minor] 问题: §7 机器校验规则过窄 | 建议: FR-THIRDREVIEW-002 现在主要禁 `^\s*\d+\.` 和 `if/else`，但 `-` 列表、`first/then`、中文条件句等仍可携带流程逻辑，容易形式满足、语义不满足。若该条是强约束，建议把禁止模式扩展到所有枚举式步骤和显式条件分支措辞，或改为人工审查规则。

## Checks

审查维度覆盖：方向、盲点、细节
- 维度[方向]：已覆盖
- 维度[盲点]：已覆盖
- 维度[细节]：已覆盖

## Required Revisions

降级理由：(未提供，需补充)
- 必须修复：3rd-review 调用契约前后冲突
- 必须修复：D2 人工确认门缺少可执行合同

