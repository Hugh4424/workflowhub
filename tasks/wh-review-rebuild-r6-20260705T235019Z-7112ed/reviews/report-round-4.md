# 审查报告 — wh-review-rebuild-r6-20260705T235019Z-7112ed (round 4)

- verdict: revise_required
- provenance: single-context

## Summary

先补齐同源模式执行合同、统一落盘路径规范、定义 D2 人工确认状态机，再进入实现。

## Findings

- [blocking] 问题: 同源审查执行路径未定义，无法落地 FR-WHREVIEW-003 | 建议: 规格要求“异源最多3轮；第4轮起强制转同源”，并把 `same-source` 列为合法 `mode`，但全文只定义了 wh-review 调用 3rd-review 的纯引擎接口，未定义同源模式由谁执行、是否仍调用 3rd-review、调用参数/合同/产物格式是否相同。当前实现者无法据此完成同源切换与 AC3-1/AC3-4 的可验证实现。
- [blocking] 问题: 报告与状态落盘路径约定冲突 | 建议: 文档同时使用了“当前任务目录”“任务目录下固定子路径”“`<task-dir>/<task-id>/reviews/`”三种说法，但没有统一 `task-dir` 与 `task-id` 的来源、拼接规则、状态文件与报告文件的准确位置。这个路径约定贯穿 AC1-3、AC3-1、AC4-2、AC-D10，现状会直接导致实现和测试各自猜路径，验收不可判定。
- [blocking] 问题: D2 人工确认门缺少可执行的流程合同 | 建议: 规格要求 make-decision / build-plan / verify-code 在 `pass` 后“暂停、等待人工回应，不自动调用下一阶段”，但没有定义暂停状态写入格式、等待点所在文件/脚本、人工批准的输入形式、恢复推进的触发命令或状态迁移。AC8-1/AC8-2 要求做集成或端到端验证，但当前规格没有给出可实现的状态机合同，无法稳定验收。
- [minor] 问题: §7 机器校验规则过窄 | 建议: FR-THIRDREVIEW-002 只列了 `^\s*\d+\.` 和 `/\bif\b.*\belse\b/` 两类模式，不能覆盖单独的 `if`、项目符号步骤、中文条件表达等变体。若保留“机器可检验”目标，建议补成完整禁止模式集。

## Checks

审查维度覆盖：方向、盲点、细节
- 维度[方向]：已覆盖
- 维度[盲点]：已覆盖
- 维度[细节]：已覆盖

## Required Revisions

降级理由：(未提供，需补充)
- 必须修复：同源审查执行路径未定义，无法落地 FR-WHREVIEW-003
- 必须修复：报告与状态落盘路径约定冲突
- 必须修复：D2 人工确认门缺少可执行的流程合同

