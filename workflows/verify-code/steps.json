{
  "schema_version": "2.0.0",
  "stage_slug": "verify-code",
  "steps": [
    { "step_id": 1, "step_slug": "read-current-materials-and-code", "order": 1, "entry_conditions": [{ "kind": "portable_skill_package", "uri_or_path": "workflows/verify-code/SKILL.md" }, { "kind": "portable_skill_dependencies", "uri_or_path": "workflows/verify-code/skill-deps.yaml" }, { "kind": "decision_log", "uri_or_path": "decision-log.md" }, { "kind": "spec", "uri_or_path": "spec.md" }, { "kind": "plan", "uri_or_path": "plan.md" }, { "kind": "tasks", "uri_or_path": "tasks.md" }, { "kind": "changed_files", "uri_or_path": "working-tree" }], "completion_evidence": [{ "kind": "tasks", "uri_or_path": "tasks.md" }, { "kind": "changed_files", "uri_or_path": "working-tree" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "The current WorkflowHub session has read the portable package, four current materials, and real changes; history is background only.", "depends_on": [] },
    { "step_id": 2, "step_slug": "architect-acceptance-review", "order": 2, "entry_conditions": [{ "kind": "tasks", "uri_or_path": "step://1" }], "completion_evidence": [{ "kind": "verify", "uri_or_path": "quality/verify.json" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "主 Agent 完成一次短架构验收，覆盖原始需求、决策、流程、架构、实现、每项适用 AC 和风险。", "depends_on": [1] },
    { "step_id": 3, "step_slug": "main-agent-repair-batch-1", "order": 3, "entry_conditions": [{ "kind": "verify", "uri_or_path": "step://2" }], "completion_evidence": [{ "kind": "repair", "uri_or_path": "working-tree" }, { "kind": "tasks", "uri_or_path": "tasks.md" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "主 Agent 逐条判断并完成第一批必要修复；无效 finding 和延期项有理由。", "depends_on": [2] },
    { "step_id": 4, "step_slug": "run-declared-check-before-independent-review", "order": 4, "entry_conditions": [{ "kind": "repair", "uri_or_path": "step://3" }], "completion_evidence": [{ "kind": "test", "uri_or_path": "quality/tests/" }, { "kind": "tasks", "uri_or_path": "tasks.md" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "按 plan.md 和 tasks.md 的路线完成必要定向检查；命令、退出码、结果和限制如实记录。", "depends_on": [3] },
    { "step_id": 5, "step_slug": "run-one-independent-architecture-review", "order": 5, "entry_conditions": [{ "kind": "test", "uri_or_path": "step://4" }], "completion_evidence": [{ "kind": "review", "uri_or_path": "quality/reviews/" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "声明的异源审查依赖对当前验收事实和未决风险只做一次 independent findings 审查；不循环，unavailable 保持 unavailable。", "depends_on": [4] },
    { "step_id": 6, "step_slug": "main-agent-repair-batch-2", "order": 6, "entry_conditions": [{ "kind": "review", "uri_or_path": "step://5" }], "completion_evidence": [{ "kind": "repair", "uri_or_path": "working-tree" }, { "kind": "tasks", "uri_or_path": "tasks.md" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "主 Agent 逐条判断异源 finding 并完成最后一批必要修复；原始 finding 保留。", "depends_on": [5] },
    { "step_id": 7, "step_slug": "run-final-check-and-handoff", "order": 7, "entry_conditions": [{ "kind": "repair", "uri_or_path": "step://6" }], "completion_evidence": [{ "kind": "test", "uri_or_path": "quality/tests/" }, { "kind": "verify", "uri_or_path": "quality/verify.json" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "必要时只跑一次最终测试，并在 quality/verify.json 如实记录结论、证据缺口和剩余风险；不开启第三轮审查。", "depends_on": [6] },
    { "step_id": 8, "step_slug": "publish-verification-attempt", "order": 8, "entry_conditions": [{ "kind": "verify", "uri_or_path": "step://7" }], "completion_evidence": [{ "kind": "verify", "uri_or_path": "quality/verify.json" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "当前验收事实写入 quality/verify.json；质量不完整时保持 unknown 或 incomplete，不创建阶段尝试对象。", "depends_on": [7] },
    { "step_id": 9, "step_slug": "approve-verification", "order": 9, "entry_conditions": [{ "kind": "verify", "uri_or_path": "step://8" }], "completion_evidence": [{ "kind": "tasks", "uri_or_path": "tasks.md" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "The current WorkflowHub session first gives a plain-language handoff covering implementation, actual effect, tests, review conclusions, evidence gaps, risks, and next action; the user's actual reply is then appended as one labeled human-alignment fact to the current tasks.md final aggregate verification card's `执行事实` field without changing status or other completion fields. Do not infer confirmation; this handoff is not a work gate and does not authorize close.", "depends_on": [8] },
    { "step_id": 10, "step_slug": "stage-end-spec-analyze", "order": 10, "entry_conditions": [{ "kind": "verify", "uri_or_path": "step://9" }], "completion_evidence": [{ "kind": "verify", "uri_or_path": "quality/verify.json" }, { "kind": "quality_facts", "uri_or_path": "quality/evidence/" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "The existing spec-analyze profile checks the original requirement and all four current materials against code, tests, AC results, review, verification, delivery facts, and failure/recovery boundaries; valid gaps are repaired in this stage and a plain-language six-field summary is produced before handoff.", "depends_on": [9] },
    { "step_id": 11, "step_slug": "publish-verification-result", "order": 11, "entry_conditions": [{ "kind": "tasks", "uri_or_path": "step://10" }], "completion_evidence": [{ "kind": "verify", "uri_or_path": "quality/verify.json" }, { "kind": "tasks", "uri_or_path": "tasks.md" }, { "kind": "stage_outcome", "uri_or_path": "quality/evidence/stage-outcomes/verify-code/<sha256>.json" }], "observable_result": "The current WorkflowHub session sends one plain-language acceptance notice citing quality/verify.json, the final aggregate card, and the user's actual reply; this step writes no new fact and creates no state object, and close remains a separate operation.", "depends_on": [10] }
  ]
}
