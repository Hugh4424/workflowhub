Superseded on 2026-08-02 by `evidence/phase-8/mechanical-move.txt`.

This earlier audit correctly identified historical-map drift, but its proposed
remedy would have mixed later semantic changes into a path-only record.  The
replacement separates the frozen mechanical baseline from current focused
quality facts.

--- historical blocked audit ---

Phase 8 T052 mechanical-move reconciliation: BLOCKED
Date: 2026-08-02
Task: workflowhub-complexity-governance-v2 / T052
Candidate HEAD: b0bb020756ac6896deaef9973b23726fcae6224f
HEAD tree: b71be032df12164a5e5decf73bf4a13472a3551f
Current governance tree: 83eef8d30aeecb6c926ac5de3542d7215a079aa1a97f9f6738859da7425e7c6b
Current tracked tree: 5bb50135cb3e9810baef94a3287e6605756c2c8e409fbe458df958dde68f09fc

Scope
-----
This audit compares every `status=move` entry in
`docs/architecture/move-map.json` with the current candidate worktree. It
does not change the move map, delete files, restore files, or claim that the
mechanical-move gate passed.

Observed reconciliation
------------------------
- move entries: 112
- destination-only entries: 101
- source and destination missing: 6
- source and destination both present: 5
- destination-only hash mismatches: 10
- destination-only entries whose recorded destination hash still matches: 91

The ten destination-only mismatches are current destinations with source
paths already absent. Their bytes differ from the recorded map because the
candidate worktree contains later implementation changes (including review,
facade, freshness, publication, and runner-release changes). They need a new
map snapshot after the design owner confirms those changes are outside the
mechanical-move equivalence claim; updating hashes now would silently mix
path migration with behavior changes.

Retired-path entries (resolved by path-specific proofs)
--------------------------------------------------------
The following six map entries have neither source nor destination in the
current worktree. Their absence is now covered by the path-specific RED/GREEN
proofs in `retired-paths.red.txt` and `retired-paths.green.txt`:

- `core/schemas/bootstrap-review.v1.json` → `runtime/schemas/bootstrap-review.v1.json`
- `core/schemas/runner-replacement-bootstrap-provider-config.v1.json` → `runtime/schemas/runner-replacement-bootstrap-provider-config.v1.json`
- `core/schemas/runner-replacement-bootstrap-sealed-bundle.v1.json` → `runtime/schemas/runner-replacement-bootstrap-sealed-bundle.v1.json`
- `core/schemas/runner-replacement-bootstrap-test-receipt.v1.json` → `runtime/schemas/runner-replacement-bootstrap-test-receipt.v1.json`
- `core/schemas/runner-replacement-path-coverage-map.v1.json` → `runtime/schemas/runner-replacement-path-coverage-map.v1.json`
- `scripts/task-migrate-runner-root.mjs` → `tools/cli/task-migrate-runner-root.mjs`

The GREEN proof scans 489 delivery files and finds no path or forbidden
legacy reference. The temporary RED proof reintroduces all twelve paths and
legacy identifiers and fails as expected. The separate legacy-pinned
bootstrap RED/GREEN proof (`task-bootstrap.test.mjs`, 1/2 then 3/3) confirms
historical loading remains read-only while fresh invocation identity is
required. These six entries are therefore no longer the blocking condition;
their final map disposition remains part of the pending move-map update.

Both-present entries (shim contract proven)
-------------------------------------------
These five pairs remain present at both paths:

- `skills/wh-review/scripts/integration-review-subject.mjs` → `runtime/review/integration-review-subject.mjs`
- `skills/wh-review/scripts/phase-review-subject.mjs` → `runtime/review/phase-review-subject.mjs`
- `skills/wh-review/scripts/review-controller.mjs` → `runtime/review/review-controller.mjs`
- `skills/wh-review/scripts/review-output.mjs` → `runtime/review/review-output.mjs`
- `skills/wh-review/scripts/schema-validator.mjs` → `runtime/review/schema-validator.mjs`

The current source files are exact one-line compatibility shims while runtime
files contain the implementation. The path-specific RED/GREEN proof in
`shim-keep.red.txt` and `shim-keep.green.txt` checks all five pairs, unique
runtime ownership, and current consumers; the existing review-layering gate
also passes (2/2). The temporary RED mutation fails on a non-re-export, so
the guard is live. These five pairs are no longer the blocking condition;
their shim disposition still needs to be represented in the final map.

Decision
--------
T052 remains incomplete. Do not update `move-map.json` or generate
`evidence/phase-8/mechanical-move.txt` from this worktree. The remaining
blockers are the ten real destination hash mismatches and the pending
move-map disposition/recomputation. The six retired paths and five shim
pairs already have focused RED/GREEN proofs; they must be carried into the
same final map decision rather than re-tested as unresolved absences.

No source files, move-map entries, or WorkflowHub main-repository files were
modified by this audit. No full test suite, phase-result, or build-code
acceptance was run or generated.
