Phase 8 T052 mechanical-move reconciliation: PASS
Date: 2026-08-02
Task: workflowhub-complexity-governance-v2 / T052

Rule
----
`docs/architecture/move-map.json` is a historical migration record.  It proves
the path-only move at its frozen migration snapshot; it is not a current-tree
execution permit and its `sha256_after` values must not be rewritten after a
later semantic change.  Current semantic changes are verified by their
own focused tests.  `tests/contract/repository-governance.test.mjs` enforces
that inventory does not consume this map as a current-hash permit.

Mechanical baseline
-------------------
- commit: `e31491792413e7046a443ff001ae009e72450496`
- tree: `9eb01a9a22b7272bc771d55ef947fd73f7c7fd90`
- map blob before this correction: `968a509519e0f9f7c76cd1e1825ac9d4afd598a9`
- `status=move` entries: 101
- source paths present: 0
- destination paths missing: 0
- exact destination hashes: 100

The only baseline discrepancy was the recorded `sha256_after` for
`core/git-worktree-snapshot.mjs -> runtime/task/git-worktree-snapshot.mjs`.
The map had copied the source hash `a7583f...`; the archived destination is
`08bc1e9f982ef93e90e4a6aacbb61981bc033ac2e9c6820d19b546031a4a5f7a`.
This correction changes only that historical typo.  It does not absorb any
later behavior change into the move record.

Current-path reconciliation
---------------------------
- 91 destination-only records still match their historical destination hash.
- 6 removed runner-replacement paths are proven retired by
  `retired-paths.red.txt` and `retired-paths.green.txt`; their old sources and
  destinations have no current production reference.
- 5 both-present review paths are intentional one-line compatibility shims;
  `shim-keep.red.txt` and `shim-keep.green.txt` prove one runtime owner and
  live re-export guards.
- 14 destination-only paths changed after the mechanical move.  They remain
  post-move semantic changes, not move-map updates:
  `audit-summary-carrier`, `boundary-confirm`, `canonical-review-result`,
  `invocation-identity`, `review-flow-authority`, `task-accepted schema`,
  `task-attempt schema`, `check-stage-quality`, `check-task-record-paths`,
  `task-bootstrap`, `runtime-facade`, `runner-release`, `freshness`, and
  `publication`.

Focused verification
--------------------
The 10 runtime/CLI changes passed four focused groups: 18 files / 120 tests.
The remaining schema/CLI changes passed:
- `tests/task-accepted-schema.test.mjs`: 5/5
- `core/__tests__/task-kernel-publish.test.mjs`: 5/5
- `tests/stage-quality.test.mjs` plus `check-stage-quality --self-test`: 7/7
  and self-test pass
- `scripts/__tests__/task-bootstrap.test.mjs`: 3/3

The relevant task-close cases passed by name (3/3).  The unrelated full
`task-close-delivery.test.mjs` file did not finish within the focused
diagnostic window and is deliberately not represented as a pass; final full
execution belongs only to Phase 9 T054/T056.

Structural guards passed: move-map JSON/path check (201 total entries, 112
move entries), `check-task-record-paths`, inventory (`1033` delivery files,
zero legacy runtime), and `verify-structure`.

Conclusion
----------
The mechanical migration is reconciled without treating historical hashes,
deleted paths, or compatibility shims as a license that can block ordinary
task progression.  No file was restored and no production behavior was
changed for this reconciliation.  The single required complete-suite run is
deferred to Phase 9, where the plan already requires it.
