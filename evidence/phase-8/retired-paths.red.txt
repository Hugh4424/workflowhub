Phase 8 T052 path-specific RED proof: retired paths and legacy bootstrap
Date: 2026-08-02
Candidate HEAD: b0bb020756ac6896deaef9973b23726fcae6224f
Move-map SHA-256: b7770e0ce5a8c3110dd70ea38561f8c961c84fa5a26461de5a80d526c0f1b610

Purpose
-------
This is a temporary mutation proof for the six source/destination pairs that
the existing mechanical-move reconciliation marked BOTH-MISSING. It does not
change the candidate worktree, move-map, deletion disposition, or task status.

Mutation
--------
In a temporary copy only, the five historical runner-replacement schema files
were restored at both their old `core/schemas/` paths and their destination
`runtime/schemas/` paths. The historical `task-migrate-runner-root.mjs` CLI was
restored at both `scripts/` and `tools/cli/`. The payloads came from the
candidate HEAD objects. No candidate source or formal test file was edited.

Focused retired-path gate
--------------------------
The temporary static gate scans current delivery roots (`core`, `runtime`,
`scripts`, `tools`, `skills`, `workflows`, `tests`, and `package.json`) and
requires all six pairs to be absent, with zero references to the five old
schema IDs, `task-migrate-runner-root.mjs`, or `migrateTaskRunnerRoot`.

Command: temporary Node path-specific gate (no full test suite)
Result: exit 1, as expected.

Observed failure:
- all 12 old/source-and-destination paths were present;
- all five old schema IDs were found in their restored schema files;
- `migrateTaskRunnerRoot` was found in both restored CLI files.

This proves the negative scan is live and rejects reintroduced legacy paths;
it is not a historical incident replay.

Legacy-pinned bootstrap RED
---------------------------
In a separate temporary copy only, `tools/cli/task-bootstrap.mjs` was mutated
to return `runner_identity: undefined` after authenticating a legacy-pinned
task. The focused gate was:

`./node_modules/.bin/vitest run scripts/__tests__/task-bootstrap.test.mjs --maxWorkers=1`

Result: exit 1; 1 test failed and 2 passed. The failing assertion expected the
fresh authenticated `git_invocation` identity and received `undefined`.
This proves the `legacy_pinned` bootstrap gate detects loss of read-only
historical loading plus fresh invocation authentication.

Cleanup
-------
All temporary mutation copies were removed after capture. No full suite,
phase-result, acceptance, move-map edit, file deletion, or restoration in the
candidate worktree was performed.
