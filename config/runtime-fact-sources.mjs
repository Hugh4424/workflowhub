/**
 * Production runtime sources are deliberately empty. Each future source needs
 * a separately approved launcher capability and input contract.
 */
export const RUNTIME_FACT_SOURCES = Object.freeze([]);
export default RUNTIME_FACT_SOURCES;
