/**
 * v2 sources remain empty until each direct source gets a separately approved
 * launcher capability and input contract.
 */
export const RUNTIME_FACT_V2_SOURCES = Object.freeze([]);
export default RUNTIME_FACT_V2_SOURCES;
