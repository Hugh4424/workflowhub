/**
 * Production transcript sources are deliberately empty. New sources require a
 * separately approved launcher capability and an explicit adapter contract.
 */
export const TRANSCRIPT_SOURCES = Object.freeze([]);
export default TRANSCRIPT_SOURCES;
