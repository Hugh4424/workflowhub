# 功能规格：make-decision 需求收敛与执行阻塞修复

> 基于已接受的需求来源（`decision-log.md`，状态 accepted）。本文件只写产品问题、行为、边界和验收，不写实现文件路径、代码符号或工程命令；用户可见面以人话为主，稳定编号仅作内部机器追溯锚点（见「来源与决策映射」的内部映射段）。

- **content_profile**：`spec-content.v3`
- **功能名**：make-decision 需求收敛机制、收敛检查、收尾与执行阻塞修复
- **来源**：`decision-log.md`（23 条原始/追加需求，12 项决定，用户最终确认 accepted；范围修正 D-011 与任务身份启动前置 D-012 已并入）
- **状态**：草稿 / 待独立审查（未接受为正式规格；build-spec 步骤 6 产品方向与独立审查完成后才可固化）

## 速读卡（30 秒）

- **一句话需求**：让 make-decision 真正「收敛」——所有原始/追加需求逐条处置并映射到决定，覆盖业务目标、未来扩展影响、UI/交互、验收目标、影响范围，结束时用大白话结束卡收尾；同时修复本任务暴露的宿主初始化、worktree 预检与审查材料交付阻塞，并用一个正常简单任务做验证演练。任务启动前置请先记住一句防错回声：**worktree 检出存在 ≠ 任务已初始化（worktree ≠ TaskHandle）**——正式阶段开始前必须经官方启动通道在受信任存储根发布/打开任务目录，目录缺失走只创建发布、已有 worktree 用显式现有绑定只校验不覆盖、会话来源不可用不回滚、真实输入错误 fail-loud。
- **核心改动点**：
  - make-decision 的收敛机制与检查：需求逐条处置 + 需求→决定映射矩阵 + 结束卡三要素 + 全流程人话化。
  - spec-analyze 补「目标达成」「方案收敛」两个验收维度，build-spec 契约比对流程/状态/FR/AC/边界/非目标。
  - Grill 每轮同卡成组提问（每题一轴、依赖题后置、无最少题数）；build-spec 的 Clarify 有实质歧义必真实触发、无歧义显式跳过。
  - 非 Codex 宿主初始化与降级：session 事件返回结构化 unavailable 并正常退出；初始化先于 session 绑定；worktree 按确定性规则预检。
  - 审查材料交付复用既有正式密封通道，禁止新打包工具、手工哈希与裸请求。
  - build-spec 执行端防错合同：严格生产形状为新产出唯一权威，写后立即校验，同一错误同一参数不重复重试；宿主等价沙箱拒绝只登记边界。
  - 任务身份与启动前置（worktree ≠ TaskHandle）：任何 Git worktree 检出都不能当作任务身份/任务存储已存在；正式阶段开始前经现有官方启动通道确认/创建；任务目录缺失走只创建发布，已有 worktree 用显式现有绑定只校验不覆盖；不手写任务清单；会话来源不可用不回滚已创建任务。
- **最大影响面**：make-decision 流程、talk/grill/spec-analyze/spec-clarify 四个技能契约、build-spec 对 Clarify 的使用契约、build-spec 写入防错（模型执行面）、非 Codex 宿主的初始化路径、审查材料交付路径、任务身份与启动前置（worktree 与任务身份/任务存储的区分），以及后续所有 WorkflowHub 任务执行体验。
- **验收信号**：23 条需求在矩阵中逐条有处置；结束卡三要素齐全且为大白话；spec-analyze 能分别判定覆盖/目标达成/验收清晰/方案收敛；Grill 输出同卡多题批次；Clarify 有真实触发或显式跳过理由；非 Codex 宿主如实降级不伪造；build-spec 产出以严格生产形状为唯一权威、写后立即校验、同一错误同一参数不重复重试；任务启动前置上能区分「worktree 存在」与「任务已初始化」、任务目录缺失时经官方只创建通道发布、现有 worktree 显式绑定不覆盖既有材料、会话来源不可用不回滚且真实错误 fail-loud；一个正常简单任务的 dogfood 六信号全部可判。

### 来源与决策映射

> 本节只保存 ID 关系，不复制 decision-log 正文；没有来源的新增需求必须回到 make-decision。以下主表以人话为主，内部锚点（原「需求」「决策」编号）在紧随其后的内部映射段保留，供机器可回指（覆盖校验链兼容，见 PFACT-010）。

| 需求（人话） | 决定（人话） | 状态 / 影响范围 | 未决 / 交接 |
| --- | --- | --- | --- |
| 收敛不随机、目标必须被问过、消除歪路线后果 | 收敛机制三层总纲 + 收敛标准 | current / 全流程 | 无 |
| spec-analyze 必须检查需求收敛语义 | spec-analyze 补目标达成/方案收敛验收 | current / 检查层 | 判定标准写死、异源核验（不新增 gate） |
| 结束时必须用大白话总结核心需求、核心目标和最终方案 | 结束卡三要素 + 人话化 | current / 收尾层 | 「最终方案」=已选方向摘要，细节留后续阶段 |
| Grill 必须一次问一组 | 升级上游 round/frontier 标准 + 消歧 + 可观察规则 | current / grill 技能 | 执行遵循度靠可观察痕迹约束 |
| build-spec 绝大部分场景 clarify 必须正常使用 | Clarify 判定标准写死 + 默认触发路径 | current / build-spec 契约 | 交互证据的机器强绑定延期到后续治理任务 |
| 不跳阶段、不依赖 build-spec 补需求；先梳理完整流程/页面/数据/边界 | 需求收敛治理总纲 + 覆盖矩阵标准 | current / 边界 | 无 |
| Talk 用大白话说明选项后果风险；决策日志记五要素 | 人话决策卡 + 日志结构 | current / 呈现层 | 无 |
| 决策必须完整解决需求（业务目标/未来扩展/UI交互/验收目标/影响范围） | 需求→决定覆盖矩阵 + 维度标注 | current / 矩阵 | 未来扩展只标影响，不做未来方案设计 |
| 执行阻塞全部登记并一并处理（宿主绑定、task 绑定、worktree、材料交付、超时） | 执行阻塞修复五子项（窄改）+ 范围修正：初始化先于绑定、复用正式入口 | current / 执行层 | 第二打包工具、无任务存储时的直写分支与审查通道超时机制不新增 |
| 验证演练怎么算成功 | dogfood 通过标准写死，只跑正常简单任务 | current / 验证层 | 单元/契约级负向回放在验收范围；端到端故意不收敛的演练延期 |
| GPT/同类模型执行 build-spec 反复出错需防错合同 | 生产形状权威 + 写后即时校验 + 同参禁重试换写入者 | current / build-spec 执行面 | 宿主等价沙箱拒绝只登记边界，不修宿主 |
| worktree 检出存在不等于任务身份/任务存储已存在；正式阶段开始前必须确认/创建任务身份，任务目录缺失走官方只创建发布，已有工作副本用显式现有绑定只校验不覆盖，不手写任务清单，会话来源不可用不回滚 | 任务身份与启动前置（区分 worktree 与任务身份；复用现有官方启动入口） | current / 任务启动前置 | 实现仍属后续阶段 |

### 内部锚点映射（机器追溯段；用户界面可隐藏此段，覆盖校验仍可回指）

| 内部需求锚点 | 内部决策锚点 | 内部 FR 锚点 | 内部 AC 锚点 |
| --- | --- | --- | --- |
| R-001、R-002、R-003 | D-001、D-002、D-003 | FR-CONV-001、FR-CONV-002、FR-ANALYZE-001 | AC-CONV-001、AC-CONV-002、AC-ANALYZE-001 |
| R-004 | D-003 | FR-ANALYZE-001、FR-ANALYZE-002 | AC-ANALYZE-001、AC-ANALYZE-002 |
| R-005 | D-006 | FR-CONV-003 | AC-CONV-003 |
| R-006 | D-004 | FR-GRILL-001 | AC-GRILL-001 |
| R-007 | D-005 | FR-CLARIFY-001、FR-CLARIFY-002 | AC-CLARIFY-001、AC-CLARIFY-002 |
| R-008 | D-001~D-008 | FR-ANALYZE-002 | AC-ANALYZE-002 |
| R-009 | D-001、D-002 | FR-CONV-001、FR-CONV-002 | AC-CONV-001、AC-CONV-002 |
| R-010 | D-001、D-006 | FR-CONV-003、FR-CONV-004 | AC-CONV-003、AC-CONV-004 |
| R-011 | D-001、D-002、D-006 | FR-CONV-001、FR-CONV-003 | AC-CONV-001、AC-CONV-003 |
| R-012 | D-008、D-012 | FR-HOST-004 | AC-HOST-004 |
| R-018 | D-008（子项②） | FR-HOST-004 | AC-HOST-004 |
| R-013 | D-002、D-003 | FR-CONV-002、FR-ANALYZE-001 | AC-CONV-002、AC-ANALYZE-001 |
| R-014 | D-007 | FR-CONV-004 | AC-CONV-004 |
| R-015 | D-008（子项⑤） | FR-HOST-001~004、FR-REVIEW-001~003 | AC-HOST-001~004、AC-REVIEW-001~003 |
| R-016 | D-008（子项①）、D-011 | FR-HOST-001、FR-HOST-002 | AC-HOST-001、AC-HOST-002 |
| R-017 | D-008（子项①）、D-011、D-012 | FR-HOST-003 | AC-HOST-003 |
| R-019、R-020 | D-008（子项③）、D-011 | FR-REVIEW-001、FR-REVIEW-002 | AC-REVIEW-001、AC-REVIEW-002 |
| R-021 | D-008（子项④） | FR-REVIEW-003 | AC-REVIEW-003 |
| dogfood 通过标准 | D-009 | FR-DOG-001 | AC-DOG-001 |
| R-022 | D-010 | FR-AUTHORING-001、FR-AUTHORING-002 | AC-AUTHORING-001、AC-AUTHORING-002 |
| R-023 | D-012 | FR-TASK-001、FR-TASK-002 | AC-TASK-001、AC-TASK-002 |

## 1. 问题与紧迫性

现在的 make-decision 对「需求收敛」完全随机：想到什么问题就问什么，原始目标可能一轮 talk、审查、grill 全部完成后一个问题都没被问过，原始需求完全没有收敛。后果是后续 spec、plan 的路子跟着歪掉，最终没有实现最初的需求。用户明确看到的四个断点：

- spec-analyze 只检查最基础的文档内容，完全不检查覆盖、目标达成、验收清晰、方案收敛这些语义；
- make-decision 结束时没有用大白话总结当前核心需求、核心目标和最终方案；
- grill 没有按上游最新标准一次问一组问题，总是一次只问一个；
- build-spec 绝大部分场景下 clarify 技能没有正常使用。

此外，本任务真实执行中暴露了一组会影响所有后续 WorkflowHub 任务的阻塞：非 Codex 宿主（如 DeepSeek Harness）没有任何会话绑定机制导致私有 session 事件与 task 绑定直接失败；正式任务开始前 worktree 被漏建；异源审查材料因手工构造密封包反复失败（哈希不一致、裸请求导致 provider 只见 prompt 不见材料）；provider 长时调用被宿主默认超时杀掉。这些问题不修，下一次任务会原样重演，因此必须与收敛治理一并处理。

## 2. 背景、目标与范围

### 背景

WorkflowHub 的标准流程固定五阶段，make-decision 位于起点，负责把原始需求收敛成可交接的决定。当前运行时（现有任务启动模块）已提供逐需求覆盖、grill 摘要与最终确认的检查，但「目标达成」没有独立判定字段、「方案收敛」完全缺失；Grill 文本与契约已支持同卡多题，逐题单问属执行层行为；Clarify 归属已独占给专门的澄清技能，缺口在歧义判定全凭执行者、无真实批次痕迹。上游最新 grill 标准已于 2026-07-31 反转推翻旧的「一次一问」，采用 round/frontier 模型（一轮问整个 frontier、每题编号+推荐、依赖题后置）。

### 术语：已认证的现有工作副本

本规格所称「已认证的现有工作副本」（又称「认证 worktree」，见 FR-TASK-002、SCN-016、AC-TASK-002 处使用）指：路径与分支符合当前任务工作区确定性规则、可读写且其中材料（decision-log/spec/plan/tasks 四份当前材料及必要工作文件）被保留的现有 Git worktree 检出。「认证」仅表示按任务工作区规则对该现有工作副本的确认，**不等于**会话来源（session provenance）已绑定到该副本，也不构成新的身份系统；任务身份/任务存储仍由官方任务存储（TaskHandle/task store）承载（见 TASK 域）。本术语只是对既有检出环境的表述约定，不新增身份载体，不改变 FR-HOST-004 的 worktree 三态预检语义。

### 目标

- make-decision 结束时：每条原始/追加需求逐条有处置（covered/延期/拒绝），需求→决定映射矩阵逐条覆盖五个维度（业务目标、未来扩展影响、UI/交互、验收目标、影响范围），无静默遗漏。
- 结束卡三要素（核心需求、核心目标、已选方向摘要）齐全且为大白话，用户确认后阶段才收尾。
- spec-analyze 能分别检查覆盖、目标达成、验收清晰、方案收敛四项，判定标准为可复核的操作性规则。
- grill 每轮同卡展示全部独立问题，每题一轴，依赖题后置，不设每批最少题数；输出暴露编号批次痕迹。
- build-spec：有实质歧义时 Clarify 真实 ask-wait-reply-resume，无歧义显式 trigger=false + 理由 + 零开放项。
- 非 Codex 宿主可如实初始化与降级：私有 session 事件返回结构化 unavailable 并正常退出；真实参数、材料、身份错误 fail-loud；初始化不被绑定失败回滚。
- 审查材料交付复用既有正式密封通道，错误保留正式错误码并标 unavailable。
- 一个正常简单任务完成 dogfood，六信号全部可判，结果不是 gate。
- build-spec 执行端：严格生产形状为新产出唯一权威；写入后、冻结前立即运行现有生产校验与验收最小要求；同一错误同一参数不重复重试、第二次换既有写入通道或受控子代理并保留原始失败；宿主等价沙箱拒绝只登记边界。

### 范围内

- make-decision 的收敛机制、检查与收尾：需求登记、需求→决定映射矩阵（五维度）、结束卡、人话化。
- spec-analyze 的 make-decision profile 与 build-spec profile 检查行为。
- talk/grill/clarify 三个交互技能的使用契约与可见痕迹。
- 非 Codex 宿主初始化与降级：session 事件、task/worktree/task store 初始化顺序、worktree 确定性预检。
- 审查材料交付：复用既有密封通道、错误语义、无宿主期限控制面、长时调用语义。
- 一个正常简单任务的 dogfood 验证演练。
- build-spec 执行端防错：生产形状权威、写后即时校验、同参重试禁止与写入者切换（复用既有校验能力，不新增机制）。

> 非目标只在第 10 节维护，避免两份真相。

## 3. 用户场景与状态覆盖

> 场景覆盖正常、边界、失败和权限路径，并说清「谁、在什么条件下、做什么、看到什么」。

### SCN-001：标准启动与需求登记

- **角色**：make-decision 执行者与用户
- **Given**：任务 worktree 已按确定性规则就位，当前材料可读写
- **When**：make-decision 启动并编译原始/追加需求
- **Then**：每条需求进入需求→决定映射矩阵并逐条标注处置状态；结束前不出现无处置的静默遗漏行

### SCN-002：Talk 按矩阵推进

- **角色**：make-decision 执行者与用户
- **Given**：需求矩阵存在且有未收敛项
- **When**：每一轮 talk 提出独立决策轴
- **Then**：问题落在矩阵相关行上推进收敛；已收敛轴不再重复提问，未收敛轴保持可见

### SCN-003：Grill 每轮同卡成组提问

- **角色**：make-decision 执行者与用户
- **Given**：Talk 与调研已产生一批独立待确认点
- **When**：执行者进入 grill 轮次
- **Then**：同卡展示全部独立问题，每题一轴且有编号与推荐项；依赖未决问题的题后置到后续轮；同批不设最少题数；等用户回复后才进下一轮

### SCN-004：大白话结束卡

- **角色**：make-decision 执行者与用户
- **Given**：需求逐条处置完成、决定链闭合
- **When**：阶段收尾呈现结束卡
- **Then**：卡上以人话给出核心需求、核心目标和已选方向摘要三要素；用户确认后阶段收尾；稳定编号不在用户可见面出现，仅内部追溯

### SCN-005：收敛检查（make-decision profile）

- **角色**：spec-analyze 执行者与验收者
- **Given**：make-decision 阶段材料齐备
- **When**：stage-end 运行收敛检查
- **Then**：覆盖（每条需求有处置且决定可见）、目标达成、验收清晰、方案收敛四项分别判定；判定依据操作性规则，不只看文件或 ID 存在

### SCN-006：契约比对（build-spec profile）

- **角色**：spec-analyze 执行者
- **Given**：build-spec 产出 spec.md 候选
- **When**：stage-end 运行比对
- **Then**：spec.md 的流程、状态、FR、AC、边界、非目标与原始需求及决策日志逐一比对，缺口当场暴露给 build-spec 修复

### SCN-007：Clarify 真实触发与等待

- **角色**：build-spec 执行者与用户
- **Given**：spec 起草中发现实质材料歧义
- **When**：执行者运行 spec-clarify
- **Then**：真实向用户呈现一批独立问题并等待真实回复，回复后继续起草；无歧义时显式记录 trigger=false + 理由 + 零开放项；回复缺失、卡片错误或中断时保持 incomplete，不推断、不重跑第二套 Clarify

### SCN-008：非 Codex 宿主初始化与降级

- **角色**：宿主（非 Codex，如 DeepSeek Harness）与 make-decision/build-spec 执行者
- **Given**：宿主能力缺失或未注册——无 session 状态，或私有 session 事件（如 codex-session-event）依赖的模块缺失；task store 缺失不属于本场景的降级输入：task/worktree/task store 初始化先于 session 绑定（见 SCN-009），无-task-store 材料直写模式已删除（D-011），不再作为降级形态
- **When**：执行者调用私有 session 事件或绑定命令
- **Then**：无 session 时命令返回结构化 unavailable 并正常退出（不伪造成功、不阻塞任务）；仅宿主能力缺失/未注册时走降级路径；真实参数、材料或身份错误 fail-loud

### SCN-009：初始化先于 session 绑定，失败不回滚

- **角色**：task-bootstrap 与宿主
- **Given**：新任务尚无 task store 与 session 绑定
- **When**：任务初始化
- **Then**：task/worktree/task store 初始化先于 session 绑定发生；绑定失败只报告绑定不可用，不回滚已初始化的内容

### SCN-010：worktree 确定性检查

- **角色**：make-decision 执行者
- **Given**：进入任务前检查 worktree 状态
- **When**：按现有 workspace 规则的确定性路径/分支校验
- **Then**：全无时创建；全有时复验路径/分支一致；仅单边存在（目录在分支不在或反之）时标 conflict 并停下报告，不自动修复

### SCN-011：审查材料密封交付

- **角色**：wh-review 执行者与第三方审查 provider
- **Given**：需要异源 direction/detail 审查
- **When**：执行者构建并发送审查请求
- **Then**：材料经既有正式密封通道（材料构建 → 成组运行 → 第三方审查）交付；附件、哈希、冻结树或身份错误保留正式错误码并标 unavailable；长时调用由执行者按文档后台运行，不被宿主短超时杀掉

### SCN-012：公共审查无宿主期限控制面

- **角色**：审查执行者与第三方审查通道
- **Given**：公共审查请求已发起
- **When**：执行者或宿主观察审查运行
- **Then**：公共审查不存在宿主侧 deadline；生命周期归属第三方审查通道；不新增 host deadline/health/recovery/session 控制面

### SCN-013：dogfood 六信号验证

- **角色**：验证执行者与验收者
- **Given**：一个正常简单任务从 make-decision 开始
- **When**：任务完整跑到交接
- **Then**：六个信号全部可判：结束卡三要素齐全、需求全覆盖无未处置、Grill 出现同卡多题批次、分析字段（目标达成/方案收敛）有值、Clarify 有真实触发或显式跳过痕迹、非 Codex 宿主路径如实降级；验证结果不是 gate

### SCN-014：非目标边界守卫

- **角色**：任一阶段执行者
- **Given**：执行中出现越界冲动：新增第五材料、新公共命令、新状态机、新 gate、GUI/UI、重写既有审查技能、改动 verify-code
- **When**：执行者识别到越界意图
- **Then**：停下并回到 decision-log 记录为需求或延期，不悄悄扩大范围
- **关联 FR**：FR-CONV-001、FR-AUTHORING-002（越界手段不得用于绕过写入失败或新开机制）

### SCN-015：模型写规格与写入失败恢复

- **角色**：build-spec 执行者（模型写入体）与写入/校验通道
- **Given**：按严格生产形状撰写或修订规格材料
- **When**：写入完成、冻结之前
- **Then**：立即运行现有生产 profile 校验与验收最小要求；错误当场定位并在当前步骤修复；同一错误同一参数不重复重试——首次失败先做失败分析，第二次切换到既有写入通道或受控子代理并保留原始失败；宿主侧等价沙箱拒绝记录为外部宿主事实（停止循环、换路径、如实登记），不修宿主

### SCN-016：已有工作副本但任务身份缺失

- **角色**：make-decision/后续阶段执行者与任务启动通道
- **Given**：受信任存储根下任务目录缺失，但已有 Git worktree 检出环境
- **When**：正式阶段开始前执行任务身份检查
- **Then**：识别「worktree 存在 ≠ 任务已初始化」这一差异；任务目录缺失时经官方任务启动通道以只创建（create-only）方式发布/打开任务目录；已有认证 worktree 显式传入现有绑定只校验绑定关系、不覆盖既有材料；会话来源（session provenance）不可用时不得删除或回滚已创建的任务目录；真实参数/材料/身份错误 fail-loud
- **关联 FR**：FR-TASK-001、FR-TASK-002

### 状态覆盖清单

- [x] **默认态**：SCN-001、SCN-002、SCN-004、SCN-005、SCN-006、SCN-013、SCN-015、SCN-016（标准流程、正常检查、正常任务与阶段启动路径）
- [x] **空态**：SCN-008（无 session 的空宿主环境）、SCN-009（无 task store/worktree 的新任务起点）
- [x] **错误态**：SCN-008（真实参数/材料/身份错误 fail-loud）、SCN-011（附件/哈希/冻结树/身份错误保留正式错误码并 unavailable）、SCN-015（写入校验错误在当前步骤修复、同参不重复重试）、SCN-016（真实输入错误 fail-loud、不因会话来源不可用掩盖）
- [x] **加载态**：SCN-007（Clarify 等待真实回复的等待期）、SCN-011（长时 provider 调用后台运行）
- [x] **取消态**：SCN-007（回复缺失/卡片错误/中断 → 保持 incomplete，不推断）
- [x] **边界态**：SCN-003（依赖题后置、无最少题数）、SCN-010（全无创建/全有复验/单边 conflict 不自动修复）、SCN-012（公共审查无宿主期限控制面）、SCN-014（越界意图被边界拦截）、SCN-016（worktree 存在但任务目录缺失；会话来源不可用不回滚）
- [x] **权限态**：SCN-008（宿主能力缺失/未注册降级 vs 真实性错误 fail-loud）、SCN-011（材料身份/冻结树校验错误）、SCN-014（非目标越界不被允许）
- [x] **竞态**：SCN-009（初始化与 session 绑定先后；绑定失败不回滚）

### 流程状态与恢复

- 成功/失败语义只在场景（SCN-007/008/009/010/011）与状态覆盖清单中表达：登记与处置收敛见 SCN-001/002，询问等待与 incomplete 见 SCN-007，降级/fail-loud 见 SCN-008，初始化与绑定不回滚见 SCN-009，worktree 三态见 SCN-010，审查交付错误语义见 SCN-011。本规格不定义独立的状态机，也不在场景之外枚举流程状态。

## 4. 产品事实与假设（PFACT）

> 每条只选择一种状态，其他状态字段删除；`unknown` 必须关联 RISK 或 OPEN。

- **PFACT-001**：需求收敛的定义已锁定：所有原始/追加需求逐条处置并映射到决定，覆盖业务目标、未来扩展影响、UI/交互、验收目标、影响范围五个维度。（状态=verified）
  - **证据或来源**：决策日志 D-001~D-012；需求→决定覆盖矩阵 23/23 covered
  - **关联**：FR-CONV-001、FR-CONV-002、AC-CONV-001、AC-CONV-002

- **PFACT-002**：spec-analyze 运行时现状为：逐条覆盖部分自报、目标达成无独立判定、验收清晰部分、方案收敛完全缺失；本需求在 make-decision profile 补「目标达成」「方案收敛」两个验收维度。（状态=verified）
  - **证据或来源**：决策日志调研 F-004（现有任务启动模块的现状审计）
  - **关联**：FR-ANALYZE-001、FR-ANALYZE-002、AC-ANALYZE-001、AC-ANALYZE-002

- **PFACT-003**：上游最新 grill 标准为 round/frontier 模型：一轮问整个 frontier、同卡多题、每题编号+推荐答案、依赖未决问题的题放后续轮、按轮等用户回复；2026-07-31 上游提交已反转推翻旧「一次一问」标准。（状态=verified）
  - **证据或来源**：决策日志调研 F-001（上游 grill 标准仓库的当前版本与 2026-07-31 反转提交核验）
  - **关联**：FR-GRILL-001、AC-GRILL-001

- **PFACT-004**：task/worktree/task store 初始化先于 session 绑定发生，绑定失败不回滚已初始化内容（正式只读调研事实，SRES-003）。（状态=verified）
  - **证据或来源**：决策日志 R-017、D-008（子项①）、D-011（范围修正确认初始化顺序）；SRES-003（正式只读调研事实）
  - **关联**：FR-HOST-003、AC-HOST-003（初始化顺序）；「绑定失败/会话来源不可用不回滚已创建任务」的验收单点归 AC-TASK-002（见 FR-TASK-002）

- **PFACT-005**：worktree 的路径与分支规则为既有确定性 workspace 行为；「全无创建/全有复验/单边 conflict」三态是本规格对该既有行为的细化，来源 D-008 + D-011，不是用户逐项选择的新行为。（状态=verified）
  - **证据或来源**：决策日志 R-012、R-018、D-008（子项②）、D-011；SRES-003（正式只读调研事实）
  - **关联**：FR-HOST-004、AC-HOST-004

- **PFACT-006**：现有正式密封材料入口已存在并可复用：材料构建 → 成组运行 → 第三方审查；本会话真实调用成功，前置方向审查的裸请求失败为真实失败事实；打包辅助=复用该既有入口，不新增第二工具（D-011）。（状态=verified）
  - **证据或来源**：决策日志 R-019、R-020、D-008（子项③）、D-011；审查处置表 direction-2 成功与 direction-1 失败事实
  - **关联**：FR-REVIEW-001、FR-REVIEW-002、AC-REVIEW-001、AC-REVIEW-002

- **PFACT-007**：公共审查 deadline=null 已是既有事实，审查生命周期归属第三方审查通道。（状态=verified）
  - **证据或来源**：决策日志 D-008（子项③、④）；SRES-003（正式只读调研：公共审查 deadline=null 事实）
  - **关联**：FR-REVIEW-003、AC-REVIEW-003

- **PFACT-008**：非 Codex 宿主（无 session）调用私有 session 事件时返回结构化 unavailable 并正常退出。（状态=verified）
  - **证据或来源**：本轮 spec-clarify 用户真实回答（见文末 Spec Clarification Record）
  - **关联**：FR-HOST-001、FR-HOST-002、AC-HOST-001、AC-HOST-002

- **PFACT-009**：dogfood 只覆盖一个正常简单任务的 happy path；单元/契约级负向样本回放（覆盖缺失/目标未回应/验收不可判/方案未收敛四类样本）属于本任务验收范围（见 AC-ANALYZE-001）；真实端到端故意不收敛 dogfood 不做并延期（RISK-004）。（状态=inferred）
  - **证据或来源**：决策日志 T-015 用户选择、D-009、RISK-004；限制：只代表正常简单任务结果，不代表端到端对抗证明
  - **关联**：FR-DOG-001、AC-DOG-001、FR-ANALYZE-001、AC-ANALYZE-001

- **PFACT-010**：人话化契约：用户可见面以人话为主，稳定编号降为内部机器锚点（渲染可隐藏、校验可回指）；spec-analyze 既有的三处编号硬校验保持兼容，不破坏覆盖链。（状态=verified）
  - **证据或来源**：决策日志 D-007、Grill G-004（runtime 三处硬校验点核查）
  - **关联**：FR-CONV-004、AC-CONV-004

- **PFACT-011**：结束卡三要素 = 核心需求（大白话）、核心目标、已选方向摘要；既有「用户完成卡」定义已并入三要素。（状态=verified）
  - **证据或来源**：决策日志 D-006、G-002；CONTEXT.md「用户完成卡」修订
  - **关联**：FR-CONV-003、AC-CONV-003

- **PFACT-013**：当前 build-spec 的 Clarify 唯一归属与判定基线为既有已验证合同：澄清唯一由专门澄清技能执行；存在实质材料歧义时必须真实触发澄清，无实质歧义时必须显式产出无歧义结果（显式跳过并说明理由、零开放项）；判定结果可被复核。（状态=verified）
  - **证据或来源**：决策日志调研 F-006、D-005；SRES-004（build-spec 与专门澄清技能的既有合同核验，产品语言）
  - **关联**：FR-CLARIFY-001、FR-CLARIFY-002、AC-CLARIFY-001、AC-CLARIFY-002

- **PFACT-014**：机制「咬人」的证明分两层：单元/契约级负向样本回放（构造覆盖缺失/目标未回应/验收不可判/方案未收敛四类样本）已含于本任务验收范围（AC-ANALYZE-001）；真实端到端故意不收敛 dogfood 的对抗证明目前不可判（延期）。（状态=unknown）
  - **owner、影响**：后续 WorkflowHub 治理任务；影响=执行层依赖遵循，端到端「咬人」失败路径只能由后续 spec-analyze 测试覆盖
  - **关联**：FR-DOG-001、AC-DOG-001、RISK-003

- **PFACT-016**：build-spec 产出形状存在双标准事实：生产校验使用严格 profile（形状字段逐项命中、缺项即失败），验收/接受侧只做兼容读取（缺项可被容忍或按默认解释）；「能读」不等于「能通过生产校验」，兼容读取不能作为写出标准；且模板/既有文本用加粗列表（如 `- **验证方法**：`）呈现验收方法，与 runtime oracle 的裸行标签（`验证：`/`验收：`/`判定：`）不一致（本规格已按 runtime 形状修订，见第 11 节）。（状态=verified）
  - **证据或来源**：现有生产校验能力与规格模板（spec-template）的形状字段审计（决策日志 D-010 依据）；runtime oracle 标签与模板加粗列表的不一致由本规格第 11 节生产形状修订实证（内部锚点段可回指）
  - **关联**：FR-AUTHORING-001、FR-AUTHORING-002、AC-AUTHORING-001、AC-AUTHORING-002

- **PFACT-017**：Git worktree 与任务存储是两个独立的位置与身份：worktree 检出存在不等于任务目录/任务身份已初始化。诊断时（官方启动执行前）当前任务的叶子任务目录在受信任存储根下不存在、没有成功启动记录，根因是只手工创建工作副本/手工写四份材料而漏执行官方启动；随后已通过官方只创建（create-only）启动成功创建任务目录与完整任务存储，并以复用现有工作副本（existing）的方式绑定当前 worktree——既有材料未被覆盖；启动输出中会话来源（session provenance）冲突仅是辅助溯源异常，不影响任务目录创建与绑定。（状态=verified）
  - **证据或来源**：人话核对官方启动前后的状态：启动前只有工作副本检出、任务目录与任务清单均缺失；启动后任务目录、任务索引、事实记录、质量与校验记录齐全，任务清单声明复用当前工作副本（只校验绑定、不覆盖既有材料），工作副本内既有材料保持原样；启动输出的会话来源冲突记录为辅助溯源异常，任务目录创建不受影响。详见决策日志 R-023/D-012 诊断事实与后续修复事实节、SRES-006
  - **关联**：FR-TASK-001、FR-TASK-002、AC-TASK-001、AC-TASK-002

## 5. 功能需求

> 沿用「叙述层 + 编号字段层」：先讲清完整行为，再做机器追溯。每条 FR 至少连接一个 PFACT、SCN 和 AC。

### 需求收敛与结束卡（CONV）

这个功能域解决「make-decision 收敛不能随机」：启动时登记全部原始/追加需求，逐条处置并映射到决定，用五个维度证明决定完整解决需求；结束时用大白话结束卡收尾，用户可见面不出现稳定编号。未来扩展只标注影响，不设计未来方案。

- **FR-CONV-001**：所有原始/追加需求在需求→决定映射矩阵中逐条处置并锁定为 covered/延期/拒绝（非目标只是分类，不是第四种处置状态；列入非目标须上游授权并按已锁定状态呈现），处置状态与对应决定可见；阶段结束前不允许出现无处置的静默遗漏行。
  - **范围边界**：包含需求登记、处置状态标注、决定可见性；不包含机器全量硬校验（判定以操作性规则呈现，不新增 gate）
  - **依据**：D-001、D-002；PFACT-001
  - **场景**：SCN-001、SCN-002
  - **验收**：AC-CONV-001

- **FR-CONV-002**：矩阵覆盖五个维度：业务目标、未来扩展影响（只标影响不设计未来方案）、UI/交互、验收目标、影响范围；每个维度有可核验的标注位置。
  - **范围边界**：未来扩展维度只标注扩展点及其影响；不为不存在的未来设计完整扩展方案
  - **依据**：D-002、G-001；PFACT-001（未来扩展只标影响不设计，见第 10 节）
  - **场景**：SCN-001
  - **验收**：AC-CONV-002

- **FR-CONV-003**：make-decision 结束卡以大白话呈现三要素：核心需求、核心目标、已选方向摘要；用户确认后才算阶段收尾。
  - **范围边界**：「最终方案」=已选方向摘要，细节（拟定方案/方案比较）留给后续阶段，detail 审查后如方向细化再刷新
  - **依据**：D-006、G-002；PFACT-011
  - **场景**：SCN-004
  - **验收**：AC-CONV-003

- **FR-CONV-004**：decision-log、spec、plan 的用户可见面以人话为主（「需求」「决策」等表述）；稳定编号只作内部机器锚点，渲染可隐藏、校验可回指，既有的覆盖校验链保持兼容。
  - **范围边界**：完全去除编号不在范围（会破坏覆盖校验链，已被拒绝）；内部锚点映射段承担机器追溯职责
  - **依据**：D-007、G-004；PFACT-010
  - **场景**：SCN-004
  - **验收**：AC-CONV-004

### Grill 批次（GRILL）

这个功能域解决「grill 总是一次只问一个问题」：按上游最新 round/frontier 标准，每轮同卡展示全部独立问题。

- **FR-GRILL-001**：grill 每轮在同卡展示当前轮全部独立问题（frontier），每题一个决策轴并带编号与推荐项；依赖未决问题的题后置到后续轮；同批不设最少题数；等用户回复后才进入下一轮。
  - **范围边界**：不新增每批 ≥2 题的机器强门（会误伤真正的依赖单题且违反不新增 gate）；文本消歧 + 可观察批次痕迹（编号+推荐项）为约束手段
  - **依据**：D-004、调研 F-001、F-002；PFACT-003
  - **场景**：SCN-003
  - **验收**：AC-GRILL-001

### 收敛检查（ANALYZE）

这个功能域解决「spec-analyze 只查基础文档」：把覆盖、目标达成、验收清晰、方案收敛做成可分别判定的检查。

- **FR-ANALYZE-001**：spec-analyze 的 make-decision profile 分别检查四项：需求覆盖（每条需求有处置且决定可见）、目标达成（处置与决策能证明原始目标被回应）、验收清晰（验收目标可判）、方案收敛（矩阵完备、无未处置需求、无相互矛盾决定）；判定标准为写死的操作性规则，结果可被异源核验。
  - **范围边界**：不新增机器 gate；判定规则写死为可复核契约，不靠自报
  - **依据**：D-003、T-017；PFACT-002
  - **场景**：SCN-005
  - **验收**：AC-ANALYZE-001

- **FR-ANALYZE-002**：spec-analyze 的 build-spec profile 把 spec.md 的流程、状态、FR、AC、边界、非目标与原始需求及决策日志逐项比对，发现语义缺口当场暴露给 build-spec 修复。
  - **范围边界**：比对产出的缺口由 build-spec 在同一任务内修复；不另开任务绕开质量缺口
  - **依据**：D-003、D-005；PFACT-002
  - **场景**：SCN-006
  - **验收**：AC-ANALYZE-002

### Clarify 契约（CLARIFY）

这个功能域解决「build-spec 绝大部分场景 clarify 没正常使用」：判定标准写死、默认触发、痕迹可复核。

- **FR-CLARIFY-001**：build-spec 存在实质材料歧义时，spec-clarify 必须真实地 ask-wait-reply-resume：同一批呈现独立问题（同卡多题），等待用户真实回复后继续起草；回复缺失、卡片错误、哈希过期或中断时保持 incomplete，不推断答案、不换第二套 Clarify 实现。
  - **范围边界**：材料歧义按既定十个维度判定；方向性歧义返回 make-decision 作为上游决策缺口，不在本阶段猜答
  - **依据**：D-005；PFACT-013（判定基线，见第 10 节默认必须成立）
  - **场景**：SCN-007
  - **验收**：AC-CLARIFY-001

- **FR-CLARIFY-002**：build-spec 无实质歧义时，必须显式记录 trigger=false + 理由 + 零开放项；每个 clarify outcome 绑定真实批次痕迹（触发批次或显式跳过理由），痕迹可被 stage-end 检查引用。
  - **范围边界**：机器强绑定（交互证据完全机器化）延期到后续任务（OPEN-003），本任务只做到痕迹可复核
  - **依据**：D-005；PFACT-013（判定基线，见第 10 节默认必须成立）
  - **场景**：SCN-007
  - **验收**：AC-CLARIFY-002

### 非 Codex 宿主初始化与降级（HOST）

这个功能域解决「非 Codex 宿主完全无法初始化、worktree 漏建、task 绑定失败」等执行阻塞：如实降级、先行初始化、确定性预检。

- **FR-HOST-001**：非 Codex 宿主且无 session 时，私有 session 事件命令返回结构化 unavailable 并正常退出；阶段按降级路径继续，不伪造成功、不阻塞任务。
  - **范围边界**：降级只覆盖宿主能力缺失/未注册；真实性错误不属于降级（见 FR-HOST-002）；无-task-store 的材料直写模式已删除（D-011：task/worktree/task store 已在 session 绑定前初始化，无该分支），不再作为降级形态
  - **依据**：D-008（子项①）、D-011、R-016；PFACT-008
  - **场景**：SCN-008
  - **验收**：AC-HOST-001

- **FR-HOST-002**：仅宿主能力缺失/未注册时使用降级路径；真实参数错误、材料错误、身份错误必须 fail-loud（报错并指出错误范围，不吞错、不降级成 unavailable）。
  - **范围边界**：unavailable（正常退出的降级）与 fail-loud（真实错误）两类语义互不替代
  - **依据**：D-008（子项①）、R-016；PFACT-008
  - **场景**：SCN-008
  - **验收**：AC-HOST-002

- **FR-HOST-003**：task/worktree/task store 的初始化先于 session 绑定发生；「绑定失败/会话来源不可用不回滚已创建任务」由 TASK 域单点承接（FR-TASK-002 / AC-TASK-002），本 FR 不再重复声明（删除 HOST/TASK 双写）。
  - **范围边界**：初始化与绑定为先后顺序而非互相依赖；「不回滚已初始化/已创建内容」的任务目录侧验收只由 AC-TASK-002 承接，AC-HOST-003 仅验收初始化顺序
  - **依据**：R-017、D-008（子项①）、D-011；PFACT-004
  - **场景**：SCN-009
  - **验收**：AC-HOST-003（仅初始化顺序）

- **FR-HOST-004**：正式任务开始前 worktree 按现有 workspace 规则的确定性路径/分支预检：全无则创建、全有则复验路径与分支一致、仅单边存在则标 conflict 并停下报告，不自动修复。
  - **范围边界**：预检只到「存在/一致/冲突」三个确定性结论；冲突处置属人工，不做自动修复
  - **依据**：R-012、R-018、D-008（子项②）；PFACT-005
  - **场景**：SCN-010
  - **验收**：AC-HOST-004

### 任务身份与启动前置（TASK）

这个功能域解决「worktree 存在不等于任务已初始化、任务目录缺失找不到任务清单」：把任务身份/任务存储与 Git worktree 严格区分，正式阶段开始前复用现有官方启动通道确认或创建任务身份，任务目录缺失时只创建发布，已有工作副本显式绑定只校验不覆盖，会话来源不可用不回滚，真实错误 fail-loud；是任务身份启动前置，不是新的推进 gate，**也不是第二启动机制**——本域只消费并核验现有官方启动入口（发布/打开/绑定/失败语义全部来自既有入口）的结果，不新增公共启动命令、不封装新入口、不新增启动/发布/绑定/回滚控制面。

- **FR-TASK-001**：正式阶段开始前必须确认/创建任务身份与任务存储（TaskHandle）：Git worktree 单独存在不算任务已初始化；任务目录缺失时复用现有官方任务启动通道以只创建（create-only）方式发布/打开任务目录，不手写任务清单（task.json）；官方启动成功的完整性事实为 task.json + index.json + facts.jsonl + quality/ 目录全部存在，缺失任一即为失败，不得视为已初始化。
  - **范围边界**：只复用现有官方启动入口，不新增公共启动命令/模式/状态机、不新增第二启动机制或 preflight 控制面；本 FR 是现有官方入口的最小消费契约（调用并核验其结果，不做任何启动能力的新实现）；worktree 场景本身由 FR-HOST-004 覆盖，本 FR 只负责任务身份与任务目录的确认/发布
  - **依据**：D-012；PFACT-017
  - **场景**：SCN-016
  - **验收**：AC-TASK-001

- **FR-TASK-002**：已有认证 worktree 时必须使用显式现有绑定（existing）复用：只校验绑定关系、不创建也不覆盖既有材料；任务存储初始化完成后，会话来源（session provenance）仅是辅助溯源，不可用时不得删除或回滚已创建的任务目录；真实参数/材料/身份错误必须 fail-loud，不得以会话来源不可用掩盖。
  - **范围边界**：显式绑定与只创建发布互斥（已存在即绑定、缺失即创建，不两者同时）；绑定失败或会话来源不可用不构成回滚理由；真实性错误与降级语义互不替代（沿用 FR-HOST-002 语义）；不新增回滚/恢复机制；「会话来源不可用不回滚已创建任务」验收单点归属 AC-TASK-002（与 FR-HOST-003 明确分工，不双写）；显式绑定语义全部来自既有官方入口，不新增第二绑定机制
  - **依据**：D-012、R-017；PFACT-017
  - **场景**：SCN-016
  - **验收**：AC-TASK-002

### 审查材料交付（REVIEW）

这个功能域解决「材料交付方式错、手工构造密封包踩坑、长时调用被杀」：一律走既有正式通道，错误语义明确，无宿主期限控制面。

- **FR-REVIEW-001**：异源审查材料通过既有正式密封材料入口交付：材料构建 → 成组运行 → 第三方审查；禁止新打包工具、手工哈希、裸请求（不带材料的请求）。
  - **范围边界**：第二打包器不新增（D-011：打包辅助=复用既有正式密封材料入口，补诊断/文档）；通道复用为唯一路径
  - **依据**：R-019、R-020、D-008（子项③）、D-011；PFACT-006
  - **场景**：SCN-011
  - **验收**：AC-REVIEW-001

- **FR-REVIEW-002**：附件缺失、哈希不一致、冻结树错误、身份错误的审查请求保留正式错误码并标 unavailable，provider 只见 prompt 不见材料的裸请求被明确拒绝并给出原因诊断。
  - **范围边界**：失败事实如实保留（不可用不是通过）；unavailable 不降级为「无问题」，也不改写成普通缺失
  - **依据**：R-019、R-020、D-008（子项③）；PFACT-006
  - **场景**：SCN-011
  - **验收**：AC-REVIEW-002

- **FR-REVIEW-003**：公共审查 deadline=null（无宿主侧期限），审查生命周期归属第三方审查通道；不新增 host deadline/health/recovery/session 控制面；长时 provider 调用由执行者按文档后台运行，不被宿主短超时杀掉。
  - **范围边界**：broker 超时机制不新增（D-008 拒绝方案），长时语义只以文档与运行方式呈现
  - **依据**：R-021、D-008（子项④）；PFACT-007
  - **场景**：SCN-012
  - **验收**：AC-REVIEW-003

### 验证演练（DOGFOOD）

这个功能域定义「dogfood 怎么算成功」：一个正常简单任务跑通完整链，六信号可判，结果不是 gate。

- **FR-DOG-001**：交付前用 1 个正常简单任务从 make-decision 完整跑通并验证六个信号：①结束卡三要素齐全（大白话）；②需求全覆盖且无未处置；③Grill 出现同卡多题批次（编号+推荐项）；④分析字段（目标达成/方案收敛）有值；⑤Clarify 有真实触发痕迹或显式跳过理由；⑥非 Codex 宿主路径如实降级不伪造。不做对抗性运行；验证结果不是推进 gate。
  - **范围边界**：只跑正常简单任务（对抗运行不做，RISK-004）；术语统一为「验证/演练」；验证结果是质量事实，不是推进许可证
  - **依据**：D-009、T-015；PFACT-009、PFACT-014
  - **场景**：SCN-013
  - **验收**：AC-DOG-001

### 规格写作与模型执行防错（AUTHORING）

这个功能域落实 build-spec 执行端的防错合同：以严格生产形状为唯一写出权威，写后立即校验，同一错误同一参数不重复重试；宿主侧等价沙箱拒绝只按外部宿主事实登记，不在 WorkflowHub 内修宿主。

- **FR-AUTHORING-001**：build-spec 的新产出以严格生产形状为唯一权威：写入按生产 profile 的形状字段落盘；写入完成后、冻结之前立即运行现有的生产校验能力与验收最小要求，任何校验错误在当前步骤内修复后再继续；验收/接受侧的兼容读取（能读、能容忍缺项）不能作为通过或写出标准。
  - **范围边界**：不新增校验器、命令、gate 或状态机——只复用现有生产校验能力与验收最小要求；宿主侧等价沙箱拒绝属外部宿主事实，只停止循环、换路径、如实记录
  - **依据**：D-010；PFACT-016
  - **场景**：SCN-015
  - **验收**：AC-AUTHORING-001

- **FR-AUTHORING-002**：同一错误同一参数不得重复提交：首次失败必须先做失败分析；第二次必须切换到既有的写入通道或受控子代理重写，并完整保留原始失败事实；禁止不换写入者用相同参数盲重试，禁止把失败改写为通过。
  - **范围边界**：切换限于既有的写入通道/回调和受控子代理（不新增写入通道）；越界手段（新机制、新命令、新状态机）不得用于绕过失败；宿主侧等价沙箱拒绝同样按外部宿主事实处理
  - **依据**：D-010；PFACT-016
  - **场景**：SCN-014、SCN-015
  - **验收**：AC-AUTHORING-002

## 6. 模块划分

> 只写产品职责，不写实现类名。

### 收敛登记与矩阵

- **负责什么**：登记全部原始/追加需求，逐条处置并映射到决定，按五个维度标注覆盖情况。
- **对外提供什么**：需求→决定映射矩阵（含维度标注）与处置状态。
- **依赖谁**：决策日志结构、talk/grill 交互结果、方向审查结论。
- **测试边界**：无未处置行、五维度标注齐全、无相互矛盾决定可独立核验。

### 结束卡与呈现

- **负责什么**：以人话呈现核心需求、核心目标、已选方向摘要，并保持稳定编号仅内部追溯。
- **对外提供什么**：大白话结束卡三要素与其确认痕迹。
- **依赖谁**：收敛登记与矩阵、最终确认卡。
- **测试边界**：三要素齐全、无编号黑话、用户确认可核验。

### 收敛检查

- **负责什么**：分别判定覆盖、目标达成、验收清晰、方案收敛；build-spec 契约比对流程/状态/FR/AC/边界/非目标。
- **对外提供什么**：可复核的检查结论与缺口清单。
- **依赖谁**：决策日志、spec.md、operational 判定规则。
- **测试边界**：每项检查的正反例可独立验收，缺口不靠自报。

### 交互技能契约

- **负责什么**：talk 大白话、grill 同卡成组、clarify 判定与痕迹。
- **对外提供什么**：编号批次问题、真实回复、trigger 痕迹与理由。
- **依赖谁**：用户真实回复、上游 grill 标准。
- **测试边界**：同卡多题、依赖后置、无歧义显式跳过、回复缺失保持 incomplete 可独立验收。

### 宿主初始化与降级

- **负责什么**：非 Codex 宿主的能力检测、如实降级、fail-loud 区分；task/worktree/task store 先行初始化；worktree 确定性预检。
- **对外提供什么**：结构化 unavailable、真实错误、初始化与绑定结果。
- **依赖谁**：宿主能力事实、workspace 规则。
- **测试边界**：无 session、能力缺失、真实性错误、单边 conflict 可独立验收。

### 审查交付与验证演练

- **负责什么**：走既有正式密封通道交付审查材料；运行正常简单任务做六信号验证。
- **对外提供什么**：密封审查请求、正式错误码、unavailable 事实、dogfood 六信号结果。
- **依赖谁**：既有密封通道、第三方审查、验证任务。
- **测试边界**：附件/哈希/冻结树/身份错误、裸请求拒绝、deadline=null、六信号可判可独立验收。

## 7. 关键实体

> 只写影响行为的字段和关系；每个实体登记 owner、consumer 与保留/删除条件（无消费者的重复实体不新增）。

- **需求条目（Requirement）**：
  - **定义**：原始或追加的用户需求，是收敛的最小单元。
  - **字段和约束**：人话标题；内部锚点（机器追溯用）；处置状态（covered/deferred/rejected——非目标是分类而非处置状态；unknown 只表示质量事实缺失，不是处置状态）。
  - **关系**：一个需求可映射到多个决定；无处置状态的需求不允许静默存在。
  - **归属/消费者/保留删除**：owner=make-decision；consumer=spec-analyze 覆盖校验、spec/plan 来源映射；保留=当前材料生命周期内随需求矩阵保留，需求废弃须用户确认并记入决策日志。

- **需求→决定映射（Coverage Mapping）**：
  - **定义**：证明每个决定完整解决原始需求的覆盖记录。
  - **字段和约束**：需求锚点、决定锚点、五维度标注（业务目标/未来扩展影响/UI交互/验收目标/影响范围）、处置状态。
  - **关系**：未来扩展维度只标注影响，不做未来方案设计。
  - **归属/消费者/保留删除**：owner=make-decision；consumer=spec-analyze「方案收敛」检查、build-spec 来源映射；保留=随需求矩阵一致更新；删除=该需求关闭且无消费引用时移除行。

- **决定记录（Decision）**：
  - **定义**：一个被用户选择的方向及理由。
  - **字段和约束**：来源（真实回复）、理由、拒绝方案、后果与风险、未决项、supersedes 链。
  - **关系**：可被矩阵引用；supersedes 链闭合证明收敛。
  - **归属/消费者/保留删除**：owner=make-decision；consumer=spec/plan 依据引用、spec-analyze 决定链校验；保留=历史原文只读保留；删除=不删除，被 supersede 时保留原文并指向新决定。

- **结束卡（Completion Card）**：
  - **定义**：make-decision 收尾时给用户的白话总结（三要素：核心需求、核心目标、已选方向摘要）。
  - **字段和约束**：核心需求、核心目标、已选方向摘要；不带稳定编号黑话。
  - **关系**：用户确认后阶段收尾；detail 后如方向细化再刷新。
  - **归属/消费者/保留删除**：owner=make-decision；consumer=阶段收尾确认、结束卡呈现；保留=当前材料生命周期内保留；删除=被 detail 后刷新版本替代，历史快照只读。

- **Grill 批次（Grill Batch）**：
  - **定义**：同一轮同卡展示的全部独立问题集合。
  - **字段和约束**：每题编号、决策轴、推荐项；依赖题后置；无最少题数限制。
  - **关系**：一批对应一轮 frontier；用户回复后进下一轮。
  - **归属/消费者/保留删除**：owner=make-decision 执行者；consumer=grill_summary 与 stage-end 检查、spec-analyze 批次痕迹核验；保留=随对话记录与决策日志保留；删除=轮次关闭后只读归档。

- **Clarify 痕迹（Clarify Trace）**：
  - **定义**：build-spec 中 Clarify 是否触发及结果的记录。
  - **字段和约束**：trigger（true/false）、批次与问题、理由、开放项计数、答复来源。
  - **关系**：痕迹被 stage-end 检查引用；回复缺失保持 incomplete。
  - **归属/消费者/保留删除**：owner=build-spec 执行者；consumer=stage-end 检查、spec-analyze Clarify 契约核验；保留=绑定真实批次痕迹；删除=不删除，回复缺失时保持 incomplete 不伪造。

- **宿主能力事实（Host Capability Fact）**：
  - **定义**：宿主支持哪些初始化与绑定能力的客观事实。
  - **字段和约束**：session 可用性、task store 可用性、依赖可用性、能力是否注册；结果可为可用/unavailable。
  - **关系**：能力缺失走降级，真实性错误走 fail-loud，两类互不替代。
  - **归属/消费者/保留删除**：owner=非 Codex 宿主初始化路径；consumer=初始化/降级决策、AC-HOST-001/002 验收；保留=每次调用时重新探测；删除=不落持久存储，unavailable 随会话记录保留。

- **审查交付件（Review Package）**：
  - **定义**：经正式密封通道交付给第三方审查的请求与材料。
  - **字段和约束**：密封附件、哈希、冻结树、身份；错误保留正式错误码并标 unavailable；公共审查 deadline=null。
  - **关系**：生命周期归属第三方审查通道；不新增 host 期限控制面。
  - **归属/消费者/保留删除**：owner=审查执行者（复用既有正式入口，不新增第二工具，D-011）；consumer=第三方审查通道、review 处置表；保留=失败事实随审查记录保留（含正式错误码）；删除=按审查记录生命周期归档，不写入 current 材料。

## 8. 数据和生命周期

- **数据粒度**：一条需求、一个决定、一个需求→决定映射行、一个 grill 批次、一条 Clarify 痕迹、一条宿主能力事实、一个审查交付件。
- **数据时效**：映射矩阵在每轮 talk/grill 收敛后更新；结束卡在阶段收尾生成、detail 方向细化后刷新；宿主能力事实在每次调用时探测；审查交付件在请求发出时产生。
- **缺失或迟到**：宿主能力缺失记录为 unavailable 并正常退出；审查失败保留正式错误码与 unavailable；Clarify 回复缺失保持 incomplete；绑定失败不回滚已初始化内容。
- **预览与正式**：矩阵与结束卡在确认前为草稿，用户确认后为正式当前材料；历史记录只读保留，不作为新 writer。
- **当前与历史**：当前工作真相只由决策日志、spec、plan、tasks 四份材料承载；旧记录、旧审查、历史快照只读保留，不覆盖、不迁移、不修复。
- **归属与清理**：矩阵与结束卡归 make-decision 持有；spec 由 build-spec 持有并只细化同一份材料；审查生命周期归第三方审查通道；降级与失败事实随当前材料保留，不伪造补齐。

### 生命周期与成功/失败语义

- **需求处置**：每条需求锁定为 covered/deferred/rejected 之一（unknown 只是质量事实缺失，不是处置状态）；非目标先分类、经上游授权后锁定处置；无处置行保持可见，不允许静默消失（SCN-001/002）。
- **宿主初始化与降级**：能力探测成功则继续；宿主能力缺失/未注册按如实 unavailable 正常退出；真实参数/材料/身份错误 fail-loud（两类语义互不替代，见 SCN-008）；初始化先于 session 绑定（SCN-009）；「绑定失败/会话来源不可用不回滚已创建任务」验收单点归属 AC-TASK-002（见 FR-TASK-002）。
- **worktree 预检**：全无→创建→复验；全有→复验一致；单边存在→标 conflict 停下报告，人工处置（SCN-010）。
- **Clarify**：有实质歧义时 ask→wait→reply→resume 真实流转，回复缺失/中断保持 incomplete；无歧义显式 trigger=false + 理由 + 零开放项（SCN-007）。
- **审查交付**：材料构建→成组运行→第三方审查；附件/哈希/冻结树/身份错误保留正式错误码并标 unavailable；公共审查无宿主 deadline（SCN-011/012）。

## 9. 兼容性预留

- **既有消费方**：spec-analyze 既有的编号覆盖硬校验保持兼容（人话化只动呈现，不动校验链）；CONTEXT 正式名「用户完成卡」定义并入结束卡三要素（在 PFACT-011 说明一次），单一术语，不制造双术语。
- **命名预留**：稳定编号（需求/决定锚点）保持稳定且只作内部追溯，后续可渲染隐藏不改编号语义；host 控制面不新增命名空间。
- **容器预留**：需求→决定矩阵可容纳新增需求行与新增维度标注，不改变已有行的处置语义；批次痕迹结构可容纳多轮记录。
- **状态预留**：unavailable（降级）、fail-loud（真实错误）、incomplete（中断/质量缺失）、conflict（单边存在）作为独立语义保留，后续可增加受控原因码但不得改旧语义。
- **扩展边界**：交互证据机器强绑定与对抗性用例延期到后续治理任务（OPEN-003/RISK-003）；上游 grill 标准后续演进只影响引用 pin 与仪式，不改变「同卡多题、依赖后置、无最少题数」的产品行为。

## 10. 明确不做与默认必须成立

### 明确不做

> 权威来源：decision-log「非目标」节（T-011 确认清单与各决定拒绝方案）；下列各条分别标注其真实确认来源，不把未经确认条目归给 T-011。

- 不新增第五份当前材料（决策日志、spec、plan、tasks 四份边界不变）。（来源：决策 D-001/D-003，T-011 确认）
- 不新增任何新的公共命令。（来源：T-011 确认）
- 不新增独立状态机。（来源：T-011 确认；本规格 §3 流程状态与恢复已合并进场景成功/失败语义）
- 不新增任何机器 gate；不做机器全量硬校验、不做 grill 每批 ≥2 题强门。（来源：D-002、D-004 拒绝方案，T-011 确认）
- 不做 GUI/UI 产品界面；不改宿主渲染层。（来源：T-011 确认，本任务是无产品页面的流程/技能治理）
- 不重写 wh-review、不重写 3rd-review、不改 dsh-code-review、不改 verify-code。（来源：T-011 确认不改 dsh-code-review、不重写 wh-review；不重写 3rd-review 为 D-008/D-011 复用既有正式密封通道、不新增第二工具的边界；verify-code 未被 T-011 确认，按本规格流程/技能/契约范围保持现状）
- 不做真实端到端故意不收敛的对抗性 dogfood（单元/契约级负向样本回放属本任务验收范围，见 AC-ANALYZE-001）。（来源：T-015 用户选择；RISK-004）
- 不为不存在的未来设计完整扩展方案（未来扩展只标影响不设计）。（来源：G-001）
- 不新增 host deadline/health/recovery/session 控制面。（来源：decision-log D-008（子项④）长时调用走文档语义、拒绝「改 broker/宿主超时配置（F10）」；PFACT-007 deadline=null 事实）
- 简单性处理删除候选机制：第二打包器、无-task-store 的材料直写分支、broker 超时机制（来源：D-008、D-011）；写入校验一律复用现有生产校验能力与验收最小要求，不新增写入校验器、写入门或重试状态机。（来源：D-010）
- 不新增写入校验器、写入门、重试状态机或新 gate；宿主侧等价沙箱拒绝不在 WorkflowHub 内修（只停止循环、换路径、如实记录）。（来源：D-010）
- 不在本任务做交互证据的完全机器强绑定与端到端对抗性测试用例，延期到后续治理任务。（来源：D-005、RISK-003）
- 不手工创建 TaskHandle、不把任务清单（task.json）放进 worktree、不新增公共任务启动命令（public bootstrap command）、不新增第二启动机制或启动/发布/绑定/回滚控制面；一律复用现有官方启动入口。（来源：D-012，task-handle-bootstrap-scope=A 用户真实确认）

### 默认必须成立

- 质量事实不是推进许可证：unavailable/incomplete 不能改写成通过或成功。（FR-DOG-001、AC-DOG-001）
- 有实质歧义才问、无歧义显式跳过并给理由；不猜答、不换第二套 Clarify。（FR-CLARIFY-001、FR-CLARIFY-002、AC-CLARIFY-001、AC-CLARIFY-002）
- 未来扩展只标影响不设计方案。（FR-CONV-002、AC-CONV-002）
- 用户可见面人话为主，稳定编号仅内部追溯且校验可回指。（FR-CONV-004、AC-CONV-004）
- 初始化先于 session 绑定（FR-HOST-003、AC-HOST-003）；绑定失败/会话来源不可用不回滚已创建任务（FR-TASK-002、AC-TASK-002 单点）；worktree 单边存在标 conflict 不自动修复。（FR-HOST-004、AC-HOST-004）
- 降级（unavailable）与真实错误（fail-loud）语义互不替代，真实错误不得被降级吞掉。（FR-HOST-002、AC-HOST-002）
- 审查材料只走既有正式密封通道，错误保留正式错误码并标 unavailable。（FR-REVIEW-001、FR-REVIEW-002、AC-REVIEW-001、AC-REVIEW-002）
- 验证/演练结果是质量事实，不是 gate。（FR-DOG-001、AC-DOG-001）
- 严格生产形状是新产出的唯一写入权威，兼容读取不能作为写出标准；写后立即校验、同一错误同一参数不重复重试。（FR-AUTHORING-001、FR-AUTHORING-002、AC-AUTHORING-001、AC-AUTHORING-002）
- worktree 单独存在不等于任务已初始化；正式阶段开始前先确认/创建任务身份（任务目录缺失走官方只创建发布、已有工作副本显式现有绑定只校验不覆盖）；会话来源不可用不回滚已创建任务，真实错误 fail-loud。（FR-TASK-001、FR-TASK-002、AC-TASK-001、AC-TASK-002）

## 11. 验收标准

- [ ] **AC-CONV-001**：需求逐条处置且决定可见
  - **需求**：FR-CONV-001
  验证：矩阵完整性与处置状态核验（拿原始/追加需求清单对照矩阵行）
  - **通过条件**：全部原始/追加需求在矩阵中有处置状态且对应决定可见，无静默遗漏行
  - **失败条件**：存在无处置状态的需求行，或需求处置存在但对应决定不可见
  - **证据类型**：`evidence`

- [ ] **AC-CONV-002**：五维度覆盖与未来扩展边界
  - **需求**：FR-CONV-002
  验证：矩阵维度标注抽查 + 未来扩展表述检查
  - **通过条件**：每条映射行五个维度都有标注位置；未来扩展只出现「影响标注」不出现「未来方案设计」
  - **失败条件**：任一维度缺失标注，或 matrix 中出现完整的未来扩展方案设计
  - **证据类型**：`evidence`

- [ ] **AC-CONV-003**：结束卡三要素且为白话
  - **需求**：FR-CONV-003
  验证：结束卡对照检查（人工阅读 + 三要素清单核验）
  - **通过条件**：核心需求、核心目标、已选方向摘要齐全且为中文大白话；用户确认后才收尾
  - **失败条件**：三要素任一缺失、出现编号黑话，或未确认即宣称阶段收尾
  - **证据类型**：`manual`

- [ ] **AC-CONV-004**：人话为主、内部锚点可回指
  - **需求**：FR-CONV-004
  验证：呈现层检查 + 覆盖校验链回放（既有编号硬校验点）
  - **通过条件**：用户可见面无稳定编号黑话；内部锚点保留且覆盖校验链全部通过
  - **失败条件**：用户可见面出现编号黑话，或移除锚点导致覆盖校验链断裂
  - **证据类型**：`evidence`

- [ ] **AC-GRILL-001**：同卡多题、依赖后置、无最少题数
  - **需求**：FR-GRILL-001
  验证：grill 输出记录检查 + 轮次回放
  - **通过条件**：每轮输出暴露编号后的完整批次（每题一轴+推荐项）；依赖题后置；同批无最少题数强制；等真实回复才进下一轮
  - **失败条件**：一轮只呈现单题、依赖题前置、强制每批 ≥2 题，或未等回复即进下一轮
  - **证据类型**：`evidence`

- [ ] **AC-ANALYZE-001**：四项收敛检查分别可判
  - **需求**：FR-ANALYZE-001
  验证：单元/契约级正反例回放（构造覆盖缺失/目标未回应/验收不可判/方案未收敛四类负向样本 + 正向样本）——负向样本回放属于本任务验收范围
  - **通过条件**：四项分别给出可复核结论，判定依据操作性规则且可由异源核验；负向样本按预期暴露缺口
  - **失败条件**：四项被合并成一个存在性检查、只看文件/ID 存在即放行、判定全靠自报，或负向样本被当成端到端对抗 dogfood 而延期（后者只限真实端到端故意不收敛，见 RISK-004）
  - **证据类型**：`test`

- [ ] **AC-ANALYZE-002**：build-spec 契约比对可定位缺口
  - **需求**：FR-ANALYZE-002
  验证：build-spec profile 对当前 spec.md 运行并与需求/决策日志比对
  - **通过条件**：流程、状态、FR、AC、边界、非目标逐项可对照；缺口列出并可定位到具体条目
  - **失败条件**：遗漏非目标、边界或 FR/AC 对应关系，或缺口无定位
  - **证据类型**：`test`

- [ ] **AC-CLARIFY-001**：实质歧义真实 ask-wait-reply-resume
  - **需求**：FR-CLARIFY-001
  验证：Clarify 批次痕迹核验 + 回复流转检查
  - **通过条件**：有实质歧义时同卡呈现独立问题并等真实回复；回复到达后继续；回复缺失/中断保持 incomplete
  - **失败条件**：跳过提问直接猜答、不等回复、无歧义也触发、中断后被推断为完成
  - **证据类型**：`evidence`

- [ ] **AC-CLARIFY-002**：无歧义显式跳过且零开放项
  - **需求**：FR-CLARIFY-002
  验证：无歧义样本的 trigger 记录检查
  - **通过条件**：无歧义时显式 trigger=false + 理由 + 零开放项；痕迹可被 stage-end 引用
  - **失败条件**：无歧义时静默跳过无理由，或开放项非零仍宣称无歧义
  - **证据类型**：`evidence`

- [ ] **AC-HOST-001**：无 session 时结构化 unavailable 正常退出
  - **需求**：FR-HOST-001
  验证：非 Codex 宿主（无 session）环境调用私有 session 事件命令回放
  - **通过条件**：返回结构化 unavailable 并正常退出，阶段按降级路径继续，不伪造成功
  - **失败条件**：无 session 时命令崩溃阻塞任务，或把 unavailable 改写成成功
  - **证据类型**：`evidence`

- [ ] **AC-HOST-002**：真实错误 fail-loud 且不被降级吞掉
  - **需求**：FR-HOST-002
  验证：分别构造宿主能力缺失与真实参数/材料/身份错误两类输入回放
  - **通过条件**：仅能力缺失/未注册才降级；真实错误 fail-loud 并指出错误范围
  - **失败条件**：真实错误被降级语义吞掉（unavailable 掩盖错误），或能力正常时也误报错误
  - **证据类型**：`test`

- [ ] **AC-HOST-003**：初始化先于绑定（不回滚语义单点归 AC-TASK-002）
  - **需求**：FR-HOST-003
  验证：新任务初始化顺序回放（绑定失败后状态检查由 AC-TASK-002 单点覆盖，避免 HOST/TASK 双写）
  - **通过条件**：task/worktree/task store 初始化先于 session 绑定
  - **失败条件**：初始化依赖绑定先行（顺序颠倒）
  - **证据类型**：`test`

- [ ] **AC-HOST-004**：worktree 三态确定性处理
  - **需求**：FR-HOST-004
  验证：全无、全有、单边存在三类 worktree 状态回放
  - **通过条件**：全无创建、全有复验路径/分支一致、单边存在标 conflict 并停下报告
  - **失败条件**：单边存在被自动修复，或全有时被误判重建
  - **证据类型**：`test`

- [ ] **AC-REVIEW-001**：只走既有正式密封通道
  - **需求**：FR-REVIEW-001
  验证：审查请求构造路径检查（材料构建 → 成组运行 → 第三方审查）
  - **通过条件**：请求全部经既有密封通道携带材料发出；无新打包工具、无手工哈希、无裸请求
  - **失败条件**：出现第二打包器、手工构造哈希，或发出不带材料的裸请求
  - **证据类型**：`evidence`

- [ ] **AC-REVIEW-002**：错误保留正式错误码并标 unavailable
  - **需求**：FR-REVIEW-002
  验证：附件缺失、哈希不一致、冻结树错误、身份错误四类请求回放
  - **通过条件**：每类错误保留正式错误码并标 unavailable，错误事实如实保留
  - **失败条件**：错误被改写成成功/无问题，或 provider 只见 prompt 不见材料的裸请求未被拒绝
  - **证据类型**：`test`

- [ ] **AC-REVIEW-003**：无宿主期限控制面、生命周期归第三方审查
  - **需求**：FR-REVIEW-003
  验证：公共审查请求属性检查 + 长时调用运行方式检查
  - **通过条件**：公共审查 deadline=null；生命周期归属第三方审查通道；无 host deadline/health/recovery/session 控制面；长时调用按文档后台运行
  - **失败条件**：存在宿主侧期限/控制面，或长时调用被宿主短超时杀掉而无规避语义
  - **证据类型**：`evidence`

- [ ] **AC-DOG-001**：正常简单任务六信号可判且结果非 gate
  - **需求**：FR-DOG-001
  验证：一个正常简单任务从 make-decision 跑到交接，逐项核对六信号
  - **通过条件**：结束卡三要素齐全、需求全覆盖、Grill 同卡多题批次、分析字段有值、Clarify 有痕迹、非 Codex 路径如实降级；六信号全部可判；结果不阻断推进
  - **失败条件**：任一信号缺失或不可判、结果为 blocking gate、或真实端到端故意不收敛的对抗运行被纳入（单元/契约级负向回放归 AC-ANALYZE-001，不在本 AC）
  - **证据类型**：`evidence`

- [ ] **AC-AUTHORING-001**：生产形状权威 + 写后即时校验
  - **需求**：FR-AUTHORING-001
  验证：写入完成后、冻结前立即运行生产 profile 校验与验收最小要求回放；构造缺项/多份形状材料样本
  - **通过条件**：形状字段逐项命中生产 profile；任何校验错误在当前步骤内修复后才继续；兼容读取不被作为通过
  - **失败条件**：缺项被凭兼容读取放行、校验错误留到下一步骤才发现、或校验失败被忽略仍声称完成
  - **证据类型**：`evidence`

- [ ] **AC-AUTHORING-002**：同一错误同一参数不重复重试
  - **需求**：FR-AUTHORING-002
  验证：同一错误同一参数重复发生样本回放（含宿主侧等价沙箱拒绝样本）
  - **通过条件**：首次失败有失败分析；第二次切换到既有写入通道或受控子代理；原始失败事实保留；宿主等价沙箱拒绝记录为外部宿主事实（停止循环、换路径、如实登记）
  - **失败条件**：同参盲重试≥2 次、不切换写入者、把失败改写为通过，或把宿主拒绝标为 WorkflowHub 可修问题
  - **证据类型**：`test`

- [ ] **AC-TASK-001**：worktree 存在但任务目录缺失时经官方只创建通道发布任务，且官方启动成功事实完整
  - **需求**：FR-TASK-001
  验证：构造「既有 worktree 且受信任存储根下任务目录缺失」样本，回放阶段启动前的任务身份检查与发布；历史缺失样本=本任务官方启动前的状态（worktree 已建、任务目录与任务清单缺失），当前成功修复样本=官方只创建启动后任务目录已创建、以复用现有工作副本方式完成绑定且既有材料未被覆盖的现状；补构造「create-only 发布后、工作区/存储准备阶段中途失败」样本，验证半创建目录不被视为已初始化
  - **通过条件**：能区分「worktree 存在」与「任务已初始化」两种状态；任务目录缺失时经现有官方启动通道以只创建方式发布/打开任务目录，不手写任务清单（task.json）；官方启动成功必须同时满足 task.json + index.json + facts.jsonl + quality/ 全部存在，缺失任一即为失败、不得视为已初始化
  - **失败条件**：把 worktree 存在误判为任务已初始化而跳过发布；只创建发布覆盖既有材料；出现手写任务清单/非官方启动通道；或把 task.json 单独存在但 store 不完整的半创建目录当作成功
  - **证据类型**：`test`

- [ ] **AC-TASK-002**：既有 worktree 显式现有绑定不覆盖、会话来源不可用不回滚、真实错误 fail-loud，且半创建目录按真实失败处理
  - **需求**：FR-TASK-002
  验证：构造「已认证 worktree 显式现有绑定」「会话来源不可用」「真实参数/材料/身份错误」三类样本回放；历史缺失样本与当前成功修复样本均可回放：本任务官方启动前任务目录缺失，启动后任务目录已创建、现有绑定复用工作副本且既有材料未被覆盖，启动输出中的会话来源冲突仅为辅助溯源异常；补构造「task.json 存在但 index/facts/quality 缺失」的半创建目录样本，验证其不被视为已初始化
  - **通过条件**：显式现有绑定只校验不覆盖既有材料；任务存储初始化后会话来源不可用不回滚已创建任务目录；真实性错误 fail-loud 且不被会话来源不可用掩盖；task.json 单独存在但 store 不完整时按真实失败事实处理，不视为成功初始化，也不继续后续阶段
  - **失败条件**：显式绑定覆盖既有材料；把会话来源不可用当作回滚/删除已创建任务的理由；真实性错误被降级成 unavailable 吞掉；或把半创建目录当作已初始化而继续运行
  - **证据类型**：`test`

## 12. 风险、未决与交接

- **RISK-001**：本任务范围过大（四个技能 + 流程 + 契约 + 执行阻塞修复）
  - **受影响 ID**：FR-CONV-001、FR-HOST-001、FR-REVIEW-001、FR-DOG-001
  - **触发条件**：实现阶段开始扩范围（补第五材料、新命令、新控制面）
  - **后果**：范围膨胀、维护成本上升、非目标被突破
  - **缓解或 STOP**：窄改边界写死（第 10 节）；触发即停止并回到 decision-log
  - **处理 Stage**：`build-spec`
  - **验证**：本规格每个 FR/AC 可映射回 23 条需求，无越界项

- **RISK-002**：认证 worktree 不含 node_modules，后续阶段跑测试需要安装依赖或复用主仓库环境
  - **受影响 ID**：AC-HOST-003、AC-HOST-004、AC-DOG-001
  - **触发条件**：build-plan/build-code 在 worktree 内直接运行测试
  - **后果**：验证成本上升、环境不一致
  - **缓解或 STOP**：复用主仓库环境或按计划安装依赖；装不上则保持 incomplete
  - **处理 Stage**：`build-plan`
  - **验证**：测试运行环境在计划中显式声明

- **RISK-003**：交互证据机器强绑定（Clarify outcome 完全机器化、判定字段异源自动核验）与机制「咬人」对抗性用例延期
  - **受影响 ID**：FR-CLARIFY-002、FR-ANALYZE-001、FR-DOG-001、PFACT-014
  - **触发条件**：执行层遵循度下降或检查未覆盖真实违规
  - **后果**：判定偏差仍可能、检查「咬人」能力不可判
  - **缓解或 STOP**：本任务写死操作性判定标准 + 可复核痕迹；机器强绑定与对抗用例交后续治理任务（OPEN-003 关闭条件）
  - **处理 Stage**：`build-plan`
  - **owner**：后续 WorkflowHub 治理任务（延期交接，不在本任务落地）
  - **验证**：后续任务落地后重新核验判定字段与对抗用例

- **RISK-004**：真实端到端故意不收敛的对抗 dogfood 延期，dogfood 只证明 happy path
  - **受影响 ID**：FR-DOG-001、AC-DOG-001、FR-ANALYZE-001
  - **触发条件**：把 dogfood 结果误读为机制在任何违规下都会失败
  - **后果**：对机制端到端「咬人」能力产生虚假信心（单元/契约级负向回放已在本任务验收范围，见 AC-ANALYZE-001）
  - **缓解或 STOP**：dogfood 定位为验证/演练不是 gate；端到端「咬人」由后续治理任务覆盖（并入 RISK-003 关闭条件）
  - **处理 Stage**：`build-plan`
  - **owner**：后续 WorkflowHub 治理任务（延期交接）
  - **验证**：验证报告只声明正常简单任务结果，不声明端到端对抗性结论

- **RISK-005**：降级（unavailable）语义被滥用于掩盖真实参数/材料/身份错误
  - **受影响 ID**：FR-HOST-002、AC-HOST-002
  - **触发条件**：执行者把无法查明的真实错误记为 unavailable 而非 fail-loud
  - **后果**：真实故障被静默吞掉，问题在后续阶段放大
  - **缓解或 STOP**：fail-loud 边界写死（FR-HOST-002）；审查材料错误保留正式错误码（FR-REVIEW-002）
  - **处理 Stage**：`build-code`
  - **验证**：AC-HOST-002、AC-REVIEW-002 两类输入回放均按语义到达正确结局

- **RISK-006**：官方启动入口在 create-only 发布后、工作区/存储准备阶段中途失败，可能留下半创建目录并令重试被 create-only 冲突卡住（登记自决策日志「任务身份与启动前置诊断事实」节隔离复现边界风险；该缺陷在本次官方 bootstrap 成功执行后未实际发生，属待评估缺陷，非本次成功运行失败）
  - **受影响 ID**：FR-TASK-001、FR-TASK-002、AC-TASK-001、AC-TASK-002
  - **触发条件**：官方启动中途失败后把半创建目录当作成功继续，或重试仍走 create-only 发布
  - **后果**：重试被 create-only 冲突卡住；半创建目录被误判为任务已初始化
  - **缓解或 STOP**：官方启动成功/完整的事实必须同时满足 task.json + index.json + facts.jsonl + quality/ 全部存在；缺失任一即判为真实失败（fail-loud），不得视为 unavailable 成功或已初始化；build-plan/build-code 实现中遇到 task.json 存在但 store 不完整时，必须停止并记录失败事实，操作员可决策复用现有官方入口（open + initializeTaskStore 幂等重入）或人工检查，但不得新增自动恢复状态机、回滚机制、第二启动路径或公共命令（D-012 边界）
  - **处理 Stage**：`build-plan`
  - **验证**：AC-TASK-001/002 验收半创建目录不被视为已初始化，且中途失败样本按真实失败事实登记

### OPEN cards（均为延期/交接机制，不是 material ambiguity 开放项；material ambiguity=0）

- **OPEN-003**：Clarify outcome 与收敛检查字段判定的完全机器化强绑定（延期交接机制，不阻塞本规格草稿；material ambiguity=0）
  - **受影响 ID**：FR-CLARIFY-002、FR-ANALYZE-001、PFACT-014
  - **owner**：后续 WorkflowHub 治理任务
  - **影响**：执行层依赖遵循；判定误报无法被机器兜底拦截
  - **处理 Stage**：`build-plan`
  - **关闭条件或 STOP**：后续任务落地机器强绑定且对抗用例通过后关闭；本任务不得为此新增机器 gate（F10/T-008 已拒绝）

## 13. 业务影响与回归范围

### make-decision 流程

- **既有行为**：需求收敛随机，想到什么问什么；需求可能全程未被问过。
- **本需求影响**：需求登记进矩阵逐条处置，每个决定可映射回需求与五维度；阶段收尾有大白话结束卡。
- **回归路径**：Talk/grill/approve/stage-end 全流程、需求新增与处置、结束卡确认。
- **验收**：AC-CONV-001、AC-CONV-002、AC-CONV-003、AC-GRILL-001

### 收敛检查与 build-spec 契约

- **既有行为**：spec-analyze 只查基础文档存在；build-spec 的 Clarify 判定凭执行者。
- **本需求影响**：make-decision profile 分别判定覆盖/目标达成/验收清晰/方案收敛；build-spec profile 比对流程/状态/FR/AC/边界/非目标；Clarify 有歧义必触发、无歧义显式跳过。
- **回归路径**：既有编号覆盖校验、stage-end 分析、Clarify 触发与跳过、分析字段有值。
- **验收**：AC-ANALYZE-001、AC-ANALYZE-002、AC-CLARIFY-001、AC-CLARIFY-002

### 非 Codex 宿主初始化与 worktree 预检

- **既有行为**：非 Codex 宿主调用 session 事件直接失败且无 task 绑定；worktree 漏建由用户纠正。
- **本需求影响**：无 session 时结构化 unavailable 正常退出；真实错误 fail-loud；初始化先于绑定（「不回滚」验收单点归 TASK 域 AC-TASK-002，与 FR-HOST-003 分工见第 5 节）；worktree 三态确定性预检。
- **回归路径**：无 session、能力缺失、真实性错误、task store 缺失、worktree 全无/全有/单边存在。
- **验收**：AC-HOST-001、AC-HOST-002、AC-HOST-003、AC-HOST-004

### 审查材料交付

- **既有行为**：手工构造密封包反复踩坑（哈希不一致、裸请求失败）；provider 长时调用被宿主超时杀掉。
- **本需求影响**：一律走既有正式密封通道；附件/哈希/冻结树/身份错误保留正式错误码并标 unavailable；公共审查无宿主期限控制面；长时调用按文档后台运行。
- **回归路径**：direction/detail 审查、裸请求拒绝、错误码与 unavailable、长时后台运行。
- **验收**：AC-REVIEW-001、AC-REVIEW-002、AC-REVIEW-003

### 验证演练

- **既有行为**：无 dogfood 或 dogfood 无观察标准。
- **本需求影响**：一个正常简单任务六信号全部可判；结果不是 gate。
- **回归路径**：结束卡、覆盖、Grill 批次、分析字段、Clarify 痕迹、非 Codex 降级六信号。
- **验收**：AC-DOG-001

### 规格写作与模型执行防错

- **既有行为**：模型写规格靠试错——同参反复重试、大规格写完才发现生产形状不符、依赖子代理才能落盘。
- **本需求影响**：严格生产形状为新产出唯一权威；写入后、冻结前立即运行现有生产校验与验收最小要求；同一错误同一参数不重复重试、第二次换既有写入通道或受控子代理并保留原始失败；宿主侧等价沙箱拒绝只登记边界。
- **回归路径**：规格写入、冻结前校验、失败重试与写入者切换、宿主拒绝记录。
- **验收**：AC-AUTHORING-001、AC-AUTHORING-002

### 任务身份与启动前置

- **既有行为**：worktree 检出存在被当作任务已初始化，任务目录缺失时找不到任务清单；会话来源不可用时误回滚已创建内容。
- **本需求影响**：正式阶段开始前先确认/创建任务身份；任务目录缺失走官方只创建发布；已有 worktree 显式现有绑定只校验不覆盖；会话来源不可用不回滚；真实错误 fail-loud。
- **回归路径**：阶段启动检查、任务目录发布、显式现有绑定、会话来源不可用、真实输入错误。
- **验收**：AC-TASK-001、AC-TASK-002

- **可能受冲击的业务规则**：五阶段固定边界、四材料唯一权威、质量事实不是推进许可证、unavailable/incomplete 不伪造成通过、未来扩展只标影响不设计、无新增 gate/状态机/公共命令。
- **明确无影响**：GUI/UI 产品界面、wh-review 与 3rd-review 的重写、verify-code 行为、第五份材料、宿主渲染层、既有密封通道内部实现。

## Spec Clarification Record

- **batch**：第 1 批，共 1 问；trigger=true；open material ambiguity=0
- **axis**：非 Codex 宿主（无 session）时私有 session 事件的行为语义
- **classification**：new ambiguity（材料歧义）
- **source**：build-spec spec-clarify executed（真实问答批次）；用户真实回复
- **answer**：`非 Codex 宿主无 session 时，私有 session-event 返回结构化 unavailable 并正常退出；仅宿主能力缺失/未注册才降级，真实参数/材料/身份错误必须 fail-loud`
- **affected**：SCN-008、PFACT-008、FR-HOST-001、FR-HOST-002、AC-HOST-001、AC-HOST-002
- **consequence**：宿主初始化契约锁定——可正常退出的 unavailable 与必须 fail-loud 的真实错误分属两类语义，不得混用；无-task-store 材料直写模式由范围修正 D-011 删除（task/worktree/task store 已在 session 绑定前初始化，无该分支），不作为降级形态
- **risk**：降级被滥用于掩盖真实错误；由 FR-HOST-002 / AC-HOST-002 的 fail-loud 边界兜底
- **superseded wording**：任何「非 Codex 宿主一律失败或一律降级」「unavailable 可掩盖真实错误」的未确认推断

- **batch**：第 2 批（scope revision 延展批）；trigger=true（两问两答）；open material ambiguity=0
- **axis1**：scope-revision-task-store——无 task store 时是否保留「材料直写」分支（对应 D-008 子项① 直写片段）
- **axis2**：scope-revision-review-helper——「可复用打包辅助」是新增第二工具还是复用现有正式入口（对应 D-008 子项③ 打包片段）
- **classification**：scope revision Q&A（真实问答两轴，均用户真实回复）
- **source**：build-spec 本规格修订 + scope-revision 真实回复（锚点 scope-revision-task-store / scope-revision-review-helper）
- **answer**：①直写分支删除——task/worktree/task store 已在 session 绑定前初始化，无 task store 起点不存在，用户答 A；②打包辅助=复用既有正式密封材料入口（材料构建 → 成组运行 → 第三方审查）并补诊断/文档，不新增第二工具，用户答 A
- **affected**：D-011、FR-HOST-001、FR-HOST-003、FR-REVIEW-001、FR-REVIEW-002、SCN-009、SCN-011、AC-HOST-001、AC-HOST-003、AC-REVIEW-001、AC-REVIEW-002
- **consequence**：本批为 scope revision 真实问答批（两问均 trigger=true 且用户答 A），不重算第 1 批结论；D-011 已锁定，来源映射与 FR/AC 表述随本批更新
- **risk**：无新增歧义（material ambiguity=0）；若把「复用正式入口」实现成第二工具则违反 D-011，由 AC-REVIEW-001/002 验收兜底
- **superseded wording**：任何「无 task store 时材料直写仍被保留」「可复用打包辅助=新增第二工具」的表述（D-008 两片段已被 D-011 修正）

- **batch**：第 3 批（scope revision 延展批）；trigger=true（1 问 1 答）；open material ambiguity=0
- **axis**：task-handle-bootstrap-scope——worktree 存在但任务目录缺失的根因与修复范围（对应 R-023/D-012）
- **classification**：scope revision Q&A（真实问答，用户真实回复）
- **source**：build-spec 本规格修订 + 用户真实确认（锚点 task-handle-bootstrap-scope=A）
- **answer**：用户选择 A——纳入当前需求并修复：严格区分 worktree 与任务身份/任务存储；正式阶段开始前经官方启动通道在受信任存储根发布/打开任务目录（缺失走只创建发布）；已有 worktree 用显式现有绑定复用、只校验不覆盖；不手写任务清单；会话来源不可用不回滚已创建任务；缺目录根因（启动漏执行）记录并修复
- **affected**：D-012、FR-TASK-001、FR-TASK-002、SCN-016、AC-TASK-001、AC-TASK-002、PFACT-017
- **consequence**：本批为第 3 批真实 scope revision 问答（trigger=true，1 问，用户答 A），不覆盖第 1/2 批结论；TASK 域已登记；当前任务已完成官方初始化（官方只创建启动已成功创建任务目录并绑定现有 worktree），但后续阶段仍必须重复确认/打开任务身份，实现契约仍属后续阶段
- **risk**：无新增材料歧义（open material ambiguity=0）；当前任务已完成官方初始化，但后续阶段仍必须重复确认/打开任务身份，不得因已有一次初始化而跳过，也不得手写任务清单；若实现时手写任务清单或新增公共启动命令则违反 D-012，由 AC-TASK-001/002 验收兜底
- **superseded wording**：任何「worktree 存在即任务已初始化」「任务目录缺失由会话绑定失败回滚造成」「手写任务清单可替代官方启动」的未确认推断

## Spec Research Record

- **SRES-001**：executed；上游 grill round/frontier 标准与本地差异核验（决策日志调研 F-001；上游 2026-07-31 反转推翻「一次一问」）→ PFACT-003、FR-GRILL-001
- **SRES-002**：executed；spec-analyze 现状四维审计（决策日志调研 F-004；覆盖部分自报、目标达成无判定、验收部分、方案收敛缺失）→ PFACT-002、FR-ANALYZE-001、FR-ANALYZE-002
- **SRES-003**：executed；task-bootstrap 顺序、workspace 规则、正式密封通道、deadline=null 事实核验（决策日志 R-017/R-018/R-019/R-020、D-008、D-011）→ PFACT-004、PFACT-005、PFACT-006、PFACT-007
- **SRES-004**：executed；Clarify 归属与判定基线核验（决策日志调研 F-006、D-005；build-spec 与 spec-clarify 技能契约）→ PFACT-013、FR-CLARIFY-001、FR-CLARIFY-002（判定基线见第 10 节默认必须成立）
- **SRES-005**：executed；严格生产形状 vs 兼容接受解析双标准审计（现有生产校验能力与规格模板的形状字段，决策日志结构审计依据）→ PFACT-016、FR-AUTHORING-001、FR-AUTHORING-002
- **SRES-006**：executed；TaskHandle 诊断记录与官方任务启动契约核验：诊断时点事实——worktree 创建记录存在、任务目录产物缺失且未发现成功启动调用（根因=漏执行官方启动，非 session 绑定失败）；官方启动顺序（创建任务 → 准备任务工作区 → 初始化任务存储 → 绑定会话）与只创建发布、会话来源不可用不回滚语义由代码与本次诊断核实（决策日志 R-023、D-012、诊断事实节）；诊断后的真实事实——官方只创建（create-only）启动已成功：任务目录与完整任务存储已创建，task.json 以 workspace_mode=existing 绑定现有工作副本（workspace_root 指向当前工作副本），工作副本内既有材料未被覆盖；启动输出中 session_binding.status=conflict 仅为辅助溯源（provenance）异常，不等于失败，不影响任务目录创建（决策日志「任务身份与启动前置诊断事实」节）→ PFACT-017、FR-TASK-001、FR-TASK-002、SCN-016

## 下游交接

- build-plan 必须把每个 FR/AC 映射到可验证的行为边界与测试层，不得重新决定收敛标准、grill 批次、Clarify 契约、宿主降级语义、审查通道复用或 dogfood 范围。
- build-plan 必须先把「复用既有正式密封通道、workspace 规则、既有覆盖校验链」的范围写清，再为缺口选择最小新增；第二打包器、无-task-store 直写分支、broker 超时机制已被删除（D-008、D-011），不得作为计划项复活。
- build-plan/build-code 兑现 AUTHORING 域时不得新增写入校验器、写入门或重试状态机——只复用现有生产校验能力与验收最小要求（D-010）；宿主侧等价沙箱拒绝不在 WorkflowHub 内修。
- OPEN-003 / RISK-003 / RISK-004 的机器强绑定与对抗性用例是延期交接，不是本任务范围；计划中不得出现为它们新增的机器 gate。
- build-plan 必须把任务身份启动前置（TASK 域）落实为「复用现有官方启动入口」的最小实现：不得新增公共启动命令、不得手写任务清单、不得把任务清单放入 worktree；会话来源不可用不得作为回滚依据（D-012，验收 AC-TASK-001/002）。当前任务已完成官方初始化（任务目录已创建并绑定现有 worktree），但后续阶段仍必须重复确认/打开任务身份，不因已有一次初始化而跳过，也不手写任务清单。build-plan 首项必须核对当前任务目录/task store 与已按任务工作区规则确认的现有工作副本（existing 绑定）的实际状态一致后再进入各阶段。
- 若官方启动入口在 create-only 发布后、工作区/存储准备阶段中途失败，不得把半创建目录当作成功：官方启动成功/完整必须同时满足 task.json + index.json + facts.jsonl + quality/ 全部存在；缺失任一即判为真实失败（fail-loud），build-plan/build-code 不得继续阶段运行；操作员可决策复用现有官方入口（open + initializeTaskStore 幂等重入）或人工检查，但不得新增自动恢复状态机、回滚机制、第二启动路径或公共命令（D-012 边界；见决策日志「任务身份与启动前置诊断事实」节的隔离复现边界风险）。
- 精确工程文件、代码符号、命令与阶段划分只写入 plan/tasks，不回填本 spec。

## build-spec 工作步骤状态（本阶段）

> 步骤编号与名称对齐既有 build-spec 步骤序列（1~14）；本规格状态=草稿 / 待独立审查，未接受为正式规格；步骤 7~9 为 UI 相关步骤，本任务无产品 UI 故整段 not_applicable。

| step | 内容 | 状态 | 证据 |
| --- | --- | --- | --- |
| 1 | 读取 decision-log 与现状材料，建立来源/决策索引 | completed | 已读 decision-log.md（23 条需求、D-001~D-012、R-022→D-010、R-023→D-012、范围修正 D-011 与任务身份启动前置 D-012 映射、R→D 矩阵 23/23）、spec-template、spec-specify/build-spec 技能；本文件「来源与决策映射」即索引 |
| 2 | 条件调研 spec-research | completed | executed（SRES-001~006，无跳过）；关键事实写入 PFACT-002/003/004/005/006/007/016；R-022 双标准事实见 PFACT-016 |
| 3 | spec-clarify 单批独立问题 | completed | 第 1 批真实问答（trigger=true，1 问，0 开放材料歧义）+ 第 2 批 scope revision 真实问答（trigger=true，2 问 2 答，锚点 scope-revision-task-store/scope-revision-review-helper，用户均答 A，0 开放材料歧义）+ 第 3 批 scope revision 真实问答（trigger=true，1 问 1 答，锚点 task-handle-bootstrap-scope=A，R-023/D-012，0 开放材料歧义）；三批互不覆盖（见文末 Spec Clarification Record） |
| 4 | spec-specify 起草/修订 spec.md（本文件） | completed | 草稿齐备：SCN/PFACT/FR/AC/风险/非目标/来源映射无占位符；R-022 已映射 D-010 并新增 AUTHORING 域（PFACT-016、FR-AUTHORING-001/002、SCN-015、AC-AUTHORING-001/002）；D-011 范围修正已并入来源映射与 FR-HOST/FR-REVIEW；本次范围修订（R-023/D-012/TASK 域）已并入本文件（PFACT-017、FR-TASK-001/002、SCN-016、AC-TASK-001/002、Clarify 第 3 批、SRES-006）；R-023/D-012 相关 TaskHandle 已按官方入口补建（任务目录与任务存储已创建并绑定现有 worktree），但实现契约仍待后续 |
| 5 | simplicity-guard 简单性处理 | 需重做（本次范围修订后复核） | 删除项：第二打包器、无-task-store 材料直写分支、broker 超时机制（来源：D-008、D-011），另含「不手工创建任务身份、不把任务清单放入 worktree、不新增公共启动命令、复用现有官方入口」（D-012）；写入校验一律复用现有生产校验能力与验收最小要求，不新增写入校验器/写入门/重试状态机（D-010，见第 10 节）；本次范围修订（R-023/D-012 新增 TASK 域）后需重新执行 simplicity-guard 复核 |
| 6 | plan-ceo-review 产品方向检查（problem/scope/value/alternatives/product direction） | 需重做（本次范围修订后复核） | 产品方向审查曾回 needs_revision，所列 findings（B2 负向回放层级、主人话表列、流程状态列表、PFACT 规则项、实体归属、非目标继承、处置状态、OPEN 卡、术语、RISK Stage、规格状态）已在本规格逐项修订处置完成；本次范围修订（R-023/D-012 新增 TASK 域）后需重新执行 plan-ceo-review（advisory）复核；本规格仍保持草稿/待独立审查，不自判已接受 |
| 7 | ui-project-init | not_applicable | 无产品 UI：本任务是对话/材料消费面（Talk 卡/结束卡/决策日志/spec 材料），不创建 UI 材料 |
| 8 | design-source-readiness | not_applicable | 同上：无屏幕、设计源或 Design 修订，无可绑定画面事实 |
| 9 | conditional-plan-design-review | not_applicable | 同上：无 UI 设计环、无 preview/外部设计返回事实 |
| 10 | freeze-spec | pending | 待 step 6 产品方向确认后冻结 |
| 11 | review-frozen-spec | pending | 待独立异源审查（provider/transport/unavailable 事实如实保留） |
| 12 | main-agent-disposes-findings | pending | 待审查 findings 处置（修复/拒绝/延期均保留理由） |
| 13 | stage-end-spec-analyze | pending | 待现有校验通道执行（另有独立审计跑，本文不自称 production 校验通过） |
| 14 | publish-spec-result | pending | 待前序步骤完成 |