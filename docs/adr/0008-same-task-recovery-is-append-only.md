# ADR 0008 — 同任务恢复采用追加记录

**状态**：已废止（仅保留历史背景）
**日期**：2026-07-25

## 历史说明

本 ADR 记录的 recovery、continuation、runner replacement 与 phase-pointer 机制已删除。
旧文件只保留审计价值，绝不作为普通工作的许可或阻塞条件。

当前任务只以四份当前材料、当前实现、当前测试和独立审查推进；正式 verify/close 对这些当前质量事实严格校验。
