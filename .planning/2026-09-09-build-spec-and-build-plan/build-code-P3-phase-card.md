# Build-code Phase P3 card — `outline_closed` and existing acceptance aggregation

- **Goal**: derive one five-conjunct `outline_closed` completion subject and preserve exact authenticated current outcome selection/conflict semantics through the existing status path.
- **Allowed files**: `runtime/stage/stage-content-contracts.mjs`, `runtime/stage/stage-handlers.mjs`, `runtime/stage/completion-predicates.mjs`, `tools/cli/stage-runtime.mjs`, the existing `runtime/review/schemas/result.schema.json` consumer schema (scope correction: optional governed `semantic_fields` projection only), the six named P3 contract/e2e/integration test files, `tests/contract/acceptance-execution-producer.mjs`, `tests/contract/acceptance-execution-producer.test.mjs`, and `docs/architecture/move-map.json`; `tests/contract/test-runtime-profile.test.mjs` is read-only regression.
- **Covered ACs**: AC-STATE-001, AC-PROOF-001, AC-CLOSE-001, AC-CLOSE-002, AC-REVISION-001, AC-REPAIR-001, AC-FLOW-001, AC-GOV-001.
- **Non-goals**: no sixth conjunct, new gate/schema/public command, second completion subject, caller/latest/env selector, dual write, other-stage predicate, or changes under `tests/acceptance/**`; the existing result schema extension is not a new schema or control plane.
- **Compatibility boundary**: preserve the existing parser → handler → predicate and status projection contracts, runtime-profile/test-routing/acceptance-currentness behavior, and one current completion authority.
- **Predesigned route**: focused feature/backend contract + serialized integration/git fixtures; exact T005/T006 command remains the historical aggregate oracle, while the new producer contract is verified by its own focused backend test because the aggregate exceeds the bounded execution window. Single Vitest fork and no file parallelism. RED must fail at named subtraction/stale/conflict/producer assertions; GREEN must pass the same command for the producer and the historical aggregate where bounded.
- **Stop conditions**: fixture/environment failures, hidden fallback parser, semantic review judgment, sixth conjunct, new control plane, or unrelated stage edits.
- **Expected stage-end summary**: actual P3 changed files, exact RED/GREEN exits/oracle, AC coverage and limits, phase review fact/disposition, T007 evidence status, and any unresolved runtime/manual gaps.

## Execution facts

- **Routing**: `feature` remains the selected tier; the phase changes one runtime feature domain and its serialized contract consumers, without a cross-endpoint/database/auth/deployment seam. Backend-testing scenarios are the existing five-conjunct, stale-binding, retry-sibling, and true-conflict paths in the exact command.
- **Review policy**: P3 implementation review is recorded through the public phase review route after GREEN; provider/manual quality remains advisory and may be unavailable.

## Execution facts

- **Routing advisor**: `feature` — P3 changes one runtime feature domain and its serialized contract consumers; no cross-endpoint, database, auth, or deployment seam.
- **Backend-testing route**: the exact T005/T006 seven-file command completed with exit `0`: 7 files, 237 passed, 1 skipped, duration `1717.39s`. File-level evidence: official-stage `94/94` (`743014ms`), current five-stage E2E `24/24` plus 1 skipped (`459885ms`), delivery-close `29/29` (`442689ms`), and the remaining contract files `114/114`. `git diff --check` also exited `0`. The required immutable T005 RED record was never captured, so this is exact GREEN evidence, not a completed RED/GREEN pair.
- **Scope correction**: the make-decision direction result already emits `semantic_fields.convergence_outline`, while the existing formal-review result schema rejected that governed projection under `additionalProperties: false`; the schema was extended in place. Verify-code stage-outcome projection was also tightened to bind only through the existing current `code_review` quality fact's nested review ref/hash. No new fact, selector, runner, or public control surface was introduced.
- **Public phase review**: `quality/reviews/attempts/a2f47a35-3b29-53a1-a77b-ebe70597aab2/attempt.json`, `terminal_status=unavailable`, `dispatch_state=dispatched`, `error.code=PROTOCOL_INCOMPATIBLE` (`managed provider timing is invalid`); no provider result or findings were recorded.
- **Acceptance route**: T007 request JSON/hash was created, but the exact T008 outer route rejects its required top-level `acceptance_data` and `evidence` fields. A valid empty-input exploratory run preserved `acceptance_execution=missing`, `stage_outcome=unavailable`, and `quality_status=incomplete`.
- **Limits**: no authenticated build-code stage outcome, integration review, or manual confirmation/conflict/Talk evidence is claimed; the exact GREEN run does not repair the missing immutable T005 RED record.
- **Same-task repair**: removed the legacy marker-string (`唯一 OI 大纲` / `current authority`) opt-in from the make-decision handler; only the authenticated `vnext-single-write` record model requires the new `outline_closed` subject. Targeted P3 contract checks remained green: 3 files/79 tests, exit `0`.
- **Producer follow-on**: the actual P3 boundary also included the T006-owned test producer, its focused contract test, and both move-map entries. `npx vitest run tests/contract/acceptance-execution-producer.test.mjs --poolOptions.forks.singleFork --no-fileParallelism` exited `0` with 12/12 tests. `node --check` for both new files, one real package-local Vitest JSON reporter smoke through `accept`, and `git diff --check` exited `0`.
- **Producer limit**: current T008 `execution.input` declares the 21 AC IDs but no `acceptance_criterion_map`. The adapter returns exact canonical rows with non-passing status and refuses order-based inference; a build-plan-owned material repair is required before this can prove acceptance. No T008 aggregate retry was run without a current authenticated build-code outcome.
