# 决策材料收口（draft-final-merge）— workflowhub-thin-core-card-02-20260919

> 性质：**收口草稿**。本文件只写自己，**不修改** `decision-log.md`、`draft-decision-blocks.md`、`draft-fidelity.md`、`research/**` 或任何被审文件；**未改任何代码**。
> 用途：把 make-decision 阶段的决策材料收口成**可直接并入 `decision-log.md`** 的片段（`## 决定` / `## 覆盖矩阵` / `## 收敛检查` ＋ 四张清单 ＋ 收口自检）。
> 追溯规则：全部引用使用**稳定 ID**（`U-xx` / `V-xx` / `REQ-<节>-nn` / `OI-xx` / `T-xxx` / `D-xxx` / `L-nn` / `X-nn` / `RS-xxx` / `K1–K12`）；**禁用行号锚点**（T-018 判据 5/6）。原草稿与源报告中**作为快照定位**保留的行号（如 `stage-content-contracts.mjs:3387`、`prd.md:175,453`）在本文件中一律改为**语义锚点**（函数名 / 节标题 / 稳定 ID）。
> 引文规则：逐字引用可截断（用「…」）但**不改写一字**；V-01..V-28 一字未动。
> 计数规则：**列表 = 计数**，每一处加总可独立复算。

## 本收口落实的 4 项用户裁决（T-044..T-047）

| 裁决 | 内容 | 本文件的落实位置 |
| --- | --- | --- |
| **T-044** | `L-19`/`L-33` 保持 B 类（→CARD-03 / →CARD-08），接受草稿补写的切片动作 | `### B 类排除清单` B-2 / B-6（去掉「本文件补全（T-038 未覆盖）」表述，改标「T-044 补登」） |
| **T-045** | DIV §6.5 四个空白逐个指派：冻结审查合同→CARD-05；核验 blind→CARD-05；**K 失效条件维护→CARD-07**；跨 phase 契约一致性→CARD-03 | `### 承接与不承接清单` 第 3–6 行；GAP-6 关闭 |
| **T-046** | 检查器字段识别兼容修复 = **build-code 交付物**；本阶段只登记需求与验收判据（**不许改代码**） | `### 承接与不承接清单` 第 8 行；收口自检 GAP-（新增） |
| **T-047** | 决策块删掉 `覆盖 T 决策` 行 → **每块 16 行**（不再压） | `## 决定` 全部 14 块：字段行 **16 行/块**；`覆盖 T 决策` 信息移入文末 `T 覆盖核对表`（无损保留） |

## 已知不一致预登记（本收口新发现，不掩盖）

| 编号 | 事实 | 处置 |
| --- | --- | --- |
| **NG-1** | `draft-decision-blocks.md` GAP-2 的加法错误：`28+32+9+27+24+52+21+20+17+26+20+16+13+12+7+7` = **331**，草稿写作 341（差 10） | 已在 `## 覆盖矩阵` GAP-2 修正 |
| **NG-2** | 主题聚类表把 **L 表编号（L-01..L-38）与 `REQ-L-nn` 混写在同一 REQ 单元列表**（第二处命名空间冲突；第一处是 `E-3`/`E-6`）。实测 L 形态唯一 token **26 个**，其中 `L-26/L-27/L-28/L-31/L-33/L-34/L-35`（7 个）**不可解析为任何 REQ 单元** | 已在 GAP-9 逐项修正 |
| **NG-3** | 主题聚类表对 316 单元的覆盖率**仅 230–249 条**（视 L 形态 token 归属而定），**不是 316 的完备分区** → 主题表**不能**与 316 直接比对 | 已在 GAP-1 说明并据此修掉「差 13」 |
| **NG-4** | 原草稿第 2 层 104/95/117 **不可由第一部分逐条重算**；重算值为 **99/128/89** | 已替换为可复算口径 |
| **NG-5** | 第一部分仅 **7 个**被拒条目在分析文字中点到归属卡（CARD-06/08/09），但**处置状态列均写「未登记排除理由」** → 第一部分层「明确排除」= **0** | 已在「零孤儿算术依据」写明：明确排除在 **L 层**由 B 类 7 条落地，不回溯改写 316 层计数（append-only） |

---

## 决定

> **覆盖声明（D-001..D-014 对 T-001..T-047 的映射）**：**T-001..T-043 共 43 条全部被覆盖，无遗漏**——其中 **42 条各有唯一主块归属**，**1 条（T-013）为过程条目**、其结论由 T-017 结清、不占独立块位（GAP-7，本轮不改判）。**T-044..T-047 为本轮（2026-09-20）追加裁决**，不属于 14 块的主块覆盖对象，分别由 `### B 类排除清单`（T-044）、`### 承接与不承接清单`（T-045/T-046）与本收口的行数执行（T-047）承接；**47 条逐条三档结论见 `### T 决策三档回填（GAP-4）`**。
> **行预算（T-047 执行结果）**：每块字段行 **16 行** = card-01 `### D-001`..`### D-010` 的 16 个字段（实测 card-01 各 16 行）；原草稿的第 17 行 `覆盖 T 决策` 已**删除**，其信息在下方 `T 覆盖核对表` **无损保留**。R-E 的「≤15 行」为**非硬性目标**；偏离理由（官方校验器要求的 `module`/`requirement_ids`/`derived_from`/`artifacts` 四字段 + card-01 已建立格式的兼容性）按 R-56 登记在案。
> **字段契约已知不兼容（沿用草稿，未改）**：官方只读检查器 `tools/cli/check-decision-log-chain.mjs` 只认「行首 + ASCII 冒号」，对本卡与 card-01 的「`- ` 列表标记 + 全角冒号」形态命中 **0/56**（card-01 亦 **0/40**）；该检查器 `failures: []` 恒空、exit 0、永不阻断。**该不兼容的兼容修复 = build-code 交付物（T-046）**，本阶段只登记需求与验收判据，**不改代码**（见 `### 承接与不承接清单`）。

### T 覆盖核对表（T-001..T-047 ↔ 14 块 + 4 条追加裁决）

| D 块 | 主块覆盖的 T | 条数 |
| --- | --- | --- |
| D-001 范围与边界 | T-001, T-002, T-003, T-031, T-036, T-038 | 6 |
| D-002 三种文档形态 | T-004, T-005, T-009, T-010, T-012, T-014, T-017, T-018, T-026, T-043 | 10 |
| D-003 不对称模型分工 | T-007 | 1 |
| D-004 文档精炼与跨卡协调 | T-008, T-023 | 2 |
| D-005 验收标准格式 | T-006 | 1 |
| D-006 验收 oracle | T-019, T-032, T-035, T-041 | 4 |
| D-007 审查链 | T-015, T-016, T-022, T-034, T-039, T-042 | 6 |
| D-008 单一权威与检查工具 | T-020, T-021, T-040 | 3 |
| D-009 质量核心保留与合并账 | T-011 | 1 |
| D-010 make-decision 根因修复 | T-029 | 1 |
| D-011 需求保真元机制 | T-024, T-026, T-037 | 3 |
| D-012 偏离重确与三分类 | T-025, T-027, T-028 | 3 |
| D-013 执行边界与扩散登记 | T-030 | 1 |
| D-014 收敛、实质变化与遗留 | T-033 | 1 |
| **主块小计** | **14 块，42 条唯一主块归属；算术闭合 6+10+1+2+1+4+6+3+1+1+3+3+1+1 = 42** | **42** |
| *(T-013)* | 过程条目（暂缓决定→结论由 T-017 结清），不占块位 | 1 |
| **T-001..T-043 合计** | 42 + 1（T-013）= **43**，无遗漏 | **43** |
| **追加裁决（无块位）** | T-044（B 类清单）/ T-045、T-046（承接与不承接清单）/ T-047（本收口行数执行） | 4 |
| **总计** | **43 + 4 = 47** | **47** |

> **两处非一一对应，如实列出**：①**T-013** 是「暂缓决定，先补做调研」的**过程条目**，结论由 T-017 结清；②**T-004 与 T-043 在 D-002/D-009 两块都出现**（T-004 含「文档形态面」与「质量核心数量面」、T-043 含「合并账面」与「交付物形态面」），**上表给出的才是唯一权威主块归属**（T-004 → D-002；T-043 → D-002），D-009 中只作指针引用。同理 T-031 在 D-001/D-010、T-036 在 D-001/D-009 各出现两次，均为跨面指针。

### D-001 本卡范围与边界：并入什么、不回写什么、往下游交什么

- question/final_option：card-02 的范围由哪些输入界定、用户扩张并入哪些、哪些部分交出去？／final_option=母 PRD CARD-02 与规划 decision-log 为方向输入；U-001 扩张并入本卡；只并入 CARD-05 ①与适配合同（含接口定义，不做实测）；扩散机制交 CARD-07；母 PRD 与兄弟卡材料只读不回写
- recommendation/plain_language：把 scope 一次性钉死——本卡只做「文档形态 + 材料结构 + 检查工具 + 审查适配合同」，凡是「改 make-decision 交互平台」「删文件」「做工具实测」的一律交出去，避免范围再次漂移。
- decision：①方向输入＝母 PRD CARD-02 卡节与规划 decision-log，只读；②U-001 扩张（E-1/E-3/E-5/E-6）并入本卡并如实登记；③CARD-05 **只并入** ①`build-plan 合并审查`（FR-56/AC-57 + AC-23/AC-26 的①子句）+ 适配合同（含接口定义，**不做工具实测**），②③④代码审查点/新工具接入/对比实验与 go/no-go 留 CARD-05；④**不回写母 PRD**，承接关系在本卡显式登记、由 CARD-10 核对；⑤扩散（发散/扩散）的**常驻机制**（触发条件+产物+验收）交 CARD-07（FR-33/AC-33、C5 G-4），本卡只登记本轮已做扩散的事实与承接关系；⑥T-038 的 5 条含 card-02 侧切片的 B 类项保持 B 类并登记切片动作（见 A/B 清单节）。
- source_type/reference/exact_excerpt：user_reply／Talk round 1 T-002、T-003、T-038 与 U-001／用户逐字选择「并入 ① + 适配合同（含接口定义，不做实测）」「不回写母 PRD，在本卡显式登记承接关系」「保持 B 类，但登记本卡要做的切片动作」
- approval_binding：待 step 11 approve-decision 的最终确认绑定；本条方向由 Talk round 1 真实答复 + U-001 口述确立
- facts_and_constraints：母 PRD CARD-02 判 `ui_applicability=non_ui`；CARD-05 共 9 项能力其中只有 ① 是纯文档审查（I-C §3.1）；母 PRD 为 `final` 且「母/兄弟材料只读」；CARD-07 母 PRD 已登记 `prd.md:175,453`；AGENTS.md vNext 永久实施边界
- Logic：本卡范围＝「文档权威分工 + 单一事实源规则的落地与可测量检查 + plan/tasks 双写删除的契约层」；范围外改动会制造重复控制面（母 PRD 风险 R1/R2），且卡间写面冲突无检查面。
- choice_reason/impact：决定本卡验收面、后续卡开工顺序与 CARD-10 的核对清单；T-037 的登记含**输入注入义务**（须把「316/38 清点结果 + 扩散需求」写进 CARD-07 承接 task 的输入清单，因母 PRD L453 的 CARD-07 最小读取集不含 card-02 材料）。
- consequences_and_risks：R-1 适配合同两卡各写一次会漂移（缓解：共享基础各写一句指向同一权威句，不复制正文）；R-2 ①的 plan 审查质量核心含「并行声明/写集」而 plan.md 被删 → 必须能只靠 phase 文档 + 紧凑索引定位（本卡必须解决的接口问题）；R-38 母 PRD 书面不一致（CARD-07 卡面说它负责 make-decision 改造）；R-44 扩散机制只登记易复发。
- rejected_alternatives：把②③④代码审查点、工具选型实测、对比实验与 go/no-go 搬进本卡；修改母 PRD 边界；把 U-001 扩张当作母 PRD 既有内容而省略登记；在本卡执行文件物理删除（归 CARD-06）。
- unresolved_items/owner：CARD-05 与 CARD-10 的核对项登记（owner=本卡 make-decision）；CARD-07 输入注入义务（owner=CARD-07 承接 task）
- Supersedes：无（本块为新增汇总块；被它汇总的 T-002/T-003/T-031/T-036/T-037/T-038 原文保留）
- module：范围与边界
- requirement_ids：[REQ-D-01, REQ-D-25, REQ-D-26, REQ-D-27, REQ-D-29, REQ-E-25, REQ-E-26, REQ-I-16]
- derived_from：[V-15]（→ REQ-Q-15 → T-003）/（→ REQ-E-25 → T-037）/（→ REQ-I-16 → T-038）
- artifacts：[本文件 `## 决定` 节 D-001、A 类补回清单「B 类 7 条」、B 类排除清单「B-1..B-7」]

### D-002 三种文档形态：decision-log / spec / phase 的分工与信息架构

- question/final_option：三种文档各自的职责、结构与权威归属是什么？spec 的门面信息架构用哪套？／final_option=decision-log=ADR 块 + OI 大纲 + 三级追溯链；spec=实现设计权威（build-plan 产出）；phase=本包差异三层；紧凑索引纯指针；spec 采用候选 D 的信封（薄叙事主干 + 契约附件），内部按 B 主干 + C 分节 + A 开头
- recommendation/plain_language：三份文档分工不重叠——decision-log 只记「为什么这么定」，spec 只记「要做成什么」，phase 只记「这一包跟别人不一样在哪」；索引只放指针不放正文，避免同一事实出现第二个权威。
- decision：①**decision-log**＝ADR 风格决策块（≤15 行）+ 保留唯一 OI 大纲 + 三级追溯链 `初始用户故事/初始需求 → 原始需求（R-###）/调研（RS-###） → 决策（D-###）`，用覆盖矩阵双向核对；②**spec**＝实现设计权威（需求翻译四件 + 架构 + 验收 + 测试标准），由 **build-plan** 产出；③**phase**＝只写本包差异（L0/L1/L2，见 D-003），写集与依赖由 phase 文档头部自声明，紧凑索引只做汇总；④**紧凑索引**＝纯指针表（权威文件路径 + phase 清单 + 每 phase 写集/依赖 + 消费者列表），**不复制正文**；⑤**spec 信息架构**采用 R-G 推荐组合 = **候选 D 信封**（薄叙事主干 + 机器可读契约附件）＋**主干用候选 B 四拍**（问题/动机 → 改成什么含非目标 → 为什么这样改含被否方案 → 具体机制按 seam 分节）＋**主干第 4 节分节轴用候选 C**（小节=可独立交付/验证的切片，每片声明写集/依赖/不做）＋**开头用候选 A**（一个具体故事 + 非目标，末段质疑预登记 + 未决带消除条件）。
- source_type/reference/exact_excerpt：user_reply（T-012/T-014）+ 调研建议（R-G §11.5）／T-012 用户原话「ADR 风格没问题，但是目前的 decision-log 中的大纲、决策来源等路径也需要保留，后续我需要检查每个决策是产自哪些原始需求或调研，这些原始需求和调研是来自哪些原始用户故事或初始需求，方便我追溯每一个决策」／T-014 用户选择「纯指针表」／R-G §11.5「**推荐倾向**：**候选 D 作为默认信封**…」
- approval_binding：T-012/T-014 由真实答复确立；**spec 结构（⑤）尚待用户对 R-G 推荐组合的确认**，未确认前标「待复核」
- facts_and_constraints：R-E 报告 ADR 节与「决策块 ≤15 行」规则；I-A 病灶 3/4/5；母 PRD FR-06/FR-08/AC-06/AC-08；R-G §12.2 逐条比对既有决议（T-010/T-012/T-014/T-008/T-013/SD-17 全部兼容）；R-G §11.4 候选 D 的**失效条件**（附件与主干不一致 → 变成新的双写）
- Logic：ADR 块保证「结论一眼可读」，追溯链保证「结论可上溯」，二者用 OI-ID 互相引用而非复制正文（R-13）；索引若复制正文就成为第二权威，直接违反 SD-13②。R-G 否掉的是「10 节表单式」的**组织轴**（按信息种类平铺），不是内容本身——10 节内容在候选 D 里**一个都没丢**（R-G §12.1 映射表）。
- choice_reason/impact：决定 spec/phase 模板的骨架与 check 面；决定 decision-log 的 ADR 化改造范围；决定强模型能否自然切分工作包。
- consequences_and_risks：R-12 追溯链与「精炼」冲突（缓解：只放链式指针、原文留证据区）；R-13 OI 裸 JSON 与 ADR 块并存会重复（缓解：OI 大纲=问题与状态权威，ADR 块=结论权威）；R-15 纯指针索引可能让读者少一次跳转看不到硬约束（缓解：spec 硬约束节本身前置）；**候选 D 最大失效条件是双写**（缓解按 R-G §11.5 表：一旦附件与主干出现同一事实两份表述且无人检查，立刻停止双读、该事实只留附件、主干改指针）。
- rejected_alternatives：**10 节表单式结构**（用户判定「太工程化，不适合拆解成 phase 文件，也不适合阅读和审查」）；索引复制正文成为第二权威；删除 plan.md 后并行/写集信息无落点；把逐字保真内容搬进正文（违反 T-018 判据 3 的「一事实一权威」）；spec 用 R-B 的 phase 骨架（R-G §12.3 修正建议：两卡分工不同，不要互相搬）。
- unresolved_items/owner：spec 结构定稿＝R-G 推荐组合的用户确认（owner=make-decision 主会话）；字段必填项与行内预算（owner=build-plan，T-018 禁数值目标故只留结构规则）；phase 正文风格由 R-F 独立决定（见 D-003）
- Supersedes：无（OI-007「未收敛」由本块第 ⑤ 项收敛；T-009 的暂挂由本块结清）
- module：文档形态
- requirement_ids：[REQ-D-07, REQ-I-01, REQ-I-05, REQ-I-07, REQ-D-12, REQ-D-16, REQ-J-01, REQ-J-12, REQ-R-02]
- derived_from：[V-26]（→ REQ-D-31 → T-009）/（→ REQ-D-16 → T-014）/（→ REQ-R-02 → T-012）
- artifacts：[本文件 `## 决定` 节 D-002、`## 覆盖矩阵` 的 spec 条目列]

### D-003 不对称模型分工：强方写规格与测试，弱方只让测试变绿

- question/final_option：如何保证高智力模型的产物能被 luna 级模型写成高质量代码？测试由谁写？phase 文档怎么随模型能力演进？／final_option=强方预写规格与测试并冻结，执行方禁改测试；L2 过渡层带过期条件与退出机制（L0/L1 契约面由 D-002 定，本块定测试作者分工与 L2 退出机制）
- recommendation/plain_language：把「想让模型聪明一点」的写法全部删掉，只留下「机器能验的契约」和「人可以判的目标」；测试必须由强的一方提前写好并冻结，否则执行方会自己写一套宽松测试来自证通过。
- decision：①**规格与测试的作者必须是强的一方**（人或强模型），build-plan 预写测试并冻结，build-code 只负责让预写测试变绿；预写测试与打分代码列入 **DO NOT TOUCH 禁改边界**（复用 build-plan 现有 NEW/MODIFY/DO NOT TOUCH 机制）；②测试变更只允许「显式登记的测试变更请求 + 独立审查」，禁止静默放宽；③**三类指令的取舍**（T-007 核心）：留「可机检的 L1 契约与预写测试」、留「L0 目标与理由」、**删「能力补偿型指令」**（「务必彻底搜索」「逐步思考」「先列出所有文件」——唯一有官方一手证据证明会随模型能力上升转负的类别）；④**L2 退出机制（四条失效条件）**：执行模型不再明显弱于规划者 → 同批历史任务跑 L0+L1 vs L0+L1+L2 无显著正确率差即删 L2；任务转探索性 → 只写边界不写结果；写不出可执行验收 → 三层退化为散文，改用小步+频繁人审；无 owner/review rhythm → 规格必漂移，只留 L0+L1。
- source_type/reference/exact_excerpt：user_reply／Talk round 1 T-007、T-013、T-017／用户选择「高智力模型预写测试，luna 禁改」「三层混合（L2 降为可作废附注）」；R-F 官方一手证据「旧模型需要的 `<maximize_context_understanding>` 指令在 GPT-5 上转为 counterproductive，必须收回」
- approval_binding：待 step 11 approve-decision 的最终确认绑定；T-017 由 R-F 回收后的真实选择确立
- facts_and_constraints：歧义使 Pass@1 降 35%–52%；METR 实测 o3 在 RE-Bench 作弊率高 43×（某任务 21/21），「不要作弊」提示无效；可见测试全前置平均 +19.3 个百分点（2,705 轨迹）；LLM 自造可执行规格仓库级最好仅 20.2%（CodeSpecBench 2026）；SecTDD 2026 实测 9 个条件中仅 7 个改善；R-F 诚实缺口——逐版核对 spec-kit 2,927 行 changelog，**无任何框架官方声明「步骤级指令是给弱模型的过渡方案」**
- Logic：不对称模型分工下，弱方无法自行判定「做对了没有」，因此可判定性必须由强方在前期用 L1 契约与预写测试注入；L2 是过渡物，必须自带过期条件与退出判据，否则会随模型能力提升变成规格债。
- choice_reason/impact：决定 build-plan 的产物清单（spec + 预写测试 + phase）与 build-code 的禁改边界；决定 verify-code 的入口功能验收不可被单元测试汇总替代。
- consequences_and_risks：R-6 预写测试与最终实现可能不同构（缓解：显式测试变更请求 + 独立审查）；R-7 可见测试全过仍可能失败于隐藏行为族（缓解：失败场景必须显式枚举 + 保留 verify-code 真实入口功能验收）；R-18 L2 过期条件若无人执行则退出机制空转（缓解：把「L2 是否仍必要」列为 CARD-10 集成验收或后续复盘的显式核对项）；R-19 L2 与 L1 冲突时的优先级规则必须被执行者读到（缓解：写在 phase 文档头部）。
- rejected_alternatives：让执行模型自己写测试或自己生成规格；写入「务必彻底搜索」类能力补偿型指令；phase 只有步骤清单而无契约与验收判据；把 L2 做成永久正文（无过期条件）。
- unresolved_items/owner：L2 退出机制的复核时机（owner=CARD-10 或后续复盘）；V1/V2/V3 可验证性分级细化（owner=build-plan）
- Supersedes：无（T-013 的「暂缓决定」由 T-017 结清）
- module：可执行性与模型分工
- requirement_ids：[REQ-B-03, REQ-H-06, REQ-H-09, REQ-I-02, REQ-R-04, REQ-R-05]
- derived_from：[V-01]（→ REQ-B-08 → T-007）/（→ REQ-R-04 → T-017）
- artifacts：[本文件 `## 决定` 节 D-003、`## 收敛检查` acceptance 行]

### D-004 文档精炼：九条语义判据，不设任何长度数值目标

- question/final_option：如何治「文档太长、太啰嗦、重要地方简写、不重要地方写很详细」，且不引入形式主义配额？／final_option=不设任何长度数值目标（硬或软）；只依靠九条语义判据 + 跨卡接口协调
- recommendation/plain_language：用「删掉它结论会不会变」代替「不能超过多少行」；复杂度不同的需求本来就该有不同篇幅，数字目标只会逼人删掉该写的东西。
- decision：①**不设任何长度数值目标（硬或软）**，治「文档太长」只依靠语义判据；②**九条语义判据**：⑴删掉后结论不变＝冗余→删；⑵删掉后行为不变＝无操作→删；⑶一事实一权威，余者只放指针（**契约字面量必须逐字复制**）；⑷过程流水与实现内容分层，不混同一段；⑸禁用行号锚点（会腐化）→改用语义锚点或章节标题；⑹禁用哈希 pin（会失效）；⑺术语唯一化；⑻隐含假设显式化；⑼省略只能用 `non-goal / deferred / TBD / TBR`；③**跨卡接口协调**：T-023 由「与 CARD-01 蓝图协调」扩为「与 CARD-01 + CARD-03 + CARD-04 的模板衔接以接口蓝图为准」；card-02 的 make-decision 阶段**不读取 card-01 的在途改动**，在 build-plan 阶段读取当时已提交/冻结的接口蓝图并对齐（记录当时 card-01 的**具体 commit** 使漂移可判定）；④所有对齐义务均为**事实记录 + 验收核对**，不阻断推进。
- source_type/reference/exact_excerpt：user_reply／T-018 用户原话「我还是觉得设置目标完全没有意义，不同的需求有的复杂有的简单，这个目标设置纯粹是形式主义」／T-023 用户选择「build-plan 时对齐 card-01 当时冻结的版本」／母 PRD「spec/phase 文档模板与 CARD-03(工作方法)、CARD-04(验收写入)的衔接以接口蓝图为准」
- approval_binding：待 step 11 approve-decision 的最终确认绑定；T-018 由用户原话直接确立
- facts_and_constraints：I-A 报告——card-01 四材料合计 2,956 行；历史 48 task 中位 2,635 行，极端 17,933 行（plan.md 单文件 13,195 行，89.5% 为零动作清单）却通过全部流程；R-A 证实无任何 SDD 框架给 spec 设行数上限；R-E「删后结论不变/行为不变」判据；card-01 spec FR-IFACE-002（阶段/材料接口蓝图）；card-01 当前处于开发中（build-code 刚起步，存在未提交改动）
- Logic：数字配额会与「没有遗漏信息」正向冲突（复杂需求被配额逼着删）；语义判据与「一事实一权威」同源，可做成可测量检查并与 FR-10「无重复契约检查」同一交付面。
- choice_reason/impact：决定 spec/phase 模板是否含长度字段（结论：否）；决定与 CARD-03/CARD-04 的模板字段对齐工作量和卡间冻结顺序。
- consequences_and_risks：R-21 无数值约束时「太长」可能复发（缓解：语义判据做成可测量检查 + ①合并审查抽查，**不引入新的机器门禁**）；R-27 对齐时点若 card-01 尚未提交，card-02 会对齐到未冻结版本（缓解：记录具体 commit）；L-14 缺该衔接会使模板规定 CARD-03/CARD-04 无法执行的方法，①合并审查的「验收可执行/架构合理」两质量核心缺对照基准；R-45 L-20 的「保持现状」意味着①的「并行声明」面仍只有写集+依赖，与 V-18（五项声明）**不完整对齐**，须在 spec 中显式登记该不完整及其归属（CARD-03）。
- rejected_alternatives：任何行数上限或字数配额（含「软目标」——T-018 明确作废 T-008 的软目标部分）；只用「写短点」这类不可执行的要求；在本卡 make-decision 阶段读取并跟随 card-01 在途改动；把对齐做成推进前置门禁。
- unresolved_items/owner：card-01 冻结版本对齐（owner=card-02 build-plan）；扩项归 CARD-03（L-20 保持现状的边界判断已登记）；模板接口面清单（owner=build-plan）
- Supersedes：本块取代 **T-008 的「软目标」部分**（T-008 的「结构规则」部分保留并升级为唯一手段；T-008 原文保留不回改，符合 append-only）
- module：文档精炼与跨卡协调
- requirement_ids：[REQ-D-03, REQ-D-29, REQ-B-35, REQ-Q-08, REQ-I-12]
- derived_from：[V-24]（→ REQ-B-35 → T-018）/（→ REQ-Q-08 → T-023）
- artifacts：[本文件 `## 决定` 节 D-004、`## 覆盖矩阵` 的「接受但有偏离」计数]

### D-005 验收标准格式：四段式 + 不可判定词黑名单 + V0 禁入

- question/final_option：验收标准采用什么格式才能既精炼又可执行、可判定？／final_option=四段式（条件→行为→可度量标准→失败场景）+ 不可判定词黑名单 + V0 禁止入库 + 人工与机器条目物理分离
- recommendation/plain_language：每条验收必须能被一段脚本或一次人工核对判出真假；写「性能良好」这种话的一律判失败，不许入库。
- decision：①验收标准统一为**四段式**：条件 → 行为 → **可度量标准** → **失败场景**；②失败场景槽位使用 EARS 的 `If <非法输入/超时/并发/越权> then <响应>` 句式；③建立**不可判定词黑名单**（`fast` / `友好` / `尽可能` / `等等` / `100%` 等）——**出现即判失败**；④**V0（不可判定）条目禁止入库**；⑤V1（人工）/ V2（机器）/ V3（机器+存量）可验证性分级细化**留 build-plan**，且 **V1 人工条目须与 V2/V3 机器条目物理分离**；⑥四段式**只用于验收节**，不扩散到全文；⑦本改动确实给 `spec-specify` 增了职责，须按 V-23 写明「当前缺陷或用户结果理由」，不得只换存放位置（R-51）。
- source_type/reference/exact_excerpt：user_reply／Talk round 1 T-006／用户选择「四段式（条件→行为→可度量标准→失败场景）+ 不可判定词黑名单」
- approval_binding：待 step 11 approve-decision 的最终确认绑定
- facts_and_constraints：四段式各段均有标准出处（条件=INCOSE R27/R28，行为=R1/R2/R31，度量=R33/R34/R35，失败=EARS If/Then）；Kiro 声称用 EARS 却产出含 "handle edge cases gracefully" 的不合规条目 ⇒ 格式须显式约定并抽检；I-A 病灶：101 条 AC 含可执行命令者＝0；母 PRD CARD-02 AC-07；R-G §12.1（被否 10 节里「5 验收四段式」落到主干第 5 节，细节下沉 phase）
- Logic：可判定性是「验收标准」的唯一硬要求；格式不显式约定则连官方 EARS 使用者也会产出不合规条目；黑名单把「不可判定」从主观判断降为字符串命中，使其可机检。
- choice_reason/impact：决定 spec/phase 模板的验收节结构；决定①合并审查能核对「验收可执行」质量核心；与 D-006 的 oracle 设计直接耦合。
- consequences_and_risks：R-5 四段式比一句话验收更长，与「精炼」目标正面冲突（缓解：长度预算作**软目标已被 T-018 作废**，改靠「引用不复制」控制总量，四段式只用于验收节）；R-51 词表改造给 spec-specify 增职责须写理由；V1 人工条目若与机器条目混写会导致一并自报通过。
- rejected_alternatives：出现「性能良好」「处理边界情况」类不可判定条目；人工条目与机器条目混写；V0 条目入库；把四段式扩散到全文（与精炼冲突）。
- unresolved_items/owner：V1/V2/V3 分级细化（owner=build-plan）；永不抑制清单的条目微调（owner=build-plan，机制本卡定稿）
- Supersedes：无
- module：验收标准
- requirement_ids：[REQ-B-07, REQ-D-18, REQ-D-20, REQ-D-23, REQ-O-08, REQ-R-14]
- derived_from：[V-08]（→ REQ-D-19 → T-040）/（→ REQ-O-08 → T-006）
- artifacts：[本文件 `## 决定` 节 D-005、`## 覆盖矩阵` 的验收类条目]

### D-006 验收 oracle：三件 oracle + 双通路 + G-2 可失败检查

- question/final_option：AC-07/AC-08 要求的「真实实施 task 样例」从哪来？本卡的 oracle 由什么构成、怎么防止「自己验自己」？／final_option=卡内造最小真实 fixture task + 检查工具 RED→GREEN 双通路 + G-2 补可失败检查
- recommendation/plain_language：光有「模板存在」不算验收——必须真的跑一个样例文档、真的跑一次工具并看到它先失败后通过，否则没人知道格式和工具到底有没有用；同时如实披露本卡没做和不宣称的事。
- decision：①本卡 oracle 由**三件**组成：㈠格式/模板文件存在且可核对；㈡无重复契约检查工具可运行并给出真实 exit/output；㈢检查工具对 card-02 **自身产物运行通过**；②**卡内造一个最小真实样例（fixture task）**，按新格式产出该样例的 `spec.md` 与 phase 文档（**至少含 2 个 phase**），并以其为 AC-07/AC-08 的抽取对象；③**G-2 处置 = 补一条可失败检查作 RED**：先构造**含重复权威的 fixture**，工具应真实报失败（非零 exit），再令真实材料下工具报通过（GREEN）；RED 必须由**目标断言**导致非零，环境故障/配置缺失/无意义断言不算有效 RED；④**双通路互不替代**：RED→GREEN 验**检查工具**，fixture task 验**文档格式**；⑤两路结论均须由**独立来源**（①合并审查）核对，抽取结论不得由实现者单独宣称；⑥披露纪律：宿主未提供 reply_ref/reply_hash 认证凭证**不补造**；incomplete **不阻止同 task 继续**；不宣称测试通过、不宣称审查完成、不宣称已交付；无法修复时可**停止并交接未完成事实**，不得以 review unavailable 替失败开脱。
- source_type/reference/exact_excerpt：user_reply／T-041 用户裁决「卡内造一个最小真实样例（fixture task）」／T-032 用户选择「补一条可失败检查作 RED」／母 PRD「G-2:若某任务确实先写不出失败测试(纯文档、探索性改动),**必须显式豁免**——补一条可失败的检查,或写明豁免理由与风险并在验收中披露;**不允许直接跳过**」
- approval_binding：待 step 11 approve-decision 的最终确认绑定；T-041 由 V-10 重确环节的真实裁决确立
- facts_and_constraints：母 PRD AC-07/AC-08 原文「抽一个**真实实施 task** 的 spec.md」「抽一个**多 phase task** 的 phase 文档」；V-14 停止规则；V-27 退出语义与披露纪律；V-01「环境故障、配置缺失或无意义断言不算有效 RED」；母 PRD 质量事实「不宣称测试通过、审查完成」「不宣称已交付」
- Logic：T-019「模板 + 工具自证」调用了本卡**自身产物**作为 oracle，落在 AC-26 明令判失败的「模拟或脚本自证」形态上（L-22）；引入**卡内真实样例 + 独立来源核对**把自证改为可被第三方复核的真实抽取；G-2 的可失败检查则让纯文档任务也留下真实 RED 证据。
- choice_reason/impact：本卡 oracle 交付物由 2 件变 3 件（新增最小真实样例）；verification 成本上升，但满足 V-27「第一条贯通路径形成时就实际试用」。
- consequences_and_risks：R-22 格式/模板自证可能「自己验自己」（缓解：检查工具输出是可复核的真实命令 exit/output；①合并审查提供独立来源核对）；R-39 纯文档任务的 RED 可能退化为「形式化 RED」（缓解：fixture 必须包含真实的两处权威声明，并由独立来源核对 RED 的非零确由目标断言导致）；R-48 fixture task 若被实现者自造并自证仍属自证（缓解：fixture task 的 spec/phase 由独立来源核对）。
- rejected_alternatives：只用「模板存在」代替完成判据（AC-07 无真实执行记录）；用不存在的样本或伪造的抽查结论充当验收证据；在真实材料上制造非零 exit 充作有效 RED；把真实多 phase task 抽验**以「删除验收依赖」方式**处置（L-04 判为偏离，改为卡内 fixture + CARD-10 带触发条件的延期项）。
- unresolved_items/owner：真实多 phase task 的**外部**抽验＝带触发条件的延期项（触发=首个使用新格式的实施 task 落地，owner=CARD-10）；fixture task 核对（owner=①合并审查）
- Supersedes：无（T-019 原文保留；本块对 T-019 做追加式补正——新增 fixture task 与 G-2 RED，不改 T-019 字面）
- module：验收 oracle
- requirement_ids：[REQ-B-08, REQ-B-09, REQ-G-01, REQ-G-02, REQ-G-09, REQ-H-14, REQ-L-02, REQ-Q-12, REQ-Q-14, REQ-Q-16]
- derived_from：[V-01]（→ REQ-B-08 → T-032）/（→ REQ-G-01 → T-041）/（→ REQ-Q-12 → T-038）
- artifacts：[本文件 `## 决定` 节 D-006、`## 收敛检查` acceptance 行、A 类补回清单 A-11]

### D-007 审查链：单次审查 + 三态处置 + 分级降为纯标签

- question/final_option：①合并审查的 finding 如何分级与处置？哪些证据永不允许被降噪？审查是否阻断？／final_option=四档仅为描述性标签、不参与处置判定；恢复「单次审查成功后不再为同一 scope 复审」；finding 处置回到「照单修复」三态；永不抑制清单改为审查自检清单
- recommendation/plain_language：把「分级 → 决定是否阻断」这条链砍断：分级只用来沟通和统计，处置只有「修好 / 拒绝并写理由 / 交人」三种；一次审完就不要再为同一范围重审。
- decision：①**四档（critical/major/minor/trivial）降为「描述性标签」**，**不参与处置判定**（只用于沟通与统计）；②**恢复「单次审查成功后不再为同一 scope 复审」**，并配 AC-24 式度量（同 scope 无复审记录）；③**永不抑制清单改为「审查自检清单」**，不参与 finding 处置；其 4 条（无 oracle / 无溯源 / 预写测试被改 / 不可逆缺确认门）仍须逐项可见；④finding 处置回到「**照单修复**」：`fixed` / 拒绝并写理由 / `needs_human` **三态**（不再用四档决定是否阻断）；⑤**写共享定义层（SD 级）区分两个对象**：**机制不得降级**（真实验收、有效 RED、oracle 分离、回读检查）← 依 V-07 母 PRD finding#16；**finding 处置默认 advisory**（①合并审查的 finding 处置默认不阻断）← 依 SD-17 零机器门禁与 T-039；两者各写一句同一权威句，由 CARD-05 与 CARD-10 引用；⑥**送审/确认材料集（packet）必须含被引用来源清单**；⑦**审查能力可测判据三项**：finding 有效率 / 锚定准确率 / 耗时——本卡**只定判据不做实测**（测量动作归 CARD-05），判据须写成可执行形式（条件→行为→可度量标准→失败场景）使 CARD-05 可直接运行。
- source_type/reference/exact_excerpt：user_reply／T-039 用户裁决「四档降为纯标签 + 恢复不复审」／T-042 用户裁决「写共享定义区分『机制』与『finding 处置』」／T-034 用户选择「定判据，不做实测」／原口径逐字「单次审查成功后不再为同一 scope 复审;审查发现照单修复,修完不重跑审查,**不建 severity 门槛或复审循环**」
- approval_binding：待 step 11 approve-decision 的最终确认绑定；T-039/T-042 由 V-06/V-07 重确环节的真实裁决确立
- facts_and_constraints：R-D §2.2（5 个主流 AI 审查工具全部默认 advisory，Copilot 官方把「阻断合并」列为不支持）、§5.2（Greptile 处置动词）、§9；CodeRabbit 官方 findings 页为 `critical/major/minor/trivial` 四档；母 PRD build-prd 审查 finding#16 明确**拒绝**「把多数机制降级为 advisory/non-blocking」；SD-17 零机器门禁；detail 审查争议#1/#4 的根因是「送审材料集不含被引用来源」；R-D 与 I-C 给出三项判据
- Logic：原口径（V-06）已明确「不建 severity 门槛或复审循环」；T-015 把分级升级为处置判据、新增清单与审计三层，与用户原话方向相反且增加审查负担（L-01 偏离）。T-039 把三层收敛为一条规则：分级只沟通，处置照单修复，**审查负担下降且与 V-06 一致**。
- choice_reason/impact：决定①合并审查的输出格式与 finding schema；决定 CARD-05 与 CARD-10 引用哪一句权威句；决定 CARD-05 实验可直接使用的判据接口。
- consequences_and_risks：R-16 四档口径需与 CARD-05 的代码审查档保持一致（缓解：分级口径写成**共享基础**，两卡引同一权威句不各写一份）；R-17 清单过长会削弱「永不抑制」的严肃性（缓解：改为自检清单后不再承担处置语义）；R-25「报失败但不阻断」可能被误读为「失败可以忽略」（缓解：检查输出列入自检清单关联证据 + 阶段末遗漏披露强制逐项列出）；R-46 纯标签可能被下游误当处置依据（缓解：「不参与处置」写在共享定义与审查输出格式中）；R-41 只定判据不实测可能重演「写了决议未实现」（缓解：判据写成可执行形式并在 T-002 承接清单中显式列为 CARD-05 输入项）；R-49 共享定义层新增需按 L-24 登记（缓解：作为**现有 SD-17 的补充句**登记，不新增 SD 编号）。
- rejected_alternatives：用四档分级决定是否阻断；保留复审循环；把永不抑制清单用于 finding 处置；审查默认阻断（与行业常态及 SD-17 冲突）；把「提升审查能力」写成无判据的口号（L-37 判为拒绝）；把 packet 内容要求留空（会使①无法核对需求翻译完整性）。
- unresolved_items/owner：判据的测量与实验（owner=CARD-05）；永不抑制（自检）清单条目微调（owner=build-plan，机制本卡定稿）；CARD-05 ①与本卡①的漂移处置（同一权威句 + 不复制正文 + 执行记录互相引用）
- Supersedes：**本块取代 T-015 的处置部分**（T-015 原文保留不回改；「四档分级作为处置判据」不再有效）；**本块取代 T-016 的清单定位**（T-016 的 6 条候选已由 T-022 收敛为 4 条，本块将其降为自检清单）；T-022 定稿的 4 条清单内容保留
- module：审查链
- requirement_ids：[REQ-B-10, REQ-B-14, REQ-B-16, REQ-E-01, REQ-E-05, REQ-E-11, REQ-E-22, REQ-E-23, REQ-L-01, REQ-L-12, REQ-R-14]
- derived_from：[V-06]（→ REQ-B-16 → T-039）/（→ REQ-E-22 → T-034）/（→ REQ-L-01 → T-039）
- artifacts：[本文件 `## 决定` 节 D-007、A 类补回清单 A-9/A-11]

### D-008 单一权威与检查工具：双轨判据 + 0 处度量 + 真报失败不阻断

- question/final_option：「无重复契约」检查靠什么判据？发现重复权威时的语义是什么？工具与「机器保证」的边界在哪？／final_option=双轨（ID 定义唯一性 + 四类事实人工核对抽样）；度量恢复为「0 处双权威声明」；真报失败但不阻断；工具只是事实记录
- recommendation/plain_language：光查「ID 有没有被定义两次」会漏掉用不同措辞对同一件事各自宣称权威的情况；所以机器查 ID，人抽样查四类关键事实；工具失败要真报错，但绝不拿来卡住开工。
- decision：①**双轨判据**：**轨 1（机器可检）** ID 定义唯一性（稳定 ID 命名空间 `U-###` / `R-###` / `RS-###` / `OI-###` / `D-###` / `FR-###` / `AC-###` / `PH-###`；每个 ID 只被定义一次；**定义用章节标题，引用用指针措辞**）；**轨 2（人工核对 + 抽样）** **四类事实**的权威归属核对——①产品目标；②完整用户流程；③跨任务需求；④总体成功/失败与验收（这四类**不必然带 ID**）；②**度量恢复**为原口径的「**0 处「两个文件对同一事实各自声明权威」**」，由轨 1 + 轨 2 共同支撑；③**失败语义 = 真报失败，但不阻断推进**（非零 exit + 具体位置清单；失败只记录事实，不阻断 build-code 开工或阶段推进）；④轨 2 的抽样须由**独立来源**（①合并审查）执行并保留抽样记录；⑤**工具定位边界**：本检查工具是**事实记录**，不构成推进门禁，**不提供机器认证 / 跨会话证据链 / 自动判断证据全部过期的保证**（写入 README/`--help` 与 spec）；⑥补**变更传播核对**（AC-10：改一处后引用处是否需手工同步）+ 一条 AC 条目；⑦**不新增第二套工具、不新增注册表**（注册表本身会成为新的重复源）。
- source_type/reference/exact_excerpt：user_reply／T-040 用户裁决「双轨（ID 定义唯一性 + 事实级判据）」／T-021 用户选择「真报失败，但不阻断推进」／原口径逐字「同一事实只有一个权威文件;删除后不得存在两个文件对同一事实各自声明权威」「AC-06…行为=检查**逐事实**核对权威归属;度量=**0 处**「两个文件对同一事实各自声明权威」」
- approval_binding：待 step 11 approve-decision 的最终确认绑定；T-040 由 V-08 重确环节的真实裁决确立
- facts_and_constraints：母 PRD FR-10 / AC-06 / SD-13②；规划 decision-log OI-013（记录层用普通文件名 + 纯文本引用，无内容寻址哈希）；母 PRD V-20「方法工具包不再承诺机器认证阶段完成、跨会话证据链或自动判断全部证据过期…**不假称纯技能提供同等机器保证**」；SD-17 零机器门禁；AC-10 原文（改一处即可、无第二处手工同步）
- Logic：T-020 的「仅 ID 定义唯一性」是**判据替换型漏检**——不带 ID 的正文（产品目标叙述、流程描述、跨任务需求段落、总体验收表述、表格）不在检查面，两个文件可用不同措辞对同一事实各自声明权威而工具报 0 重复（L-02）。双轨让「0 处」这一可证伪度量重新可用。
- choice_reason/impact：检查工具的判定面从「ID 计数」扩为「ID 计数 + 四类事实抽样」；决定工具说明必须写清无 ID 事实的核对方式；决定检查能力与①合并审查的分工。
- consequences_and_risks：R-23 命名空间若被滥用（给非事实内容也编 ID）检查会失去意义（缓解：ID 只授予「有独立权威归属的事实」）；R-24 引用形式措辞不统一会被误判为定义（缓解：定义形式限定为**章节标题**，其余一律视为引用）；R-25 见 D-007；R-29 L-01/L-05 涉及审查机制核心语义，若保留偏离需与 CARD-05 共享权威句一致；R-47 轨 2 的人工核对不可机检可能退化为形式（缓解：抽样由独立来源执行并保留记录）；L-29 的「不假称机器保证」由本块第 ⑤ 项承接。
- rejected_alternatives：只做 ID 定义唯一性（判据替换型漏检）；维护额外注册表（成为新的重复源）；工具存在但发现重复时仍 exit 0；检查依赖哈希/行号而非稳定 ID；用工具失败阻断 build-code 开工（与 SD-17 冲突）。
- unresolved_items/owner：变更传播核对的判据细化（owner=build-plan）；轨 2 抽样记录格式（owner=build-plan）
- Supersedes：**本块取代 T-020 的单轨判据**（T-020 原文保留；T-040 细化后「ID 定义唯一性」仅为轨 1，不再单独构成判据）
- module：单一权威与检查工具
- requirement_ids：[REQ-B-29, REQ-D-01, REQ-D-14, REQ-D-19, REQ-D-23, REQ-K-23, REQ-N-06]
- derived_from：[V-08]（→ REQ-B-29 → T-040）/（→ REQ-D-23 → T-040）/（→ REQ-N-06 → T-021）
- artifacts：[本文件 `## 决定` 节 D-008、`## 覆盖矩阵` 计数行、A 类补回清单 A-1]

### D-009 质量核心保留与两阶段合并：合并账不是新增清单

- question/final_option：build-spec / build-plan 的流程与质量核心是哪些、如何保留？两个阶段合并成一个时是否纯新增？／final_option=以 K1–K12 全清单为保留对象；合并账显示净新增 0 步、真正新建最小集为空集；build-plan 作薄主体技能只调度
- recommendation/plain_language：不是「加一个新阶段 + 一堆新技能」，而是「把原来两个阶段的东西逐项标清楚：哪些留着、哪些并了、哪些改了、哪些删了、哪些真的没有承接者」——合并账算下来没有一项需要真新建。
- decision：①**保留对象 = I-B §4「质量核心清单（必须保留）」K1–K12 全 12 条**（K1 需求翻译完整性+未溯源回退 ｜ K2 稳定 ID+双向追溯闭环 ｜ K3 AC 四段式可执行验收 ｜ K4 场景与状态覆盖矩阵（八态）｜ K5 唯一 Clarify 交互+十维度完整性检查 ｜ K6 一次独立异源审查+findings 四态处置 ｜ K7 stage-end spec-analyze 跨材料语义一致性 ｜ K8 顾问透镜组（simplicity-guard/plan-eng/plan-design/plan-ceo）｜ K9 RED/GREEN 可证伪对+精确文件边界（NEW/MODIFY/DO NOT TOUCH）｜ K10 测试蓝图+测试路由判类 ｜ K11 阶段末逐项遗漏披露+阶段复盘 ｜ K12 人工确认（真实用户回复））；**K1–K12 每一条须有逐条承接者与去向**，不得只写「8 条」；②**流程去向**：build-spec 15 步 + build-plan 13 步 = 28 步，合并账为 **保留 11 / 合并 11 / 改造 6 / 删除 0 → 净新增 0 步；28 → 13（净减 15）**；③**26 项质量机制**：保留 18 / 合并 3（两次审查合一次、两次 spec-analyze 合一次）/ 改造 5 / **语义丢失 0**；另删除 D1–D5；④**真正需要新建的最小集 = 空集**（0 技能 / 0 stage / 0 控制面 / 0 模板）；**无职责空白**；⑤**build-plan 作薄主体技能只做调度**，不内嵌质量逻辑（T-011 此部分继续有效）；⑥交付物 = 「**新的 13 步 build-plan + 6–8 个现有技能改造 + 1 份模板合并（plan-template 吸收 tasks-template）+ 3 个阶段文件**」；⑦**新增控制面登记义务据此收紧**：**新建项为 0**，只需登记**改造项**的 consumer/owner/删除条件，且每项改造须按 V-23 写明「当前缺陷或用户结果理由」，不得只换存放位置。
- source_type/reference/exact_excerpt：user_reply／T-036 用户原话「为什么会新增这么多标准和技能？不是应该替换合并原来的技能吗？怎么可能单纯新增？两个stage合并成1个，怎么可能纯新增？」／T-011 用户原话「这些步骤都是在 build-plan，但是应该各自有各自的独立技能才对，就像现在的 build-spec 和 build-plan。主体技能是薄核心的，具体的步骤由其他技能执行」／T-043 用户裁定「接受合并账结论（本卡交付「合并账」而非「新增清单」）」
- approval_binding：待 step 11 approve-decision 的最终确认绑定
- facts_and_constraints：`research/MERGE-two-stages-consolidation.md` 的 K1–K12 映射（完全覆盖 10 / 部分覆盖 2 / 无承接者 0；部分覆盖者 K11/K12 缺独立 SKILL 文件但已有 `stage-reflection` + 阶段内联协议 + handler consumer 承接）；仓库已有约 30 个技能中大量为 K1–K12 的现成承接者（`spec-specify`/`spec-clarify`/`spec-plan`/`spec-tasks`/`spec-analyze`/`wh-review`/`simplicity-guard`/`plan-eng-review`/`plan-design-review`/`plan-ceo-review`/`testing-system-blueprint`/`test-routing-advisor`/`stage-reflection` 等）；I-B §4 质量核心清单；CONSTITUTION「薄核心、窄契约：能力下沉到技能层，核心只做调度」
- Logic：T-011 曾表述为「8 条质量核心各自成为 `skills/` 下的独立可搬运技能」——该表述**暗示新建**且忽视了仓库已有的现成承接者（T-036 如实登记为本卡此前的错误）。合并账用「保留/合并/改造/删除/新建」逐项标明，净新增为 0，从而**证实**用户「不应纯新增、应替换合并」的判断。K1–K12 与「8 条」的口径冲突（OI-009 的 8 技能清单含 K11 不含 K4，与「K1–K8」自相矛盾）必须书面澄清（L-03 判定）。
- choice_reason/impact：本卡交付物由「8 个新技能」改为「新的 13 步 build-plan + 6–8 个现有技能改造 + 1 份模板合并 + 3 个阶段文件」；L-24 的义务范围收紧；R-43 不成立、风险关闭。
- consequences_and_risks：**R-50 `merged-review` 的伪合并风险**——若给每个 lens 配独立 consumer/receipt 即为伪合并，**必须只留 1 个 `safeReviewFacts` consumer**；**R-51** T-006 四段式词表虽属「改造」但**确实给 `spec-specify` 增了职责**，须按 V-23 写明理由；**R-52** `frontend-prototype-render` 原只在 skill-deps、**未在 steps.json 成步**，新阶段须**显式**纳入 UI 路径，否则能力静默丢失；**R-53** plan/tasks 双写删除使 `spec-plan` + `spec-tasks` 合并为 1 步（净减 1 的来源），须在 spec 中登记该合并的**语义等价性**；R-10 技能数量增加使调用链与依赖声明变复杂（缓解：可复用现行 `skill-deps.yaml` 模式）；R-11 技能间输入输出契约若定义不清会退化为「各自为政」（缓解：spec 中为每个技能写明唯一 consumer 与产物落点）。
- rejected_alternatives：把 K1–K12 内联进 build-plan 单文件；新增无 consumer 的重复控制面；把 K1–K12 再压缩为「8 条」而不给出 K9–K12 的去向；「纯新增」设计（用户明确裁定不成立）；为每个 lens 配独立 consumer/receipt（R-50 伪合并）。
- unresolved_items/owner：OI-009 需重写为「K1–K12 → 现有技能映射 + 合并账 + 真正新建项的最小集（空集）」（owner=本卡 make-decision）；6–8 个技能改造项的 consumer/owner/删除条件登记（owner=build-plan）；L-20「phase 头部字段集是否扩为五项声明」保持现状、扩项归 CARD-03（owner=CARD-03）
- Supersedes：**本块取代 T-011 的「8 条质量核心各自成为独立可搬运技能」表述**（T-011 原文保留不回改；「build-plan 作薄主体技能只做调度」部分继续有效）；**本块取代 T-004 的「8 条质量核心」数量口径**（T-004 原文保留；保留对象修正为 K1–K12）
- module：质量核心与阶段合并
- requirement_ids：[REQ-R-07, REQ-I-05, REQ-R-14, REQ-K-03, REQ-J-08, REQ-I-19, REQ-I-20, REQ-I-21, REQ-P-04]
- derived_from：[V-28]（→ REQ-R-07 → T-043）/（→ REQ-I-05 → T-036）/（→ REQ-P-04 → T-043）
- artifacts：[本文件 `## 决定` 节 D-009、`## 覆盖矩阵` 的 K1–K12 行]

### D-010 make-decision 遗漏的机制根因与最小修复面

- question/final_option：make-decision 为什么会产生如此大规模的需求遗漏？根因在哪一层？修复放本卡还是改母 PRD 的 CARD-07？／final_option=根因是「根因对 R-1 + R-2」，互为必要；修复落本卡（对齐 14 条偏离裁决与覆盖校验粒度）
- recommendation/plain_language：不是「某个 agent 偷懒」，而是两道机制同时失效：检查器只数类别（不逐条比对），而唯一能逐条比对的检查器写好了却没接上生产路径；只修任一道都不够，必须同时修。
- decision：①**根因对 = R-1 + R-2，二者互为必要条件**：**R-1** 覆盖矩阵与收敛校验是**类别级**（`runtime/stage/stage-content-contracts.mjs` 的 `analyzeDecisionConvergence` 与 `parseCoverageRows`）→ **让遗漏算不出来**；**R-2** 唯一真正的逐条强校验器 `assertDecisionCoverageReadyForConfirmation` **存在于代码但生产路径 0 调用者**（仅测试引用）→ **让本该算出来的那套不生效**；②信息丢失发生在 **OI 录入端**（316 条 → 约 17 条 OI），`analyzeDecisionOutline` 的签名**没有 `originalRequirement` 入参**；③**同一根因第三次复发**（2026-08-04、2026-08-28 两次「修复」均停在类别级/消息级）；④**最小修复面（按投入产出排序）**：㈠接线 `assertDecisionCoverageReadyForConfirmation` 到 make-decision approve 路径、`sourceItems` **改取原始需求清点**而非材料自报（直击 R-2）；㈡矩阵/覆盖校验从「类别是否出现」改为「**逐条是否有 disposition + 强度对照**」（直击 R-1）；㈢OI 行新增「原始声明逐字引用 / source 锚点」必填字段（直击 R-3/R-4，注意按 AGENTS.md 先登记职责/consumer/删除条件）；㈣完成判据补「逐条 disposition（covered/accepted_omission/deferred/rejected）或显式排除理由」（直击 R-6）；㈤放大器优先做 R-7（取消 500 字回传上限）与 R-8（给审查者加只读「原始声明锚点 + 当前口径」字段）；⑤本卡**只改「校验器粒度 + 接线」这一最小面，不改 make-decision 的 SKILL 文本**（SKILL 文本仍归 CARD-07）。
- source_type/reference/exact_excerpt：user_reply／T-029 用户原话「需要把当前发现的问题调查清楚根本原因，make-decision 为什么会有这么大的遗漏，如何彻底解决优化？是放在当前任务解决，还是修改 prd 里的 card-07？」／T-031 用户选择「card-02 本卡做」（选项甲）／RC 报告「**判定：存在一个"根因对"，不是单一根因，也不是 9 条并列均摊。**」
- approval_binding：待 step 11 approve-decision 的最终确认绑定
- facts_and_constraints：`runtime/stage/stage-content-contracts.mjs:3387/3418/3422-3434`（类别级校验）、`:3038-3042`（`analyzeDecisionOutline` 签名无原始需求入参）、`:2519-2569`（`validateRequirementCoverage` 以「认证消息」为单位）、`:2701-2750/3582-3636`（逐条强校验器定义）、`:3182-3197` 与 `stage-handlers.mjs:1021-1028`（审查输入为 questions-only 投影）；`check-decision-log-chain.mjs` 设计上**永不失败**（只出 advisory warning，`failures: []` 恒空、exit 0）；母 PRD 已登记 SD-09/SD-10 实现属 CARD-07（`prd.md:175,453`）；母 PRD L21/L428 把 make-decision 改造归 CARD-07
- Logic：只修 R-1（矩阵改逐条强度）→ 压缩会在 step 1/step 12 被逼出来；只修 R-2（接线）→ `source_items` 仍由材料自报为「22 条消息」，遗漏照样通过 ⇒ **必须同时修 R-1 的粒度**。R-3（OI 大纲前置）不是独立主因而是 R-1/R-2 的**承载面**：OI 行若逐字保留原始声明且校验器逐条对齐，316 条会在 OI 层就暴露为缺失行。共同失明模式：所有检查器都在回答「**这份材料内部自洽吗**」，没有一个在回答「**这份材料相对原始需求完整吗**」。
- choice_reason/impact：card-02 范围新增「`runtime/stage` 覆盖校验器粒度修复 + 接线」这一实现面；交付物新增 `assertDecisionCoverageReadyForConfirmation` 接入 make-decision approve 路径 + `sourceItems` 改取原始需求清点 + 覆盖校验由类别级改为逐条 disposition + 强度对照；与 L-09（强度校验）、NEW-01（计数）合并为**同一交付面**。
- consequences_and_risks：**R-35 越 CARD-07 写面**（母 PRD L21/L428 把 make-decision 改造归 CARD-07）；**R-36 落在 card-02 原 oracle 之外**（原 oracle 是文档格式 + 检查工具，现新增运行时校验器）；**R-37 与 CARD-07 将来的 build-code 撞车**；**R-38 母 PRD 边界书面不一致**（需按 T-003 方式显式登记承接关系，由 CARD-10 核对）；R-33 若根因位于 make-decision 的 SKILL/steps/校验器则与「母 PRD 只读」存在潜在冲突（缓解：本卡只改校验器粒度 + 接线，不改 SKILL 文本，并显式登记「本卡改了什么、CARD-07 仍负责什么」）；R-34 调查范围可能再次扩张（缓解：以 U-002 触发的「真实用户问题」定性为停止规则的正当例外）。
- rejected_alternatives：把 SD-09/SD-10 的**完整实现**放进本卡（完整实现归 CARD-07）；在本卡改 make-decision 的 SKILL 文本；只修 R-1 或只修 R-2（RC 已证二者互为必要）；在本卡执行 CARD-07 的交互平台改造。
- unresolved_items/owner：CARD-07 仍负责什么（SKILL 文本、交互平台、发散机制的常驻机制）＝须在 spec/decision-log 显式登记的承接划分（owner=本卡 make-decision）；与 CARD-07 的写面冲突核对（owner=CARD-10）
- Supersedes：无
- module：make-decision 根因修复
- requirement_ids：[REQ-O-07, REQ-O-01, REQ-J-12, REQ-D-31, REQ-Q-13]
- derived_from：[V-13]（→ REQ-O-07 → T-031）/（→ REQ-O-01 → T-029）
- artifacts：[本文件 `## 决定` 节 D-010、`## 覆盖矩阵` 的计数行、A 类补回清单 A-1]

### D-011 需求保真元机制：逐字双层 + 三档结论 + 强度校验 + 待复核

- question/final_option：如何建立需求保真结构，使「压缩」这件事从「机制上不可见」变成可见？／final_option=先建 SD-09 保真机制（元机制先行），再逐条补回；四条规则常驻
- recommendation/plain_language：先把「原话存哪儿、有没有被改」这件事做成常驻结构，再谈补哪几条；否则补回来的需求会在下一轮又被压缩一次。
- decision：①**四条常驻规则（元机制先行）**：**规则 1 双层结构**——原始声明层（逐字、只追加、不可改写；不得「精简」/「归纳」/「换个说法让它更好读」；截断必须用「…」）+ 派生层（每条派生项必须带 `source` 锚点指向原始声明层稳定 ID）；**规则 2 三档结论**——每个需求单元落且只落一档 `接受` / `接受但有偏离` / `拒绝`，偏离必须写明「**改了什么**」+「**为什么**」且「原口径 vs 当前口径」逐字对照，拒绝必须写明理由与依据，**不许留空**；**规则 3 强度校验**——覆盖核对必须**逐条比对原始需求是否被削弱**（三问：①限定词还在吗？②动作被替换成什么了？③削弱是否可见？），产物＝偏离/拒绝清单必须逐条可见；**规则 4 待复核**——原始声明一旦改动，所有派生项标记「待复核」（当前状态：**无**，V-01..V-28 为首次建立，无改动）；②**物理分离**：逐字区只在本节内且只写一次；正文 / spec / phase / 索引只放稳定 ID 指针，**不粘贴原话**；**契约字面量例外**（接口契约、错误码、命令、验收判据这类必须逐字复制才能正确执行的，按 T-018 判据 3 允许逐字复制）；③**五条防压缩判据（可操作）**：㈠任一派生条目无 `source` 锚点即失败；㈡任一需求单元无三档即失败；㈢覆盖矩阵无计数即失败；㈣偏离项无「改了什么+为什么」即失败；㈤原始声明层出现改写版本即失败；④**执行边界（T-030）**：本轮调查与三分类**只产出事实与选项，不替用户做取舍**；C 类清单必须逐条附冲突对方原文锚点。
- source_type/reference/exact_excerpt：user_reply + 母 PRD 共享定义／T-024 用户选择「先建保真机制，再逐条补回」／T-026 用户选择「先建立 SD-09 保真机制（包含在 T-024 的第一步）」／T-030 约束「以上两路调查与三分类**只产出事实与选项，不替用户做取舍**；C 类清单必须逐条附冲突对方原文锚点」／母 PRD SD-09「需求保真采用逐字双层结构——**原始声明不得改写**,派生需求带 source 锚点」／「结论分三档:**接受 / 接受但有偏离 / 拒绝**;偏离必须写明**改了什么、为什么**;原话一改,派生项**标记待复核**」
- approval_binding：待 step 11 approve-decision 的最终确认绑定
- facts_and_constraints：`research/REQ-full-inventory.md` 316 个需求单元，未完整承接 38 条（压缩 6、丢失 27、未登记排除理由 5）；机制诊断 #6「覆盖核对只按类别、不校验强度…**弱化在检查里不可见，所以既没被发现也没被告知**」；T-001..T-023 期间**不存在**原始声明层与三档结论，派生条目只写「用户选择」，**无从判断哪些是「接受但有偏离」**（V-13 已点名的缺陷在本卡的实际复发）；母 PRD 局部风险 D-31 已点名「刚性文档仪式」风险
- Logic：压缩之所以能发生且不被发现，是因为**对照面不可见**。五条判据把「有没有被压缩」从主观判断降为可核对事实（任一条为否则判压缩发生）。限定词守恒有实证：L-01 丢的是否定词「不建 severity 门槛」，L-03 丢的是数量词「12 条」，L-06 丢的是条件词「仅当…时」与例外词「纯文档等」。
- choice_reason/impact：本卡后续所有需求处置都必须带三档结论；覆盖矩阵从「零孤儿」升级为「**零孤儿 + 零未标偏离 + 零无计数**」；决定 decision-log / spec / phase 三种文档的「长文从哪来」——唯一被允许的长文是逐字区，且只出现在单一节内。
- consequences_and_risks：**R-28 保真机制本身可能变成新的刚性文档仪式**（缓解：逐字区与指针区物理分离，逐字只保留在原始声明层，正文只放指针）；**R-30 三档表本身可能成为新的长文档负担**（缓解：316 条中只有被判「偏离」或「拒绝」的需逐条展开，其余按主题聚类汇总计数）；R-21 无数值约束时「太长」可能复发（缓解：语义判据做成可测量检查 + ①合并审查抽查，不引入新的机器门禁）；R-34 调查范围可能再次扩张（缓解：以 U-002 触发的「真实用户问题」定性为停止规则的正当例外，本轮只做清点/扩散/补回/根因）。
- rejected_alternatives：直接逐条补回而不先建保真机制（补回的需求会被再次压缩）；按类别查「有无覆盖」即判通过（V-13 点名的缺陷形态）；把逐字原文复制进正文/spec/phase（违反「一事实一权威」）；把三档表做成 316 条全文展开（与精炼冲突）；替用户在 A/B/C 三分类中做取舍。
- unresolved_items/owner：保真结构的维护与复核＝owner 为 make-decision 主会话，复核时机＝每次原始声明改动（触发待复核）、每次覆盖矩阵更新（触发计数核对）、①合并审查时抽查
- Supersedes：无（T-024/T-026/T-030 为追加式建立，不取代既有条目）
- module：需求保真元机制
- requirement_ids：[REQ-B-21, REQ-B-22, REQ-O-07, REQ-R-10, REQ-R-11, REQ-R-15, REQ-R-16]
- derived_from：[V-03]（→ REQ-B-21 → T-026）/（→ REQ-B-22 → T-026）/（→ REQ-O-07 → T-030）
- artifacts：[本文件 `## 覆盖矩阵`、`### A 类补回清单`、`### B 类排除清单`]

### D-012 偏离项重确与 25 条拒绝项的三分类处置

- question/final_option：已判为「接受但有偏离」和「拒绝」的需求如何处置？用户需要逐条裁多少？／final_option=偏离项重确范围＝全部 14 条；25 条拒绝项先三分类（A 11 / B 7 / C 7），用户只裁 C 类
- recommendation/plain_language：先把 25 条分成「明显该补回的」「明摆着不属于本卡的」「真的需要你拍板的」三堆，这样你只需要看第三堆那 7 条，其余按规则自动处置。
- decision：①**偏离项重确范围 = 全部 14 条**：L-01、L-02、L-03、L-04、L-05、L-17、L-21、L-22、L-28、L-29、L-30、L-31、L-32、NEW-01（T-025 原列 3 条，按同一判定口径实有 14 条）；②**25 条「拒绝」项的处理方法 = 先三分类**：**A 应补回 11 条**（L-09、L-10、L-11、L-12、L-13、L-14、L-15、L-18、L-24、L-25、L-38）→ 直接补回、无需用户逐条确认（仍记录为「接受」）；**B 应登记排除理由 7 条**（L-07、L-19、L-20、L-23、L-27、L-33、L-34）→ 登记归属卡号或非目标原文作为排除理由（满足母 PRD「每项需求有负责工作包或明确排除理由」）；**C 需用户裁决 7 条**（L-06、L-08、L-16、L-26、L-35、L-36、L-37）→ 逐条给「需求原文 + 冲突对方原文 + 补/不补代价 + 2–3 可选处置」，由用户裁决；③**C 类判定硬条件**：**必须存在明确的冲突对方原文锚点**；无冲突对方者不得入 C 类（R-32 缓解）；④**三分类的自检**：须回答「有没有『看似 B 类实则 A 类』的误判」并给出 C 类占比（**C = 7/25 = 28.0%**；A 11 条 44.0%，B 7 条 28.0%）；⑤**偏离的最终处置由用户在重确环节决定**：保留偏离（写明理由）／撤销回到原口径／改为第三种方案；该重确属于 U-002 触发的「真实用户问题」，符合停止规则的正当例外。
- source_type/reference/exact_excerpt：user_reply + 调研产出／T-027 用户选择「全部 14 条重确」／T-028 用户选择「先做三分类（A 应补回 / B 应登记排除理由 / C 需用户裁决），用户只对 C 类逐条裁决」／T-025 用户选择「标为「接受但有偏离」并请用户重新确认」／RC-rejected-25-triage「**计数：A = 11（44%）／B = 7（28%）／C = 7（28%）／合计 25。**」
- approval_binding：**本块 ① 与 ②C 本身就是「待用户真实裁决」的动作**；待 step 11 approve-decision 的最终确认绑定
- facts_and_constraints：L 表三档分布＝`接受` **0** / `接受但有偏离` **13** / `拒绝` **25**（合计 38，另加 NEW-01 后实为 **14 偏离 + 25 拒绝**）；其中 L-17、L-21 在 `draft-fidelity.md` 的「拒绝」表内标 `拒绝` 而在分布中计为 `接受但有偏离`（**COUNT-03**，并入前须处理）；偏离项「原口径 vs 当前口径」逐字对照见 `draft-fidelity.md` 第二部分
- Logic：三分类把「用户工作量」从 25 条逐条压到 7 条，同时不牺牲「每项需求有负责工作包或明确排除理由」的母 PRD 要求：A 类补回、B 类登记排除理由都是**登记/引用/落位**动作（不新增机制、不新增文件、不新增门禁），只有真正存在**方向级取舍**且**有冲突对方原文**的才交用户。
- choice_reason/impact：决定本轮 Talk 的议题量与用户裁决面；决定 A/B 两类可以批量自动处置、C 类必须逐条呈现；决定偏离项的「保留/撤销/第三种方案」三选一形态。
- consequences_and_risks：**R-29 L-01/L-05 涉及审查机制核心语义，若保留偏离需与 CARD-05 的共享权威句一致**，否则两卡口径分裂（缓解：偏离一旦确认，写入共享定义并由两卡引用同一句）；**R-31 14 条逐条重确耗时较长**（缓解：每条给出逐字对照与 2–3 个可选处置，用户可批量同意低争议项）；**R-32 三分类可能把 C 类误判为 A/B 以减少用户工作量**（缓解：分类自检 + C 类必须引用冲突对方原文锚点）；若 A 类实施时发现某条需要新增机制或新增文件，应回到本表重判 C。
- rejected_alternatives：25 条逐条都交用户裁决（工作量不可接受）；把 C 类压缩为 A/B（R-32 点名）；无冲突对方原文锚点者列入 C 类；把偏离直接按当前口径定稿而不重确（L-01/L-04/L-05 已被判偏离）；把 B 类的 card-02 侧切片一并推给别的卡（T-038 已裁定登记本卡切片动作）。
- unresolved_items/owner：14 条偏离的用户重确（owner=make-decision 主会话 + 用户）；C 类 7 条的用户逐条裁决（owner=用户）；T-001..T-023 的**三档结论回填**（owner=make-decision 主会话，见 `draft-fidelity.md` 对账发现 4）；**COUNT-01**（T3 标称 9、实列 11）、**COUNT-02**（T15 含不存在的 `J-25`、非 REQ 的 `SD-10`、占位 `R-…`）、**COUNT-03**（拒绝表含 27 行 vs 分布 25 条）的重核（owner=make-decision 主会话）
- Supersedes：无（T-025/T-027/T-028 为追加式建立；T-027 扩大 T-025 的重确范围，T-025 原文保留）
- module：偏离重确与三分类
- requirement_ids：[REQ-L-14, REQ-J-12, REQ-R-10, REQ-R-11, REQ-B-22]
- derived_from：[V-04]（→ REQ-B-22 → T-027）/（→ REQ-J-12 → T-028）
- artifacts：[本文件 `### A 类补回清单`、`### B 类排除清单`、`## 覆盖矩阵` 的偏离/拒绝计数]

### D-013 L-16 之外的执行边界与遗漏补回范围

- question/final_option：本轮调查与补回的执行边界是什么？哪些事不在本轮做？／final_option=两路调查与三分类只产出事实与选项；本轮只做清点/扩散/补回/根因；范围不再扩张
- recommendation/plain_language：这一轮是「把漏掉的找出来、把选项摊开」，不是「顺手把能改的都改了」；边界写死，避免调查本身变成新范围。
- decision：①**T-030 的执行边界**：三分类与根因调查**只产出事实与选项，不替用户做取舍**；C 类清单必须逐条附冲突对方原文锚点；②**本轮范围锁定**：以 U-002 触发的「真实用户问题」定性为停止规则的正当例外（X-22），本轮只做**清点（REQ-full-inventory）/ 扩散（DIV-option-space）/ 补回（A 类 + 偏离项）/ 根因（RC 两份）**，**不夹带猜测式流程优化**；③**调查范围不再扩张**（本轮已新增 3 路分析：根因、CARD-07 边界、三分类）；④**扩散产物登记**：`research/DIV-option-space.md` 的五轴 **103 个展开选项 + 36 个补充变体**（轴 1 = 16 / 轴 2 = 16 / 轴 3 = 16 / 轴 4 = 40 / 轴 5 = 15；算术闭合 16+16+16+40+15 = 103），含 **§6 组合性总览**（组合关系记号 `⊞ 可叠加` / `⊖ 互斥` / `⚙ 需改造`；高频可叠加组合 C-A..C-J；互斥对 18 组；需改造组合 11 组）；⑤**§6.5 四个空白如实登记**（见本文件末「覆盖缺口与不闭合项」的 GAP-6）。
- source_type/reference/exact_excerpt：user_reply／T-030 约束原文「以上两路调查与三分类**只产出事实与选项，不替用户做取舍**；C 类清单必须逐条附冲突对方原文锚点」／T-030 风险原文「调查范围可能再次扩张（本轮已新增 3 路分析）。缓解：以 U-002 触发的「真实用户问题」定性为停止规则的正当例外（张力 X-22），本轮只做清点/扩散/补回/根因，不夹带猜测式优化」
- approval_binding：待 step 11 approve-decision 的最终确认绑定
- facts_and_constraints：`research/DIV-option-space.md`（1474 行，232,066 字节）自述「**五个轴的选项数分别为 16 / 16 / 16 / 40 / 15**，合计 103…另有 36 个补充变体」；§8.1 逐轴主标记分布 实证 94 / 间接 8 / 缺口 1（实证占比 91.3%）；§10 自我限制
- Logic：U-002 的两条元反馈是「调研分析太片面」（M-1：扩大调研面）与「一直在收敛，没帮我扩散」（M-2：先做无推荐无排序的选项空间枚举）。把扩散**做成产物**（103 选项 + 组合总览 + 四个空白）是对 M-2 的可核对应答；同时用 T-030 把「扩散」与「猜测式优化」区分开。
- choice_reason/impact：决定本轮 Talk 的议题边界与用户的挑战面；决定 `DIV-option-space.md` 的哪些结论（尤其 §6.5 四个空白）需要落到 spec 或交下游卡。
- consequences_and_risks：**R-44 与根因报告的历史教训同型（只登记易复发）**——本卡的登记须包含**输入注入义务**：把「316/38 清点结果 + 扩散需求」显式写进 CARD-07 承接 task 的输入清单（母 PRD L453 的 CARD-07 最小读取集不含 card-02 材料）；R-34 调查范围可能再次扩张（缓解见 ②）；§6.5 的四个空白**没有任何归属卡或 owner**（本文件不擅自指派，见 GAP-6）。
- rejected_alternatives：本轮顺手做 C 类之外的流程优化（违反停止规则）；把扩散做成一次性动作而不留产物（U-002 M-2 会复发）；替用户在三个选项间做取舍；为 §6.5 的四个空白指定归属卡（超出本卡边界，需用户或母 PRD 裁决）。
- unresolved_items/owner：§6.5 四个空白的归属（**owner 未指定**，待用户或母 PRD 裁决）；扩散需求的输入注入（owner=CARD-07 承接 task）；DIV-option-space 在 card-02 材料中的产出登记（owner=make-decision 主会话）
- Supersedes：无
- module：执行边界与扩散登记
- requirement_ids：[REQ-O-03, REQ-O-04, REQ-O-05, REQ-O-06, REQ-L-17, REQ-L-18, REQ-N-10, REQ-I-16]
- derived_from：[V-21]（→ REQ-I-16 → T-030）/（→ REQ-L-17 → T-037）
- artifacts：[`research/DIV-option-space.md`（§6 组合性总览、§6.5 四个空白）、本文件「覆盖缺口与不闭合项」GAP-6]

### D-014 收敛检查、实质变化程序与遗留项归属

- question/final_option：本轮收敛到什么程度算结束？U-001 的范围扩张走了实质变化程序没有？哪些事项显式延期？／final_option=本卡补做前后承诺展示 + 停止规则定性 + 逐项登记延期归属
- recommendation/plain_language：范围变了就得把「原来答应什么、现在改成什么」摆出来让人确认，而不是事后记录一句；另外把「还有哪些不在这轮做」一条条写清归属，避免悬空。
- decision：①**本卡补做实质变化程序（前后承诺展示）**：U-001 的 E-1/E-3/E-5/E-6 属实质变化、当时未走该程序，本轮补做「原承诺 vs 扩张后承诺」的逐条展示并取得用户真实确认，**并在材料中如实写明「该程序在 U-001 发生时未执行，本轮补做」，不得伪装为事前执行**（R-40）；②**停止规则**：`CARD-10 通过后进入停止规则——只有真实用户问题或真实失败才触发新一轮修改，不做猜测式流程优化`；本轮 U-002 定性为「**真实用户问题**」触发（正当例外，X-22 调和方向），本轮只做**清点 / 扩散 / 补回 / 根因**，不夹带猜测式优化；③把 U-001 收尾「再也不来来回回的优化」登记为**目标属性 + 停止条件**；④**延期项逐项登记归属**：㈠长度不设数值目标（**永久，非遗留**）；㈡K1–K12 的逐条承接者与 6–8 个改造技能的具体切分与命名（build-plan）；㈢V1/V2/V3 可验证性分级细化（build-plan）；㈣自检清单条目微调（build-plan，机制本卡定稿）；㈤L2 退出机制的复核时机（CARD-10 或后续复盘）；㈥无障碍/中文可读性度量（**未采用**，理由：R-E 判定公式对中文技术文档失效）；㈦spec 结构中的字段必填项与行内预算（build-plan）；㈧真实多 phase task 的**外部**抽验＝带触发条件的延期项（触发=首个使用新格式的实施 task 落地，owner=CARD-10）；⑤**受影响的条款/卡片与在途影响登记**：母 PRD CARD-02 卡节、CARD-05 卡节、PRD 非目标句；CARD-10 核对项；CARD-07 写面冲突（D-010 的 R-35/R-38）。
- source_type/reference/exact_excerpt：user_reply + 母 PRD 交付说明／T-033 用户选择「本卡补做前后承诺展示」／母 PRD「**实质变化**(方向、权限、范围、设计行为、验收)须先展示前后承诺并获得真实确认,然后记录来源 revision、受影响条款/卡片与在途影响。」／母 PRD「**停止规则**:CARD-10 通过后进入停止规则——**只有真实用户问题或真实失败才触发新一轮修改,不做猜测式流程优化**」
- approval_binding：**本块的 ① 本身就是「待用户真实确认」的动作**；待 step 11 approve-decision 的最终确认绑定
- facts_and_constraints：U-001 明确改变了方向（SDD 流程优化）、范围（并入 CARD-05）、验收（四属性）→ 属典型实质变化；母 PRD 变更说明 S8；母 PRD 交付说明 L644-645；V-21 三条约束；V-14 停止规则；R-E §8 可读性度量节（公式对中文失效）
- Logic：母 PRD 的变更纪律要求「先展示前后承诺 + 获得真实确认」再记录影响面；以「如实记录 + Talk 中澄清」替代它会使范围扩张缺少真实确认（L-16 判为拒绝）。停止规则把「反复优化」从无终点变为有触发条件。
- choice_reason/impact：决定本轮 Talk 的交互形态（重建目标 → 揭示方案 → 逐条挑战）；决定哪些事项在本卡不做且由谁承接；决定 CARD-10 与 CARD-07 的核对清单。
- consequences_and_risks：**R-40 补做程序属「事后补程序」**（缓解：如实写明未事前执行，不伪装）；R-18 L2 复核时机若无人执行则退出机制空转；遗留项若无人承接会在后续卡之间悬空；V-26 形态须以 Q17 定稿为准（本块与 D-002 联动）。
- rejected_alternatives：把补做伪装为事前执行；用「如实记录 + Talk 中澄清」替代实质变化程序；把本轮扩为「猜测式流程优化」；本卡擅自定死技能命名与字段预算（会使 build-plan 无设计空间）；采用无障碍/中文可读性度量公式。
- unresolved_items/owner：见本块 ④㈠–㈧ 逐项 owner；受影响条款/卡片与在途影响的最终登记（owner=make-decision 主会话）；Q17 定稿形态（owner=CARD-10 核对）
- Supersedes：无
- module：收敛与遗留
- requirement_ids：[REQ-Q-13, REQ-Q-15, REQ-Q-07, REQ-K-19, REQ-K-20, REQ-I-21, REQ-G-02, REQ-G-13]
- derived_from：[V-14]（→ REQ-Q-13 → T-033）/（→ REQ-Q-15 → T-033）/（→ REQ-K-19 → T-033）
- artifacts：[本文件 `## 收敛检查` 四行表、`## 覆盖矩阵` 的延期计数]

---

## 覆盖矩阵

> 立项目的：修复 **NEW-01**（「追踪表追加行 + 计数同步」被降为定性「零孤儿」→ 不可证伪）。
> **重算口径（唯一权威）**：本节第 2 层的三档分布**以 `REQ-full-inventory.md` 第一部分的逐条处置状态为唯一权威**逐条重算，**不采用主题聚类表（第三部分）的标称数**，也不采用草稿自述值。判定映射（可复核、可反驳）：
> - `已登记…` → **接受**；`部分登记…` → **接受但有偏离**；`丢失…` / `未处理…` / `被压缩…` → **拒绝**。
> - 复合标签逐条登记：`已登记→又回退`（REQ-O-08）→ **接受但有偏离**；`被压缩/冲突`（REQ-Q-10）→ **拒绝**。
> - **敏感度**：若上述两条复合标签按相反口径映射（`REQ-O-08`→接受、`REQ-Q-10`→偏离），则为 **100/128/88**，**仍合计 316**（闭合不依赖这两条的取舍）。

### 计数行（可核算术）

**第 1 层：18 来源组 → 316**

| 项 | 数值 | 算术 |
| --- | --- | --- |
| 原始需求单元总数 | **316** | 8+36+12+36+26+8+14+15+39+16+23+25+3+10+9+4+16+16 = **316** |
| 自述数（`REQ-full-inventory.md`「附：清点方法与自检」） | **316** | 「本报告覆盖 A–R 共 **316 个需求单元**…」 |
| 差额 | **0** | 逐节 ID 连续、无缺号、无重号（脚本核对：316 行、无重复 ID） |

**第 2 层：三档结论（重算，口径来源 = 第一部分逐条处置状态）**

| 档位 | 值 | 占比 | 算术 |
| --- | --- | --- | --- |
| `接受` | **99** | 31.3% | — |
| `接受但有偏离` | **128** | 40.5% | 含复合标签 1 条（REQ-O-08） |
| `拒绝` | **89** | 28.2% | 含复合标签 1 条（REQ-Q-10） |
| **合计** | **316** | 100% | 99+128+89 = **316** ✅ 与第 1 层闭合 |
| *（对照）旧草稿自述值* | *104/95/117* | — | *不可由第一部分重算，**已废弃**（NG-4）* |

**第 3 层：L 表（L-01..L-38 + NEW-01）三档结论**

| 档位 | 值 | 条目 | 算术 |
| --- | --- | --- | --- |
| `接受` | **0** | — | — |
| `接受但有偏离` | **14** | L-01, L-02, L-03, L-04, L-05, L-17, L-21, L-22, L-28, L-29, L-30, L-31, L-32, NEW-01 | — |
| `拒绝` | **25** | L-06..L-16（11）+ L-18..L-20（3）+ L-23..L-27（5）+ L-33..L-38（6） | — |
| **合计** | **39** | 38 条 L 条目 + NEW-01 | 0+14+25 = **39** = 38+1 ✅ |

**三层关系（GAP-1 修复）**

| 关系 | 结论 | 算术 |
| --- | --- | --- |
| 第 1 层内部 | ✅ 闭合 | 18 组加总 = 316 |
| 第 2 层内部 | ✅ 闭合 | 99+128+89 = 316 |
| 第 3 层内部 | ✅ 闭合 | 0+14+25 = 39 = 38+1 |
| 第 2 层 ↔ 第 3 层 | ✅ **本次改判为「不同粒度视图包含关系」，不再是「不闭合」** | 第 3 层是第 2 层所辖 316 个单元中**被判偏离/拒绝的那部分单元的聚合视图**（38 条 L 条目各聚合 1–10 个第一部分单元）；其 14 条偏离与 25 条拒绝**已计入**第 2 层的 128 / 89 之内，**不得与 316 相加** |
| 主题聚类表（第三部分） | ❌ **不能与 316 比对**（非完备、且跨主题不去重） | 覆盖率仅 230–249/316；合计 329 是多计视图（NG-3） |

### GAP-1 修复：「差 13」为何产生 + 重算后的正确数字

- **旧结论（已废弃）**：第 2 层 316 与第 3 层 329「跨层差 13」，并推测 13 = L 表 13 条偏离（不含 NEW-01）被主题表重复计。
- **核查结果（失败，如实登记）**：该推测**不成立**。若 13 真是「L 表偏离被重复计」，则主题表的「偏离」列应比第一部分重算值**多 13**；实测为 **100（主题表）vs 128（第一部分重算）**，方向相反且差 −28。因此「13 = 13 条偏离」只是**数值巧合**。
- **真实成因（两条，均可复算）**：
  1. **口径混用**：把**去重后的 316 单元分区**（第 2 层）与**跨主题不去重的多计视图**（主题表）相减。`329 − 316 = 13` 是两个**不同计量单位**之差，不是同一口径的缺口。
  2. **净差在三个档位间被重新分配**：主题表（101/100/128）− 第一部分重算（99/128/89） = **接受 +2、偏离 −28、拒绝 +39**，代数和 = 2−28+39 = **+13**。可见「13」是**三档错配后的净值**，不是任何单一类别的重复计。
  3. **主题表本身不封闭**（NG-1/NG-3）：标称逐行 331、本表计 329、合计行写 341（三口径）。
- **修掉后的正确数字**：
  - 第 2 层（316 单元的完备分区）= **99 / 128 / 89**，合计 **316**；
  - 第 3 层（L 表）= **0 / 14 / 25**，合计 **39**；
  - 二者是**包含关系**：第 3 层的偏离/拒绝条目**已含在**第 2 层的 128/89 内，**不相加、不比对总数**；
  - 主题聚类表自本节起**降级为「映射用导航视图」**，其合计**不得**作为覆盖证据（表头须显式声明「同一 ID 可属多主题、不去重、且不覆盖全部 316 单元」）。

### 主表：来源组 → 处置 → 档位（18 行，可逐组加总）

> 粒度说明：316 个需求单元的**逐条**「一句话原始需求 + 逐条处置决策」表在现存材料中不存在（这正是 L-09/NEW-01 要修的对象）。本表按**来源组**给出一行一组（18 行，可加总核对），并在其后给出已逐条落地的 **L 层 39 条**与 **X 张力 22 条**。
> 「原始需求（一句话）」列逐字引用自 `REQ-full-inventory.md` 的节标题与首个需求单元原文，截断用「…」，**不改写限定词**。

| 来源 ID | 组内需求数 | `接受` | `接受但有偏离` | `拒绝` | 明确排除（第一部分层） | 算术核对 | 主处置块 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `REQ-A` 母 PRD 产品总览 | 8 | 5 | 2 | 1 | 0 | 5+2+1+0 = 8 | D-001 / D-009 |
| `REQ-B` 母 PRD 共享定义 SD-01..SD-17 | 36 | 11 | 10 | 15 | 0 | 11+10+15+0 = 36 | D-002/004/006/007/008/009/011/012 |
| `REQ-C` 母 PRD 任务地图 / 需求覆盖结论 | 12 | 8 | 4 | 0 | 0 | 8+4+0+0 = 12 | D-008 / D-011 / D-012 |
| `REQ-D` 母 PRD CARD-02 整节 | 36 | 13 | 14 | 9 | 0 | 13+14+9+0 = 36 | D-001/002/004/005/006/008 |
| `REQ-E` 母 PRD CARD-05 审查链替换 | 26 | 12 | 9 | 5 | 0 | 12+9+5+0 = 26 | D-001 / D-007 |
| `REQ-F` 母 PRD CARD-06 薄核心删除与窄工具保留 | 8 | 2 | 5 | 1 | 0 | 2+5+1+0 = 8 | D-001 / A-9 / B-7 |
| `REQ-G` 母 PRD CARD-10 与集成验收套件 | 14 | 0 | 7 | 7 | 0 | 0+7+7+0 = 14 | D-006 / D-014 / A-11 / B-7 |
| `REQ-H` 母 PRD CARD-03 / CARD-04 相关部分 | 15 | 1 | 4 | 10 | 0 | 1+4+10+0 = 15 | D-003 / D-006 / B-1 / B-3 |
| `REQ-I` 规划 decision-log 原始需求 U-001..U-010 | 39 | 17 | 16 | 6 | 0 | 17+16+6+0 = 39 | D-001/003/013/014 / C-5 |
| `REQ-J` 规划 decision-log 唯一 OI 大纲 OI-001..OI-014 | 16 | 6 | 7 | 3 | 0 | 6+7+3+0 = 16 | D-002 / D-008 / D-012 |
| `REQ-K` 规划 decision-log 决定草案 D-001 等节 | 23 | 4 | 12 | 7 | 0 | 4+12+7+0 = 23 | D-002/009/012/014 |
| `REQ-L` 规划 decision-log Talk round 4 Q1-Q17 | 25 | 6 | 9 | 10 | 0 | 6+9+10+0 = 25 | D-007 / D-012 / D-013 / A-8 |
| `REQ-M` 规划 decision-log Talk round 5 | 3 | 3 | 0 | 0 | 0 | 3+0+0+0 = 3 | D-008 / D-014 |
| `REQ-N` 规划 decision-log Grill G-1..G-4 | 10 | 1 | 7 | 2 | 0 | 1+7+2+0 = 10 | D-008 / D-014 |
| `REQ-O` 规划 decision-log make-decision 机制诊断 | 9 | 1 | 5 | 3 | 0 | 1+5+3+0 = 9 | D-010/011/013 / A-1 |
| `REQ-P` 规划 decision-log CPU 与温度实测诊断 | 4 | 0 | 1 | 3 | 0 | 0+1+3+0 = 4 | D-013 / A-9 / A-10 |
| `REQ-Q` 母 PRD 风险与交付说明、变更说明 | 16 | 2 | 8 | 6 | 0 | 2+8+6+0 = 16 | D-011/012/014 / A-3/A-10/A-11 |
| `REQ-R` card-02 用户原始陈述 U-001 / U-002 及分解 | 16 | 7 | 8 | 1 | 0 | 7+8+1+0 = 16 | D-001/004/011/012/014 |
| **合计（18 组）** | **316** | **99** | **128** | **89** | **0** | **99+128+89+0 = 316** ✅ | D-001..D-014 全覆盖 |

> 组内一句话原始需求（逐字节选，不改限定词）：`REQ-A`=「目标=把 workflowhub 从…」；`REQ-B`=「需求保真采用逐字双层结构——**原始声明不得改写**,派生需求带 source 锚点」（SD-09）与「同一事实只有一个权威文件;删除后不得存放两个文件对同一事实各自声明权威」（SD-13②）；`REQ-C`=「⑦追踪表追加 R-018..R-020 与 OI-012..OI-014 行」与矩阵结论计数更新；`REQ-D`=「AC-07…条件=抽一个**真实实施 task** 的 spec.md」「AC-08…抽一个**多 phase task** 的 phase 文档」「**验收依赖**:一个真实 task 样例」；`REQ-E`=「②build-plan 产 spec.md…完成后、build-code 开工前做合并审查一次」「**选型被预设为 OCR(纪律禁止)**…」；`REQ-F`=「保留并可脱离 kernel 独立调用的 5 项窄工具」（SD-04）与 SD-12 损失清单四项；`REQ-G`=「具体重构验收标准——可观察的成功/失败用例 + **所需证据** + **承接负责人**」「用户可按需抽验任何卡的任何 AC 事实,CARD-10 须提供**定位与原文通路**」；`REQ-H`=「FR-17:验收 oracle 与实现者分离——测试目录对实现者只读或隐藏」与 SD-06 有条件 TDD；`REQ-I`=「不要浪费任何机制统计 token、时间等…**不是复杂的流程制造机**」「**弱化所有流程中的严格验收**！…」「**不要搞这么多流程、质量、文件**！」；`REQ-J`=OI-013「校验机器彻底全删；普通文件名 + 纯文本引用」；`REQ-K`=「删除 plan.md/tasks.md 双写及其相等性协议」「多层重复 evidence 包装」；`REQ-L`=「送审/确认材料集 \| 纳入被引用来源…」「最终确认的交互形态 \| 要：先重建用户目标，再揭示当前已选方案，再让用户逐条挑战，而不是直接问"同意吗"」；`REQ-M`=「只留两道人为门，其余全删」；`REQ-N`=「方法工具包不再承诺机器认证阶段完成、跨会话证据链或自动判断全部证据过期…**不假称纯技能提供同等机器保证**」；`REQ-O`=「**覆盖核对只按类别、不校验强度**…后果：**弱化在检查里不可见，所以既没被发现也没被告知**」；`REQ-P`=「收窄上下文可降资源占用」与 skill-bundle/catalog/move-map 同步义务；`REQ-Q`=「**停止规则**:CARD-10 通过后进入停止规则…」「**实质变化**…须先展示前后承诺并获得真实确认」「需求追踪表须内嵌、计数须同步」；`REQ-R`=「我的原始需求都被你压缩了」「一直在收敛，没帮我扩散」「避免：文档太长」「避免：太啰嗦」「避免：缺少合理的验收标准」「避免：缺少测试流程」。

### 明细 A：L 层 39 条逐条处置（L-01..L-38 + NEW-01）

| 来源 ID | 原始需求锚点（稳定 ID + 逐字节选） | 处置决策 | 处置档 | 规格条目（待 build-plan） |
| --- | --- | --- | --- | --- |
| L-01 | V-06（SD-07 / Talk round 4 Q13） | **T-039**（取代 T-015）→ D-007 | `接受但有偏离` | 四档降为纯标签 + 恢复不复审 + 三态处置 |
| L-02 | V-08（AC-06 / SD-13②） | **T-040**（细化 T-020）→ D-008 | `接受但有偏离` | 双轨判据 + 「0 处双权威声明」度量 |
| L-03 | V-28（I-B §4 K1–K12） | **T-036 + T-043** → D-009 | `接受但有偏离` | K1–K12 全清单 + 合并账 |
| L-04 | V-10（AC-07 / AC-08 / oracle / 验收依赖） | **T-041** → D-006 | `接受但有偏离` | 卡内 fixture task + CARD-10 带触发条件延期 |
| L-05 | V-07（build-prd 审查 finding#16 处置） | **T-042** → D-007 | `接受但有偏离` | 共享定义区分「机制」与「finding 处置」 |
| L-06 | V-01, V-02（SD-06 + G-2） | **T-032**（C-1 裁决）→ D-006 | `拒绝` → 补回 | 可失败检查 RED（fixture 含重复权威） |
| L-07 | V-12（FR-17 / AC-17） | **B 类 B-1**（T-038/T-044 切片） | `拒绝` → 登记排除理由 | 模板「验收流程」字段引用 |
| L-08 | V-03, V-04（SD-09） | **T-026**（C-2 已落地）→ D-011 | `拒绝` → 补回 | 原始声明节 + 需求保真节（常驻） |
| L-09 | V-13（机制诊断#6） | **A 类 A-1** → D-011 | `拒绝` → 补回 | 规则 3 强度校验 + 覆盖矩阵强度列 |
| L-10 | V-05（SD-10） | **A 类 A-2** → D-011 | `拒绝` → 补回 | append-only 规则声明（decision-log 顶部） |
| L-11 | V-09（AC-10） | **A 类 A-3** → D-008 | `拒绝` → 补回 | 变更传播核对 + AC 条目 |
| L-12 | V-25（CARD-02 流程/状态 + 规划 DL 文档权威） | **A 类 A-4** → D-002 | `拒绝` → 补回 | spec 形态节 + 台账禁令 |
| L-13 | V-26（母 PRD CARD-02 局部风险） | **A 类 A-5** → D-014 | `拒绝` → 补回 | 风险清单 + 避免刚性仪式说明 |
| L-14 | 母 PRD CARD-02 共享资源冲突与集成责任 | **A 类 A-6** → D-004 | `拒绝` → 补回 | 跨卡接口协调登记 + 模板接口面 |
| L-15 | V-14（停止规则） | **A 类 A-7** → D-014 | `拒绝` → 补回 | 交付说明节停止规则 |
| L-16 | V-15（实质变化程序） | **T-033**（C-3 裁决）→ D-014 | `拒绝` → 补回 | 前后承诺展示（本轮补做） |
| L-17 | V-17（Talk round 4 Q1） | D-007 ⑥ | `接受但有偏离` | packet 必须含被引用来源清单 |
| L-18 | V-16（Talk round 4 Q12） | **A 类 A-8** → D-014 | `拒绝` → 补回 | Talk 规则条目 |
| L-19 | V-22（U-009 / FR-11 / FR-12） | **B 类 B-2**（**T-044 补登**） | `拒绝` → 登记排除理由 | 执行遵守（AGENTS.md）+ 可观察指标 |
| L-20 | V-18（方向契约 / OI-012） | **B 类 B-3**（T-038 切片） | `拒绝` → 登记排除理由 | phase 头部「写集+依赖」两字段 |
| L-21 | V-19（SD-17 / OI-012） | **A 类 A-11** → D-008 | `接受但有偏离` | 记录层严 / 推进层松 双层结构 |
| L-22 | V-11（FR-20 / SD-15 / AC-26） | D-006 ⑥ + D-011 | `接受但有偏离` | oracle 反例判据 + N/A+reason |
| L-23 | 母 PRD SD-16 / FR-52 / AC-53 | **B 类 B-4**（T-038 切片） | `拒绝` → 登记排除理由 | 适用范围区分句 |
| L-24 | V-23（母 PRD / 控制面纪律） | **A 类 A-9** → D-009 | `拒绝` → 补回 | 新增控制面登记表 |
| L-25 | V-24（SD-17 记录层规则 / 风险 R7） | **A 类 A-10** → D-004 | `拒绝` → 补回 | 命名规范 + R7 风险引用 |
| L-26 | U-001#1 / U-002-6（测试流程） | **T-035**（C-4 裁决）→ D-006 | `拒绝` → 补回 | 测试流程字段（结构要求） |
| L-27 | 母 PRD SD-06/FR-18/AC-18 + CARD-10 SD-05 | **B 类 B-5**（T-038 切片） | `拒绝` → 登记排除理由 | 「不机械重跑」执行纪律 + 一次试用 |
| L-28 | V-27（退出语义与披露纪律） | D-006 ⑥ + D-014 ③ | `接受但有偏离` | 退出语义 + 披露纪律三句 |
| L-29 | V-20（定位代价） | D-008 ⑤ | `接受但有偏离` | 检查工具定位句（README/`--help` + spec） |
| L-30 | 母 PRD CARD-02 最小读取集 | D-004 + D-012 | `接受但有偏离` | 最小读取集三层（必需/条件/通常不用） |
| L-31 | 母 PRD SD-13① / CARD-10 FR-47 | D-006 + A 类 A-11 | `接受但有偏离` | 移交三要素 + 命名交付项 |
| L-32 | 母 PRD CARD-10 存在性核对 / 用户抽验权 | D-014 + A 类 A-11 | `接受但有偏离` | CARD-10 核对点 + 留档接口 |
| L-33 | 母 PRD CARD-08 SD-03 / FR-39..FR-42 | **B 类 B-6**（**T-044 补登**） | `拒绝` → 登记排除理由 | 非目标节登记 |
| L-34 | 母 PRD CARD-06 SD-04 / SD-12 | **B 类 B-7**（T-038 切片） | `拒绝` → 登记排除理由 | 写入/授权纪律（SD-04③④） |
| L-35 | V-21（三条用户约束） | **T-036**（C-5 裁决）→ D-009 | `拒绝` → 补回 | 三条自我检查约束 + 逐项理由 |
| L-36 | 规划 DL L827-L829 / OI-006 / G-4 | **T-037**（C-6 裁决）→ D-013 | `拒绝` → 补回 | DIV 产物登记 + G-4 触发规则 |
| L-37 | 母 PRD FR-56/AC-57 + CARD-05 局部风险 | **T-034**（C-7 裁决）→ D-007 | `拒绝` → 补回 | 审查能力判据三项 + 两条纪律 |
| L-38 | 母 PRD 质量事实 + 变更说明 S8 + 加固注记⑤ | **A 类 A-11** → D-008 | `拒绝` → 补回 | 披露纪律 + 本文件计数行 |
| NEW-01 | 母 PRD CARD-12 加固注记⑦ + 变更说明 S8 | D-008 + D-011 | `接受但有偏离` | 本文件 `## 覆盖矩阵` 计数行 |
| **合计** | **39 条** | — | **0 接受 / 14 偏离 / 25 拒绝 = 39** ✅ | — |

### 明细 B：X 张力 22 条处置（X-01..X-22）

> **X 层与三档的关系（如实说明，未改判）**：`REQ-full-inventory.md` 第四部分（X-01..X-22）**原表没有「处置状态」列**、无「原始声明锚点」列、无 `V-xx` 锚点，表前导语为「只列张力两端与可能的调和方向，**不做取舍**」。因此**不给 X 编号指派三档结论**；下表「处置档」列只标注该张力在 **L 层/决策层已被处置**时的档位，**不构成对第 2 层计数的追加**。**X 的 22 条不计入第 2 层的 316 / 99 / 128 / 89。**

| X# | 张力名（一句话） | 承接决策 | 处置档（引用 L 层/决策层） |
| --- | --- | --- | --- |
| X-01 | 精炼 ↔ 无遗漏 | D-004 + D-011 | `接受`（逐字区/指针区物理分离） |
| X-02 | 少文件/薄核心 ↔ 8 技能 + 工具 + 模板 | D-009 | `接受`（合并账：净新增 0） |
| X-03 | 弱化严格验收 ↔ 验收更可执行 | D-005 + D-008 | `接受`（记录层严 / 推进层松） |
| X-04 | 保证现有审查质量 ↔ 提升审查能力 | D-007 ⑦ | `接受但有偏离`（L-37） |
| X-05 | 不建 severity 门槛 ↔ 四档分级 | T-039 → D-007 | `接受但有偏离`（L-01） |
| X-06 | 审查默认 advisory ↔ 母 PRD 拒绝降级 | T-042 → D-007 | `接受但有偏离`（L-05） |
| X-07 | 不阻断推进 ↔ 真报失败/验收失败事实 | D-007 + D-008 | `接受但有偏离`（L-21） |
| X-08 | 每 task 四独立子代理 ↔ 不强制每 task 一代理 | B 类 B-2 | `拒绝`（L-19，登记排除理由） |
| X-09 | 并行 2–5 ↔ 交互/依赖顺序 + 主会话上下文干净 | B 类 B-3 | `拒绝`（L-20，登记排除理由） |
| X-10 | 详细程度 ↔ luna 级可执行 | D-002 + D-003 | `接受` |
| X-11 | 短文档/省 token ↔ 低智力模型需要细节 | D-004 + D-003 | `接受` |
| X-12 | 一次到位不再反复优化 ↔ "等等等等"不完备 + G-4 | D-013 + D-014 | `接受但有偏离`（L-36） |
| X-13 | 母 PRD 只读 ↔ U-001 扩张改母 PRD 边界 | D-001 + D-014 | `接受但有偏离`（L-30） |
| X-14 | 真实 task 抽验 ↔ 首个新格式 task 尚未存在 | T-041 → D-006 | `接受但有偏离`（L-04） |
| X-15 | 不复审 ↔ 修复可能引入新问题 | T-039 → D-007 | `接受但有偏离`（L-01） |
| X-16 | 记录层禁哈希 ↔ F1–F7 依赖身份校验 | B 类 B-4 | `拒绝`（L-23，登记排除理由） |
| X-17 | 好读（叙事）↔ 可机检（字段化契约） | D-002 | `接受` |
| X-18 | 逐字保真 ↔ 精炼 | D-011 | `接受` |
| X-19 | 低智力模型可执行 ↔ 过度规格化 | D-003 | `接受` |
| X-20 | 机器产物按路线适用（可 N/A）↔ 检查须真报失败 | D-006 ⑥ | `拒绝` → 补回（L-22） |
| X-21 | 上下文控制 ↔ 需求全量清点完整到达 | D-013 + D-012 | `接受但有偏离`（L-19） |
| X-22 | 停止规则 ↔ 用户要求继续检查/扩散/补回 | D-013 + D-014 ② | `接受` |

### GAP-9 修复：REQ 清单的非 ID token 与不存在的 ID（逐项修正值）

| # | 原 token（位置） | 问题 | **修正值** | 依据 |
| --- | --- | --- | --- | --- |
| 1 | `J-25`（T15 列表） | `REQ-J` 只到 `REQ-J-16`，**该 ID 不存在** | **`REQ-J-12`** | T15=历史只读/append-only；`REQ-J-12`（OI-010 resolution）含「收敛为 append-only，发现缺口只追加不覆盖」——语义唯一匹配（`REQ-J-16` 是合并审查，语义不符） |
| 2 | `SD-10`（T15 列表） | **不是 REQ 编号**（是母 PRD 共享定义编号） | **`REQ-B-23`** | 共享定义 SD-10 在第一部分的对应单元即 `REQ-B-23`（append-only） |
| 3 | `R-…（card-02 T-018 作废写法）`（T15） | **占位符**，非 ID | **移出 REQ 单元表**，改写为 **`card-02 T-018`**（决策 ID） | `T-018` 是 card-02 决策，不是 `REQ-*` 单元；占位符不得留在 REQ 清单 |
| 4 | `R-… (card-02 T-021)`（T9） | 同上 | **移出 REQ 单元表**，改写为 **`card-02 T-021`** | 同上 |
| 5 | `T6` 标称 **52** | 展开 `B-10..B-20`(11) + `E-01..E-26`(26) + 尾段 14 = **51**，差 1 | **标称改为 51** | 尾段逐项可列（`J-07,J-16,K-18,K-19,L-01,L-12,L-17,M-03,N-03,N-06,N-07,Q-05,R-14,R-16` = 14）；现存材料中**不存在第 52 个单元**，差额无对应 ID → 判为标称错误 |
| 6 | `E-3（card-02 E-3）`（T3）、`E-6（card-02 E-6）`（T7） | 与 `REQ-E-03` / `REQ-E-06` **同形冲突** | 改写为 **`card-02 E-3` / `card-02 E-6`**（不带 `REQ-` 命名空间连字符） | 命名空间隔离；`REQ-E-03`（只换工具不减审查点）与 card-02 自身分解 E-3 语义不同 |
| 7 | `L-26/L-27/L-28/L-31/L-33/L-34/L-35`（T5/T8/T11/T13） | **非 REQ 单元**（`REQ-L` 只到 `REQ-L-25`）；本项为草稿 GAP-9 **漏列**（NG-2） | **全部移出 REQ 单元表**，改写为 **`L-26` … `L-35`（L 表条目）** | 这 7 个 token 不可解析为任何第一部分单元 |
| 8 | `L-01..L-25` 形态 token（26 个唯一值中的其余 19 个） | 与 `REQ-L-nn` **同形**，语义为 L 表条目 | **保留 `L-nn` 前缀但显式标注「L 表条目，非 `REQ-L-nn`」**，并**不得**计入 REQ 单元覆盖 | 例：T8 的 `L-13/L-14/L-17/L-18/L-24/L-25` 语义为 L 表条目，而 `REQ-L-13/L-14/L-17/L-18/L-24/L-25` 是另外 6 条完全不同的需求 |
| 9 | `OI-005 相关: R-04`（T3） | `OI-005` 非 REQ 单元 | 保留 `REQ-R-04`，`OI-005` 改写为 **`规划 DL OI-005`（非 REQ 单元）** | `OI-005` 是规划 decision-log 的 OI 编号 |
| 10 | `T-023 相关`（T11） | 非 REQ 单元 | 改写为 **`card-02 T-023`（决策 ID）**；对应的 REQ 单元为 `REQ-D-25/D-26/D-28` | `T-023` 是 card-02 决策 |
| 11 | `U-001 E-5`（T11） | 非 REQ 单元 | 改写为 **`card-02 E-5`**；对应 REQ 单元 **`REQ-R-06`** | `E-5` 是 card-02 的 U-001 分解项 |
| 12 | 「COUNT-01 / COUNT-02」字样 | `REQ-full-inventory.md` **全文不存在**这两个编号（grep 命中 0），也**无任何 `V-xx` 锚点** | 保留为 **`decision-log.md` / `draft-fidelity.md` 引入的编号**，并注明来源文件本身未登记 | 编号来源须可追溯 |

> **修正后的可核对结论**：修正 1–4、7–11 之后，T 表（第三部分）**不再包含任何不存在的 ID**；修正 5 之后 T6 标称 = 51；修正 6、8 之后**命名空间冲突全部显式标注**。**但主题表仍不是 316 的完备分区**（覆盖率 230–249/316，见 GAP-1/NG-3），因此主题表**不得**作为覆盖证据，只作导航。

### 「零孤儿」的算术依据（每组 需求数 = 接受 + 偏离 + 拒绝 + 明确排除）

**恒等式（先说口径，避免重复计）**：

```
每组 需求数 = 接受 + 接受但有偏离 + 拒绝
拒绝 = 明确排除（已登记归属卡或排除理由） + 无主拒绝（未登记排除理由）
```

> ⚠️ **如实指出**：任务给定的写法「需求数 = 接受 + 偏离 + 拒绝 **+ 明确排除**」若把「明确排除」作为**第 4 个加项**会**重复计数**（明确排除本身就是拒绝的子集）。本节按上述**子项**口径给出，算术仍闭合。

**第一部分层的「明确排除」= 0**（NG-5）：第一部分中仅 7 条被拒条目在分析文字里点到归属卡（`REQ-B-05`/`REQ-B-27`/`REQ-J-09`/`REQ-K-21`/`REQ-L-10`/`REQ-N-04` → CARD-08、CARD-06；`REQ-I-35` → CARD-09），但**其处置状态列均写「未登记排除理由」或仅记归属卡而未登记理由**，故第一部分层的「明确排除」计数为 **0**，全部 89 条拒绝均属「无主拒绝」。

**明确排除在本收口后的落地位置**：由 **L 层 B 类 7 条**登记（`L-07`→CARD-04、`L-19`→CARD-03、`L-20`→CARD-03、`L-23`→CARD-06、`L-27`→CARD-04/03/10、`L-33`→CARD-08、`L-34`→CARD-06）。按 append-only（V-05）：**不回溯改写第一部分的 316 层计数**，两层各自登记、互不覆盖。

**逐组算术（列表 = 计数）**：

| 来源 ID | 需求数 | = 接受 | + 偏离 | + 拒绝 | （其中明确排除） | 验算 |
| --- | --- | --- | --- | --- | --- | --- |
| `REQ-A` | 8 | 5 | 2 | 1 | 0 | 5+2+1 = 8 ✅ |
| `REQ-B` | 36 | 11 | 10 | 15 | 0 | 11+10+15 = 36 ✅ |
| `REQ-C` | 12 | 8 | 4 | 0 | 0 | 8+4+0 = 12 ✅ |
| `REQ-D` | 36 | 13 | 14 | 9 | 0 | 13+14+9 = 36 ✅ |
| `REQ-E` | 26 | 12 | 9 | 5 | 0 | 12+9+5 = 26 ✅ |
| `REQ-F` | 8 | 2 | 5 | 1 | 0 | 2+5+1 = 8 ✅ |
| `REQ-G` | 14 | 0 | 7 | 7 | 0 | 0+7+7 = 14 ✅ |
| `REQ-H` | 15 | 1 | 4 | 10 | 0 | 1+4+10 = 15 ✅ |
| `REQ-I` | 39 | 17 | 16 | 6 | 0 | 17+16+6 = 39 ✅ |
| `REQ-J` | 16 | 6 | 7 | 3 | 0 | 6+7+3 = 16 ✅ |
| `REQ-K` | 23 | 4 | 12 | 7 | 0 | 4+12+7 = 23 ✅ |
| `REQ-L` | 25 | 6 | 9 | 10 | 0 | 6+9+10 = 25 ✅ |
| `REQ-M` | 3 | 3 | 0 | 0 | 0 | 3+0+0 = 3 ✅ |
| `REQ-N` | 10 | 1 | 7 | 2 | 0 | 1+7+2 = 10 ✅ |
| `REQ-O` | 9 | 1 | 5 | 3 | 0 | 1+5+3 = 9 ✅ |
| `REQ-P` | 4 | 0 | 1 | 3 | 0 | 0+1+3 = 4 ✅ |
| `REQ-Q` | 16 | 2 | 8 | 6 | 0 | 2+8+6 = 16 ✅ |
| `REQ-R` | 16 | 7 | 8 | 1 | 0 | 7+8+1 = 16 ✅ |
| **合计** | **316** | **99** | **128** | **89** | **0** | **99+128+89 = 316** ✅ |

**零孤儿结论（可证伪表述）**：18 组**逐组**满足 `需求数 = 接受 + 偏离 + 拒绝`，加总 = 316；**没有任何一个需求单元被排除在三档之外**；**没有任何一档留空**。**「零孤儿」在本收口后带计数、可逐组复算**（满足 NEW-01 与 L-09 规则 3）。

---

## 收敛检查

| 项 | 内容 | 依据 |
| --- | --- | --- |
| **target** | **本轮收敛由真实用户答复驱动，未引入任何新需求。**①方向输入全部来自母 PRD CARD-02 卡节与规划 decision-log（只读）；②U-001 的范围扩张（E-1 三种文档形态设计 / E-3 精炼·好读·无遗漏·可执行四属性 / E-5 CARD-05 plan 审查并入 / E-6 build-spec·build-plan 质量核心保留 / E-7 现有四材料病灶）为用户口述追加；③U-002 为用户口述复查要求 + 流程方法批评（M-1 调研太片面 / M-2 只收敛不扩散 / M-3 原始需求被压缩），是**真实用户问题**；④本轮**未产生新需求**：T-001..T-047 全部落在 U-001/U-002 的分解面或母 PRD 原文面；U-002-14「等等等等」被显式登记为**开放性声明**而非新需求；⑤`REQ-full-inventory.md` 自述「本报告覆盖 A–R 共 316 个需求单元，但 U-002-14「等等等等」明示用户清单不完备，故本表是**最小覆盖集**，不是完备集」——即 **316 是下界**，本文件不把它当作完备集宣称。 | U-001（E-1/E-3/E-5/E-6/E-7）；U-002（U-002-1..U-002-14，含 M-1/M-2/M-3）；T-001（任务类型＝`普通任务`）；母 PRD CARD-02 卡节 + 规划 decision-log 为只读输入（T-003）；`V-14` 停止规则；`X-22`（U-002 定性为真实用户问题）；`REQ-full-inventory.md`「附：清点方法与自检」已知局限 |
| **scope** | **同 target：范围由上述真实答复界定，未自行扩边。**㈠**并入**：CARD-05 的 ①`build-plan 合并审查`（FR-56/AC-57 + AC-23/AC-26 的①子句）+ 适配合同（含接口定义，**不做工具实测**）；U-001 的 E-1/E-3/E-5/E-6；L-35「不应纯新增，应替换合并」→ 交付**合并账**。㈡**交出**：②③④代码审查点、新工具接入、对比实验与 go/no-go → CARD-05；文件物理删除 → CARD-06；窄状态集 7 值 / 旧任务只读 / 回读检查 / 跨会话接续 → CARD-08（L-33）；5 窄工具具名与损失清单 → CARD-06（L-34）；五阶段派子代理与并行五件套 → CARD-03（L-19/L-20）；验收 oracle 与实现者分离 → CARD-04（L-07）；结果复用/第一条贯通路径即试用/RED→GREEN 证据归属 → CARD-04/CARD-03/CARD-10（L-27）；发散/扩散常驻机制与 G-4 → CARD-07（T-037）；真实多 phase task 外部抽验 → CARD-10（带触发条件延期）；**DIV §6.5 四个空白**（冻结审查合同、blind 核验）→ CARD-05、**K 失效条件维护**→ CARD-07、**跨 phase 契约一致性**→ CARD-03（T-045，见承接清单）。㈢**本卡做的两个最小越界面**：`runtime/stage` 覆盖校验器粒度修复 + 接线（T-031，已登记 R-35/R-36/R-37/R-38）；检查器字段识别兼容修复（T-046，登记为 **build-code 交付物**，本阶段不改代码）。㈣**非目标七条**：不回写母 PRD / 不做工具选型实测 / 不做物理删除 / 不高风险用户可见 / 不新增第五份材料·public command·持久对象 / 不引入机器门禁 / 不修改 CARD-01 冻结的阶段·材料接口蓝图。㈤**输入边界**：母 PRD 最小读取集（必需＝本卡 + SD-02/SD-13 + D-001 对应节；条件＝Q6/Q17、形态疑义时；通常不用＝其他卡细节、历史 review 原件），行号口径改为**语义锚点/节标题**。 | T-002；T-003；T-031 + R-35/R-36/R-37/R-38；**T-046**（检查器修复＝build-code 交付物）；T-036/T-043；T-037；T-038 + **T-044**；**T-045**（四个空白指派）；OI-016（七条非目标）；L-30；`X-13`；`X-14` |
| **solution** | **已选方案**：D-001..D-014（14 个决策块），覆盖 T-001..T-047 共 47 条（主块 42 + 过程条目 1 + 追加裁决 4；算术闭合 6+10+1+2+1+4+6+3+1+1+3+3+1+1 = 42，42+1+4 = 47）。核心形态：decision-log = ADR 块（**16 行/块**，T-047）+ OI 大纲 + 三级追溯链；spec = 候选 D 信封（薄叙事主干 + 契约附件，主干按 B 四拍、分节轴用 C、开头用 A）；phase = L0/L1/L2 三层 + 禁能力补偿型指令 + 过期条件与退出机制；紧凑索引 = 纯指针；验收 = 四段式 + 黑名单 + V0 禁入；单一权威 = 双轨（ID 定义唯一性 + 四类事实人工抽样）+「0 处双权威声明」度量；审查 = 单次审查 + 三态处置 + 四档降为纯标签；质量核心 = K1–K12 全保留、合并账 28→13 步、新建最小集＝空集。<br>**tradeoff（显式取舍）**：①**精炼 ↔ 无遗漏**（X-01/X-18）→ 用**位置**而非字数调和：逐字区与指针区**物理分离**，正文只放稳定 ID 指针，契约字面量例外允许逐字复制；②**弱化严格验收 ↔ 验收更可执行**（X-03）→ 「严格」限定在**产物真实性**（真命令/exit/output/oracle），「宽松」留给**推进许可**（不阻断），新增严格项一律写成「验收失败事实」而非 gate；③**不建 severity 门槛 ↔ 四档分级**（X-05）→ 四档降为**描述性标签**、不参与处置判定；④**审查默认 advisory ↔ 母 PRD 拒绝降级**（X-06）→ 共享定义**区分两个对象**：机制不得降级 / finding 处置可 advisory；⑤**不复审 ↔ 修复可能引入新问题**（X-15）→ 区分**修复复验**（测试域，必做）与**复审**（审查域，不做）；⑥**记录层禁哈希 ↔ F1–F7 依赖身份校验**（X-16）→ 区分**内部协议一致性**（过渡基线）与**推进前置校验**（全删）；⑦**母 PRD 只读 ↔ U-001 扩边**（X-13）→ 不改母 PRD，改用显式承接关系登记 + CARD-10 核对；⑧**行预算 ↔ 官方校验器四字段**（T-047）→ 删 `覆盖 T 决策` 至 **16 行/块**，R-E「≤15」降为非硬性目标（R-56 登记）。<br>**被否选项（逐条）**：任何长度数值目标（硬或软）——T-018 判为形式主义；10 节表单式 spec 结构——用户判「太工程化，不适合拆解成 phase 文件，也不适合阅读和审查」；让执行模型自写测试或自写规格；写入能力补偿型指令（「务必彻底搜索」等）；索引复制正文成为第二权威；只做 ID 定义唯一性（判据替换型漏检）；维护额外注册表；工具发现重复权威仍 exit 0；用工具失败阻断 build-code 开工；把 K1–K12 再压缩为「8 条」而不给出 K9–K12 去向；「纯新增」设计（用户明确裁定不成立）；为每个 lens 配独立 consumer/receipt（R-50 伪合并）；把真实多 phase task 抽验以「删除验收依赖」处置；把补做程序伪装为事前执行；在本卡改 make-decision 的 SKILL 文本；只修 R-1 或只修 R-2（RC 已证二者互为必要）；25 条逐条都交用户裁决；**在本阶段执行检查器代码修复**（T-046 明确其为 build-code 交付物）。<br>**open-item disposition（未决项归属，逐条）**：㈠**用户重确 14 条偏离**（L-01、L-02、L-03、L-04、L-05、L-17、L-21、L-22、L-28、L-29、L-30、L-31、L-32、NEW-01）→ owner＝make-decision 主会话 + 用户；㈡**用户裁决 C 类 7 条**（L-06、L-08、L-16、L-26、L-35、L-36、L-37）→ owner＝用户；㈢**spec 结构定稿**（R-G 推荐组合的用户确认）→ owner＝make-decision 主会话；㈣**K1–K12 逐条承接者 + 6–8 个改造技能命名**→ owner＝build-plan；㈤**V1/V2/V3 分级细化 + 自检清单条目微调 + 字段必填项与行内预算**→ owner＝build-plan；㈥**L2 退出机制复核时机**→ owner＝CARD-10 或后续复盘；㈦**真实多 phase task 外部抽验**→ 带触发条件的延期项（触发＝首个使用新格式的实施 task 落地），owner＝CARD-10；㈧**审查能力判据的测量与实验**→ owner＝CARD-05；㈨**无障碍/中文可读性度量**→ **未采用**（R-E 判定公式对中文技术文档失效）；㈩**§6.5 四个空白 → 已由 T-045 指派归属**：冻结审查合同→CARD-05、blind 核验→CARD-05、**K 失效条件维护→CARD-07**、跨 phase 契约一致性→CARD-03（owner 空白关闭，R-54 悬空风险由承接清单 + CARD-10 存在性核对承接）；⑪**检查器字段识别兼容修复**→ **build-code 交付物**（T-046，本阶段只登记需求与验收判据）；⑫**校验器粒度修复 + 接线**→ **build-code 交付物**（T-031，本阶段只登记）。 | T-018；T-017；T-020/T-040；T-021；T-036/T-043；T-039/T-042；T-041；T-011；T-031 + RC「根因对 R-1 + R-2，二者互为必要条件」；**T-044/T-045/T-046/T-047**；R-G §11.5 推荐组合 + §11.4 失效条件；R-E「删后结论不变/行为不变」判据；V-06/V-07/V-08/V-10 原口径；X-01..X-22 逐条调和方向；`research/DIV-option-space.md` §6.2/§6.3/§6.4/§6.5 |
| **acceptance** | **场景**：**S1 格式/模板自证**——新格式的 spec 模板、phase 模板、紧凑索引模板存在且逐项可核对（含四段式验收节、L0/L1/L2 三层、写集/依赖字段、不可判定词黑名单）；**S2 工具真实运行（RED→GREEN 双通路）**——无重复契约检查工具在**含重复权威的 fixture** 上真实报失败（非零 exit + 位置清单），在真实材料上报通过；**S3 卡内 fixture task 抽取**——按新格式产出的**最小真实样例（fixture task，≥2 phase）**的 `spec.md` 与 phase 文档，可被 AC-07（spec 含四件翻译内容）与 AC-08（phase 只含差异 + 索引）真实抽取；**S4 独立来源核对**——S2/S3 结论由①合并审查（独立来源）核对，不由实现者单独宣称；**S5 覆盖零孤儿（可复算）**——`## 覆盖矩阵` 三层各自加总闭合、18 组逐组满足 `需求数 = 接受 + 偏离 + 拒绝`、无未标偏离；**S6 检查器兼容（build-code 阶段）**——修复后同一检查器对 **card-01 与 card-02 全部现行 decision-log 的 14 个块均可识别四字段**（T-046/R-55）。<br>**数据来源**：①`research/REQ-full-inventory.md`（316 个需求单元，A–R 共 18 节）；②`research/DIV-option-space.md`（103 个展开选项 + 36 个补充变体，轴 1..5 = 16/16/16/40/15）；③`draft-fidelity.md`（L-01..L-38 逐字对照 + 三档分布）；④`research/MERGE-two-stages-consolidation.md`（K1–K12 映射、28→13 步、26 机制、D1–D5）；⑤`research/RC-make-decision-omission-root-cause.md`（R-1..R-11 + 最小修复面）；⑥`research/RC-card-07-scope-and-boundary.md`；⑦`research/RC-rejected-25-triage.md`（A 11 / B 7 / C 7）；⑧`research/R-G-spec-narrative-and-decomposability.md`（4 候选 + 推荐组合）；⑨母 PRD 与规划 decision-log（只读）；⑩本文件 `## 覆盖矩阵` 重算计数（第一部分逐条处置为唯一权威）。<br>**通过条件**：㈠14 条偏离项 + C 类 7 条完成用户真实裁决并记录（V-06/V-07/V-08/V-10 的原口径已逐字对照）；㈡S1 的三份模板文件存在且逐字段可核对；㈢S2 的工具真实运行输出（命令 + exit + output 位置）可复核，且 RED 的非零确由**目标断言**导致（非 setup 失败）；㈣S3 的 fixture task 存在且 ≥2 phase，其 spec/phase 可被 AC-07/AC-08 抽取；㈤S4 的独立核对记录存在；㈥**`## 覆盖矩阵` 三层各自加总闭合（316 / 316 / 39），18 组逐组加总闭合，且 GAP-1..GAP-9 逐项有处置结论**（不要求全部消除，但**必须逐项可见**）；㈦每项新增控制面已按 A-9 登记唯一 consumer / owner / 测试 / 删除条件 / 当前缺陷或用户结果理由，**新建项为 0**（重建项为 0 依据 T-043 合并账）；㈧**7 条 B 类项**（L-07/L-19/L-20/L-23/L-27/L-33/L-34）逐项有归属卡与本卡切片动作（T-038 + T-044）；㈨T-045 的四个空白归属已写入承接清单、T-046 与 T-031 的 build-code 交付物已在承接清单标注阶段归属；㈩`REQ` 主题表的非 ID token 已按 GAP-9 逐项修正、命名空间冲突已显式标注。<br>**失败条件**：㈠任一防压缩判据为「否」（无 `source` 锚点 / 无三档 / 覆盖矩阵无计数 / 偏离项无「改了什么+为什么」/ 原始声明层出现改写版本）；㈡覆盖矩阵任一组不能加总闭合，或「零孤儿」被作为无计数的定性宣称；㈢模板文件缺失，或验收节出现不可判定词黑名单命中项，或存在 V0（不可判定）条目；㈣无重复契约检查工具对**含重复权威的样本**仍 exit 0，或对现行材料误报；㈤RED 由环境故障/配置缺失/无意义断言导致非零（V-01「不算有效 RED」）；㈥任一验收结论为**模拟或脚本自证**而无独立来源核对（AC-26 明令失败）；㈦实现者单独宣称 fixture task 的抽取结论；㈧母 PRD / 兄弟卡材料被回写；㈨在缺**适用**产物或**伪造**产物时未判失败，或用了「不适用」为伪造开脱（FR-20）；㈩新增机制无「当前缺陷或用户结果理由」，或新建项不为 0 而未回到 C 类重裁；⑪**在本阶段（make-decision）执行了检查器或校验器的代码改动**（违反 T-046 阶段纪律）；⑫把主题聚类表的合计当作覆盖证据与 316 比对（违反 GAP-1 修复口径）。 | AC-07/AC-08；AC-26（「任一节奏点缺真实执行记录,或任一为**模拟或脚本自证**,即失败」）；FR-20；`V-01`；`V-02`；T-032；T-041；T-019（本文件做追加式补正）；SD-05；`V-14`；`V-27`；L-08/L-09（防压缩判据五条）；NEW-01；D-011 ③；**T-031/T-044/T-045/T-046/T-047**；`draft-fidelity.md`「保真机制自检」判据 1–5；`research/RC-make-decision-omission-root-cause.md` §5 最小修复面 |

---

### T 决策三档回填（GAP-4）

> 口径（V-04 规则 2）：`接受` = 该 T **忠实承接**对应原始声明（给 source 锚点）；`接受但有偏离` = 与原始声明**有偏差**（写清改了什么 + 为什么）；`拒绝` = 结论**被后续裁决否定**或**含未登记偏离**。
> 47 条逐条，不合并、不省略。`T-015` / `T-011` 已被 `T-039` / `T-043` 取代 → 判 `拒绝`；`T-020` 被 `T-040` 细化 → 判 `接受但有偏离`。

| T# | 内容一句话 | 三档 | 偏离说明 / source 锚点 |
| --- | --- | --- | --- |
| T-001 | 任务类型＝`普通任务` | `接受` | source：U-003「由人工决定是规划任务还是普通任务」+ 用户真实选择（T-001） |
| T-002 | CARD-05 并入范围＝①＋适配合同（不做实测） | `接受但有偏离` | 相对 U-001 E-5「card-05 的 plan 审查改造**也要移到当前任务来做**」，并入范围**收窄为①＋适配合同**，②③④/工具/实验留 CARD-05；收窄为用户真实选择且已登记（REQ-R-06 / T-002 后果节），但相对原话仍是范围收窄 |
| T-003 | 不回写母 PRD，本卡显式登记承接关系 | `接受但有偏离` | source：母 PRD「母/兄弟材料只读，不触发母任务 close」。偏离：规划 DL 文档权威另要求「确有全局方向变化时**明确交回其 owner**，不在子任务悄悄改写」（REQ-K-05）——「交回 owner」未登记（draft-fidelity 判为「本卡最关键的程序性缺口」），由 T-033 的实质变化程序部分补 |
| T-004 | build-spec 8 条质量核心全部保留（K1–K8） | `拒绝` | 数量口径被 **D-009 取代**（Supersedes）：权威清单 I-B §4 为 **K1–K12（12 条）**，「8 条」漏 K9/K10/K11/K12（V-28） |
| T-005 | 唯一 Clarify 并入 build-plan | `接受` | source：K5（唯一 Clarify 交互 + 十维度完整性检查）；MERGE §2.2「保留 1 步 `spec-clarify`」 |
| T-006 | 验收四段式 + 不可判定词黑名单 + V0 禁入 | `接受` | source：母 PRD FR-16/FR-17 面 + R-C 四段式出处；R-51「确实给 `spec-specify` 增了职责」已按 V-23 登记 |
| T-007 | 高智力模型预写测试，luna 禁改 | `接受但有偏离` | source：SD-02「强关联的实现、测试、修复可在同一上下文连续完成，独立复核换上下文」。偏离：U-001#4「实现、审查、测试、修复应该是一个独立的子代理」（**四个角色**）被改写为**两角色**（测试作者 vs 实现者），且丢失「强关联实现/测试/修复可同上下文连续完成」半条（REQ-I-11 / REQ-B-03） |
| T-008 | 文档长度＝软目标 + 结构规则 | `拒绝` | 结论被 **T-018 明确否定**（「T-008 的『软目标』部分作废，纯粹是形式主义」）；结构规则部分保留并升级为唯一手段，由 D-004 承载 |
| T-009 | spec 信息架构＝等 R-A/R-E 回收后再定（暂挂） | `接受但有偏离` | source：R-G 调研建议。偏离：暂挂由 D-002 ⑤ 结清（R-G 推荐组合），但**用户对 R-G 推荐组合的确认仍未取得**，D-002 `approval_binding` 标「待复核」（落点未闭环） |
| T-010 | phase 头部声明写集 + 依赖，紧凑索引汇总 | `接受但有偏离` | source：SD-11 并行声明**五项**（读集/写集/文件 owner/接口符号清单/合并责任）。偏离：**只落两项**（写集+依赖），R-45 已登记「与 V-18 五项声明不完整对齐，扩项归 CARD-03」 |
| T-011 | build-plan 薄主体技能 + 8 条核心各自独立技能 | `拒绝` | 被 **T-043 取代**（Supersedes 明示）：「各自独立成技能」表述作废；「薄主体技能只做调度」部分继续有效 |
| T-012 | decision-log＝ADR 风格 + 保留 OI 大纲 + 三级追溯链 | `接受` | source：用户原话「ADR 风格没问题，但是目前的 decision-log 中的大纲、决策来源等路径也需要保留…方便我追溯每一个决策」（T-012） |
| T-013 | phase 文档方向＝暂缓决定，先补做调研 | `接受` | source：用户原话「这里需要仔细调研…看看是否要综合一下」；过程条目，调研 R-F 已派发并回收，结论由 T-017 结清（GAP-7） |
| T-014 | 紧凑索引＝纯指针表 | `接受` | source：用户选择「纯指针表」（T-014）；母 PRD OI-001「常驻紧凑索引」 |
| T-015 | ①合并审查 finding 分级＝四档（CodeRabbit 式）+ 默认 advisory + 三条件阻断 | `拒绝` | 被 **T-039 取代**（Supersedes 明示）：四档降为纯标签、恢复不复审、清单改自检清单；原口径 V-06「不建 severity 门槛或复审循环」被违背（L-01） |
| T-016 | 永不抑制清单（本卡定义 6 条候选） | `接受但有偏离` | source：R-D「永不抑制清单」。偏离：条目被 T-022 由 **6 条收窄为 4 条**，定位被 **T-039/D-007 改为「审查自检清单」（不参与 finding 处置）**；两处变更均已登记 |
| T-017 | phase 形态＝L0/L1/L2 三层混合（L2 可作废附注） | `接受` | source：R-F 官方一手证据（`<maximize_context_understanding>` 在 GPT-5 转 counterproductive）+ 用户选择「三层混合」；D-003 ④ 承接退出机制 |
| T-018 | 不设任何长度数值目标（硬或软） | `接受` | source：用户原话「设置目标完全没有意义…这个目标设置纯粹是形式主义」（T-018）；D-004 ① 逐字承接 |
| T-019 | 交付＝格式+模板+可测量检查工具；AC-07/AC-08 改「模板+工具自证」，真实抽验移交 CARD-10 | `拒绝` | 被后续裁决**否定**：①`AC-26` 明令「任一为**模拟或脚本自证**，即失败」；②真实抽验被**删除而非登记为不可得**（L-04 判「未按 SD-09 标注偏离」，属含未登记偏离）；③由 **T-041** 改判为「卡内造最小真实样例（fixture task）」。原文保留，D-006 做追加式补正 |
| T-020 | 「无重复契约」判据＝ID 定义唯一性（单轨） | `接受但有偏离` | 被 **T-040 细化**（细化声明在案）：ID 定义唯一性仅为**轨 1**，不再单独构成判据；轨 2 为四类事实人工核对 + 抽样，「0 处双权威声明」度量恢复（V-08 / L-02） |
| T-021 | 真报失败，但不阻断推进 | `接受但有偏离` | source：SD-17「一切机器/流程校验…不构成…前置」。偏离：只落「不阻断」半条，**「未做或做假＝验收失败事实」未落**（L-21 判偏离）；由 A-11 + D-008「记录层严 / 推进层松」补回 |
| T-022 | 永不抑制清单定稿 4 条（无 oracle / 无溯源 / 预写测试被改 / 不可逆缺确认门） | `接受但有偏离` | 4 条**内容保留**（D-007 ⑤ 明示），但**定位**由 T-039/D-007 改为「审查自检清单」，不再承担「永不抑制/不参与 finding 处置」的处置语义 |
| T-023 | 与 CARD-01 的写面协调＝build-plan 时对齐当时冻结版本 | `接受但有偏离` | source：REQ-D-25/D-26/D-28（准备/实现/合并依赖）。偏离：原口径另要求「spec/phase 模板与 **CARD-03（工作方法）、CARD-04（验收写入）的衔接以接口蓝图为准**」（REQ-D-29 / L-14 判拒绝）；D-004 ③ 已扩为 CARD-01+CARD-03+CARD-04，扩项已登记但原文义务曾缺失 |
| T-024 | 先建保真机制，再逐条补回（四步整改） | `接受` | source：REQ-B-21/B-22 面 + M-3「我的原始需求都被你压缩了」；四步均产出（保真节 / L 表 / X 张力 / 重确） |
| T-025 | 三处偏离项（L-01/L-04/L-05）标偏离并请用户重确 | `接受但有偏离` | source：V-04 三档结论。偏离：**重确范围只列 3 条**，按同一口径实有 **14 条**；由 **T-027 扩大为全部 14 条**（T-025 原文保留） |
| T-026 | 建立 SD-09 保真机制（元机制） | `接受` | source：母 PRD SD-09「需求保真采用逐字双层结构——**原始声明不得改写**,派生需求带 source 锚点」+「结论分三档…标记待复核」；四条规则已常驻 |
| T-027 | 偏离项重确范围＝全部 14 条 | `接受` | source：T-025 的扩大 + `draft-fidelity.md` 对账发现 1；清单逐条可核（L-01..L-05、L-17、L-21、L-22、L-28..L-32、NEW-01） |
| T-028 | 25 条拒绝项先三分类，用户只裁 C 类 | `接受` | source：母 PRD「每项需求有负责工作包**或明确排除理由**」+ T-030「只产出事实与选项」；三分类 11/7/7 = 25 已逐条落地 |
| T-029 | 调查 make-decision 大规模遗漏的根因 | `接受` | source：用户原话「需要把当前发现的问题调查清楚根本原因…是放在当前任务解决，还是修改 prd 里的 card-07？」；两路 RC 报告已产出 |
| T-030 | 三分类与根因调查的执行边界＝只产出事实与选项 | `接受` | source：T-030 约束原文「以上两路调查与三分类**只产出事实与选项，不替用户做取舍**；C 类清单必须逐条附冲突对方原文锚点」；RC 报告 §5.4 已声明未替用户排序 |
| T-031 | 校验器粒度修复（R-1/R-2）＝card-02 本卡做 | `接受但有偏离` | source：RC「根因对 R-1 + R-2，二者互为必要条件」+ 用户选择选项甲。偏离：①越 CARD-07 写面（R-35/R-36/R-37/R-38 已登记）；②**交付阶段被 T-046 纪律收窄**——属 build-code 交付物，make-decision 阶段只登记需求与验收判据，不改代码 |
| T-032 | L-06 处置＝补一条可失败检查作 RED | `接受` | source：V-02 G-2「补一条可失败的检查，或写明豁免理由与风险并在验收中披露」二选一；D-006 ③ 承接（RED 须由目标断言导致非零） |
| T-033 | L-16 处置＝本卡补做前后承诺展示 | `接受但有偏离` | source：V-15「**须先展示前后承诺并获得真实确认**」。偏离：属**事后补程序**——母 PRD 要求的「先展示」在 U-001 发生时未执行（R-40 已登记须如实写明，不得伪装为事前执行） |
| T-034 | L-37 处置＝定审查能力判据，不做实测 | `接受` | source：U-002-10/11「在保证现在的审查质量的同时**提升审查能力**，对 spec 和 phase 都能做到高效高质」；判据三项（finding 有效率/锚定准确率/耗时）已定义，测量归 CARD-05 |
| T-035 | L-26 处置＝只定测试流程的「结构要求」 | `接受但有偏离` | source：U-001#1「所有 phase 做完了但一次真实测试验收都没做过，一直在用脚本验收」+ U-002-6「避免：缺少测试流程」。偏离：只留模板字段，**本卡自身仍无真实验收设计**（L-26 判拒绝），I-A P-09 病灶（测试流程只有一句 gate_cmd）承接未闭环，留 build-plan/CARD-04 |
| T-036 | L-35 处置＝接受「不应纯新增，应替换合并」，交付合并账 | `接受` | source：用户原话「不是应该替换合并原来的技能吗？怎么可能单纯新增？两个 stage 合并成 1 个，怎么可能纯新增？」；MERGE 账净新增 0 证实该判断 |
| T-037 | L-36 处置＝扩散交 CARD-07 | `接受但有偏离` | source：母 PRD CARD-07 FR-33/AC-33（发散/扩散）与 C5（G-4）。偏离：L-36 判拒绝的另三项——「**G-4 自动再发散一轮**的触发规则」「允许用户写第四方案（旧禁令须废止）」「方案内容为主线」——未在 T-037 内登记；D-013 ④只登记 DIV 产物与承接关系（R-44 输入注入义务已登记） |
| T-038 | 5 条含 card-02 侧切片的 B 类项处置＝保持 B 类 + 登记切片动作 | `接受但有偏离` | source：T-038 用户选择「保持 B 类，但登记本卡要做的切片动作」。偏离：标题称 **5 条**，而 B 类实有 **7 条**——`L-19`/`L-33` 未被覆盖（GAP-5）；**缺口经 T-044 关闭**（接受草稿补写、保持 B 类并补登切片动作） |
| T-039 | L-01 裁决＝四档降为纯标签 + 恢复不复审（取代 T-015） | `接受` | source：V-06「单次审查成功后不再为同一 scope 复审…**不建 severity 门槛或复审循环**」；四档降纯标签、三态处置、清单改自检，忠实回归原口径 |
| T-040 | L-02 裁决＝双轨（ID 定义唯一性 + 事实级判据）（细化 T-020） | `接受` | source：V-08「同一**事实**只有一个权威文件」+「检查**逐事实**核对权威归属」+「度量=**0 处**…」；双轨 + 度量恢复均忠实承接 |
| T-041 | L-04 裁决＝卡内造最小真实样例（fixture task，≥2 phase） | `接受` | source：V-10「AC-07…抽一个**真实实施 task** 的 spec.md」「AC-08…抽一个**多 phase task** 的 phase 文档」+「**验收依赖**:一个真实 task 样例」；fixture 在卡内真实可抽取 |
| T-042 | L-05 裁决＝写共享定义区分「机制」与「finding 处置」 | `接受` | source：V-07「把多数机制降级为 advisory/non-blocking」——**拒绝**；机制不得降级 / finding 处置可 advisory 分写两句，作为现有 SD-17 的补充句（不新增 SD 编号） |
| T-043 | 两阶段合并账裁决（取代 T-011 的「独立技能」表述） | `接受` | source：用户「不应纯新增、应替换合并」；MERGE 账：K1–K12 完全覆盖 10 / 部分覆盖 2 / 无承接者 0；28 步保留 11/合并 11/改造 6/删除 0 → 净新增 0；26 机制语义丢失 0；新建最小集＝空集 |
| T-044 | GAP-5 处置＝接受草稿补写（L-19/L-33 保持 B 类） | `接受` | source：`RC-rejected-25-triage.md` §5.1「B 类 7 条」+ T-038 只列 5 条的事实；`L-19`→CARD-03、`L-33`→CARD-08 已补登切片动作（见 B-2/B-6） |
| T-045 | GAP-6 处置＝本卡逐个指派四个空白归属 | `接受` | source：`research/DIV-option-space.md` §6.5 四个空白 + 指派：冻结审查合同→CARD-05、blind 核验→CARD-05、**K 失效条件维护→CARD-07**、跨 phase 契约一致性→CARD-03；R-54 悬空风险由承接清单 + CARD-10 存在性核对缓解 |
| T-046 | 检查器格式不兼容处置＝本卡修检查器（登记为交付物） | `接受` | source：实测「card-01 亦 0/40 命中」「四字段实际存在，非字段缺失」；**阶段纪律：该修复是 build-code 交付物，make-decision 阶段只登记需求与验收判据**；R-55 的验收判据＝同一检查器对 card-01 与 card-02 全部现行 decision-log 均可识别 |
| T-047 | 决策块行数处置＝删 `覆盖 T 决策` 行 → 16 行（与 card-01 一致） | `接受` | source：T-047 用户裁决「**删掉冗余的 `覆盖 T 决策` 行 → 16 行**；**不再压**，不删 card-01 的既有字段」；R-56 偏离理由（官方校验器四字段 + card-01 格式兼容）已登记；本文件 14 块全部 16 行 |

**T 表三档计数**：`接受` **26** / `接受但有偏离` **16** / `拒绝` **5** / 合计 **47** ✅
- `接受`（26）：T-001, T-005, T-006, T-012, T-013, T-014, T-017, T-018, T-024, T-026, T-027, T-028, T-029, T-030, T-032, T-034, T-036, T-039, T-040, T-041, T-042, T-043, T-044, T-045, T-046, T-047
- `接受但有偏离`（16）：T-002, T-003, T-007, T-009, T-010, T-016, T-020, T-021, T-022, T-023, T-025, T-031, T-033, T-035, T-037, T-038
- `拒绝`（5）：T-004, T-008, T-011, T-015, T-019
- 验算：26+16+5 = **47** ✅；且 `拒绝` 5 条**全部**挂有取代/否定链（T-004→D-009；T-008→T-018；T-011→T-043；T-015→T-039；T-019→T-041+AC-26）。

---

### A 类补回清单

> 来源：`research/RC-rejected-25-triage.md` 第 2 节（A 类 **11 条**，44%）。共同特征：**补回 = 登记、引用、落位**（不新增机制、不新增文件、不新增门禁）。若实施时发现某条要求新增机制或新增文件，应回到三分类重判 C。
> 格式：`L-nn` → 落点 → **可直接抄的文字**。

**A-1 `L-09` 覆盖核对强度校验**
- **落点**：`decision-log.md` → `## 需求保真` 节「规则 3 — 强度校验」；`## 覆盖矩阵` 增加「强度结论」列。
- **可直接抄的文字**：
  > 覆盖核对必须**逐条比对原始需求是否被削弱**，不得只按类别查「有无覆盖」。强度校验三问：① 限定词还在吗？② 动作被替换成什么了？③ 削弱是否可见？强度校验的产物 = 偏离/拒绝清单，必须逐条可见。
- **产物**：偏离/拒绝逐条清单（已有）+ 覆盖矩阵的强度结论（只展开偏离/拒绝，其余按主题计数）。**代价：无新增文件、无新增门禁。**

**A-2 `L-10` append-only 与历史不可改边界**
- **落点**：`decision-log.md` **顶部规则声明**；并把 T-018 的「修正 T-008」写法改为**追加式**。
- **可直接抄的文字**：
  > 收敛为 append-only——**发现缺口只追加、不覆盖既有记录**；历史决定、确认、审查、测试、物理事实**不可改**。旧结论作废一律采用 **supersession-by-reference**：新增条目并标注被取代的 ID，**不删字、不改原条目**。
- **产物**：规则声明段（可复用文件内「原始声明（verbatim，append-only）」节）+ ADR 化改造前的「历史保留方式」声明。**代价：ADR 化改造须先声明历史保留方式（旧节保留原位、新结构只追加）；不新增文件。**

**A-3 `L-11` AC-10 变更传播**
- **落点**：`## 决定` D-008 ⑥ + `spec.md` 验收节新增一条 AC。
- **可直接抄的文字**：
  > AC-10（对应 FR-06）：条件=产品目标发生变化；行为=只在其唯一权威文件更新，引用处随引用调整；度量=**无第二处需手工同步的副本**。失败场景=同一变更需在两份各自声明权威的文件中分别改写，即失败。
- **产物**：一条核对说明 + 一个 AC 条目（人工核对 + 抽样）。**代价：检查工具多一类核对动作（人工/抽样），不新增工具、不新增文件。**

**A-4 `L-12` 无 PRD 普通任务形态 + 禁令**
- **落点**：`spec.md` 形态节 + `decision-log.md` 台账禁令条目。
- **可直接抄的文字**：
  > 无 PRD 的普通任务：**精简** spec 引用用户原始需求（U-001/U-002）与 decision-log 产品事实。
  > **不得把实现方便性写成用户已批准的新需求。**
- **产物**：spec 一节 + 一条禁令；对已新增机制补「理由来源」（与 A-9 同一动作，不重复计）。**代价：一段形态说明 + 逐项理由；禁令生效后可能要求删减机制，该取舍属 C（L-35），不在本条内自行决定。**

**A-5 `L-13` 母 PRD 点名的两条局部风险**
- **落点**：`decision-log.md` 风险清单追加两条（编号沿用 `R-xx` 追加）+ `spec.md` 写「避免刚性仪式的具体方式」。
- **可直接抄的文字**：
  > **局部风险**：把「单一权威」做成新的刚性文档仪式（与 R-007 减阻塞目标冲突）；reopen 历史（文档形态曾被推翻）提示**形态须以 Q17 定稿为准**。
- **产物**：2 条 `R-xx` + 一段避免方式说明。**代价：「避免方式」若触及机制删减，须走 C（L-35/L-36）裁决；登记本身零代价。**

**A-6 `L-14` 与 CARD-03 / CARD-04 的模板衔接义务**
- **落点**：`## 决定` D-004 ③（跨卡接口协调）+ `spec.md` 模板接口面可核对项。
- **可直接抄的文字**：
  > spec/phase 文档模板与 **CARD-03（工作方法）、CARD-04（验收写入）的衔接以接口蓝图为准**。
- **产物**：一条合并登记 + 模板接口面清单（表格式）。**代价：build-plan 时需与 CARD-03/CARD-04 冻结接口对齐（协调成本，非门禁）。**

**A-7 `L-15` 停止规则**
- **落点**：`decision-log.md` 交付说明节 + `## 收敛检查` target 行。
- **可直接抄的文字**：
  > **停止规则**：CARD-10 通过后进入停止规则——**只有真实用户问题或真实失败才触发新一轮修改，不做猜测式流程优化**。本轮 U-002 属**真实用户问题**触发（正当例外），只做清点/扩散/补回/根因。
- **产物**：一段文字。**代价：无。**

**A-8 `L-18` 最终确认交互形态**
- **落点**：Talk 规则条目 + 本轮 C 类清单的呈现形态。
- **可直接抄的文字**：
  > 最终确认的交互形态：要**先重建用户目标，再揭示当前已选方案，再让用户逐条挑战**，而不是直接问「同意吗」。
- **产物**：一条 Talk 规则 + 本轮 C 类清单的呈现形态。**代价：用户需逐条挑战（时间成本，即 R-31/R-32 已登记风险）；不新增机制。**

**A-9 `L-24` 新增控制面登记 + 理由义务（本卡部分）**
- **落点**：`spec.md` 新增「新增控制面登记表」。
- **可直接抄的文字**：
  > 每一项新增控制面必须写明：**唯一 consumer、owner、测试、删除条件、当前缺陷或用户结果理由**。**每项新增规则必须给出当前缺陷或用户结果理由，不得只换存放位置。**
- **产物**：一张登记表（沿用 AGENTS.md 已有「本任务新增控制面登记」形态，约 10 行 × 5 字段）。**登记对象（逐项）**：8 技能 / 检查工具 / 模板 / 四段式 / 黑名单 / 四档分级 / 永不抑制清单 / L0-L1-L2 / ID 命名空间 / ADR 块行数。**并按 T-043 收紧**：**新建项为 0**，只需登记**改造项**的 consumer/owner/删除条件；**并按 T-046 标注**「检查器兼容修复」与「校验器粒度修复」为 **build-code 交付物**。**代价：一份登记表；迁移表七类面的本卡条目另交 CARD-06。**

**A-10 `L-25` 不可变命名与只追加纪律**
- **落点**：`decision-log.md` 执行纪律补一段 + 风险清单引用 R7。
- **可直接抄的文字**：
  > 记录层不用内容寻址哈希：普通文件名（**不可变命名：日期+序号+描述**，append-only）+ 纯文本路径引用。记录层停用内容寻址后的**文件名碰撞/覆盖风险**，以不可变命名与只追加纪律缓解（风险 R7）。
- **产物**：一段执行纪律 + 风险清单一条引用。**代价：新产物命名受约束（现有 `research/` 下多为无日期文件名，统一改名成本低且发生在 build-plan）。**

**A-11 `L-38` 不补造凭证 / 需求计数 / 第二轮降级事实**
- **落点**：`decision-log.md` 披露纪律段 + `## 覆盖矩阵` 计数行 + 引用 OI-012。
- **可直接抄的文字**：
  > 宿主未提供 reply_ref/reply_hash 认证凭证，**不补造**；interaction aggregate 及正式 completion 可能 incomplete，但**不阻止同 task 继续**。**质量事实**：不宣称测试通过、不宣称审查完成；**物理动作及授权**：不宣称已交付。
  > 需求追踪表须**内嵌**、**计数须同步**（见本文件 `## 覆盖矩阵` 计数行）。
  > 第二轮降级事实（OI-012）：并行声明制由「未声明不得并行」**降级**为事实记录 + 验收核对，不阻断派发；接口蓝图冻结同降级。
- **产物**：一段纪律 + 一行计数 + 一条引用。**代价：几乎无（计数与 NEW-01 同一动作）。**

> **A 类计数核对**：A-1..A-11 = **11 条** ✅ 与 `RC-rejected-25-triage.md` §5.1「**A 类 11 条**：L-09、L-10、L-11、L-12、L-13、L-14、L-15、L-18、L-24、L-25、L-38」逐条一致（11/11，无多无缺）。

---

### B 类排除清单

> 来源：`research/RC-rejected-25-triage.md` 第 3 节（B 类 **7 条**，28%）。格式：`L-nn` → **排除理由（可直接抄）** → **归属卡号** → **本卡切片动作**。
> **T-044 落实**：`L-19`/`L-33` 由 **T-044 裁决补登**（接受草稿补写、保持 B 类、归属 CARD-03 / CARD-08）；本清单 7 条**全部**有归属与切片动作。

**B-1 `L-07` 验收 oracle 与实现者分离**
- **排除理由**：`L-07 排除：验收 oracle 与实现者分离是实收机制，归 CARD-04（FR-17/AC-17）；card-02 只在 spec/phase 模板的「验收流程」字段引用该要求，不另建 oracle 分离机制。本卡检查工具自身 oracle 的独立性随 L-04/L-22 偏离项一并重确。`
- **归属卡号**：**CARD-04**（FR-17 / AC-17；追踪表 R-001 行「真实验收机制归 04」）。
- **本卡切片动作（T-038）**：本卡检查工具的**判据独立性** → 登记为引用 CARD-04 的 FR-17，并在本卡工具说明中写明**判据来源**。

**B-2 `L-19` 全阶段子代理与主会话职责**
- **排除理由**：`L-19 排除：五阶段派子代理、主会话只派发/回收、2–5 并行上限与减少自动压缩是 CARD-03 的方法机制（FR-11..FR-15、SD-11、AC-11..AC-15），效果验证归 CARD-09 FR-46；card-02 的执行遵守由 AGENTS.md「重活放进子代理上下文执行」与执行期派发覆盖，本卡不另建机制。`
- **归属卡号**：**CARD-03**（FR-11..FR-15 / SD-11 / AC-11..AC-15）；效果验证 **CARD-09 FR-46**。
- **本卡切片动作（T-044 补登）**：本卡**执行遵守**——全量阅读由子代理承接（`REQ-full-inventory` / `DIV-option-space` / `RC-*` / `R-G` 九份报告均由子代理或只读分析产出），主会话只收结构化结论；把「减少自动压缩次数」登记为**可观察指标**。

**B-3 `L-20` 并行五件套 / 声明制五项 / G-1**
- **排除理由**：`L-20 排除：并行规则五件套、并行声明五项（读集/写集/文件 owner/接口符号清单/合并责任）、「未声明或声明不实=验收失败事实」与 G-1 四步均为 CARD-03 的机制（SD-11、FR-14/15、AC-13/14）；「并行声明」作为质量核心由 CARD-05 的 build-plan 合并审查核对（FR-56）；card-02 的 phase 文档头部只承载「写集+依赖」两个字段（T-010 现行口径），不复制其余三项声明。`
- **归属卡号**：**CARD-03**（SD-11 / FR-14 / FR-15 / AC-13 / AC-14）；「并行声明」核对面归 **CARD-05**（FR-56 / AC-57）。
- **边界提示（R-45，须显式登记）**：本卡「保持现状（写集+依赖）」意味着①合并审查的「并行声明」面**仍与 V-18（五项声明）不完整对齐**，该不完整**及其归属（CARD-03）须在 spec 中显式登记**。
- **本卡切片动作（T-038）**：phase 头部字段集是否扩为五项声明 → **本卡保持现状（写集+依赖）**，扩项归 CARD-03；在 decision-log 登记该边界判断。

**B-4 `L-23` F1–F7 过渡基线**
- **排除理由**：`L-23 排除：未提交 runtime/review/*、skills/wh-review/* 修复（F1–F7）的「过渡基线」定性、转只读历史或删除的处置归 CARD-06（SD-16、FR-52、AC-53）；card-02 只登记该基线与 OI-013「记录层不用哈希/身份校验」的适用范围区分（内部协议一致性 vs 推进前置校验），不处置其删除或留存。`
- **归属卡号**：**CARD-06**（SD-16 / FR-52 / AC-53；OI-006 行）。
- **本卡切片动作（T-038）**：本卡登记「**内部协议一致性 vs 推进前置校验**」的区分句。

**B-5 `L-27` 结果复用 / 第一条贯通路径即试用 / RED→GREEN 证据归属**
- **排除理由**：`L-27 排除：有条件 TDD 的结果复用/不机械重跑/RED→GREEN 证据归属是 CARD-04 与 CARD-03 的机制（SD-06、FR-18、AC-18、FR-11）；「第一条贯通路径形成时就实际试用」与整体真实入口联通验收由 CARD-10 承接（SD-05、E2E-2）；card-02 只把「不机械重跑」写入本卡执行纪律，并在 build-plan 时对检查工具做一次试用。`
- **归属卡号**：**CARD-04**（SD-06 / FR-18 / AC-18）+ **CARD-03**（FR-11）+ **CARD-10**（SD-05 / E2E-2）。
- **本卡切片动作（T-038）**：本卡**做一次**「第一条贯通路径即试用」——即 **T-032 的可失败检查 RED→GREEN 实跑**（与 D-006 ②③ 同一动作）。

**B-6 `L-33` 窄状态集 / 旧任务只读 / 回读检查 / 跨会话接续**
- **排除理由**：`L-33 排除：窄状态集 7 值、旧任务/历史只读、记录完整性回读检查与跨会话接续记录都是 CARD-08 的承接面（SD-03、FR-39..FR-42、AC-39..AC-42）；card-02 不开工该面，显式排除并归 CARD-08。`
- **归属卡号**：**CARD-08**（SD-03 / FR-39..FR-42 / AC-39..AC-42；OI-007 行）。
- **本卡切片动作（T-044 补登）**：在 `## 决定` D-001 的**非目标**中**显式登记排除理由**（「属 CARD-08 范围：窄状态集与接续记录是 CARD-08 承接面；card-02 不开工该面」），满足母 PRD「每项需求有负责工作包**或明确排除理由**」（C-06「无无主需求」）。

**B-7 `L-34` 5 项窄工具 / 删除前损失清单**
- **排除理由**：`L-34 排除：5 项窄工具的具名与独立可用、删除通用事实引擎前的书面损失清单（SD-12 四项）归 CARD-06（SD-04、SD-12、FR-28/FR-30/FR-52）；card-02 只引用 SD-04③原子写入与④不可逆 Git 授权作为本卡写入/授权纪律，不新建工具。`
- **归属卡号**：**CARD-06**（SD-04 / SD-12 / FR-28 / FR-30 / FR-52；追踪表 R-008 行、OI-003 行）。
- **本卡切片动作（T-038）**：本卡**显式登记** SD-04③ **原子写入**、④ **不可逆 Git 授权**作为本卡的写入/授权纪律。

> **B 类计数核对**：B-1..B-7 = **7 条** ✅ 与 `RC-rejected-25-triage.md` §5.1「**B 类 7 条**：L-07、L-19、L-20、L-23、L-27、L-33、L-34」逐条一致（7/7，无多无缺）。
> **切片动作计数**：含本卡侧切片的 = **7 条全部**（其中 5 条由 T-038 规定，2 条 `L-19`/`L-33` 由 **T-044** 补登）。
> **A+B 与 C 的合计核对**：A 11 + B 7 + C 7 = **25** ✅ 与「25 条拒绝项」口径闭合；C 类 7 条由 D-012 承接（T-025/T-027/T-028）。

---

### 承接与不承接清单

> 落实 **T-045**（四个空白指派）、**T-046**（检查器修复＝build-code 交付物）、**T-031**（校验器粒度修复＝build-code 交付物）、**T-003**（母 PRD 边界差异显式登记）。
> 纪律：**承接 = 登记归属 + 本卡动作 + 依据**；**不承接 = 显式排除理由**（母 PRD「每项需求有负责工作包**或明确排除理由**」）。**本阶段（make-decision）不改任何代码。**

| 项 | 归属卡 | 本卡动作 | 依据 |
| --- | --- | --- | --- |
| CARD-05 审查改造（并入①合并审查 + 适配合同） | **CARD-05**（新工具接入/对比实验/go-no-go/②③④代码审查点/wh-review 物理删除）；①与适配合同**已并入本卡** | 本卡承接①`build-plan 合并审查` 本体（FR-56/AC-57 + AC-23/AC-26 的①子句）与**适配合同的文档审查分档**（输入形态=frozen-material packet、finding schema、超时语义、unavailable 状态集、provider 身份、事实写入位置）；**不做工具实测**、不做代码审查实测；①的 finding 处置默认 advisory、机制不得降级（T-042 共享定义）。两卡各写一次适配合同的漂移风险（R-1）：共享基础各写一句指向**同一权威句**，不复制正文 | T-002；REQ-E-05/FR-53；REQ-B-10/FR-56；T-042；R-1；OI-016② |
| CARD-06 排除项（薄核心删除 / 窄工具 / 损失清单） | **CARD-06** | 不承接物理删除（不定死删除清单）；不承接 5 窄工具具名与 SD-12 损失清单；**只**引用 SD-04③原子写入、④不可逆 Git 授权作为本卡写入/授权纪律；迁移表七类面**只提交本卡自身条目** | REQ-D-09；REQ-F-01..F-08；B-4/B-7；L-23/L-34；SD-04③④ |
| CARD-08 排除项（窄状态集 / 旧任务只读 / 回读检查 / 跨会话接续） | **CARD-08** | **不开工**该面，显式登记排除理由（`L-33 排除：…`），满足「无无主需求」 | REQ-B-05；REQ-J-09；REQ-K-21；REQ-N-04；B-6；C-06 |
| **空白 1：谁冻结审查合同 / 何时解冻** | **CARD-05**（T-045） | 登记归属与**输入注入义务**：把「适配合同六要素 + 冻结前置」写入 CARD-05 承接 task 的输入清单（母 PRD 的 CARD-05 最小读取集不含 card-02 材料，漏注入则永久悬空，R-54/R-44 同型） | `DIV-option-space.md` §6.5 空白 1；I-C §2 C3「未冻结不得接入」；FR-53；T-045；R-54 |
| **空白 2：如何核验审查者真的 blind** | **CARD-05**（T-045） | 登记归属；本卡在①合并审查的**输入规则**中登记「**对 phase 差异 blind、对 spec 可见**」的**可核验形态**（写入适配合同的输入形态与 packet 组成） | `DIV-option-space.md` §6.5 空白 2（4B-06 blind 属输入裁剪，无核验机制）；FR-53；T-045 |
| **空白 3：K 的失效条件由谁维护** | **CARD-07**（T-045） | 登记归属；本卡登记 **R-F 给出的四条失效条件** 作为输入（①执行模型不再明显弱于规划者→L0+L1 vs L0+L1+L2 无显著差即删 L2；②任务转探索性→只写边界；③写不出可执行验收→退化为散文 + 小步人审；④无 owner/review rhythm→只留 L0+L1），并与 D-014 ④㈤（L2 退出机制复核时机）**合并处置** | `DIV-option-space.md` §6.5 空白 3；R-F §10.2；T-017；D-014 ④㈤；T-045 |
| **空白 4：跨 phase 契约一致性由谁保证** | **CARD-03**（T-045） | 登记归属；本卡登记「**phase 头部写集/依赖 + 紧凑索引**」为**当前可核对面**，并如实登记该面与 V-18（五项声明）的不完整对齐（R-45） | `DIV-option-space.md` §6.5 空白 4；I-C §5 I6；R-A §6.5（cc-sdd 用 `cross-spec review`，自认仍属概率性检查）；T-010；R-45；T-045 |
| **检查器字段识别兼容修复** | **build-code 交付物**（**T-046**；检查器本体 `tools/cli/check-decision-log-chain.mjs` 归属不变） | **本阶段只登记需求与验收判据，不改代码**。需求：抓取放宽为「可选的前置列表标记 + 兼容全角冒号」；验收判据：修复后**同一检查器对 card-01 与 card-02 全部现行 decision-log 的 14 个块均可识别 `module`/`requirement_ids`/`derived_from`/`artifacts` 四字段**（R-55：若只修 card-02 侧则行为不一致） | T-046；本文件「已知不兼容」段实测（本卡 0/56、card-01 0/40）；R-55 |
| **`runtime/stage` 覆盖校验器粒度修复 + 接线** | **build-code 交付物**（T-031 + **T-046 阶段纪律**） | **本阶段只登记需求与验收判据，不改代码**。需求：①`assertDecisionCoverageReadyForConfirmation` 接入 make-decision approve 路径，`sourceItems` 改取**原始需求清点**而非材料自报（R-2）；②覆盖校验由「类别是否出现」改为「**逐条 disposition + 强度对照**」（R-1）；③OI 行新增「原始声明逐字引用 / source 锚点」必填字段（R-3/R-4，按 AGENTS.md 先登记职责/consumer/删除条件）；④完成判据补「逐条 disposition 或显式排除理由」（R-6）。**不改 make-decision 的 SKILL 文本**（仍归 CARD-07） | T-029/T-031；`research/RC-make-decision-omission-root-cause.md`（根因对 R-1+R-2）；R-35/R-36/R-37/R-38；T-046 |
| **母 PRD 边界差异（书面不一致）** | **CARD-10** 核对（承接关系登记在本卡） | **不回写母 PRD**；在本卡显式登记「承接了原 CARD-02 / CARD-05 的哪些部分 + 改了哪些 + 仍不承接哪些」，由 CARD-10 存在性核对；如实登记 R-38（母 PRD 卡面说 CARD-07 负责 make-decision 改造，card-02 实际改了其校验器）与 R-54（四个空白全外派） | T-003；T-031 R-38；REQ-K-05（「确有全局方向变化时明确交回其 owner」）；T-045 R-54；X-13 |

---

### 收口自检

#### GAP-1..GAP-9 逐条修复状态

| 缺口 | 是否已修复 | 修复方式 / 未修复原因 |
| --- | --- | --- |
| **GAP-1** 第 2 层 316 与第 3 层 329「差 13」 | ✅ **已修复** | ①第 2 层**重算为 99/128/89**（第一部分逐条处置为唯一权威），废弃不可复算的 104/95/117；②第 2/3 层关系**改判为「不同粒度视图的包含关系」**（第 3 层已含于第 2 层），不再相加或比对总数；③「差 13」**真实成因两条**：口径混用（去重分区 vs 跨主题不去重视图）+ 三档错配净额（接受 +2 / 偏离 −28 / 拒绝 +39）；④原推测「13 = L 表 13 条偏离被重复计」**核查不成立，如实登记为数值巧合**；⑤主题表**降级为导航视图**，其合计不得作覆盖证据 |
| **GAP-2** 主题表标称列与逐行合计不闭合 | ✅ **已修复（并纠正草稿自身的错算）** | **NG-1**：草稿称「16 行标称之和 = 341」，实际 = **331**（差 10 是草稿**加法错误**）。真实状态三口径：**逐行标称 331**（两次独立计数一致）／**本表计 329**（101/100/128）／**主题表合计行写 341（无来源，判为错误数字）**。修正值：合计行应写 **331（标称口径）与 329（本表计口径）**，并声明二者差异来源：T3 本表计 11 > 标称 9（+2）、T15 本表计 5 < 标称 7（−2）、T16 本表计 5 < 标称 7（−2）→ 331−4+2 = **329** ✅ |
| **GAP-3** 主题表「第三部分」读数缺 T13–T16 | ✅ **已修复** | 补齐 T13–T16 逐行读数：T13 = 5/4/4（计 13）、T14 = 0/3/9（计 12）、T15 = 0/1/4（计 5）、T16 = 2/0/3（计 5）；四行 acc/dev/rej = **7/8/20 = 35**。与 T1–T12 的 **94/92/108 = 294** 相加 → **101/100/128 = 329** ✅ **完全闭合**（先前的「分配不一致」是缺 4 行读数所致） |
| **GAP-4** T-001..T-047 三档未回填 | ✅ **已修复（范围扩至 47）** | `### T 决策三档回填（GAP-4）` **47 条逐条**给出三档 + 偏离说明/source 锚点；计数 **26/16/5 = 47**（验算见该节） |
| **GAP-5** T-038 只覆盖 5 条 B 类，实有 7 条 | ✅ **已修复** | **T-044 裁决**：接受草稿补写；`L-19`→CARD-03、`L-33`→CARD-08 **保持 B 类**并补登本卡切片动作（B-2 / B-6）；B 类 7/7 全部有归属与切片动作 |
| **GAP-6** DIV §6.5 四个空白无归属/owner | ✅ **已修复** | **T-045 裁决逐个指派**：冻结审查合同→CARD-05、blind 核验→CARD-05、**K 失效条件维护→CARD-07**、跨 phase 契约一致性→CARD-03；全部写入 `### 承接与不承接清单` 并附**输入注入义务**（R-54 缓解：由 CARD-10 存在性核对） |
| **GAP-7** T-013 无独立 D 块 | ⚠️ **维持原处置（未改判，理由见下）** | 维持「T-013 为过程条目、结论由 T-017 结清、不占块位」并**在 `T 覆盖核对表` 中单列一行**（42 主块 + 1 过程条目 = 43，算术闭合）。未为其新设 D 块，理由：T-013 无独立决策实体（用户选择为「暂缓决定」），为其占块位会与「一处一权威」冲突。**该处置已在材料中可见**（非掩盖） |
| **GAP-8**（草稿补充）`draft-fidelity.md` 拒绝表 27 行 vs 表头 25 条（COUNT-03） | ✅ **已按分布口径统一** | 本收口在明细 A 中采用**分布口径**：`L-17`、`L-21` = `接受但有偏离`（与 `decision-log.md` 三档分布一致）；L 层合计 **0/14/25 = 39** 自洽。**残留**：`draft-fidelity.md` 该表本体仍标 27 行/标 `拒绝`——**未修改被审文件**，差异如实登记于本表（需主会话在并入时改写该表头或加「含 2 条边界项」声明） |
| **GAP-9**（草稿补充）T 表非 ID token 与不存在的 ID | ✅ **已修复，且发现草稿漏列 7 项** | 见 `### GAP-9 修复`：`J-25`→`REQ-J-12`；`SD-10`→`REQ-B-23`；`R-…`→移出 REQ 表（改 `card-02 T-018` / `card-02 T-021`）；`T6` 标称 52→**51**；`E-3`/`E-6`→`card-02 E-3`/`card-02 E-6`；`OI-005`/`T-023 相关`/`U-001 E-5` 逐项改写；**新增 NG-2**：`L-26/L-27/L-28/L-31/L-33/L-34/L-35`（7 个）为草稿**漏列**的非 REQ token，且 `L-01..L-35` 形态与 `REQ-L-nn` **同形冲突**，已要求显式标注 |

#### 三层计数是否算术闭合（验算式）

| 层 | 验算式 | 结果 |
| --- | --- | --- |
| 第 1 层（18 来源组） | 8+36+12+36+26+8+14+15+39+16+23+25+3+10+9+4+16+16 | = **316** ✅ |
| 第 2 层（三档） | 99+128+89 | = **316** ✅ 与第 1 层闭合 |
| 第 2 层逐组 | 18 组逐组 `需求数 = 接受+偏离+拒绝`（表见「零孤儿算术依据」，18/18 验算通过） | ✅ |
| 第 3 层（L 表） | 0+14+25 = 39 = 38+1 | = **39** ✅ |
| 第 2↔3 层 | 第 3 层为第 2 层的**聚合子视图**（14 偏离 ≤ 128、25 拒绝 ≤ 89），**不相加** | ✅ 关系自洽 |
| 主题表（导航视图） | 逐行标称 331；本表计 94+7 / 92+8 / 108+20 = 101/100/128 = **329**；合计行「341」判为**错误数字** | ✅ 三口径已分别声明 |
| T 表三档 | 26+16+5 | = **47** ✅ |
| A/B/C | 11+7+7 | = **25** ✅ |
| 决策块行数 | 14 块 × 16 字段行（T-047） | ✅ 0 块含 `覆盖 T 决策` |

#### T-001..T-047 是否逐条有三档

✅ **是，47/47 逐条有三档，无合并、无省略**。计数：`接受` **26** / `接受但有偏离` **16** / `拒绝` **5**，合计 **47**。
- 与 L 层的对照：T 层 5 条拒绝**全部**有取代/否定链（T-004→D-009、T-008→T-018、T-011→T-043、T-015→T-039、T-019→T-041+AC-26），符合「拒绝必须写明理由与依据」（V-04 规则 2）。
- 与 L 层的差异说明：T 层的 26 条接受**不等于** L 层的 0 条接受——L 表构造口径是「没有完整承接的需求条目」，T 表覆盖的是**决策条目**（决策本身忠实承接，其覆盖缺口另由 L 层登记），两者**不是同一对象**，故数字不可直接相加或比较。

#### 收口过程中新发现的缺口 / 不一致

| 编号 | 新发现 | 性质 | 处置 |
| --- | --- | --- | --- |
| **NG-1** | 草稿 GAP-2 的加法错误（341 应为 331） | 收口材料自身的算术错误 | 已修正；同时把主题表三口径（331/329/341）逐一声明 |
| **NG-2** | 主题表 `L-nn` 与 `REQ-L-nn` **命名空间冲突**；7 个 `L-26..L-35` token 不可解析 | 源报告缺陷（草稿 GAP-9 漏列） | 已逐项给修正值与标注要求 |
| **NG-3** | 主题表**覆盖率仅 230–249 / 316**（视 L token 归属而定），**不是完备分区** | 源报告结构性缺陷 | 已在 GAP-1 写明：主题表降级为导航视图，**不得**与 316 比对 |
| **NG-4** | 原第 2 层 104/95/117 **不可由第一部分重算** | 计数不可复算（正是 NEW-01/L-09 要修的对象） | 已替换为 99/128/89（附敏感度 100/128/88） |
| **NG-5** | 第一部分层「明确排除」= 0（7 条点到归属卡者仍标「未登记排除理由」） | 无主拒绝未在第一部分层关账 | 已写明：明确排除在 **L 层**由 B 类 7 条落地，**不回溯改写** 316 层计数（append-only） |
| **NG-6** | 任务给定恒等式写法「需求数 = 接受+偏离+拒绝+**明确排除**」会把作为**拒绝子集**的明确排除**重复计** | 口径歧义 | 已改为子项口径呈现并显式指出（不掩盖、不擅自改写计数） |
| **NG-7** | `T-045` 把「K 的失效条件维护」派给 **CARD-07**，而 K1–K12 的失效条件（含 L2 退出机制）由**本卡 D-003/D-009 定义** → 存在「定义者 ≠ 维护者」的跨卡责任缝 | 新发现的边界缝隙 | **如实登记**：本卡按 T-045 登记归属；并把 R-F 四条失效条件作为**输入**登记（承接清单第 6 行），由 CARD-10 存在性核对。**未擅自改判**（属用户裁决范围） |
| **NG-8** | `T-046` 修复面若**只修 card-02 侧**，则 card-01 与规划任务仍不兼容 | 未闭合的验收条件 | 已按 R-55 写成验收判据：**同一检查器对 card-01 与 card-02 全部现行 decision-log 均可识别**（acceptance S6） |
| **NG-9** | 原草稿自检称「14/14 块均含 `Supersedes`，其中 **7 块**为实质取代」，但逐块点数实为 **4 块**（D-004/D-007/D-008/D-009）、涉 **6 条** T 条目（T-004/T-008/T-011/T-015/T-016/T-020） | 源材料计数不自洽 | 已改为可复算表述（4 块 / 6 条 T），见 `## 本文件的自检` 的 `Supersedes` 行 |

#### 有没有为了「看起来干净」而掩盖任何不一致？

**没有。逐条声明如下**：
1. **未抹平**「原草稿第 2 层数字不可复算」——直接废弃并给出重算值与敏感度（NG-4）。
2. **未接受**草稿对「差 13」的解释——核查后**判定该解释不成立**，并把真实成因与「数值巧合」写明（GAP-1）。
3. **未掩盖**草稿自身的加法错误（NG-1）与漏列（NG-2）。
4. **未掩盖**主题表覆盖率不足（230–249/316）这一结构性事实（NG-3）。
5. **未掩盖**第一部分层「明确排除 = 0」以及 89 条拒绝全部为「无主拒绝」（NG-5）。
6. **未掩盖**任务给定恒等式的重复计风险，也未擅自改写计数（NG-6）。
7. **未擅自改判** T-045 产生的「定义者 ≠ 维护者」责任缝（NG-7），只登记。
8. **未掩盖**以下**仍未闭合**的项（不改判、不漂白）：
   - `draft-fidelity.md` 的「拒绝」表仍为 27 行/标 `拒绝`（GAP-8 残留）——**未修改被审文件**；
   - `T-031`/`T-046` 两个 build-code 交付物**尚未实现**（本阶段按纪律不改代码，只登记需求与验收判据）；
   - 14 条偏离重确、C 类 7 条裁决、spec 结构定稿（R-G 推荐组合确认）**尚待用户**；
   - `REQ-full-inventory.md` 的 316 是**下界**（U-002-14「等等等等」明示清单不完备），本文件不宣称完备；
   - 本卡材料中仍**保留**源快照行号（如 `stage-content-contracts.mjs:3387` 类证据坐标与 `prd.md:175,453` 类节位置）——**本文件正文已全部改为语义锚点**，但**被审源文件中的行号未改动**（不改被审文件）。
9. **未替用户做取舍**：全部 C 类 7 条、14 条偏离、四空白归属的争议面均按 T-030/T-045 的边界处理，未自行裁决。

## 本文件的自检（收口材料层）

| 判据 | 结论 |
| --- | --- |
| 是否修改了被审文件 | ✅ **未修改**（只新建本文件；未改 `decision-log.md`、`draft-decision-blocks.md`、`draft-fidelity.md`、`research/**`） |
| 是否改动了任何代码 | ✅ **未改动**（T-046/T-031 均按纪律登记为 build-code 交付物） |
| 是否改写了原始声明 | ✅ **未改写**（V-01..V-28 一字未动；逐字引用只用「…」截断） |
| 是否使用行号锚点 | ✅ 本文件正文**零行号锚点**（全部改为稳定 ID / 函数名 / 节标题）；仅在被审源文件的**既有文字**中出现处如实说明 |
| 决策块是否 16 行 | ✅ **14/14 块 = 16 字段行**，0 块含 `覆盖 T 决策`（T-047） |
| 四字段是否齐备 | ✅ **14/14 块含** `module` / `requirement_ids` / `derived_from` / `artifacts`（56/56 可检出）；官方检查器因「行首 ASCII 冒号」口径报 0/56 属**既有格式不兼容**，修复为 build-code 交付物（T-046） |
| `Supersedes` 是否正确 | ✅ 14/14 块均含 `Supersedes`；其中 **4 块为实质取代**，分别取代 **6 条** T 条目（D-004→T-008 软目标；D-007→T-015 处置部分 + T-016 清单定位；D-008→T-020 单轨判据；D-009→T-011 独立技能表述 + T-004 数量口径），其余为「无」或「追加式补正」（原草稿自称「7 块实质取代」为计数不自洽，见 NG-9） |
| 计数是否可核算术 | ✅ 每处「列表 = 计数」，全部给出加总验算式 |
