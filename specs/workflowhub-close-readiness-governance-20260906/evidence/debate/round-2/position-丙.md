# 立场书 · 丙队（杠精）——方向卡 v4 整体不成立

**组A**：方向期"锁定语义"=把实现细节提前拍死。卡内自认载体形态未定（II-3.1、DEFERRED-005），却要求方向期锁定：实质是"用方向期语言写 spec"的伪装——锁死则过拟合（#64 负向 oracle 无分类规范，锁了必错），留白则违反"不依赖 build-spec 补需求"（blocking 项）。

**组B**：#40 两头堵。全量审查=维持现状还添摘要生成成本（28.2 万字符×5×2 已发生），成本不减反增；摘要注入=掐断异源审查独立取证（审查环境禁读仓库文件），RISK-006 自认召回可能下降。

**组C**：#33×3 两头堵。"必须发起"=强制动作=gate 另一写法（违"记录事实不阻断"）；"只记录"=零机制变化，执行率≈0 现状原样保留。

**组D**：成功标准改"可判"→每条成功/失败线都需定义阈值（"绕过后消失""召回下降"无可机检度量），方向卡胀成 500 行；否则标准空转。而 #67 成本无基线、#39 时序倒置、#53 六状态外置皆是可判承诺下的暗雷。

**组E**：I 部分 5 项+II 部分 3 大块（模板/校验器+verify 独立性+索引/派发/单源）叠加=任务扩 2 倍（案卷组E）；多机制多 phase 叠加历史上均失控，非目标四行"不做"恰说明边界失守。

**组F**：索引只省主会话导航；成本大头=子代理独立上下文+审查全量注入（research-Q2 原数据），索引零作用；read 工具 offset 行为未验证（案卷组F已标注），先实测再引入，否则是模拟优化。

**结论与替代**：v4 不成立，需大改。替代：范围砍到 I+II-1，II-2/II-3 拆后续任务；方向卡降为"问题+证据+约束"三栏，机制形态归 build-spec 记录决策依据；审查输入改文件引用+按需读、保留异源；一切"必须"改事实记录；索引凭 dogfood 实测数据再定。
