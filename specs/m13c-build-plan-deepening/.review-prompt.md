# 异源审查请求：m13c-build-plan-deepening plan/tasks/cross-artifact-analysis

你是独立第三方审查者（codex，异源）。你的任务是对以下三份产物做独立工程审查，给出 verdict + 逐条 blocking/non-blocking 发现。

## 审查目标

三份产物：
1. `specs/m13c-build-plan-deepening/plan.md`
2. `specs/m13c-build-plan-deepening/tasks.md`
3. `specs/m13c-build-plan-deepening/cross-artifact-analysis.md`

权威参考（只读，不审查）：
- `specs/m13c-build-plan-deepening/spec.md`（FR 权威来源）
- `constitution-checklist.md`（21 条条款权威名称）

## 审查范围

回答以下六个问题（只看实际文字，不靠假设）：

1. **FR coverage**：spec.md 中每个 FR-[DOMAIN]-NNN ID 是否在 plan.md + tasks.md 中都有对应任务实际执行覆盖（不只是标签引用）？
2. **Constitution check 正确性**：plan.md 引用的 constitution-check.md 是否使用了当前 constitution-checklist.md 的正确 21 条条款名称？有无对错条款名或虚假 21/21 pass？
3. **F10 gate 完整性**：plan.md 的 F10 章节是否覆盖了所有新引入机制/验证/schema/CI 检查？有无遗漏？
4. **执行语义一致性**：spec.md 中 FR-RESEARCH-002（research.md 缺失时 pause/escalate）、FR-DATACONTRACTS-002（data-contracts 先决条件）、FR-TASKS-001（no-placeholder 阻断 vs 继续）三条的失败语义，在 plan.md 和 tasks.md 中是否与 spec.md 一致？有无矛盾？
5. **task_dir 实际消费者**：tasks.md 是否有任务将 task_dir 解析器接入真实 SKILL.md 消费者（T002 只建解析器；有无任务把它 wire 进 build-plan/spec-research/spec-analyze/spec-tasks 的 SKILL.md）？
6. **cross-artifact-analysis 可靠性**：cross-artifact-analysis.md 是否报告了 0 CRITICAL/HIGH 且建议可启动 build-code？与你在前5问发现的问题相比，有无漏报阻断项？

## 待审产物全文

### plan.md

```
# 实施计划：m13c-build-plan-deepening

**Task ID**: `m13c-build-plan-deepening` | **Date**: 2026-07-01 | **Spec**: spec.md | **Status**: Draft

## 概述

本计划为 build-plan 深化（M13c）的实施步骤。核心目标是在 `workflows/build-plan/SKILL.md` 补入四类质量缺口：Phase 0 research 先行（spec-research）、data-contracts 落盘、simplicity-guard 四阶梯接入、plan-reviewer 独立工程审查；同时在 spec-analyze 补 ambiguity_items[]、spec-tasks 强化 no-placeholder 铁律、新建 task_dir 解析器、更新 reuse-registry。

设计原则：最小化核心改动（薄核心），所有新能力下沉到独立 skill 层；失败语义统一为"记录+升级人工，非硬阻断"（F4/F3）。

## Technical Context

Language/Version: Markdown (SKILL.md), Node.js v20 (task_dir 解析器)
Dependencies: 已有 skills/spec-research 框架; vitest
Storage: Filesystem specs/m13c-build-plan-deepening/, skills/, workflows/build-plan/, config/
Testing: npx vitest run（task_dir 解析器测试）
Target Platform: workflowhub 仓库，macOS/CI

## Constitution Check

宪法合规判定见 constitution-check.md（21/21 通过，当前版 F1-F10 / Q1-Q3 / S1-S8 条款）。

## F10 Anti-Over-Engineering Gate

### 机制 1：spec-research skill
1. 防御真实威胁：技术选型盲区在 plan 阶段才暴露（M13b 执行记录实例）
2. 现有覆盖：无。gap 已确认。
3. 可绕过性：低。
4. 维护成本：低。
结论：合理引入，保留。

### 机制 2：data-contracts 步骤
1. 防御：spec/plan 接口约定缺失导致 build-code 返工
2. 覆盖：无
3. 可绕过：低
4. 维护：低
结论：合理引入。

### 机制 3：plan-reviewer（复用 3rd-review）
1. 防御：plan 工程可行性未独立评估
2. 覆盖：3rd-review plan-reviewer 已存在，D3=复用
3. 可绕过：低
4. 维护：极低
结论：合理引入，已最优化。

### 机制 4：task_dir 解析器
1. 防御：任务记录误写入 repo（D2 执行事故）
2. 覆盖：config 字段存在但无消费者
3. 可绕过：中（各 skill 可绕过不调用），测试覆盖降低风险
4. 维护：低
结论：合理引入。

注：plan.md F10 章节中 task_dir 解析器明确排除于 F10 gate（plan.md:144 附近注：task_dir 解析器已在 Step 1.2 独立评估，此处不重复）。

## 实施步骤

### Phase 0：准备

Step 0.1：读取并确认 spec.md 已3rd-review通过
Step 0.2：确认 simplicity-guard SKILL.md 已存在（M13b 落盘）

### Phase 1：基础设施

Step 1.1：新建 skills/spec-research/SKILL.md（FR-RESEARCH-001/002/003）
Step 1.2：新建 core/task-dir-parser.mjs（FR-TASKDIR-001）；任务跟踪文件消费者须通过解析器取路径（注：消费者接入在 Phase 2 各 SKILL.md 修订中完成）
Step 1.3：新建 core/task-dir-parser.test.mjs（FR-TASKDIR-002）

### Phase 2：核心 SKILL.md 修订

Step 2.1：修订 workflows/build-plan/SKILL.md（Phase 0 step 调 spec-research；Phase 1 加 data-contracts；加 plan-reviewer；加 simplicity-guard 前置判断）（FR-RESEARCH-001, FR-DATACONTRACTS-001/002, FR-SIMPLICITY-001/002, FR-PLANREVIEW-001/002/003）
Step 2.2：修订 skills/spec-analyze/SKILL.md（ambiguity_items[]）（FR-ANALYZE-001/002）
Step 2.3：修订 skills/spec-tasks/SKILL.md（no-placeholder 铁律 + STOP/Knowledge 约定）（FR-TASKS-001/002）

### Phase 3：收尾

Step 3.1：更新 reuse-registry.md（upstream_delta 列 + spec-research 行）（FR-REGISTRY-001）
Step 3.2：确认 simplicity-guard + build-spec 接入（grep 核实，不改动）
Step 3.3：plan-reviewer 跨仓路径验证约定（已含于 Step 2.1）

### Verification Mapping

| AC | 来源 | 验证方式 |
|----|------|----------|
| AC-01 | FR-RESEARCH-001 | grep spec-research 步骤存在于 build-plan SKILL.md |
| AC-02 | FR-RESEARCH-002 | grep fail-loud 语义存在于 spec-research SKILL.md |
| AC-03 | FR-RESEARCH-003 | grep 跳过选项条件存在 |
| AC-06 | FR-DATACONTRACTS-001 | grep data-contracts 步骤存在 |
| AC-07 | FR-DATACONTRACTS-002 | grep non-blocking 失败语义 |
| AC-08 | FR-PLANREVIEW-001 | grep plan-reviewer 步骤存在 |
| AC-09 | FR-PLANREVIEW-003 | grep 路径可访问性验证存在 |
| AC-10 | FR-ANALYZE-001 | grep ambiguity_items[] 存在于 spec-analyze SKILL.md |
| AC-11 | FR-ANALYZE-002 | grep escalation_path 字段存在 |
| AC-12 | FR-TASKS-001 | grep no-placeholder 铁律存在 |
| AC-13 | FR-TASKS-001 | grep blocking_item:true 标记逻辑存在 |
| AC-14 | FR-TASKS-002 | grep STOP/Knowledge 标签 + upstream_delta 约定存在 |
| AC-15 | FR-REGISTRY-001 | grep upstream_delta 列 + spec-research 行存在于 reuse-registry.md |
| AC-16 | FR-TASKDIR-001 | grep 解析器调用存在于各消费者 SKILL.md |
| AC-17 | FR-TASKDIR-002 | npx vitest run 绿 |
| AC-18 | — | 由 codex/3rd-review 产物路径验证（本文件路径存在） |
| AC-19 | FR-SIMPLICITY-001/002 | grep simplicity-guard SKILL.md 存在 + build-spec 含引用 |

### Metrics（build-plan 阶段）

全部 5 项 unknown，原因明确（upstream build-code/verify-code 数据未产生）。
```

### tasks.md

```
# Tasks: m13c-build-plan-deepening

Input: specs/m13c-build-plan-deepening/
Prerequisites: spec.md (authoritative, 3rd-reviewed), plan.md
Tests: Vitest (npx vitest run) 针对 task_dir 解析器

Organization: 4 Stage，Stage 1 建基础设施，Stage 2 修订核心 SKILL.md（可并行），Stage 3 收尾，Stage 4 验收。

## Stage 1

- [ ] T001 新建 skills/spec-research/SKILL.md：Phase 0 research skill（输入 task-id+功能描述，产出 specs/{task-id}/research.md，含跳过选项），fail-loud、non-blocking 失败语义 (stage:1, depends:无)
  FR: FR-RESEARCH-001, FR-RESEARCH-002, FR-RESEARCH-003

- [ ] T002 [P] 新建 core/task-dir-parser.mjs：单一解析函数，读 config/workflowhub.yaml task_dir 字段，回退默认路径 ~/Knowledge/workflowhub/；不引入第三方依赖 (stage:1, depends:无)
  消费者列表：workflows/build-plan/SKILL.md、skills/spec-research/SKILL.md、skills/spec-analyze/SKILL.md、skills/spec-tasks/SKILL.md（在读写任务跟踪文件前须通过此解析器取路径，不得硬编码）
  FR: FR-TASKDIR-001

- [ ] T003 [P] 新建 core/task-dir-parser.test.mjs：vitest 测试，覆盖"显式配置路径"和"默认回退路径"两场景，npx vitest run 须绿 (stage:1, depends:T002)
  FR: FR-TASKDIR-002

## Stage 2

- [ ] T004 [P] 修订 workflows/build-plan/SKILL.md：新增 Step 0（调 spec-research，产出 research.md）；Phase 1 新增 data-contracts 步骤（产出 data-contracts.md，失败 record+escalate non-blocking）；插入 simplicity-guard 前置判断（输出 minimal-path 字段）；新增 plan-reviewer 步骤（调 3rd-review，失败 record+escalate non-blocking；调前验证跨仓路径可访问性） (stage:2, depends:T001)
  FR: FR-RESEARCH-001, FR-DATACONTRACTS-001, FR-DATACONTRACTS-002, FR-SIMPLICITY-001, FR-SIMPLICITY-002, FR-PLANREVIEW-001, FR-PLANREVIEW-002, FR-PLANREVIEW-003

- [ ] T005 [P] 修订 skills/spec-analyze/SKILL.md：stage-result.facts 新增 ambiguity_items[]（每项含 description、escalation_path，可选值 human_confirm/next_iteration/acceptable_ambiguity）；escalation_path 缺失时 warn 写 quality-contract，不阻断 (stage:2, depends:无)
  FR: FR-ANALYZE-001, FR-ANALYZE-002

- [ ] T006 [P] 修订 skills/spec-tasks/SKILL.md：新增 no-placeholder 铁律（禁止 TODO/TBD/placeholder/待定/暂缺）；发现时 blocking_item:true，记录 friction，stage-result.human_intervention=true，spec-tasks 继续完成 tasks.md 写入，不阻断 build-plan stage 推进；新增 STOP/Knowledge 标签约定（软要求，缺失 warn）和 upstream_delta 字段说明 (stage:2, depends:无)
  FR: FR-TASKS-001, FR-TASKS-002

## Stage 3

- [ ] T007 更新 reuse-registry.md：新增 upstream_delta 列，为 spec-research 添加登记行（含 upstream_delta 值） (stage:3, depends:T001)
  FR: FR-REGISTRY-001

- [ ] T008 [P] 确认 skills/simplicity-guard/SKILL.md 存在；grep 确认 workflows/build-spec/SKILL.md 含 simplicity-guard 引用（仅核实，不改动） (stage:3, depends:无)
  FR: FR-SIMPLICITY-001, FR-SIMPLICITY-002

## Stage 4

- [ ] T009 运行 npx vitest run，确认 task_dir 解析器测试绿 (stage:4, depends:T003)
  FR: FR-TASKDIR-002

- [ ] T010 grep 验证各消费者 SKILL.md 含 task_dir 解析器调用（build-plan/spec-research/spec-analyze/spec-tasks SKILL.md 各含 task-dir-parser 引用）；产出 AC-16 验证报告 (stage:4, depends:T004,T002,T008)
  FR: FR-TASKDIR-001

- [ ] T011 [P] 确认 AC-02~AC-15 覆盖（grep 各新字段/步骤是否存在）；输出各 AC 通过/待定清单 (stage:4, depends:T004,T005,T006,T007)
  FR: FR-RESEARCH-001, FR-DATACONTRACTS-001, FR-DATACONTRACTS-002, FR-SIMPLICITY-001, FR-SIMPLICITY-002, FR-PLANREVIEW-001, FR-PLANREVIEW-002, FR-PLANREVIEW-003, FR-ANALYZE-001, FR-ANALYZE-002, FR-TASKS-001, FR-TASKS-002, FR-REGISTRY-001

## Dependencies Execution Order

- Stage 1：无依赖，立即开始
- Stage 2：T004 依赖 T001；T005/T006 无 Stage 1 依赖，可与 Stage 1 并行或随即开始
- Stage 3：T007 依赖 T001；T008 无依赖
- Stage 4：T009 依赖 T003；T010 依赖 T004/T002/T008；T011 依赖 T004-T007

## Parallel Opportunities

- Stage 1：T001 和 T002 可并行；T003 依赖 T002 完成后立即开始
- Stage 2：T004/T005/T006 可并行（不同文件）
- Stage 3：T007/T008 可并行
- Stage 4：T009/T010/T011 可并行（T009 依赖 T003）
```

### cross-artifact-analysis.md（全文已提供，关键结论）

- 覆盖摘要：spec.md 17 FR/19 AC；plan.md 17 FR 全部引用；tasks.md T001-T011 FR 标注完整
- CRITICAL: 0, HIGH: 0, MEDIUM: 0, LOW: 4
- 结论："良好。4 条 LOW 级发现均为细节歧义，不影响 build-code 阶段启动。"

### spec.md 中的关键 FR 语义（节选，供比对）

FR-RESEARCH-002：research.md 为 Phase 1 输入先决；缺失时 fail-loud 记录+报告，**不允许继续 Phase 1**（pause/escalate）。
  - 场景：research.md 缺失 → 停止，记录 blocking_item，human_intervention=true

FR-DATACONTRACTS-002：tasks.md 的执行需要 data-contracts.md 存在且非空；缺失时 warn+escalate，**tasks 继续执行**（记录低置信度）。
  - 注：spec 原文在这里有内部矛盾——"must"先决与"continue"并存。

FR-TASKS-001（场景 4.6）：发现 placeholder → **"不允许继续分解 / 强制人工解决后再推进"**（blocking）
  - 但 tasks.md T006 描述：发现时 blocking_item:true，spec-tasks **继续完成** tasks.md 写入，不阻断 build-plan stage 推进。

### constitution-checklist.md 当前 21 条条款名（供核对）

F1 薄核心, F2 窄契约, F3 物理事实靠机器校验但不阻断, F4 质量靠异源审查与人而非阻断式质量门, F5 gate 谨慎添加, F6 统一外置执行记录, F7 推进/不可逆操作经人确认, F8 简单优先、可证伪, F9 让问题尽早暴露, F10 防止过度工程化
Q1 外包视角验证可行, Q2 gate 三类划分, Q3 独立异源审查
S1 能用外部就不造轮子, S2 外部技能可针对项目改造合宪, S3 技能与项目宪法对齐, S4 自定义技能必须有指标系统, S5 自定义技能方便子代理调用省主上下文, S6 自定义技能参考市面方案不闭门造车, S7 一阶段一技能一工作流一文件夹, S8 自定义技能可独立调用可搬运

## 输出格式要求

输出以下结构：

```
verdict: pass | revise_required

blocking_count: N

findings:
1. [blocking|non-blocking] file:line — 一句话问题描述
2. ...

bottom_line: 一句话结论
```

只列 blocking 和 non-blocking 发现。每条 <= 80 字。不写冗余分析过程。
