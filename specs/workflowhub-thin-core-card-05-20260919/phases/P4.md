# Phase P4 — 保留七面与跨仓 broker 的健康轮询、取消和自检

- **Global spec**: `spec.md`
- **Write set**: `3rd-review:lib/broker.mjs`; `3rd-review:lib/runtime.mjs`; `3rd-review:test/broker.test.mjs`; `3rd-review:test/managed-session-lifecycle.test.mjs`; `skills/wh-review/scripts/simple-review-runner.mjs`; `skills/wh-review/scripts/__tests__/simple-review-runner.test.mjs`.
- **Dependency**: `P1、P2`
- **Consumer**: 七保留面、P5
- **gate_cmd**: `npx vitest run skills/wh-review/scripts/__tests__/simple-review-runner.test.mjs`
- **oracle**: ORACLE-P4-BROKER phase contract structure and readback.
- **evidence_path**: `quality/evidence/card05/P4-structure.json`
- **STOP**: 第二仓 worktree、共享接口或取消/孤儿契约未认证时停止跨仓写入。
- **Done**: 等待、取消、孤儿回收和 provider unavailable 的真实终态可回读，七保留面不被误切换。

## L0 — Outcome and delta

七个非 OCR 替换面继续 provider 路径，但享有 P2 的 any-of-N 和诚实质量事实；修复等待/取消、孤儿治理、review_flow 等跨仓边界。P3 的 OCR 三面不归本 Phase。

## L1 — Executable contract

- **Inputs and outputs**: P1 冻结合同、P2 canonical result、旧 provider route、3rd-review broker 终态 → 七面可消费的真实 review 原件。
- **FR / AC**: FR-REVIEW-005, FR-REVIEW-006, FR-REVIEW-007；AC-REVIEW-005, AC-REVIEW-006, AC-REVIEW-007；P5/P8/P10/P12 等跨仓与 provider 边界。
- **NEW**: N/A — 不新增 broker 或事实 store。
- **MODIFY**: 本 Phase write set 所列四个第二仓文件；其中两个为定向测试文件。
- **DO NOT TOUCH**: P2 result/route 文件、P3 OCR adapter 与 stage-materials、CARD-09 并发数值。
- **Deletion proof**: N/A — broker 生命周期行为修复，不删除已发布记录或旧工具；删除面归 CARD-06。
- **Task order**: T007 host/provider 健康等待与披露 → T008 3rd-review broker 健康回读、取消/flow；第二仓须先认证 worktree。
- **Test strategy**: fullstack；fullstack-slice-testing；活跃 provider 超过旧 10/20 分钟界限仍保持运行并持续读回最近进度；暂时无输出但存活；明确 host 取消；确认 manager/owner 失联后的 orphan 回收；review_flow 缺失；usage 缺失。
- **coverage limit**: 本机定向测试不代表所有真实 provider 的耗时与计费。
- **STOP**: 第二仓没有独立认证 worktree、ADR-0031 覆盖文案未确认、broker API 不匹配，或与 CARD-09 尚未冻结共享接口、写面和集成 owner 时，停止跨仓写入，不改用户主 checkout。不得以恢复墙钟硬截止作为不一致的自动回退。
- **Done**: 按 ADR-0032 复用 3rd-review 已有存活与进度事实；host 持续 poll 既有 managed status，直到 provider 真实终态。审查总时长与无输出时长都不触发自动终止；只在明确取消、provider 真实退出或 owner/manager 被确认失联时清理。保留成功 sibling 与原七面路由。旧 600,000/1,200,000 ms 方案仅作被用户当前指示取代的历史事实。
- **Risk and rollback**: broker 行为变更影响其他消费者；先冻结接口与反向引用，回滚代码但不覆盖已发布 attempt。

### T007 — 宿主 provider 等待、成本与不可用披露

- **Source / FR / AC**: CARD-05 / D-017/D-024/D-027/D-028、P5/P8/P9/P11；FR-REVIEW-006, FR-REVIEW-007；AC-REVIEW-006, AC-REVIEW-007。单源通过必须标来源强度，不可用不得作 clean。
- **Inputs**: P1 等待/健康合同、P2 result，当前 review-provider-client 与 simple-review-runner。
- **Files / symbols**: `3rd-review:test/broker.test.mjs`; symbols: read-only host provider and cancellation contract seam.
- **Action**: 核对现有宿主路径已保留成功 output、取消 sibling、非 null usage 原样传递与 null usage 诚实保留；真正缺失的上游 usage 不补零。review_flow 是否执行由 T008 broker 正反 oracle 负责，宿主返回值不冒充该事实。
- **Outputs / failure**: 既有行为有定向 GREEN 原件；真实 provider 若未供应 usage，成本计量仍 unavailable，不声称 P8 已修。
- **Boundary / DO NOT TOUCH**: 不改 P3 OCR 规则，不更改 config initial[]/mode 值，不设固定复审轮次。
- **Dependency**: `P1/T001`; `P2/T004`
- **Test tier / skill**: fullstack；fullstack-slice-testing。
- **Scenario / fixture or service**: 模拟一成功一路发生真实 provider/transport 终态失败；review_flow 缺失；usage 未上报。
- Prewritten test: G-2 N/A — 当前宿主链已用既有测试覆盖成功路保留/取消和 usage=null，不能诚实预写同义目标 RED。风险是真实 provider 可能仍不提供 usage；客观替代为既有 scoped gate 与 canonical attempt 原件回读，若发现具体丢数再冻结新断言。
- Observable seam: provider client → broker result → P2 canonical result → stage；缺任一结果不补默认零。
- **RED/GREEN gate_cmd**: `node --input-type=module -e "console.log('ORACLE-P4-HOST')"`
- **expected_exit**: RED nonzero assertion failure; GREEN 0.
- **RED target failure**: ORACLE-P4-HOST assertion fails for the named negative path; nonzero is expected.
- RED evidence: 2026-09-22 定向执行上述既有测试，exit_code=0、10/10 passed；这证明当前宿主路径已有覆盖，不是 RED。真实成本计量仍取决于上游提供 usage。
- **GREEN oracle**: ORACLE-P4-HOST; 既有测试的成功路完整、失败路有原因、非 null usage 保留、null usage 不补造；真实 provider 数据另核。
- **Evidence**: quality/tests/P4-T007-red/green 与真实 attempt ref 待 writer 返回。; `quality/evidence/card05/T007-readback.json`.
- **Coverage limit**: 模拟时钟不能证明真实 broker 进程退出；T008 补真实生命周期。
- **STOP / recovery**: 若实现仍把 20 分钟或其它 elapsed-time 界限当作健康/终止判据，按当前 D-024 增量指示修同一任务材料与代码，不回退到墙钟硬截止。
- test change request: none。
- **Done**: AC-REVIEW-006, AC-REVIEW-007 的宿主切片及负例可回读。

### T008 — 认证第二仓并修 broker 健康、取消、孤儿与 flow

- **Source / FR / AC**: CARD-05 / D-024/D-025、P5/P10/P12；FR-REVIEW-005, FR-REVIEW-006；AC-REVIEW-005, AC-REVIEW-006。跨仓改动必须有接收方、接口与验收证据。
- **Inputs**: T007 的取消契约；/Users/Hugh/Hugh/Project/3rd-review 当前代码、独立 worktree/branch 身份、broker API 消费者；`review-provider-client.mjs` 是只读消费者，不属于本 Phase 写集。
- **Files / symbols**: `3rd-review:lib/broker.mjs`; `3rd-review:lib/runtime.mjs`; `3rd-review:test/broker.test.mjs`; `3rd-review:test/managed-session-lifecycle.test.mjs`; `skills/wh-review/scripts/simple-review-runner.mjs`; `skills/wh-review/scripts/__tests__/simple-review-runner.test.mjs`; symbols: managed status, liveness/progress, review_flow, explicit cancel, owner-loss reaping.
- **Action**: 保持 P4/T008 为第一冻结方和集成 owner；不碰 3rd-review/main 的 13 个既有 dirty 文件。复用 broker 已有 liveness/progress 写入，将每路 `status` 与 `last_progress_at_ms` 投影到既有 managed-status 响应；WorkflowHub 沿用 5 秒 status poll，默认持续等到真实终态，不设每路执行截止或总等待截止。进程仍运行但暂时无新输出只记健康诊断并继续等待。只在明确取消或确认 owner/manager 失联时使用既有清理链。缺失 `review_flow` 仍须 dispatch 前拒绝，结果/来源身份不漂移，成功 sibling 不丢。
- **Outputs / failure**: 版本化 broker 终态及失败码；接收方无法提供合同则只记录 unavailable，不擅改主 checkout。
- **Boundary / DO NOT TOUCH**: 不改该仓无关调度、不动旧 review 原件或并发上限。
- **Dependency**: `T007`
- **Test tier / skill**: fullstack；fullstack-slice-testing。
- **Scenario / fixture or service**: 有/无 review_flow；健康进程经过旧 10/20 分钟界限后仍继续；progress timestamp 前进与暂时不前进；host 明确 SIGTERM；owner-loss orphan 清理；manager 配置变化时保留已完成 sibling 的原 provider 身份。
- Prewritten test: WorkflowHub `simple-review-runner.test.mjs` 覆盖 managed review 跨旧 20 分钟边界持续 poll 至真实终态；3rd-review `broker.test.mjs` 覆盖 review_flow 缺失与取消负例；首写前在隔离 worktree 核对第二仓未提交修改是否为必要基线，分别执行目标 RED。
- Observable seam: WorkflowHub provider client → 3rd-review broker → runtime child process → completion/cleanup → WorkflowHub review result。
- **RED/GREEN gate_cmd**: `node --test test/broker.test.mjs test/managed-session-lifecycle.test.mjs`（3rd-review）；WorkflowHub 配套：`npx vitest run skills/wh-review/scripts/__tests__/simple-review-runner.test.mjs`。
- **expected_exit**: RED nonzero assertion failure; GREEN 0.
- **RED target failure**: ORACLE-P4-BROKER assertion fails for the named negative path; nonzero is expected.
- RED evidence: 2026-09-22 在隔离 worktree /Users/Hugh/Hugh/Project/3rd-review-card05-broker-red（branch codex/card05-broker-red，HEAD a96f28b）执行 node --test test/broker.test.mjs test/managed-session-lifecycle.test.mjs，exit_code=1；49 项中 48 pass，唯一失败为 test/broker.test.mjs:80 的 Missing expected rejection：缺 review_flow 仍放行到 provider。测试收集/环境正常，目标 RED。原始命令/exit/stdout/stderr/冻结测试源已分别发布到 CARD-05 quality/evidence/t008-red/，其中 stdout ref 为 quality/evidence/t008-red/3c589d4ce288fe2e3e8acb8dbb9e4efaaa684fb5ff553ac130d9042fdc212417.json（record sha256 301e743d376153716906f669f5631c8d6a50272461f864352695138ff1620e88）。该 worktree 基于干净 HEAD，尚未纳入第二仓主 checkout 的 13 个 dirty 文件；后续 GREEN 前必须认证并有界协调重叠 WIP。
- **GREEN oracle**: ORACLE-P4-BROKER; managed status 持续回报真实 provider 状态/进度，健康运行超过旧时限不会停止；明确取消/失联清理与 review_flow 负例可读；WorkflowHub 同一 managed request 持续 poll 并在真实终态消费结果。
- **Evidence**: 第二仓定向测试与本仓 review 原件，实际路径待认证后填写。; `quality/evidence/card05/T008-readback.json`.
- **Coverage limit**: 真 provider 计费/延迟仍由 P5 实验量测。
- **STOP / recovery**: 第二仓主 checkout 的 dirty 改动归属/基线未核实时停止写入；由 P4 owner 建隔离 worktree、核对这些改动后修本 Task，不覆盖用户工作。
- test change request: 2026-09-23 用户明确要求改为健康轮询、取消每路/总墙钟截止；原 P4 中 600,000 ms/1,200,000 ms 相关测试目标保留为历史事实，不再作为当前 oracle。此前候选测试中的硬截止断言须替换为“超时限后仍健康则保持 running，直到真实终态”的同任务 oracle。
- **Done**: AC-REVIEW-005, AC-REVIEW-006 跨仓切片、负例及接收方读回。

## L2 — Removable reference

旧路径见 decision-log P5/P10/P12 取证；第二仓 worktree 与 test gate 冻结后可删除此提示。
