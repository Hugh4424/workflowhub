# Phase P3 — 三个代码面隔离候选 OCR 委托

- **Global spec**: `spec.md`
- **Write set**: `runtime/review/stage-materials.json`; `runtime/review/schemas/stage-materials.schema.json`; `runtime/review/ocr-delegation-adapter.mjs`; `skills/wh-review/scripts/review-materials.mjs`; `tests/contract/review-materials-contract.test.mjs`; `tests/contract/ocr-delegation-route.test.mjs`; `skills/wh-review/scripts/__tests__/simple-contracts.test.mjs`
- **Dependency**: `P1、P2`
- **Consumer**: build-code/phase、build-code/integration、verify-code
- **gate_cmd**: `npx vitest run tests/contract/review-materials-contract.test.mjs tests/contract/ocr-delegation-route.test.mjs`
- **oracle**: ORACLE-P3-CANDIDATE phase contract structure and readback.
- **evidence_path**: `quality/evidence/card05/P3-structure.json`
- **STOP**: 候选 packet 缺全文 AC、真实 diff 或独立执行者边界时停止，不切生产三面。
- **Done**: 三代码面候选 packet、独立执行者事实和失败路径可回读，生产默认路由仍未提前切换。

## L0 — Outcome and delta

三个指定面只在显式隔离候选中读取真实 diff，并通过 OCR 的确定性文件筛选/规则解析交给独立执行者审查。OCR 不生成 finding，WorkflowHub 负责 schema 与事实。本 Phase 不拥有生产 review workflow；build-code 的普通 production no-go/unknown 路由由 P5 接到 Architect-Code-Review。未有 go 前不把 OCR 接入生产。

## L1 — Executable contract

- **Inputs and outputs**: P1 合同、真实 git diff、全文 AC、可读仓库、config initial[] → OCR packet、逐路执行者发现、P2 result。
- **FR / AC**: FR-REVIEW-003, FR-REVIEW-004, FR-REVIEW-005, FR-REVIEW-008, FR-REVIEW-011, FR-REVIEW-012, FR-REVIEW-013；AC-REVIEW-003, AC-REVIEW-004, AC-REVIEW-005, AC-REVIEW-008, AC-REVIEW-011, AC-REVIEW-012, AC-REVIEW-013；D-002/D-004/D-006/D-023/D-032–D-035。P21 CLI 修复移至 P5/T010。
- **NEW**: runtime/review/ocr-delegation-adapter.mjs；只有在 P1 证明其真实 consumer、owner、替代关系和删除条件后创建。
- **MODIFY**: 本 Phase write set 的 stage-materials、review-materials、隔离候选 adapter 与定向测试；两个 workflow 是既有 consumer，本 Phase 只读，路由和所有权由 P5 接管；生产 CLI 与 run argv 入口归 P5。
- **DO NOT TOUCH**: config.json、P2 review-record-route.mjs、P4 broker 文件、CARD-06 物理删除面。
- **Deletion proof**: N/A — 本 Phase 只新增隔离候选适配入口，不物理删除 wh-review；旧原件只读保留，不作为 fallback。
- **Task order**: P2/T004 在真实代码 diff 后先采旧路基线并冻结 ref/hash → T005 packet/接口 → T006 三面隔离候选与真实委托；两个 workflow 只作现有 consumer/read-only 输入，普通生产路由归 P5。
- **Test strategy**: feature；backend-testing；正常 diff、AC 全文、unsupported_ext 负例、verify-code AC 禁止冲突、执行者隔离与 prompt 违约。
- **coverage limit**: fixture 可证明输入与接线，不能证明真实 LLM 的审查质量或耗时；P5 用实验原件证明。
- **STOP**: 若 review 入口无可调用 OCR adapter seam，先在 P1 明确跨 Phase 单 owner，不把同一文件偷列进 P2/P3；不能拿虚构 adapter 名称当已验证设计。
- **Done**: 三面候选各有真实 diff packet、独立 executor 事实及错误路径；生产三面和其余七面未被切换。SD-07 多 Phase 时序、独立终末审查与 SD-08 替代路径的验收仍归 P5/T011。
- **Risk and rollback**: OCR CLI 或 host-subagent 不可用时如实记 unavailable，并按 SD-08 恰好尝试一次未参与实现者的独立替代；替代也不可用记 unverified。候选撤回不改已发布 findings。

### T005 — 构造可审需求与 diff 的委托 packet

- **Source / FR / AC**: CARD-05 / D-002/D-019/D-023/D-032–D-034/D-041，requirements 53/54；FR-REVIEW-004, FR-REVIEW-008；AC-REVIEW-004, AC-REVIEW-008。AC 必须全文，diff 不得因扩展名被排除。
- **Inputs**: P1 六要素、spec Appendix A、真实 git diff、现有 stage-materials schema、OCR rule.json 规则。
- **Files / symbols**: `runtime/review/stage-materials.json`; `runtime/review/schemas/stage-materials.schema.json`; `runtime/review/ocr-delegation-adapter.mjs`; symbols: packet projection and profile schema.
- **Workflow consumer boundary**: `workflows/build-code/SKILL.md` 与 `workflows/verify-code/SKILL.md` 是既有 consumer，本 T005 只读，不纳入 P3 write set；no-go/unknown production routing 归 P5。
- **Action**: 预置三面 OCR profile 的真实 diff 合同，仅供 `candidate_experiment: true` 的隔离候选请求选择；普通生产 request 的默认 profile 保持原样，P5 go 后才可选用此 OCR profile。把 AC 全文与代码/相关源码纳入候选 packet；diff 用受支持扩展名+include 或 git commit，单设规则；派发前校验禁止 Agent/子代理工具及等待/轮询两条提示词约束。
- **Outputs / failure**: packet manifest 列出每个输入与排除原因；verify-code 新 OCR 合同按 D-041 允许全文 AC，缺失或不完整 AC 时将该 packet 记为 `MATERIAL_INCOMPLETE`/unavailable，并保留 `cannot_review_requirement_fidelity` 作为具体原因，不派发后再声称该维度通过。
- **Boundary / DO NOT TOUCH**: 不修改 OCR 上游、不改现有 config 键、不制造 OCR finding。
- **Dependency**: `P1/T001`; `P2/T004`
- **Test tier / skill**: fullstack；fullstack-slice-testing。
- **Scenario / fixture or service**: 一份含 AC、.diff 输入与相关源码的 fixture；负例缺 AC、unsupported_ext、缺提示词约束。
- Prewritten test: review-materials-contract.test.mjs 新增 ORACLE-P3-PACKET；执行目标 RED 后冻结断言。旧 simple-contracts.test.mjs 只补 adapter 输入契约。
- Observable seam: spec/AC + git diff → packet materializer → OCR included_files → 独立 executor；被排除项必须可回读。
- **RED/GREEN gate_cmd**: `npx vitest run tests/contract/review-materials-contract.test.mjs。`
- **expected_exit**: RED nonzero assertion failure; GREEN 0.
- **RED target failure**: ORACLE-P3-PACKET assertion fails for the named negative path; nonzero is expected.
- RED evidence: 2026-09-22 在 CARD-05 worktree 执行 npx vitest run tests/contract/review-materials-contract.test.mjs -t ORACLE-P3-PACKET，exit_code=1；2 个目标断言失败、45 skipped。第 1208 行 integration provider packet 缺 P3_DIFF_MARKER；第 1220/1226 行 verify acceptance_criteria 被 MATERIAL_FORBIDDEN。测试正常收集和执行；WebSocket 端口提示不构成目标失败。此为 T005 真 RED，尚待正式质量 writer 入库。；T005 canonical RED manifest quality/evidence/review-baseline-red/9db91dea2d2671a4ab5e3cf1a84c787c0fa05eb6a259109392d77ba36c03e8db.json (record sha256 819627fc8f1f768ff9ae1bb6f69182b1e3462976a18f50e32b6c916df09461fd)
- **GREEN oracle**: ORACLE-P3-PACKET; 三面 packet 有真实 diff 与全文 AC；verify-code 新 OCR 合同允许 AC；缺或截断 AC 使 packet unavailable 且写明需求保真不可审；OCR included_files 含转写 diff。
- **Evidence**: quality/tests/P3-T005-red 与 quality/tests/P3-T005-green 待正式 ref。; `quality/evidence/card05/T005-readback.json`.
- **Coverage limit**: 不证明执行者实际读完全部内容；上下文截断交 P2/P5 质量账本。
- **STOP / recovery**: 新 OCR packet 与旧 wh-review reviewed_execution 的材料合同必须分清；若无法只对新路径放宽 AC，先保留缺 AC 的不可审事实，回 P1 修合同，不静默放宽旧路径。
- test change request: none。
- **Done**: AC-REVIEW-004, AC-REVIEW-008 的 packet 切片与负例有真实 reader 回读。

### T006 — 委托独立执行者并接线隔离候选三面

- **Source / FR / AC**: CARD-05 / U-001#5、D-004/D-006/D-007/D-010/D-023/D-035、SD-07/SD-08、requirement 57；FR-REVIEW-003, FR-REVIEW-011, FR-REVIEW-012, FR-REVIEW-013；AC-REVIEW-003, AC-REVIEW-011, AC-REVIEW-012, AC-REVIEW-013。每路独立上下文、可读仓库，所有成功路入账。
- **Inputs**: T005 packet、P2/T004 候选实跑前旧路代码基线 ref/hash（或真实 unavailable）、P2 result API、现有 config initial[] 和 host-subagent 能力；实验 ref/hash 仅供可比性，不作派发闸门。
- **Files / symbols**: `runtime/review/ocr-delegation-adapter.mjs`; `tests/contract/ocr-delegation-route.test.mjs`; symbols: candidate route seam.
- **Action**: 核对 P2/T004 已先于候选实跑冻结旧路同型基线或真实 unavailable；仅当本卡隔离 review request 明确 `candidate_experiment: true` 时，按 stage+scope 三面注入 OCR adapter 的 runRound。普通 production build-code 路由不归本 Phase，P5 在无 go 时使用 Architect-Code-Review；七保留面保持各自现有入口。一配置路一独立 executor，各自仅读任务 worktree，验证 prompt 禁令，把 findings 按 WorkflowHub schema 送 P2 result。工具不可用时仅一次未参与实现者独立替代并记录来源/缺口；替代也不可用则 unverified 并披露。实验身份、hash、snapshot 缺失只降低可比性，不阻断普通 Architect review。P21 的未知 run 参数由 P5/T010 修复。
- **Outputs / failure**: phase、integration、verify-code 显式候选各有独立 attempt；无子代理能力或 OCR 失败只记录事实，不伪造通过；普通生产路由由 P5 管理，OCR 不默认接入。
- **Boundary / DO NOT TOUCH**: 七保留面继续旧 provider；不重写 P2 聚合逻辑；不改 config.json。
- **Dependency**: `T005`; `P2/T004`
- **Test tier / skill**: fullstack；fullstack-slice-testing。
- **Scenario / fixture or service**: 三面各一份真实 diff；一成功一失败两路；缺 host-subagent 负例；多 Phase 时序 fixture；测试夹具禁止调用真实付费 provider。
- Prewritten test: TCR-P3-001：旧 ORACLE-P3-ROUTE 的 RED/原件只读保留，不沿用作 GREEN。现行 ORACLE-P3-CANDIDATE 只断言 `candidate_experiment: true` 时三面走隔离 OCR；普通 build-code no-go/unknown 路由由 P5 的 cutover contract test 验证，verify-code 一次性流程保持原样；真实 provider 与第七个 build_prd 的独立入口另验。
- Observable seam: 显式隔离候选 → P2 public review record 的 runRound seam → OCR adapter → executor → P2 canonical result；不带候选标志不启用 OCR。生产 no-go/unknown 的 Architect 接线由 P5 验证。
- **RED/GREEN gate_cmd**: `npx vitest run tests/contract/ocr-delegation-route.test.mjs`
- **expected_exit**: RED nonzero assertion failure; GREEN 0.
- **RED target failure**: ORACLE-P3-CANDIDATE assertion fails for the named negative path; nonzero is expected.
- RED evidence: 2026-09-22 在 CARD-05 worktree 执行 `npx vitest run tests/contract/ocr-delegation-route.test.mjs -t ORACLE-P3-CANDIDATE`，exit_code=1，3 个三代码面候选目标失败、15 pass、1 skip；失败均为 OCR spy=0、旧 runner spy=1，候选字段到达 runner，非收集/环境错误。原始输出、命令、exit、测试字节 SHA 在 research/red-evidence/T006-candidate/manifest.json；canonical manifest `quality/evidence/review-baseline-red/6365a5ff80e16da52e075fe75419f0877e9921ce06020ddc70beafc075fe88fa.json`（record sha256 `e25cc254a3f9fe62fa6a842a9a3ae5695425b2f0bf0a080477796a55ccbe2169`）。旧 ORACLE-P3-ROUTE RED manifest `quality/evidence/review-baseline-red/1544529561b0e9de505ca69c95b85fac98cb248edab923eea59fb579a25b9d32.json` 只读保留；P21 旧 RED manifest `quality/evidence/review-baseline-red/326ef7ff8ea5df236ae193372d5a1386b4b33ab64398c0fa3e6f2f6075952361.json` 交 P5/T010 消费。
- TCR-P3-001 disposition: 旧 RED 的原始字节与证据保留，独立合并审查 F-0ffc 证明原 oracle 违反先实验后接入；现行 ORACLE-P3-CANDIDATE 已获真实目标 RED。T006 的新 GREEN 仍待 build-code，不从旧断言推断。
- **GREEN oracle**: ORACLE-P3-CANDIDATE; 显式候选三面到 adapter；候选请求不落回旧 runner；第七面 `non_stage/build_prd` 走独立 report-only 入口，须从该入口另取真实路由/结果原件，不能用本 CLI 的 skipped 测试冒充已验七面；每路独立身份与错误事实。
- **Evidence**: quality/tests/P3-T006-red/green；真实三面 attempt 另存 quality/reviews，refs 待 writer 返回。; `quality/evidence/card05/T006-readback.json`.
- **Coverage limit**: mock 路由不能证明实际 finding 质量；P5 实验负责。`non_stage/build_prd` 不受本 CLI route 测试覆盖，P3 完成前须在其独立 report-only 入口核验仍走旧 provider；若入口不可用，七面结论保持 partial。
- **STOP / recovery**: P2 旧路基线在候选实跑前既未取得也未记真实 unavailable 时，不作可比结论；候选无法隔离时不切生产三面，回 P1 调整接口和写集。任何材料身份/hash/readback 缺失不因此阻止普通审查派发。
- test change request: TCR-P3-001，独立审查依据 F-0ffc；旧/新 oracle 与两份 RED 原件如上，禁止删除旧失败事实。
- **Done**: AC-REVIEW-003 的隔离候选三面真实 smoke 与负例各有可回读原件；默认生产三面未切且七面归属可核，`non_stage/build_prd` 独立入口缺读回则七面声明 partial；AC-REVIEW-011, AC-REVIEW-012, AC-REVIEW-013 的完整生产验收与 OCR go/no-go 仍待 P5。

## L2 — Removable reference

OCR 委托一手 packet 配方见 research/RF-18-ocr-delegate-packet-recipe.md；adapter seam 落实后本提示可删。
