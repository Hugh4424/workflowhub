# Arm A — kimi/coding CLI 作为 OCR 委托宿主 Agent 的审查产出

> 调用：`kimi --prompt "Read .ocr-review-prompt.md ..." --output-format stream-json --model kimi-for-coding/kimi-for-coding`
> 原始 stream-json：`/tmp/ocr-e2e-kimi2.jsonl`（42 行）；退出码 0。
> 第一次尝试（提示词未禁止派子代理/等待）：41 次调用 → 派 3 子代理 + WaitFor×600s×3 → 900s 被超时杀掉，**0 findings**。

---

我将按提示要求亲自读取所有待审文件，然后直接输出最终 JSON 审查结果。先并行读取小文件和大文件的前半部分。

小文件已读完。现在分页读完大文件 review-record-route.mjs 的剩余部分。

我已读完全部六个文件（review-record-route.mjs 2159 行全覆盖）。现在按要求对快照 `/tmp/ocr-e2e-context/` 核实关键 import/数据声明。

标记全部在 tasks.md 中找到。再核实 `simple-review-runner.mjs` 对 `compactVerifyCodeMaterials` 返回 `.diff` 的消费方式，以及 wh-review-cli-bounds 测试的断言。

核实完成。所有 import/consumer/数据声明均与 `/tmp/ocr-e2e-context` 快照比对过。直接给出最终审查结果。

[{"severity":"high","category":"bug","path":"code/runtime/review/review-record-route.mjs","start_line":2107,"end_line":2114,"content":"recordTaskBoundE2eReviewResult 在为 task-bound E2E 审查合成 canonical provider output 时，把每条 finding 的证据锚点有效性无条件写成 true：evidence_anchor_valid: providerFindings.map(() => true)。这与本文件自己在 prepareSimpleReviewRecord 中建立的约定直接冲突——第 1689 行要求调用方提供的 evidence_anchor_valid 与 findings 逐条对应且可为 false，第 1651-1654 行的注释明确说明「Providers can report a completed lifecycle with a broker error (for example an invalid finding anchor)」，即 completed 且无 error 的 provider 结果仍可能带无效锚点。此处丢弃 result.provider_results[0] 原有的锚点信息并全部伪造为 true，会把锚点无效的 finding 写进不可变的 canonical output/result 记录，构成证据完整性假绿。建议：透传该 provider 结果自带的 evidence_anchor_valid（并校验其长度等于 findings 长度），或在上游先验证全部锚点为真再进入本函数。","evidence":"const providerFindings = (result.findings ?? []).filter((finding) => finding.provider === provider.provider); const outputContent = JSON.stringify({ findings: providerFindings.map(({ provider: _provider, ...finding }) => finding) }); ... const outputRecord = { schema_version: \"wh-review-provider-output.v1\", ..., content: outputContent, content_hash: textHash(outputContent), evidence_anchor_valid: providerFindings.map(() => true) };（review-record-route.mjs:2107-2113）；对照 :1651-1654 注释 \"Providers can report a completed lifecycle with a broker error (for example an invalid finding anchor)\" 与 :1689 对 evidence_anchor_valid 的逐条校验"},{"severity":"medium","category":"bug","path":"code/tests/acceptance/card-02-current.mjs","start_line":157,"end_line":171,"content":"produceCard02Current 接受 cwd 参数并用它作为 spawnSync 的工作目录（第 160 行 { cwd, ... }），但第 171 行回读任务材料时却用 readFileSync(\"specs/workflowhub-thin-core-card-02-20260919/tasks.md\") 相对进程当前目录读取，没有把材料路径解析到传入的 cwd 下。一旦调用方以非仓库根目录的 cwd 运行（该参数显然为此存在），子命令在 cwd 下执行而材料从 process.cwd() 读——轻则读到错误任务的 tasks.md 导致 materialMarkers 判定失真，直接 ENOENT 崩溃。建议改为 readFileSync(resolve(cwd, \"specs/workflowhub-thin-core-card-02-20260919/tasks.md\"), \"utf8\")。","evidence":"export function produceCard02Current({ run = spawnSync, cwd = process.cwd(), taskMaterials = null } = {}) { ... const result = run(command, args, { cwd, encoding: \"utf8\", timeout, ... });（:157-160） vs const materials = taskMaterials ?? readFileSync(\"specs/workflowhub-thin-core-card-02-20260919/tasks.md\", \"utf8\");（:171）——readFileSync 未使用 cwd"},{"severity":"low","category":"test","path":"code/tests/acceptance/card-02-current.mjs","start_line":39,"end_line":64,"content":"NON_ACHIEVED_READBACKS 的 materialPresent 判定（第 141 行 taskMaterials.includes(marker)）是对整份 tasks.md 做子串匹配，若干 marker 的区分度趋近于零：\"deferred\"、\"CARD-10\"、\"CARD-05\" 在 tasks.md 中大量出现（如全 AC 枚举行与 coverage limits 行），\"AC-TEST-004\"/\"AC-REVIEW-001\" 也出现在与本 AC 无关的条目里。这意味着即便该 AC 的延期/不可用事实被改写或删除，只要 tasks.md 任意位置仍出现这些常见词，readback 依旧判 present。虽然外层还有命名断言与 commandsPassed 把关（故评 low），但作为「current task-material boundary」证据，其证伪能力很弱。建议把 marker 收紧为与该 AC 专属的四要素文本（owner/trigger/reason_or_limit/stop_condition 的具体取值），或校验 marker 出现在该 AC 对应的条目块内。","evidence":"materialMarkers: [\"AC-TEST-004\", \"real Luna E2E\", \"CARD-10\"]（:44）、[\"AC-REVIEW-003\", \"CARD-05\", \"deferred\"]（:62）；const materialPresent = readback.materialMarkers.every((marker) => taskMaterials.includes(marker));（:141）；grep 快照 tasks.md：\"deferred\" 出现 20+ 处（如 :646 coverage limits），\"AC-TEST-004\" 亦出现在 :626 的全 AC 枚举行——marker 与该 AC 的延期事实无定位绑定"}]

逐文件覆盖情况：（1）card-02-current.mjs 全读（183/183）；review-input-bounds.mjs 两个副本均全读（25/25，逐字一致）并与快照中的两个 consumer（review-packet-identity.mjs:103-117、simple-review-runner.mjs:672-673/1316-1318）及 wh-review-cli-bounds 测试断言核对；task-store.mjs 全读（420/420），其 import（SHA256_HEX、STAGE_ROW_FINDING_DISPOSITION_FIELDS、summarizeStageRowFindingDispositions）已在快照中确认存在；review-record-route.mjs 全读（2159/2159 分四段），16 个外部 import 全部在快照中核实存在且签名匹配；card-02-spec-head.md 全读（80/80）。（2）无法判断的项：各命名断言指向的 vitest/node 测试文件本身不在包内，「测试是否真会失败」只能核验到 stdout 解析逻辑与命令清单的自洽，无法核验被引用测试的断言强度；review-input-bounds 两副本的逐字节一致有快照中的 portability 测试守护，但该测试的 HEAD 版本未见，只能确认快照版本与包内实现一致；「public command 登记」类治理要求无法从包内证据判定。（3）OCR 产物足够：文件清单+自定义规则覆盖了本次审查所需的筛选与标准；缺的是被引用测试文件的 HEAD 内容（见上），如有 diff/card-02-selected.diff 的内容（因 unsupported_ext 被排除）可进一步核对实现与改动的对应关系，但非完成审查的硬缺口。需求保真、边界情况、测试有效性三类均有实质发现如上；与现有代码交互类经快照核对未发现实质问题（所有新 export 的依赖均真实存在，无 __all__ 登记缺失的证据）。