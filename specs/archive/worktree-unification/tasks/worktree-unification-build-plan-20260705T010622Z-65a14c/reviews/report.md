# 审查报告 — worktree-unification-build-plan-20260705T010622Z-65a14c (round 4)

- verdict: escalate_to_human
- provenance: single-context

## Summary

Round 4 review package still leaves all five round-3 blocking findings open: forbidden-file conflict, stage-result filename conflict, specs/task_tracking_root boundary conflict, incomplete FR-WORKTREE-COMMIT-004 coverage, and missing objective verification gates. These same blockers have persisted across consecutive review rounds; under FR-REV-001 / same-finding escalation rules, this review escalates to human rather than issuing another revise_required. Required skills were executed read-only via SKILL.md fallback and independent subagents; no artifacts were modified.

## Findings

- [blocking] 位置: specs/worktree-unification/tasks.md:33 | 问题: T005 still permits minimal edits to workflows/build-spec/SKILL.md and workflows/build-plan/SKILL.md when required logic is missing, while plan.md marks workflows/build-spec/SKILL.md and workflows/build-plan/SKILL.md as forbidden/read-only and limits scope to four other files. This is the same blocker from round 3 and remains unresolved in round 4. Real-world consequence: an implementer can either violate the forbidden-file boundary or skip FR-WORKTREE-SCOPE-008 coverage, so the plan cannot be executed deterministically. | 建议: Escalate for human decision. Choose one scope rule: either make T005 strictly read-only and fail/open a follow-up task if build-spec/build-plan lack required behavior, or remove the forbidden-file declarations and add those files to allowed scope, tasks, impact coverage, and verification mapping.
- [blocking] 位置: specs/worktree-unification/tasks.md:35 | 问题: stage-result filename contract still conflicts. T006 requires stage-result-verify-code.json, while spec.md still requires {{task_tracking_root}}/tasks/{task-id}/stage-result.json in the close contract, and plan.md mixes generic stage-result, stage-result.json, and stage-result-verify-code.json. This is the same blocker from round 3 and remains unresolved in round 4. Real-world consequence: close may write one file while verification reads another, making verdict, needs_human, review_status, and failure-step evidence untrustworthy. | 建议: Escalate for human decision. Select exactly one authoritative filename, then update spec.md, plan.md, tasks.md, verify-code FR-PATH references, and every acceptance check to that same path.
- [blocking] 位置: specs/worktree-unification/plan.md:56 | 问题: repo specs/ boundary still contradicts the current package. plan.md lists research.md and data-contracts.md under specs/worktree-unification, and the actual directory also contains baseline-report.md, constitution-check.md, cross-artifact-analysis.md, human-brief.md, plan-summary-draft.md, stage-result.json, and checklists/requirements.md. But spec.md FR-WORKTREE-SCOPE-009 says specs/{task-id}/ only allows spec.md, plan.md, tasks.md. T006 only checks evidence/stage-result-like files, not all non-allowed files. This is the same blocker from round 3 and remains unresolved in round 4. Real-world consequence: the storage-boundary verification will either fail forever on existing planned artifacts or pass falsely by checking only a subset. | 建议: Escalate for human decision. Either broaden FR-WORKTREE-SCOPE-009 to explicitly allow build-plan/research/checklist artifacts in repo specs/, or enforce the strict boundary by moving process artifacts to {{task_tracking_root}} and making T006 scan for every file outside spec.md/plan.md/tasks.md.
- [blocking] 位置: specs/worktree-unification/tasks.md:21 | 问题: FR-WORKTREE-COMMIT-004 still promises per-stage/per-phase commit or no-change records for all five pipeline stages, but tasks.md only lands make-decision R7 and close archive behavior. There is no concrete task for build-spec, build-plan, build-code phase commits, verify-code commit/no-change records, or matrix-wide verification. This is the same blocker from round 3 and remains unresolved in round 4. Real-world consequence: stage artifacts can remain uncommitted or untraceable, causing downstream stages/reviews to read stale or incomplete outputs. | 建议: Escalate for human decision. Either add explicit implementation and verification tasks for every row of the FR-WORKTREE-COMMIT-004 coverage matrix, or narrow the FR to only the stages this task actually modifies.
- [blocking] 位置: specs/worktree-unification/tasks.md:13 | 问题: T001-T007 still lack objective verification commands or equivalent machine-checkable gates. The tasks describe behavior but do not provide gate_cmd/display_cmd, test files, fixture setup, expected exit codes, or stable pass/fail checks. This is the same blocker from round 3 and remains unresolved in round 4. Real-world consequence: implementation can produce fake green results based on subjective reading instead of tests or exit-code-preserving checks. | 建议: Escalate for human decision. Add per-task objective gates: parser tests for env/yaml/missing/non-dir/nonexistent cases; grep/script checks for required SKILL.md clauses and removed fallback; git worktree list before/after checks; specs allowlist scan; canonical stage-result path check; and non-piped gate commands with meaningful exit codes.
- [minor] 位置: specs/worktree-unification/tasks.md:37 | 问题: Late finding: T007 references spec §7 验收标准 1-9, but current spec §7 is Out of Scope and success criteria are in spec §5 with four items. Because this ambiguity was detectable in earlier rounds, it is recorded as minor under the late-finding rule, not a new blocker. | 建议: Define one acceptance-source anchor, preferably explicit AC-01..AC-09 IDs, then update plan.md and tasks.md references to use those IDs instead of spec §7.
- [minor] 位置: specs/worktree-unification/cross-artifact-analysis.md:119 | 问题: Late finding: cross-artifact-analysis.md reports blocking=0 and 达标, but current artifacts still contain all five repeated blockers above. Because this false-green evidence was detectable in earlier rounds, it is recorded as minor rather than a new blocker. | 建议: Regenerate or revise cross-artifact-analysis.md after resolving the repeated blockers. Until then, do not use it as pass evidence.

## Checks

审查维度覆盖：方向、盲点、细节
- 维度[方向]：已覆盖
- 维度[盲点]：已覆盖
- 维度[细节]：已覆盖

## Required Revisions

降级理由：(未提供，需补充)
- 必须修复：T005 still permits minimal edits to workflows/build-spec/SKILL.md and workflows/build-plan/SKILL.md when required logic is missing, while plan.md marks workflows/build-spec/SKILL.md and workflows/build-plan/SKILL.md as forbidden/read-only and limits scope to four other files. This is the same blocker from round 3 and remains unresolved in round 4. Real-world consequence: an implementer can either violate the forbidden-file boundary or skip FR-WORKTREE-SCOPE-008 coverage, so the plan cannot be executed deterministically.
- 必须修复：stage-result filename contract still conflicts. T006 requires stage-result-verify-code.json, while spec.md still requires {{task_tracking_root}}/tasks/{task-id}/stage-result.json in the close contract, and plan.md mixes generic stage-result, stage-result.json, and stage-result-verify-code.json. This is the same blocker from round 3 and remains unresolved in round 4. Real-world consequence: close may write one file while verification reads another, making verdict, needs_human, review_status, and failure-step evidence untrustworthy.
- 必须修复：repo specs/ boundary still contradicts the current package. plan.md lists research.md and data-contracts.md under specs/worktree-unification, and the actual directory also contains baseline-report.md, constitution-check.md, cross-artifact-analysis.md, human-brief.md, plan-summary-draft.md, stage-result.json, and checklists/requirements.md. But spec.md FR-WORKTREE-SCOPE-009 says specs/{task-id}/ only allows spec.md, plan.md, tasks.md. T006 only checks evidence/stage-result-like files, not all non-allowed files. This is the same blocker from round 3 and remains unresolved in round 4. Real-world consequence: the storage-boundary verification will either fail forever on existing planned artifacts or pass falsely by checking only a subset.
- 必须修复：FR-WORKTREE-COMMIT-004 still promises per-stage/per-phase commit or no-change records for all five pipeline stages, but tasks.md only lands make-decision R7 and close archive behavior. There is no concrete task for build-spec, build-plan, build-code phase commits, verify-code commit/no-change records, or matrix-wide verification. This is the same blocker from round 3 and remains unresolved in round 4. Real-world consequence: stage artifacts can remain uncommitted or untraceable, causing downstream stages/reviews to read stale or incomplete outputs.
- 必须修复：T001-T007 still lack objective verification commands or equivalent machine-checkable gates. The tasks describe behavior but do not provide gate_cmd/display_cmd, test files, fixture setup, expected exit codes, or stable pass/fail checks. This is the same blocker from round 3 and remains unresolved in round 4. Real-world consequence: implementation can produce fake green results based on subjective reading instead of tests or exit-code-preserving checks.

