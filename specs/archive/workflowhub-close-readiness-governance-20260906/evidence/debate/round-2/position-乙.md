# 乙队立场：25 findings 需改方向卡

组A 锁定：blocking(L33) 把红线条款（守卫形态、索引载体）延期 build-spec，恰违"不得依赖 build-spec 补需求"；L39 载体未定即无合规实现；L25 负向 oracle 缺分类元数据，无法判型。甲"延期无妨"=纵容跳阶段，DEFERRED-005 正是红线悬空。→守卫=只读事实记录；索引=内嵌导航节、非持久；oracle 用语法规则或机器标签。

组B 注入：L40×2"摘要+文件引用"赖子代理读文件，异源审查禁读仓库→引用变盲区；瘦身无样本/基线/回退触发，RISK-006 落空。甲"瘦身不影响异源"不成立：二者同写一行。→异源注入自包含切片/全文；全量对照+关键 finding 零遗漏后再切换。

组C gate：L33×3"步骤级或收口条件"即新 gate，违"记录事实不阻断"；unavailable 未定→故障即阻塞或不可判胜；"异源"需身份比对，与"不做模型绑定"冲突。甲"动作非结果"与原文相抵。→三态 executed/failed/unavailable，仅记录不阻断；异源改声明式来源字段。

组D 可判性：L65×2"历史不再出现"与"不改历史"互斥、不可证伪；L67×2 无基线且省/派发无法对账；L53 六状态外置不在 manifest；L26×2"四件套"名三实三、漏 unknown+零 digest；L46 索引时序倒置；L3 新需求无审查覆盖；L64 Q1/Q3 无交付物。→标准限本任务 dogfood；字符数/重读/派发定基线阈值；状态矩阵入卡；校验补 digest/绑定/unknown；索引按阶段前置；新需求补审查；Q1/Q3 加可判交付物。

组E 范围：任务确扩 2 倍，但机制皆用户三问硬要求；错不在大，在 A~D 未锁——T-020 次序对，锁定并入即分批可交付。

组F 索引：省上下文未经验证，read 按 offset 读取是前提；本轮增导航节+offset 对照，不可行回退分段读取。

自述：最弱=以规范"锁死"对抗"延期锁死"，同样放大方向卡；每条 major 仅一句，证据链薄，易被抓"为字数牺牲可判性"。
