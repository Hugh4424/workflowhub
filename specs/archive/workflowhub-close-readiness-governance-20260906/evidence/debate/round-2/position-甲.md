# 甲队立场书（辩护方向卡 v4 + D-301~D-306）

总纲：方向已由 Talk Q1~Q8 确认；卡锁语义，细节归 build-spec 属"只细化"，非补需求。

## 组A 方向期锁定之争：反驳
L25 负向 oracle 已锁字段级要求，L39 锁索引"非权威/可再生/不持久"，L33 守卫注明构建期验证——锁语义不锁形态，不违宪。微调：补 #64"可证伪+行为变更"分类原则。

## 组B 审查注入形态：部分接受
L40 注入包须自包含（审查环境禁读仓库）：关键段为主、摘要为辅；RISK-006 留全量回退，不削弱异源隔离。

## 组C 独立审查 gate 边界：部分接受
发起审查=动作非结果条件（L33），事实不阻断合宪；但守卫若成步骤完成条件确有 gate 嫌疑，微调：默认事实记录型、unavailable 如实记录、禁止阻断。

## 组D 验收与时序闭合：接受大部
#39 成立——L46 索引生于 build-plan 而成本大头在 build-spec，索引只读可再生，接受提至 build-spec 入口；#67 加"v3 轮次对比"观察项；#26 补第四项；#53 引 decision-log；#25 并入组A。

## 组E 范围与复杂度：反驳
L59 非目标+L73 已围栏（不扩四阶段、不重做红蓝）；任务大但 M5 确认、T-020 分治。

## 组F 索引省上下文之疑：接受验证
read 按 offset 为既有能力；不支持则退为导航节仍省定位轮，build-spec 实测。

## 致命缺陷自述
① L23"锁定"与 L33/L39/L73 三处"build-spec 定义/验证"并存，对手可指负向 oracle 分类、载体形态、守卫形态三大关键规范靠 build-spec 补需求——锁定名实不符。
② L67 成本无基线且不做 token 度量，"轮次下降"不可证；叠时序倒置，II-3 收益对象（build-spec）与生效点（build-plan）错位，收益不可判。
