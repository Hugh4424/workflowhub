# Phase 2 Third-Party Review Report

**Reviewer:** code-reviewer agent
**Date:** 2026-07-01
**Scope:** `.mcp.json`, `skills/anysearch/SKILL.md`, `skills/anysearch/.env.example`, `tests/moat-skills-phase2.test.mjs`

---

## Check Results

### A. `.mcp.json` has `mcpServers.muyu-search-mcp` key

**PASS**

File line 3: `"muyu-search-mcp": {` — the key exists directly under `mcpServers`.

---

### B. `.mcp.json` has NO literal API keys (no values starting with `sk-`, `tvly-`, `fc-`)

**PASS**

All three sensitive env fields use placeholder syntax:
- `"MUYU_API_KEY": "${MUYU_API_KEY}"` (line 10)
- `"TAVILY_API_KEY": "${TAVILY_API_KEY}"` (line 12)
- `"FIRECRAWL_API_KEY": "${FIRECRAWL_API_KEY}"` (line 14)

No `sk-*`, `tvly-*`, or `fc-*` literal strings found anywhere in the file.

---

### C. `.mcp.json` secret values are env var refs like `${VAR}`

**PASS**

All three API key values use the `${VAR}` pattern. The `args` array also uses `${MUYU_MCP_PATH}` (line 6) rather than a hardcoded path. No non-placeholder secret values present.

---

### D. `skills/anysearch/SKILL.md` frontmatter has non-empty `name` field

**PASS**

Line 2 of SKILL.md: `name: anysearch` — field is present and non-empty. Frontmatter is properly delimited with `---` on lines 1 and 12.

---

### E. `grep -r "/Users/\|/home/" skills/anysearch/` — any hits = fail

**PASS**

Grep over the entire `skills/anysearch/` directory returned zero matches. No hardcoded absolute paths to local user directories found in any file under that skill.

---

### F. Test file exists and has >3 non-trivial assertions

**PASS**

`tests/moat-skills-phase2.test.mjs` exists and contains the following non-trivial assertions (count: 12):

1. Line 45: `assert.match(content, /muyu-search-mcp/, ...)` — MCP key string present
2. Line 48: `assert.ok(config.mcpServers, ...)` — mcpServers object exists
3. Line 50: `assert.ok(server, ...)` — muyu-search-mcp server entry exists
4. Line 51: `assert.deepEqual(server.args, [...])` — args array exact match
5. Line 58: `assert.equal(/"sk-[^"]+"/.test(serialized), false, ...)` — no sk-* key
6. Line 59: `assert.equal(/"tvly-[^"]+"/.test(serialized), false, ...)` — no tvly-* key
7. Line 60: `assert.equal(/"fc-[^"]+"/.test(serialized), false, ...)` — no fc-* key
8. Line 61: `assert.equal(serialized.includes("/Users/"), false, ...)` — no /Users/ path
9. Line 62: `assert.equal(serialized.includes("/home/"), false, ...)` — no /home/ path
10. Line 73-74: `assert.ok(nameLine, ...)` — SKILL.md name frontmatter non-empty
11. Lines 80-81: `assert.equal(content.includes("/Users/"), false, ...)` / `/home/` — no local paths in skill files (loop over all files)
12. Line 87-88: `assert.match(envExample, /^ANYSEARCH_API_KEY=.+/m, ...)` / `MUYU_MCP_PATH` — .env.example placeholders present
13. Lines 95-98: `assert.ok(/^\.env$/m.test(...), ...)` — .env is gitignored

Well above the 3-assertion threshold.

---

## Summary Table

| Check | Status | Notes |
|-------|--------|-------|
| A. `mcpServers.muyu-search-mcp` key present | PASS | Line 3 of .mcp.json |
| B. No literal API keys (sk-/tvly-/fc-) | PASS | All three fields use ${VAR} refs |
| C. Secrets are env var refs `${VAR}` | PASS | Consistent throughout |
| D. SKILL.md frontmatter `name` non-empty | PASS | `name: anysearch` |
| E. No `/Users/` or `/home/` in skill dir | PASS | Zero grep matches |
| F. Test file has >3 non-trivial assertions | PASS | 13 assertions found |

---

## Verdict

**PASS** — All six checks pass with no blocking findings.

### Non-blocking Observations (LOW severity, informational only)

1. `.env.example` line 11 uses `your_api_key_here` as a placeholder value for `ANYSEARCH_API_KEY`. This is intentional per the file's comment header ("Optional but recommended") and poses no security risk since the file is an example only. The test at line 87 accepts any non-empty value after `=`, so this passes.

2. `.mcp.json` `args` array references `${MUYU_MCP_PATH}` which must be set in the runtime environment. If unset, `uvx` will receive a literal `${MUYU_MCP_PATH}` string and fail at runtime. This is an ops/deployment concern, not a code defect — the MCP JSON convention for env var substitution requires the host client to expand these references. No action needed at this layer.

3. The test suite at line 91-98 checks for a `.env` rule in either the root `.gitignore` or `skills/anysearch/.gitignore`. Both paths are acceptable; the test is correctly permissive. No issue.

---

*Report generated by code-reviewer agent. No blocking findings. Safe to proceed to next phase.*
