# Phase P2 — 有序头脑风暴作者合同

- **Global spec**：`spec.md#5-功能需求`、`spec.md#实现设计全局权威`、`spec.md#appendix-a--验收判据唯一权威`。
- **Write set**：`workflows/make-decision/SKILL.md`; `workflows/make-decision/steps.json`; `workflows/make-decision/skill-deps.yaml`; `skills/talk-with-zhipeng/SKILL.md`; `skills/talk-with-zhipeng/skill-bundle.json`; `skills/deep-research/SKILL.md`; `skills/deep-research/skill-bundle.json`; `skills/decision-log/SKILL.md`; `skills/decision-log/templates/decision-log-template.md`; `skills/decision-log/skill-bundle.json`; `skills/wh-review/contracts/make-decision.md`; `tests/decision-log-content-contract.test.mjs`; `tests/interaction-quality-contract.test.mjs`; `tests/stage-interaction-contract.test.mjs`; `tests/workflow-v2-contract.test.mjs`; `tests/contract/stage-order-and-host-interaction.test.mjs`。
- **Dependency**：`P1`；先固定物理材料与 runtime 读回，作者技能才可引用新的 post 交接。
- **Consumer**：make-decision 主会话、portable skill loader、decision-log writer、P3 真实切片。

## L0 — Outcome and delta

把固定 Round/旧顺序改成“最小入口 → 可证伪大纲 → 内外研究与按角度发散 → 异源方向挑战 → 用户定大纲 → 动态逐模块收敛 → 逐字保真与确认”。Talk 根据真实未决项回边，不靠固定轮次；只升级重要、方向和无法合理消解的模糊问题，普通项由 agent 记录并披露最小合理假设。P2 建立作者、步骤和测试合同；P3 用正式入口验证运行时是否按合同执行。

## L1 — Executable contract

- **Inputs and outputs**：输入为用户逐字原话、当前 OI、可证伪大纲、带来源/强度的研究和候选、真实 review 配置；输出为当前 `decision-log.md` 中的来源绑定、模块状态、append-only 裁决，以及用户可见的候选与问题卡。review 只挑战不选方向；不可用保留 provider/transport/error。
- **FR / AC**：FR-33～FR-45 / AC-33～AC-45；下方逐卡给出来源、具体动作、正负 oracle。编号出现本身不算覆盖。
- **NEW**：N/A — 复用现有 workflow、skills 和 tests。
- **MODIFY**：仅头部 Write set；每卡再限定子集。skill 内容稳定后重算相应 bundle hash；不以目录或通配符扩大写集。
- **DO NOT TOUCH**：P1 材料/runtime owner、provider 配置、CARD-03/04 代码、历史 review/evidence。各卡先写的目标 RED 断言在配对 GREEN 时冻结；合法变更要记录原因、原失败和新失败并交独立审查，不靠放宽测试取得 GREEN。
- **Task order**：T007 → T008 → T009 → T010 → T011 → T012；全 task 的 T ID 唯一，不在 P2 从 T001 重启。T007/T008 的测试设计可在 OI-016 冻结前开展；共享作者文件的生产编辑串行，避免覆盖。
- **Testing tier / skill**：feature / backend-testing；每卡指定目标命令、正常/负例与覆盖限制。
- **Scenario and fixture/service**：模糊需求、研究不可用/全文 >500 字、无新方向、5 假设证伪 3 条、零用户问题、第二轮缺替代、迟到答复、blind 输入携带 `current_selection`；均先用确定性 fixture，不假称在线 provider 已执行。
- **gate_cmd**：`npx --no-install vitest run tests/decision-log-content-contract.test.mjs tests/interaction-quality-contract.test.mjs tests/stage-interaction-contract.test.mjs tests/workflow-v2-contract.test.mjs tests/contract/stage-order-and-host-interaction.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`。
- **expected_exit**：各卡新目标断言 RED nonzero、同卡命令 GREEN 0；收集或环境失败不算 RED。
- **oracle**：ORACLE-CARD07-P2-AGG；六卡正负行为均可定位，不用关键词出现代替执行观察。
- **evidence_path**：计划 `quality/tests/card07-p2-aggregate.json`；实际命令/输出/退出码只进入 task facts/quality evidence。
- **coverage limit**：作者合同测试不能证明 runtime publication、在线 provider、CARD-04 接口冻结或 P3 的正式用户交互。
- **STOP**：OI-016 未冻结而实施要触碰 CARD-04/03/05 共享文件或符号；或者需要新 stage/skill/control plane、改 provider 配置、绕过 questions-only/reveal、恢复 aggregate 时，停止受影响编辑并交回对应 owner。未受影响的来源核对与测试设计可继续。
- **Done**：六卡的目标 RED 与同命令 GREEN 原件、bundle/catalog 读回、原始来源和正负 oracle 双向定位齐全；P3 正式入口另行验收，本文件不预写已完成状态。
- **Risk and rollback**：取消固定轮次可能误删真实确认；只回滚本 Phase 作者文件与测试变更，不回滚用户原话、decision 或 review 原件。

### Shared-write preflight — OI-016

在声称 build-plan 被接受**之前**（且早于任何 T007–T012 共享生产编辑），对照 `decision-log.md#oi-016`、CARD-04 当前规划与认证工作区，逐文件/符号列出本 Phase 与 CARD-04/03/05 写集交集、唯一 owner、先后顺序、冻结接口版本和集成责任；按 OI-016 的 `requires_user_decision=true` 留下实际确认，冻结证据引用进现有 decision/task facts/quality，不新建材料。OI-016 当前为 `deferred`，此处不宣称已冻结。交集不明时，`workflows/make-decision/*`、`skills/talk-with-zhipeng/*`、`skills/decision-log/*` 等共享编辑 STOP，build-plan 质量结论保持 `incomplete`；只读核对与测试设计可继续。此协调前置沿用已存在的 OI，不是新的 stage 或 gate。

### Target-RED seam preflight — AC-33/34/36/38/42

`spec.md#行为验收的可观察合同` 是本 Phase 的先决接口设计，不是已存在的 runtime 能力。T007/T008/T010/T012 和 P3/T013 首先要分别冻结字段、writer、reader、缺失语义、原始分母及读回路径，再在真正读取这些产物的测试中预写目标 RED。现有 `research-report.v1`、问题卡 validator、OI 终态检查各只能证明局部合同；它们现在全绿不证明上述完整 AC，更不能作为目标 RED。若 OI-016 或 reader 未就绪，受影响卡保持 `incomplete`，先做不越界的只读设计/fixture 准备；不得用文案关键词、虚构字段或环境错误充数。正式 build-plan 的预写 RED 和 build-code 的 GREEN 分属不同事实，实际输出只能存 task facts/quality。

### T007 — 最小入口、真实发散与可证伪大纲

- **Source / FR / AC**: U-002-01/02/05/22、D-009/D-011/D-013/D-025 → FR-33、FR-39、FR-42 → AC-33、AC-39、AC-42；首周期候选数必须大于用户原始候选数，origin 差集至少一条真正非用户方向，同义改写不充数；5 假设证伪 3 条须废弃并重画。
- **Inputs**: 仅模糊需求和痛点、逐字用户候选、当前 OI/outline_version；研究缺席时可用内部角度但标来源/强度；共享编辑前需 OI-016 冻结证据。
- **Files / symbols**: `workflows/make-decision/SKILL.md` 的 `Procedure`；`workflows/make-decision/steps.json` 的 `triage-scope`；`skills/decision-log/SKILL.md` 的 `Divergence and falsifiable outline`、`skills/decision-log/templates/decision-log-template.md` 的 `发散候选与可证伪大纲`；`tests/interaction-quality-contract.test.mjs`、`tests/stage-interaction-contract.test.mjs`。symbol: N/A — Markdown/workflow anchors. P1/T003 提供的 runtime reader/status 接口仅为只读依赖，不在 P2 写集内。
- **Action**: 先加只给两字段仍启动、2 原候选→≥3 且新方向差集非空、同义扩写被拒、5 假设/3 反证触发新版本的断言并捕获目标 RED；再在同一 `decision-log.md` 定义角度表先于填候选、用户逐字 intake/source、原候选回映、`origin=user|internal|research`、angle/source/strength、与原候选的 `novelty_against`/改变维度、假设的 falsifier/evidence、`redraw_of`/原因和 current OI version。纯 reader 只产出 `incomplete` diagnosis/root cause，不新增 gate/store。可证伪问题只判大纲，不限缩研究；派生不冒充原话。
- **Outputs / failure**: 候选、origin 差集、被推翻假设与新版大纲可回读；候选不足、缺用户逐字来源、删原候选、任意 origin、无 angle、只改文本、反证无依据、过半失败仍用旧版、重画未绑当前 OI version，均明确 incomplete/再发散。
- **Dependency**: P1/T006 与 OI-016 冻结；本卡无并行共享写入。
- **Test tier / skill**: feature / backend-testing；目标断言而非文案关键词。
- **Scenario / fixture or service**: 用户两条原候选、两角度生成一条新方向；负例仅同义扩写；另用 5 假设/3 反证；无外部服务。
- **Prewritten test**: T007 作者在 `tests/interaction-quality-contract.test.mjs` 预写两原候选→至少一条独立 origin 新方向、同义改写不算新增、五假设三反证必须新版本的命名断言；在 `tests/stage-interaction-contract.test.mjs` 锁两字段最小入口。须先核实候选/大纲真实 writer 与 reader，缺的断言待写、待验证。
- **Observable seam**: producer=用户逐字候选、角度表与当前 OI/outline writer；artifact=带 origin/source/strength 的候选集合和 outline 版本/反证；reader=现有 interaction/decision-log 读者；consumer=用户大纲与后续 Talk。分母是用户原候选和五条原假设；缺 origin 或无法区分同义方向时为 incomplete，不能字符串不同即判新方向。
- **RED/GREEN gate_cmd**: `npx --no-install vitest run tests/interaction-quality-contract.test.mjs tests/stage-interaction-contract.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`；同一命令先 RED 后 GREEN。
- **expected_exit**: RED nonzero（新增目标断言）；GREEN 0。
- **RED target failure**: ORACLE-CARD07-P2-T007：旧入口要求多字段、同义扩写算新方向或 3/5 反证只修旧版时，新增目标断言非零；setup 失败不算。
- **GREEN oracle**: ORACLE-CARD07-P2-T007；正常入口继续且满足 AC-33 两条件，同义负例拒绝，旧版废弃并列反证。
- **Evidence**: 计划 `quality/tests/card07-p2-t007-red.json`、`quality/tests/card07-p2-t007-green.json`；实际状态由 build-code task facts 发布。
- **RED evidence**: 尚无 canonical raw output ref/当前 task 身份；本轮未保留 T007 的命名 RED 原始输出，不能把事后 GREEN 或其他 T008 reader 的历史失败伪称为本卡 RED 证据。
- **GREEN evidence（本轮实现，非正式 stage 完成）**：`tests/interaction-quality-contract.test.mjs`、`tests/contract/status-derivation.test.mjs`、`tests/decision-log-content-contract.test.mjs`、`tests/stage-interaction-contract.test.mjs`、`tests/skill-provenance-strict.test.mjs`、`tests/contract/acceptance-single-writer-empty-values.test.mjs` exit 0（68 项）。覆盖两字段 user-verbatim intake、两原候选加一非用户语义方向、原候选不可删除/改写、角度先行绑定、只改文本拒绝、3/5 反证废弃 r0 并以 `redraw_of` r1 替代、falsifier/evidence 与 OI version 漂移 root cause、官方 make-decision handler 读回及无 aggregate/outline 当前运行；真人 Talk、外部研究和 canonical stage fact 仍未执行。
- **STOP / recovery**: OI-016 未冻结或候选 origin 无逐字来源时停共享编辑，回 source/OI owner 补证；不拿字符串不等同冒充语义新方向。
- **Coverage limit**: 作者测试不证明真实研究、真人讨论或正式 stage 运行。
- **Boundary / DO NOT TOUCH**: 本卡 RED 断言在 GREEN 时冻结；不改 P1 runtime、CARD-04 或 provider 配置。
- **test change request**: none；冻结后需改时登记理由、旧/新 assertion、原 RED ref 与独立审查 ref。
- **Done**: AC-33/39/42 正负断言与来源映射可回读；未执行前为 planned。

### T008 — 外部与内部研究完整到达

- **Source / FR / AC**: U-002-03/04/22、D-015 → FR-34 → AC-34；研究是扩散来源，不为可证伪问题填空；每条用户可见候选有出处、完整材料路径和推荐/不推荐理由。
- **Inputs**: T007 的角度/候选、真实 `research-report.v1` 或其 unavailable 事实；`skills/deep-research/SKILL.md` R0–R5 和现有 `deep-research` 依赖声明。
- **Files / symbols**: `skills/deep-research/SKILL.md` 的 `R0 缺口问题`/`R4 落盘`/`消费与所有权`，`skills/deep-research/skill-bundle.json`，`skills/decision-log/SKILL.md` 的 `调研候选交付`，`skills/decision-log/skill-bundle.json`，`workflows/make-decision/SKILL.md` 的研究/用户呈现段，`workflows/make-decision/steps.json` 的 `research-inputs`，以及 `tests/interaction-quality-contract.test.mjs`。symbol: N/A — Markdown/workflow and bundle anchors. P1/T003 提供的 research report schema、writer、reader 与 status 接口仅为只读依赖，不在 P2 写集内。
- **Action**: 先写 >500 字全文、逐条候选出处/路径、缺路径与 unavailable 负例并取目标 RED；再把范围改为外部方案/最佳实践/新方向，与内部角度并列。每个 completed report 明示候选集（无候选写 `[]`）；每条候选有大白话摘要、推荐/不推荐和理由、source refs，且以具名 evidence ref 反向绑定该 candidate，禁止借另一候选证据。用户在同一 `decision-log.md` 收到表格（推荐/不推荐和理由）+ 全文 ref/path + 按需展开；同材料 JSON binding 逐字段绑定表格和报告。缺表、错绑、缺声明都以 status root cause 保留，不新建 store/gate。
- **Outputs / failure**: `research-report.v1` 原件留原证据区；`decision-log.md` 是唯一用户可见摘要和表格。缺出处/路径、候选集未声明、证据属于其他候选、表格/绑定缺失、仅截断摘要或 unavailable 写成空成功均失败。
- **Dependency**: T007；OI-016 冻结后编辑共享 workflow；因文件重叠，后续卡串行。
- **Test tier / skill**: feature / backend-testing。
- **Scenario / fixture or service**: 仓内 >500 字全文、两候选不同出处、缺路径与 unavailable 负例；无 live search/provider。
- **Prewritten test**: `tests/interaction-quality-contract.test.mjs` 现有 `AC-34: two researched candidates are not delivered by a report ref and summary alone` 经 `readResearchReport → deriveResearchStatus` 实读报告，锁“有全文 ref/hash 但无逐候选推荐理由/交付记录仍不得称到达”；>500 字、两候选各有来源。完整路径与 unavailable 的同组断言仍待补，正例须先冻结 candidate delivery 合同，不能伪造 fixture。
- **Observable seam**: producer=`deep-research` 的实际 research writer 与内部角度作者；artifact=`research-report.v1` 全文 ref、候选出处和用户可见摘要；reader=make-decision 研究消费/用户呈现；consumer=候选筛选与方向挑战。分母是所有呈现候选和其来源，不以 500 字摘要代全文；缺 ref/path 报 incomplete，provider 不可用保留 unavailable。
- **RED/GREEN gate_cmd**: `npx --no-install vitest run tests/interaction-quality-contract.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`；同一命令目标 RED→GREEN。
- **expected_exit**: RED nonzero（新增目标断言）；GREEN 0。
- **RED target failure**: ORACLE-CARD07-P2-T008：旧 R0 限缩调研、固定截断或缺出处/路径仍宣称到达时新增断言失败。
- **GREEN oracle**: ORACLE-CARD07-P2-T008；>500 字可回读，候选均有出处/路径，缺路径及 unavailable 负例不标完成。
- **Evidence**: 计划 `quality/tests/card07-p2-t008-red.json`、`quality/tests/card07-p2-t008-green.json`。
- **RED evidence**: 本地目标子切片 `npx --no-install vitest run tests/interaction-quality-contract.test.mjs -t 'AC-34: two researched candidates are not delivered by a report ref and summary alone' --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot` exit 1，命名断言在第 86 行因 `candidate_delivery=undefined` 失败，非 setup error；其余 7 项 skipped。尚无 canonical raw output ref/当前 task 身份与完整 gate_cmd 的 RED，不能标本卡完成。
- **GREEN evidence（本轮实现，不替代真实外部研究）**：同一命名断言在 `readResearchReport → deriveResearchStatus` 后通过；定向组 `tests/interaction-quality-contract.test.mjs`、`tests/contract/research-report.test.mjs`、`tests/contract/status-derivation.test.mjs`、`tests/stage-decision-contract.test.mjs`、`tests/contract/stage-runtime-preflight.test.mjs` exit 0（84 项）。它覆盖候选集缺失、两候选缺字段、两候选完整交付、来源/具名证据双绑定、跨候选借证拒绝、推荐/不推荐理由、报告 hash/ref、用户可见 decision-log 表格/同材料 binding、官方 make-decision handler 读回与 status root cause；未产生 canonical stage fact，真实外部来源与用户按需展开仍留 P3 验收。
- **STOP / recovery**: 若现有 research writer/schema 不支持全文引用，停并交 P1/runtime owner 确认；不捏造来源。
- **Coverage limit**: fixture 不证明真实外网来源质量或 provider 可用。
- **Boundary / DO NOT TOUCH**: 预写 AC-34 测试冻结；不新增报告 store 或改 research runtime。
- **test change request**: none；冻结后需改时登记理由、旧/新 assertion、原 RED ref 与独立审查 ref。
- **Done**: AC-34 正负到达断言有证据；真实外部研究留 P3 验收。

### T009 — 发散后早审、未收敛输入与模块顺序

- **Source / FR / AC**: U-001、U-002-07～16、U-002-23/24、D-002/D-008/D-014/D-017/D-026/D-034 → FR-40、FR-43 → AC-40、AC-43；review 看未收敛空间，只挑战，不替用户选方向；实现方案和验收标准的关键分歧须在 make-decision 与用户共同梳理。
- **Inputs**: T007/T008 候选、研究来源和未决 OI；当前 questions-only `convergence_outline`、真实 review 配置与 canonical attempt/result；OI-016 冻结事实。
- **Files / symbols**: `workflows/make-decision/steps.json` 的 `talk-round-1`/`research-inputs`/`direction-advice`/`grill-with-docs` step_slug，`workflows/make-decision/SKILL.md` 的 `Procedure`，`skills/wh-review/contracts/make-decision.md` 的 `direction`，`workflows/make-decision/skill-deps.yaml` 的 review consumer，`tests/contract/stage-order-and-host-interaction.test.mjs`、`tests/workflow-v2-contract.test.mjs`。symbol: N/A — Markdown/manifest 锚点。
- **Action**: 先写先定大纲后研究、review 晚于收敛、direction 泄漏已选项、unavailable 伪空 findings 的目标断言并取 RED；另预写“只有后续 spec/AC、无 make-decision 用户答复却称共同梳理”的目标断言。再重排现有步骤职责为痛点澄清→研究+双源发散→direction challenge→用户定大纲→逐模块收敛。direction packet 保留原始需求/未收敛候选与盲挑战事实，遮蔽 current selection；每轮回显模块级已定/待定/本轮及重要/方向/真实模糊的方案与验收标准问答及裁决，普通细节由 agent 假设并披露。
- **Outputs / failure**: manifest/skill 顺序一致；review 可用只出 findings，preflight 缺路由/材料与 provider 已尝试后 unavailable 分别保留真实状态，不推导 pass。
- **Dependency**: T007/T008、OI-016 冻结；共享 workflow/steps 的后续卡串行。
- **Test tier / skill**: feature / backend-testing。
- **Scenario / fixture or service**: 未收敛候选/OI、provider unavailable；一个方案和一个验收标准的真实问答/裁决 fixture；负例提前携 current selection、review 晚于收敛、仅后续 spec/AC 存在而无本阶段用户答复；确定性 fixture。
- **Prewritten test**: T009 作者在 `tests/contract/stage-order-and-host-interaction.test.mjs` 先写方向挑战必须先于用户定稿、packet 遮蔽 current selection、关键方案/验收标准须有 make-decision 用户答复而不能由后续 spec 代偿的负例；`tests/workflow-v2-contract.test.mjs` 写 review unavailable 不等于空 findings。针对真实 `steps.json`、问答 producer/reader 与 packet，而非只检查技能关键词；缺的断言待写、待验证。
- **Observable seam**: producer=未收敛候选/OI/research、step manifest 与 make-decision 用户问答；artifact=direction review packet、canonical attempt/result、同版模块方案/验收裁决；reader=review adapter 与模块台账/用户大纲呈现；consumer=方向挑战及下一轮 Talk、后续 spec 作者。分母是全部未决候选和需用户决定的方案/验收项；无用户答复、错序或泄漏不能由后续文档代偿，provider 未完成保持 unavailable。
- **RED/GREEN gate_cmd**: `npx --no-install vitest run tests/contract/stage-order-and-host-interaction.test.mjs tests/workflow-v2-contract.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`；同一命令目标 RED→GREEN。
- **expected_exit**: RED nonzero（新增目标断言）；GREEN 0。
- **RED target failure**: ORACLE-CARD07-P2-T009：旧 step 3/4/5 次序、direction 缺未收敛事实/泄漏选择或 unavailable 伪绿被新增断言捕获。
- **GREEN oracle**: ORACLE-CARD07-P2-T009；AC-40 顺序、回显及关键方案/验收标准的本阶段用户共同梳理成立，只有后续 spec 的负例拒绝；direction 只挑战且泄漏负例拒绝，审查错误可回读。
- **Evidence**: 计划 `quality/tests/card07-p2-t009-red.json`、`quality/tests/card07-p2-t009-green.json`。
- **RED evidence**: 未核得本卡实际命令、failing assertion、exit、raw output ref；定向 fixture 计划不是独立 review 原件，待验证。
- **STOP / recovery**: 若现有 review packet 无法容纳未收敛事实而必须改 P1 runtime/review 写面，停并交 P1 owner 对齐；不借旧 review 冒本次结果。
- **Coverage limit**: 作者顺序测试不代替 P3 正式 review packet 与真实 provider 观察。
- **Boundary / DO NOT TOUCH**: 目标测试冻结；不改 runtime/review producer 或 provider 配置。
- **test change request**: none；冻结后需改时登记理由、旧/新 assertion、原 RED ref 与独立审查 ref。
- **Done**: AC-40/43 的顺序、盲输入、不可用负例有证据；未证明 provider 成功。

### T010 — 动态未决项、分级提问及逐轮问题卡

- **Source / FR / AC**: U-002-07～14/20、D-012/D-016/D-017 → FR-36、FR-38、FR-41 → AC-36、AC-38、AC-41；每一轮都要有替代方案和错误清单自检，不能用一张卡代替整轮范围。
- **Inputs**: 当前 OI status/impact、真实用户 reply、T009 后模块台账；已答卡/版本只读，零待问项合法；OI-016 冻结事实。
- **Files / symbols**: `skills/talk-with-zhipeng/SKILL.md` 的 `建立本轮候选队列`/`影响排序提问`/`大白话单轴决策卡`/`每答重排`/`返回最小交互事实`，`workflows/make-decision/SKILL.md` 的 Talk flow，`workflows/make-decision/steps.json` 的 talk step_slug，`skills/talk-with-zhipeng/skill-bundle.json`，`tests/stage-interaction-contract.test.mjs`、`tests/contract/stage-order-and-host-interaction.test.mjs`。symbol: N/A — Markdown/manifest 锚点。
- **Action**: 先为 0/1/3 真实未决项及第二轮缺替代/自检写测试并取 RED；再移除固定 `round_count=3/4` 与 14 步锁的行为前提，保留每次 ask→wait→真实 reply→resume 身份校验。high/medium 且为重要/方向/真模糊才问用户；普通项写最小假设并披露。每轮按错误清单检查来源、必要性、互斥选项、推荐、含义、后果、风险、替代方案与迟到 reply；角度不足回 T007 再发散。
- **Outputs / failure**: 每轮 2～3 个互斥选项及推荐理由/后果/风险，至少一个原方案外的替代和本轮自检；零问题不制造空卡，旧版迟到 reply 不覆盖当前 OI。
- **Dependency**: T009、OI-016 冻结；共享作者文件的后续卡串行。
- **Test tier / skill**: feature / backend-testing。
- **Scenario / fixture or service**: 0/1/3 未决项；三轮中第二轮缺替代或自检；普通/方向问题与迟到答复各一；仓内 fixture。
- **Prewritten test**: T010 作者在 `tests/stage-interaction-contract.test.mjs` 先写 0/1/3 OI、第二轮缺替代/自检、普通项误升级的命名断言；`tests/contract/stage-order-and-host-interaction.test.mjs` 写迟到 reply 不能覆盖新版 OI。每轮都要进入实际卡片 reader，缺的目标断言待写、待验证。
- **Observable seam**: producer=当前 OI writer 与真实 ask→reply→resume 事件；artifact=各 round 的问题卡、替代/自检及 OI 版本；reader=interaction validator/decision-log writer；consumer=用户决策与下一轮。分母是每一实际 round 和当前未决项，不只首卡；零问题不造假卡，缺 reply/旧版本不默认选项。
- **RED/GREEN gate_cmd**: `npx --no-install vitest run tests/stage-interaction-contract.test.mjs tests/contract/stage-order-and-host-interaction.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`；同一命令目标 RED→GREEN。
- **expected_exit**: RED nonzero（新增目标断言）；GREEN 0。
- **RED target failure**: ORACLE-CARD07-P2-T010：固定 Round、零问题异常、普通项被问、或第二轮缺替代/自检时新断言失败；不只看首卡。
- **GREEN oracle**: ORACLE-CARD07-P2-T010；每个实际 round 满足 AC-38，零问题和迟到答复负例拒绝，方向问题仍交用户。
- **Evidence**: 计划 `quality/tests/card07-p2-t010-red.json`、`quality/tests/card07-p2-t010-green.json`。
- **RED evidence**: 当前无逐轮目标 failing assertion、exit、raw output ref 的正式捕获；计划路径待验证。
- **STOP / recovery**: 缺真实 reply、OI 级别不明或必须改 runtime lifecycle 时停受影响动作，由 OI/interaction owner 补事实，不默认推荐。
- **Coverage limit**: 确定性问答测试不证明真人答复或宿主问题卡实际展示。
- **Boundary / DO NOT TOUCH**: 逐轮/零问题/迟到答复目标测试冻结；不改 CARD-04 接口或原始话语。
- **test change request**: none；冻结后需改时登记理由、旧/新 assertion、原 RED ref 与独立审查 ref。
- **Done**: AC-36/38/41 每轮和零问题负例有独立结果，不以问答次数当完成度。

### T011 — 用户原话、派生层与 append-only 裁决

- **Source / FR / AC**: U-002-06/12、D-010/D-017/D-020、SD-09/SD-10 → FR-35、FR-37 → AC-35、AC-37；结论三档为接受、接受但有偏离、拒绝。
- **Inputs**: 原始声明逐字 bytes/内容 hash、source atom ID、用户确认/纠正与当前 ADR/OI；OI-016 冻结事实。
- **Files / symbols**: `skills/decision-log/SKILL.md` 的 `原始声明层`/`Decision chain and module grouping`，`skills/decision-log/templates/decision-log-template.md` 的 `原始需求`/`原始声明层`/`三级追溯链`/`决定`，`workflows/make-decision/SKILL.md` 的 writer 责任，`skills/decision-log/skill-bundle.json`，`tests/decision-log-content-contract.test.mjs`。symbol: N/A — Markdown/manifest 锚点。
- **Action**: 先写只存 ID 不绑定逐字、标点变更未失效派生、改旧 ADR、偏离缺改变/理由的负例并取 RED；再在现有模板定义不可变原话层与独立派生层，派生绑定 source atom 和内容 hash。原话修正追加新声明并使旧 hash 依赖待复核；每项结论入三档，偏离写改变和原因；修正只追加 `Supersedes`/当前视图，不覆盖旧正文。
- **Outputs / failure**: 原话、派生、三档/偏离、取代链可回读；缺 source/hash、依赖未失效或旧正文被覆盖均失败。
- **Dependency**: T010 真实回复和当前 OI、OI-016 冻结。
- **Test tier / skill**: feature / backend-testing。
- **Scenario / fixture or service**: 含标点中文原话、派生两项、偏离/修正/取代各一次；负例 ID 不变但内容变更、旧正文覆盖。
- **Prewritten test**: T011 作者在 `tests/decision-log-content-contract.test.mjs` 写同 ID 原话标点变更使派生 hash 失效、旧 ADR 原位覆盖拒绝、偏离无改变/理由拒绝的断言；正例读取 supersedes 前后原字节。先核实真实 decision-log writer/reader，缺的断言待写、待验证。
- **Observable seam**: producer=用户逐字声明与 append-only decision writer；artifact=原话 bytes/hash、派生 source binding、裁决/supersedes；reader=decision-log 解析与后续 build-plan 分析；consumer=用户确认/下游 spec。分母是每条原话单元，不以同 ID 或派生文字替代；hash 漂移/覆盖旧正文必须报冲突或待复核。
- **RED/GREEN gate_cmd**: `npx --no-install vitest run tests/decision-log-content-contract.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`；同一命令目标 RED→GREEN。
- **expected_exit**: RED nonzero（新增目标断言）；GREEN 0。
- **RED target failure**: ORACLE-CARD07-P2-T011：旧模板无内容 hash/三档偏离或覆盖历史时新断言失败。
- **GREEN oracle**: ORACLE-CARD07-P2-T011；逐字可比较，修正使派生待复核，旧正文仍在且当前视图指向新裁决。
- **Evidence**: 计划 `quality/tests/card07-p2-t011-red.json`、`quality/tests/card07-p2-t011-green.json`。
- **RED evidence**: 未核得本卡真实失败断言、exit、raw output ref 与当前材料 revision；计划路径待验证。
- **STOP / recovery**: writer 不能承载内容 hash/不变历史时停并交 P1 owner；不原位改旧原件。
- **Coverage limit**: 模板/作者测试不证明 runtime writer 对真实记录已强制这些语义。
- **Boundary / DO NOT TOUCH**: 目标测试冻结；不改历史 decision/review 或另建需求账本。
- **test change request**: none；冻结后需改时登记理由、旧/新 assertion、原 RED ref 与独立审查 ref。
- **Done**: AC-35/37 正负断言与原始来源、现行/历史读回均有证据。

### T012 — 已知需求唯一处置与 blind 确认

- **Source / FR / AC**: U-002「等等等等」、D-018/D-021/D-023/D-024/D-027、母 PRD CARD-07 卡面 → FR-44、FR-45 → AC-44、AC-45；已知范围为明确但非穷尽分母，reconstruct 先于 reveal/challenge。
- **Inputs**: T011 原话/派生/ADR、原始需求清单、OI 终态、真实确认与当前 selection；OI-016 冻结事实。
- **Files / symbols**: `skills/decision-log/SKILL.md` 的 `Minimum content contract`/`One current OI authority`，`skills/decision-log/templates/decision-log-template.md` 的 `原始需求`/`唯一 OI 大纲`/`收敛检查`，`skills/wh-review/contracts/make-decision.md` 的 `direction`/`detail`，`workflows/make-decision/SKILL.md` 的确认/最终分析，`tests/decision-log-content-contract.test.mjs`、`tests/stage-interaction-contract.test.mjs`。symbol: N/A — Markdown/manifest 锚点。
- **Action**: 先写漏一条 source、同 source 冲突处置、blind 输入含当前选项/推荐/排序负例和完整正例，取 RED；再令现有覆盖核对从原始已知清单逐项推出唯一处置、可复算总数与“已知非穷尽”；review/确认在 reconstruct 剥离 selection/recommendation/ranking，reveal 后才显示并挑战。保留审查不可用与确认缺失，不用空 findings 代替。
- **Outputs / failure**: 每个已知 source 有唯一 terminal disposition 或 deferred owner/trigger；漏项/冲突列出 ID；盲重建提前携选择时 `MATERIAL_FORBIDDEN` 或等价明确失败。
- **Dependency**: T011 来源与 append-only，T009 direction 合同，OI-016 冻结。
- **Test tier / skill**: feature / backend-testing。
- **Scenario / fixture or service**: 完整已知 source 清单、漏一项/重复冲突；正常 blind/reveal/challenge 与提前传 current_selection 负例。
- **Prewritten test**: T012 作者在 `tests/decision-log-content-contract.test.mjs` 写已知 source 少一条/一条双重处置的命名拒绝断言；`tests/stage-interaction-contract.test.mjs` 写 reconstruct 输入含 current_selection/recommendation/ranking 的负例和 reveal 后正例。核实 coverage producer 与 blind payload reader 后补缺，待验证。
- **Observable seam**: producer=decision-log 逐字 source、当前 OI/裁决与 review packet author；artifact=可复算 source→terminal/deferred 映射和 reconstruct/reveal 两侧 payload；reader=coverage audit 与 review adapter；consumer=最终确认。分母取逐字 source 而非自报 ID 清单；缺项/冲突给 source 锚，blind 泄漏在 review 前拒绝。
- **RED/GREEN gate_cmd**: `npx --no-install vitest run tests/decision-log-content-contract.test.mjs tests/stage-interaction-contract.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`；同一命令目标 RED→GREEN。
- **expected_exit**: RED nonzero（新增目标断言）；GREEN 0。
- **RED target failure**: ORACLE-CARD07-P2-T012：旧覆盖只数 ID 或 blind reconstruct 见 current selection 时新增负例断言失败。
- **GREEN oracle**: ORACLE-CARD07-P2-T012；完整清单唯一可复算，漏项/冲突拒绝，blind 无当前选择，reveal 后才可见并挑战。
- **Evidence**: 计划 `quality/tests/card07-p2-t012-red.json`、`quality/tests/card07-p2-t012-green.json`。
- **RED evidence**: 尚无本卡具名漏项/泄漏目标失败的实际命令、exit、raw output ref；计划路径待验证。
- **STOP / recovery**: 现有 coverage/review producer 不给完整原始分母或盲边界时停并交 P1/runtime 或方向 owner；不把合同文案当生产接线。
- **Coverage limit**: 作者合同不证明 validator 已接生产路径；P3 正式 stage/review 输入再验 AC-44/45。
- **Boundary / DO NOT TOUCH**: 目标测试冻结；不创建第四套 validator 或改 review runtime。
- **test change request**: none；冻结后需改时登记理由、旧/新 assertion、原 RED ref 与独立审查 ref。
- **Done**: AC-44/45 的漏项、冲突、泄漏三负例与正例有证据；分母不明保持 incomplete。

## L2 — Removable reference

旧 Round1/2/3 与 14 步文件仅供定位旧消费者，不能作为目标行为。P3 从正式入口证明动态 OI 驱动、真实 review packet 和当前 decision-log consumer 读新合同且旧引用为零后，本节可删除，不改变 L0/L1。
