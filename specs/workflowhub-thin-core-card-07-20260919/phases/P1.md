# Phase P1 — CARD-02 post 文档合同与 CARD-07 runtime consumer

- **Global spec**：`spec.md#card-02-缺口修复`；母 PRD `CARD-02 文档权威与单一事实源` 仍是跨任务产品权威。
- **Write set**：`runtime/task/material-workspace.mjs`; `runtime/task/task-kernel-implementation.mjs`; `runtime/task/git-worktree-snapshot.mjs`; `runtime/stage/completion-predicates.mjs`; `runtime/stage/stage-context.mjs`; `runtime/stage/stage-content-contracts.mjs`; `runtime/stage/stage-handlers.mjs`; `runtime/stage/stage-runner.mjs`; `runtime/stage/stage-handoff.mjs`; `tools/cli/stage-runtime.mjs`; `skills/spec-specify/SKILL.md`; `skills/spec-specify/templates/spec-template.md`; `skills/spec-plan/SKILL.md`; `skills/spec-plan/templates/phase-template.md`; `skills/spec-tasks/SKILL.md`; `skills/spec-tasks/templates/index-template.md`; `workflows/build-plan/SKILL.md`; `workflows/build-plan/steps.json`; `workflows/build-plan/skill-deps.yaml`; `workflows/build-code/SKILL.md`; `workflows/build-code/steps.json`; `workflows/verify-code/SKILL.md`; `workflows/verify-code/steps.json`; `CONTEXT.md`; `CONSTITUTION.md`; `constitution-checklist.md`; `AGENTS.md`; `docs/standard-workflow.md`; `tests/contract/post-phase-contract.test.mjs`; `tests/contract/post-cohort-authoring-files.test.mjs`; `tests/contract/post-cohort-runtime-binding.test.mjs`; `tests/contract/material-workspace.test.mjs`; `tests/contract/stage-progress-contract.test.mjs`; `tests/acceptance/card-02-current.mjs`; `runtime/evidence/canonical-receipt-writer.mjs`; `runtime/evidence/freshness.mjs`; `runtime/evidence/invocation-identity.mjs`; `runtime/evidence/quality-fact.mjs`; `runtime/review/integration-review-subject.mjs`; `runtime/review/provider-material-projection.mjs`; `runtime/review/review-packet-identity.mjs`; `runtime/review/review-record-route.mjs`; `runtime/review/schemas/stage-materials.schema.json`; `runtime/review/stage-materials.json`; `runtime/task/workspace.mjs`; `skills/spec-plan/skill-bundle.json`; `skills/spec-specify/skill-bundle.json`; `skills/spec-tasks/skill-bundle.json`; `skills/wh-review/contracts/build-plan.md`; `skills/wh-review/scripts/__tests__/simple-contracts.test.mjs`; `skills/wh-review/scripts/review-materials.mjs`; `skills/wh-review/scripts/review-runner.mjs`; `skills/wh-review/scripts/simple-review-runner.mjs`; `tests/acceptance/card-02-current.test.mjs`; `tests/contract/filled-plan-task-production.test.mjs`; `tests/contract/four-material-non-gate-contract.test.mjs`; `tests/contract/phase-quality-handoff.test.mjs`; `tests/contract/spec-stage-artifact-closure.test.mjs`; `tests/contract/stage-routing-and-concrete-testing.test.mjs`; `tests/contract/stage-skill-consumer-contract.test.mjs`; `tests/stage-risk-acceptance.test.mjs`; `workflows/build-code/skill-deps.yaml`; `workflows/verify-code/skill-deps.yaml`; `tests/contract/post-cohort-downstream-workflows.test.mjs`; `tests/contract/post-cohort-governance-materials.test.mjs`; `tests/contract/post-cohort-spec-design-authority.test.mjs`; `tests/contract/post-integration-review-subject.test.mjs`; `tests/contract/post-ordinary-review-material-binding.test.mjs`; `tests/contract/post-phase-official-handler.test.mjs`; `tests/contract/post-review-cohort-binding.test.mjs`; `skills/catalog.yaml`; `tests/contract/review-materials-contract.test.mjs`; `skills/wh-review/skill-bundle.json`; `skills/test-routing-advisor/SKILL.md`; `skills/test-routing-advisor/skill-bundle.json`; `skills/testing-system-blueprint/SKILL.md`; `skills/testing-system-blueprint/skill-bundle.json`; `tests/contract/post-cohort-executable-authoring.test.mjs`。
- **Dependency**：`none`。
- **Consumer**：post `build-plan` 作者、正式 handler、`build-code` 与 `verify-code`；pre/history 只读旧四材料。

## L0 — Outcome and delta

本 Phase 把 CARD-02 说定却未交付的 post 文档接口变成真实、可执行的材料链：build-plan 产含全局工程设计的 `spec.md`、每包独立 `phases/P<n>.md` 与纯指针 `phases/index.md`；build-code 可只读 spec+所属 Phase+index 定位首个编辑、预期 RED、GREEN 与恢复，不再补做计划。旧 pre/history 四材料仅作为只读历史。本包只负责共同底座；CARD-07 头脑风暴行为由 P2，真实入口及逐 AC 回读由 P3。

执行顺序为 T001 原始需求与空壳反例 → T002 作者/模板/测试设计 → T003 当前材料身份与正式 handler → T004 review packet → T005 下游读回/旧路径清点 → T006 母 PRD/当前任务对照。T001 的失败必须是“原需求被弱化/任务卡缺内容”的目标断言，而不是 setup；T002 后冻结该目标测试，实施者需改测试时先登记变更请求与独立审查。P1 自身先冻结来源读取与正式分析接口，再实现并交给 P4 末端复核；P4 发现缺口返 P1 同 task 修复，P4 完成不是 P1 前置。每个 Task 的真实执行状态、命令输出及 review result 只记录在 task facts/quality，不写回本文件。

## L1 — Executable contract

- **FR / AC**：FR-36、FR-40、FR-44、FR-46、FR-47、FR-48、FR-49、FR-50、FR-51、FR-52、FR-53、FR-54、FR-55 / AC-36、AC-40、AC-44、AC-46、AC-47、AC-48、AC-49、AC-50、AC-51、AC-52、AC-53、AC-54、AC-55；归档 CARD-02 AC-DOC-003/004/005 与 AC-FLOW-003 只作来源，不冒充当前 AC。
- **Inputs and outputs**：输入为认证 task/cohort、当前 decision/spec、索引和逐 Phase 原文、母 PRD R-002 与归档审计；输出为当前 post 材料集合、报告型内容诊断、正式 build-plan/handoff、完整审查 packet 和下游同集合读回。缺件、额外、逃逸、漂移、空壳 Task、无全局设计均保持明确原因。
- **NEW**：Phase/index 模板及头部 Write set 所列 post 定向测试；新增生产命令/持久控制面为 N/A — 复用现有 writer/reader/quality 路径。
- **MODIFY**：头部 Write set 中已有作者 skill/bundle、runtime、review、workflow/deps、governance 与目标测试；每项由下方 Task 标出首要 owner。
- **DO NOT TOUCH**：母 PRD、`specs/archive/**`、CARD-01 topology owner、历史 task bytes、T001 目标断言和预写测试的既有 oracle；禁止用 active plan/tasks 或第二相等性协议代偿 Phase 内容。
- **Task order**：T001 → T002 → T003 → T004 → T005 → T006；T003/T004 可在 T002 结构合同固定后分支实现，但合并到 T005 前必须同一索引/材料身份。P4/T017～T019 消费 P1 交出的接口与材料，后续缺陷以返修回边处理，不作循环依赖。任务事实不写回 Phase。
- **Test strategy**：物理缺件/额外/越界、索引与正文漂移、空全局设计、一行 Task、需求错绑、review 取消/坏输出、pre 历史读回均有目标负例；权限/网络/数据库在纯本地 fixture 不适用（N/A — 无此依赖），并发只检查重叠写集与同 task 身份。
- **gate_cmd**：`npx --no-install vitest run tests/contract/post-phase-contract.test.mjs tests/contract/post-phase-official-handler.test.mjs tests/contract/post-review-cohort-binding.test.mjs tests/contract/post-integration-review-subject.test.mjs tests/contract/post-ordinary-review-material-binding.test.mjs tests/contract/post-cohort-authoring-files.test.mjs tests/contract/post-cohort-runtime-binding.test.mjs tests/acceptance/card-02-current.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`。
- **oracle**：ORACLE-CARD02-PHASE-FILES-001；此 Phase 级汇总仅核合同/读回，不代替逐 Task 或逐 AC oracle。
- **evidence_path**：`quality/tests/card02-post-phase-repair.json`（若未由正式 capture 发布，状态仍为 planned）。
- **STOP**：若现行来源要求新的产品选择、共享写面 owner 未定却需越界、或仅靠生成更多字段才能过检，回 decision/spec owner；材料缺失保持 incomplete，安全同 task 修复继续。
- **Done**：T001～T006 各自的正负例、原始需求 trace、pre/post 读回、review 真实终态及缺失项均可回放；没有 provider/用户确认不得声明正式 build-plan 质量完成。
- **Risk and rollback**：P1 写集大，原因是作者→身份→handler→review→下游跨同一材料接口；每 Task 只改其列出的文件子集，失败回滚该子集，保留所有原始 review/失败事实。若一处文件需跨 Task 修改，按串行交接并重新验证，不创造第二 owner。

### T001 — 原始需求与空壳计划的目标 RED

- **Source / FR / AC**：R-030、R-031、U-006；FR-47、FR-49、FR-50、FR-51；AC-47、AC-49、AC-50、AC-51。结果是母 PRD CARD-02 原验收与弱模型可执行性成为测试分母，而非当前模板自证。
- **Files / symbols**：`tests/contract/post-phase-contract.test.mjs`（symbol: post-cohort independent Phase authority）、`tests/acceptance/card-02-current.test.mjs`（symbol: livePostMaterials）、`tests/contract/post-cohort-spec-design-authority.test.mjs`（symbol: spec design assertion）；均在 P1 Write set。
- **Action**：先加真实多 Phase/当前 CARD-07 反例：保留物理文件和所有 FR/AC ID，但删全局设计或把 Tasks 压成一行，断言必须不再报告可执行；另把 AC-FLOW-003 与原母 PRD R-002/AC-09 绑定为“无 active 双写”而非 merged-review 路由，并对 AC-33/38 弱化做原文比对。RED 失败必须来自这些命名断言，不是测试收集或路径故障。
- **Inputs**：母 PRD CARD-02 与 CARD-07 原文、CARD-02 归档 spec/plan/tasks、当前初稿 spec/P1～P3、`research/phase-detail-loss-audit.md`；不需要 provider。
- **Outputs / failure**：输出具名 RED 断言与精确遗漏 ID；任一故意漏项仍返回 `ok=true` 就是本 Task 的目标失败。测试环境不可运行时报告 setup/unavailable，不伪装 RED。
- **Boundary / DO NOT TOUCH**：只写上述目标测试与诊断研究；不改母 PRD、归档、生产代码或已存在的真实质量原件。GREEN 阶段不得静默删/弱化这些断言。
- **Dependency**：`none`；独立建立目标判据，后续消费者在 Task order 中声明。
- **Test tier / skill**：feature / backend-testing；跨作者合同与正式 handler 的定向结构测试。
- **Scenario / fixture or service**：两独立 Phase 正例；删除 P2、插未索引 P3、只保留一行 Tasks、空全局设计、错绑 AC、pre 历史四材料；本地临时 fixture 在测试后清理。
- **Prewritten test**：T001 作者在 `tests/contract/post-phase-contract.test.mjs` 写物理文件齐但 P2 只剩一行 Task 的拒绝断言；在 `tests/acceptance/card-02-current.test.mjs` 写 AC-FLOW-003 错绑拒绝；在 `tests/contract/post-cohort-spec-design-authority.test.mjs` 锁空全局设计。核对已有同义用例后补缺，未执行者标待验证。
- **Observable seam**：producer=当前 decision-log 逐字原话、母 PRD 与 spec/index/Phase 作者；artifact=当前物理材料；reader=`validatePostPhaseContract` 和 CARD-02 acceptance producer；consumer=正式 build-plan/实施者。分母取原话/母 PRD，不取待测 spec 自报；缺内容/错绑须给来源 ID 与路径。
- **RED/GREEN gate_cmd**：`npx --no-install vitest run tests/contract/post-phase-contract.test.mjs tests/acceptance/card-02-current.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`。
- **expected_exit**：RED nonzero 来自“空壳 Phase/原始 AC 仍被接纳”的目标断言；GREEN 0 且保留负例。
- **RED target failure**：ORACLE-RAW-TRACE-001；断言当前一行 Task/漏原文/错绑 AC 不得得到 `ok:true`，旧实现此前错误接纳。
- **GREEN oracle**：ORACLE-RAW-TRACE-001；同一命令中完整材料通过，故意删除来源或仅留 ID 的负例明确报告来源 ID/缺字段。
- **Evidence**：`quality/tests/card07-plan-detail-red.json` 与 `quality/tests/card07-plan-detail-green.json`；未由正式 capture 写出之前仅为 planned ref。
- **RED evidence**：同身份 command、exit、具名 failing assertion 和 raw output ref 尚未核得；上列仅计划路径，状态待验证，不把已有结构绿例冒充 RED。
- **Coverage limit**：结构 RED 不证明所有产品行为已实现，也不代替 CARD-10 弱模型实跑。
- **STOP / recovery**：目标 RED 因 setup/外部 provider/不相干旧断言失败时停止，不修改生产代码换绿；修复 fixture 后同命令重取目标失败。
- **test change request**：none；冻结后若需改目标断言，保留旧/新 assertion、既有 RED ref、理由与独立审查 ref。
- **Done**：目标 RED 原件、同命令 GREEN、误绑负例和原始来源定位均可回读；缺任一项记 incomplete。

### T002 — 作者技能与模板保存执行前信息

- **Source / FR / AC**：R-030、R-031、U-007；FR-47、FR-50、FR-52；AC-47、AC-50、AC-52。结果是 spec 全局设计和 Phase 逐 Task 卡由作者直接产出。
- **Files / symbols**：`skills/spec-plan/SKILL.md`（symbol: Post-cohort authoring contract）、`skills/spec-plan/templates/phase-template.md`（symbol: L1 task card）、`skills/spec-specify/templates/spec-template.md`（symbol: 实现设计）、`skills/testing-system-blueprint/SKILL.md`（symbol: output contract）、`skills/test-routing-advisor/SKILL.md`（symbol: output contract）、`workflows/build-plan/skill-deps.yaml`（symbol: spec-plan/blueprint/routing consumer）、`skills/catalog.yaml`（symbol: skill bundle hash）、`tests/contract/post-cohort-executable-authoring.test.mjs`（symbol: authoring contract）；bundle 文件同目录按内容 hash 同步。
- **Action**：让 spec-plan 把已核实全局锚点/接口/依赖/验证放 `spec.md`，每 Phase L1 重复一张独立 `### Tnnn` 执行卡而非一行 Tasks；blueprint/routing 将场景、tier、oracle、coverage limit 送回所属 Task 卡；`skill-deps.yaml` post consumer 改指 spec/index/Phase，不再把 plan/tasks 当 active 输出。保留 pre 历史分流。
- **Inputs**：T001 目标断言、旧 plan/task 模板的执行前字段、当前规范 `AGENTS.md` 与 `CONSTITUTION.md`。
- **Outputs / failure**：输出可生成完整 Task 卡的作者合同；缺字段、仍指旧 tasks、bundle hash 不符须报告具体文件/字段，不能让 `spec-tasks` 在 index 复制执行正文补洞。
- **Boundary / DO NOT TOUCH**：不修改母 PRD、归档、当前 P2/P3 产品选择；预写测试断言与 oracle 受 T001 保护，合法变化先登记请求并由独立审查决定。
- **Dependency**：`T001`；先固定失败判据再改模板与依赖声明。
- **Test tier / skill**：feature / backend-testing；skills、bundle/catalog 与 stage deps 定向闭包。
- **Scenario / fixture or service**：正例完整 spec+双 Phase 逐 Task 模板；反例 Tasks 一行、blueprint 仍写 `tasks.md`、skill-deps 有 active `artifacts.plan.md`；无外部 service。
- **Prewritten test**：T002 作者在 `tests/contract/post-cohort-executable-authoring.test.mjs` 先断言模板逐卡含测试/恢复/证据字段；`tests/contract/post-cohort-authoring-files.test.mjs` 拒绝 blueprint/routing post 输出旧 plan/tasks；`tests/contract/post-cohort-spec-design-authority.test.mjs` 查全局设计。缺的目标断言待写、待验证。
- **Observable seam**：producer=`spec-specify`/`spec-plan`/blueprint/routing 作者；artifact=spec+Phase+纯索引及 bundle/deps；reader=正式 build-plan 材料检查与 build-code；consumer=逐卡实施者。分母为 T001 原文/AC；缺字段或旧输出报具体文件/字段，index 不复制正文补洞。
- **RED/GREEN gate_cmd**：`npx --no-install vitest run tests/contract/post-cohort-executable-authoring.test.mjs tests/contract/post-cohort-authoring-files.test.mjs tests/contract/post-cohort-spec-design-authority.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`。
- **expected_exit**：RED nonzero 为作者合同目标断言失败；GREEN 0，旧 pre 路由仍可读。
- **RED target failure**：ORACLE-PHASE-EXECUTABLE-001；断言一行 Tasks 或蓝图/路由旧输出被接纳时失败。
- **GREEN oracle**：ORACLE-PHASE-EXECUTABLE-001；逐 Task 卡和全局设计具体字段可定位，旧 post plan/tasks consumer 负例拒绝。
- **Evidence**：`quality/tests/card07-post-authoring-red.json`、`quality/tests/card07-post-authoring-green.json`；正式捕获前为 planned。
- **RED evidence**：尚无本卡同命令、目标断言、raw output ref 与当前身份的正式 capture；路径是计划而非 RED，待验证。
- **Coverage limit**：作者模板通过不说明当前 CARD-07 实际材料已填实，也不证明 provider 质量。
- **STOP / recovery**：若需要新增 Skill 或第五份材料才能表达，先回 D-031/本 spec 检查单一权威；不要以复制任务正文到 index 过关。
- **test change request**：none；合法修改须登记旧/新 assertion、原因、原 RED 与独立审查 ref。
- **Done**：模板、技能、deps、bundle/catalog hash 与正负例同读；真实 P1～P3 另由 T006 抽验。

### T003 — post 材料身份、正式 handler 与旧 cohort 分流

- **Source / FR / AC**：R-030、R-031、R-032、U-008；FR-46、FR-47、FR-48、FR-53、FR-54；AC-46、AC-47、AC-48、AC-53、AC-54。结果是物理 Phase 与认证原始分母由同一当前材料/分析边界读取。
- **Files / symbols**：`runtime/task/material-workspace.mjs`（symbol: materialFilesForCohort/inspectMaterialWorkspace）、`runtime/task/git-worktree-snapshot.mjs`（symbol: material snapshot）、`runtime/task/task-kernel-implementation.mjs`（symbol: current material revision）、`runtime/stage/stage-content-contracts.mjs`（symbol: validatePostPhaseContract）、`runtime/stage/stage-handlers.mjs`（symbol: currentPostPhases/officialStageHandler）、`runtime/stage/stage-runner.mjs`（symbol: currentMaterialBinding）、`tools/cli/stage-runtime.mjs`（symbol: isDesignArtifact/deriveCurrentStatusDomains）。
- **Action**：从 index 严格解析连续 Phase 路径并打开原始字节；作者写入、status、material revision、stage packet、正式 build-plan 和 handoff 使用同集合。校验 Phase header 与 index 的写集/依赖/consumer，并对逐 Task 内容缺失给诊断；post 不再读写 active plan/tasks，pre/history 保留旧分支且不新增第二 registry。另在 `runtime/stage/stage-content-contracts.mjs` 的既有只读边界定义 decision-log 候选/研究交付/逐轮自检/假设重画的可观察解释器，缺字段报 unknown/incomplete。本卡先从当前 `decision-log.md` 的逐字声明层独立取得原始需求单元，预写漏中间项与语义弱化目标测试，再让同一 validator 对调用方 `original_requirements`/`coverage` 双向核验；量词/否定/形态/错绑失败给 source 与目标锚，不能由 substring 判 pass。P4/T017/T018 随后复核该接口，发现缺陷返本卡修复，不要求 P4 先完成。正式 build-plan handler 复用既有 test receipt，认证目标 assertion 的真实 RED、原始输出、测试文件与冻结 hash，不能以 `command_oracle_checks.valid` 冒充 `no_prewritten_test`。先明确 schema/consumer/失败语义并预写负例；OI-016 已由 U-010/D-035 冻结后，CARD-07 可编辑共享生产行为，CARD-04 仅消费。
- **Inputs**：T002 的独立 Phase 模板；认证 task manifest activation_cohort、当前 worktree 的 decision/spec/index/Phase 原件。
- **Outputs / failure**：post 缺/额外/逃逸/重复/断号 Phase 均给具体路径和 `incomplete`，不能退回旧文件；改变一个 Phase 使现有身份诊断事实变化，但该事实不成为工作许可证。
- **Boundary / DO NOT TOUCH**：不改 `runtime/task/task-topology.mjs`、母 PRD、旧 task 字节或 CARD-01 owner；不把 in-memory 拼接 plan/tasks 存成 active 材料。
- **Dependency**：`T002`；作者形态先稳定，再更新所有正式 consumer。
- **Test tier / skill**：feature / backend-testing；物理文件身份与正式 CLI/handler 回归。
- **Scenario / fixture or service**：post 仅 decision 冷启动、两独立 Phase、缺 P2/额外 P3/逃逸、改 P1 后身份差异、pre 四材料、临时 Git worktree 自动清理。
- **Prewritten test**：T003 作者在 `tests/contract/post-phase-official-handler.test.mjs`/`post-cohort-runtime-binding.test.mjs` 预写缺 P2 被旧文件代偿、改 P1 后复用旧 revision 的拒绝断言；`material-workspace.test.mjs` 保留 pre 正例；另写 decision-log 中间原话漏项及“每轮→仅首轮”弱化负例。缺的子例待写、待验证。
- **Observable seam**：producer=认证 worktree 的 decision-log/spec/index/Phase 原始字节；artifact=同 cohort/snapshot/revision 的材料列表及原话分母；reader=`inspectMaterialWorkspace`/`validatePostPhaseContract`/`validateStageSpecAnalyzeProfile`/official handler；consumer=build-plan/status/handoff/P4。分母独立来自 decision-log 逐字层；缺件、漏项或弱化给路径/原话锚。
- **RED/GREEN gate_cmd**：`npx --no-install vitest run tests/contract/post-cohort-runtime-binding.test.mjs tests/contract/post-phase-official-handler.test.mjs tests/contract/material-workspace.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`。
- **expected_exit**：RED nonzero 为旧 consumer 缺 Phase 或需 plan/tasks 的目标断言；GREEN 0 且 pre 回归仍绿。
- **RED target failure**：ORACLE-CARD02-PHASE-FILES-001；断言缺 P2 却由旧 plan/tasks 代偿、或 status/handler 看不到 P2 时失败。
- **GREEN oracle**：ORACLE-CARD02-PHASE-FILES-001；正式 post 读回 P1/P2 与 spec/index，缺件/越界负例给具体路径；pre 四材料不变。
- **Evidence**：`quality/tests/card07-post-material-red.json`、`quality/tests/card07-post-material-green.json`；正式捕获前 planned。
- **RED evidence**：本卡未取得同身份、同命令的具名失败 assertion/exit/raw output 正式原件；计划路径仍为 planned，待验证。
- **Coverage limit**：只证明材料与正式 build-plan 读链，不证明每个 CARD-07 产品行为已实现。
- **STOP / recovery**：若更改 topology owner 或必须保留永久兼容双写才能通过，则停回 spec；先修唯一物理读链再重验。
- **test change request**：none；合法修改须登记旧/新 assertion、原因、原 RED 与独立审查 ref。
- **Done**：当前材料/身份/handler/handoff 的正负例与旧 cohort 回归可回放，缺正式 stage 事实仍标 incomplete。

### T004 — 合并审查 packet 交付每份真实 Phase

- **Source / FR / AC**：R-030、R-031；FR-48、FR-52；AC-48、AC-52。结果是 reviewer 看见原始需求和每份独立 Phase，而不是旧 plan/tasks 或合成摘要。
- **Files / symbols**：`runtime/review/stage-materials.json`（symbol: build-plan post profile）、`runtime/review/schemas/stage-materials.schema.json`（symbol: post required keys）、`runtime/review/provider-material-projection.mjs`（symbol: providerMaterialEntries）、`runtime/review/review-packet-identity.mjs`（symbol: provider material path/hash）、`runtime/review/review-record-route.mjs`（symbol: bindBuildPlanReviewCohort）、`skills/wh-review/scripts/review-materials.mjs`（symbol: buildReviewMaterials）、`skills/wh-review/scripts/simple-review-runner.mjs`（symbol: runSimpleReview）、`skills/wh-review/contracts/build-plan.md`（symbol: post packet contract）。
- **Action**：审查从认证 task cohort 选 post profile，完整交付 raw_requirement、draft_spec、acceptance_criteria、phase_index 与 `phase_authorities` 各物理原件，provider 路径/字节/hash 一致；caller 伪 pre、缺 Phase、旧 draft_plan/draft_tasks 或静默截断在派发前报真因。保留旧 pre profile 和 unavailable attempt 原件。
- **Inputs**：T002/T003 的 spec+index+Phase 集合、认证 review task/manifest、现有 provider route；不依据当前目录自动挑 latest attempt。
- **Outputs / failure**：输出不可变 packet 与 canonical attempt/result 或真实 unavailable。取消、timeout、坏输出、provider 未返回不合成 findings=[]；同一 unchanged packet 不为 clean 标签重派。
- **Boundary / DO NOT TOUCH**：不改 provider 配置、母 PRD 或历史 review；测试禁止用假的空 findings 宣称合并审查通过。
- **Dependency**：`T003`；物理路径与 cohort 认证完成后才能给 reviewer 冻结同一集合。
- **Test tier / skill**：feature / backend-testing；审查投影、路径身份及失败 transport 定向测试。
- **Scenario / fixture or service**：P1/P2 分文件、缺 P2、caller pre/post 冲突、索引含未提供文件、provider cancellation；测试用注入 client，不消耗真实 provider。
- **Prewritten test**：T004 作者在 `post-review-cohort-binding.test.mjs`/`post-ordinary-review-material-binding.test.mjs` 写缺 P2、伪 cohort、旧 draft_plan/tasks 代偿的拒绝断言；`review-materials-contract.test.mjs` 查每个 provider 路径/hash，`simple-contracts.test.mjs` 锁 cancellation 保真。缺项待写、待验证。
- **Observable seam**：producer=认证材料读取与 post review profile；artifact=不可变 packet/provider manifest 及 attempt/result；reader=review route/quality writer；consumer=独立 reviewer 与 build-plan 处置。分母为 index 全部 Phase 加 decision/spec；缺件在派发前失败，provider 失败保持 unavailable。
- **RED/GREEN gate_cmd**：`npx --no-install vitest run tests/contract/review-materials-contract.test.mjs tests/contract/post-review-cohort-binding.test.mjs tests/contract/post-ordinary-review-material-binding.test.mjs skills/wh-review/scripts/__tests__/simple-contracts.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`。
- **expected_exit**：RED nonzero 为 packet 仍发旧材料/丢 Phase 的目标断言；GREEN 0 且缺件失败路径可回放。
- **RED target failure**：ORACLE-POST-REVIEW-001；断言 reviewer 只收到 plan/tasks、缺 P2 却 dispatched 或 cohort 伪造时失败。
- **GREEN oracle**：ORACLE-POST-REVIEW-001；provider manifest 列原始 spec/index/P1/P2 的真实独立路径/hash，缺件和 transport 保真。
- **Evidence**：`quality/tests/card07-post-review-packet-red.json`、`quality/tests/card07-post-review-packet-green.json`；正式 capture 前 planned。
- **RED evidence**：未核得当前身份的真实 failing assertion、exit 和 raw output ref；计划路径不是审查或 RED 原件，待验证。
- **Coverage limit**：packet 完整不证明 provider 已给语义 findings；已取消的 attempt 不用作新材料审查结果。
- **STOP / recovery**：broker 不能送完整包或 provider 错误时记录 unavailable/原码，继续安全材料修复；不得截断、拼接旧 plan 或重派相同冻结包求 clean。
- **test change request**：none；合法修改须登记旧/新 assertion、原因、原 RED 与独立审查 ref。
- **Done**：材料 manifest、cohort 负例、pre 回归与真实 attempt 状态可读；没有结果就报告 unavailable。

### T005 — build-code/verify-code 和遗留消费者同组读回

- **Source / FR / AC**：R-030、R-031、R-032、U-002、U-008；FR-36、FR-40、FR-44、FR-48、FR-52、FR-55；AC-36、AC-40、AC-44、AC-48、AC-52、AC-55。结果是下游按 Phase Task 执行，build-plan 最后分析也必须实际执行并发布。
- **Files / symbols**：`runtime/stage/stage-context.mjs`（symbol: material context）、`runtime/stage/completion-predicates.mjs`（symbol: stage quality subjects）、`runtime/stage/stage-runner.mjs`（symbol: runOfficialStage/publishStageEndSpecAnalyzeFact）、`runtime/evidence/freshness.mjs`（symbol: ordinaryReviewMaterialRevision）、`runtime/review/integration-review-subject.mjs`（symbol: currentMaterials）、`workflows/build-plan/SKILL.md`（symbol: final-spec-analyze work sequence）、`workflows/build-plan/steps.json`（symbol: final-spec-analyze）、`workflows/build-plan/skill-deps.yaml`（symbol: spec-analyze dependency）、`workflows/build-code/SKILL.md`（symbol: Work loop）、`workflows/verify-code/SKILL.md`（symbol: original requirement spot-check）、`tools/cli/stage-runtime.mjs`（symbol: review/status dispatch）、`tests/contract/post-cohort-downstream-workflows.test.mjs`（symbol: downstream contract）。
- **Action**：build-code 按索引打开当前 Phase 的逐 Task 卡，只用卡里已定文件/符号、预写测试和 STOP；verify-code 抽原始需求→实现→真实证据。清点 active writer/reader/validator/review/verify，post 不再要求 plan/tasks；pre/history 继续读旧四材料。本卡自身先在 `workflows/build-plan/steps.json#final-spec-analyze` 与 `runtime/stage/stage-runner.mjs#runOfficialStage/publishStageEndSpecAnalyzeFact` 的现有 owner 处冻结并预写正式调用反例：handler 后/事务发布前从当前 decision-log 逐字声明层及 `preflight.materials` 打开 source/spec/index/全部 Phase，直接跑 `validateStageSpecAnalyzeProfile`，runtime 自算 status，附 skill bundle/source/current identity，送现有 writer；不从普通 invocation 自报 `consistent`、不走 legacy stage outcome/adapter。无逐字源为 material_incomplete，无执行为 missing，同 task 可修复。P4/T019 在接口交付后复核，缺陷返 P1/T005 修复。旧 `outline_closed` 不再做 active permit，需求覆盖不足仍是可见质量事实。
- **Inputs**：T003 的材料原件、T004 的审查状态、当前实现 diff/test/AC facts；没有真实执行证据时只消费已知材料。
- **Outputs / failure**：输出下游读取/当前质量状态；post acceptance execution 或 E2E 证据缺失时 `unavailable`，不能把 Phase 中 `gate_cmd` 写成已执行。旧 pre 读链失败须单独报告，不由 post 修复掩盖。
- **Boundary / DO NOT TOUCH**：不改历史 task/receipt、CARD-05 provider 选型、CARD-10 真实弱模型 E2E；不可把 quality missing 变为继续工作机器门。
- **Dependency**：`T003`、`T004`；消费者在材料和 review profile 固定后对齐，未拿到 provider 结果不阻止结构修复。
- **Test tier / skill**：feature / backend-testing；status、handoff、integration review/verify 的 post/pre 双 cohort 测试。
- **Scenario / fixture or service**：post 无 plan/tasks 但 P1/P2 有效；pre 只有四材料；review partial/unavailable、E2E 未执行、旧 aggregate 无 active producer；临时 task fixture 清理。
- **Prewritten test**：T005 作者在 `post-cohort-downstream-workflows.test.mjs` 先断言 post build-code/verify-code 不要求 tasks.md；`phase-quality-handoff.test.mjs` 锁缺执行/E2E 不称完成；`post-phase-official-handler.test.mjs` 补 final-spec-analyze 同版调用/发布及缺 source 负例。不得等待 P4/T019 才写，缺项待验证。
- **Observable seam**：producer=build-plan final analyzer 与当前材料 reader；artifact=同 task/stage/snapshot/revision 的 `result.spec_analyze` 和既有 quality fact；reader=public status/handoff、build-code/verify-code；consumer=实施与质量核查。来源分母取 decision-log 逐字层；未调用/旧版/缺原话分别报 missing/stale/material_incomplete。
- **RED/GREEN gate_cmd**：`npx --no-install vitest run tests/contract/post-integration-review-subject.test.mjs tests/contract/post-cohort-downstream-workflows.test.mjs tests/contract/phase-quality-handoff.test.mjs tests/contract/post-cohort-runtime-binding.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`。
- **expected_exit**：RED nonzero 为 post 消费者硬读 tasks/质量假通过断言；GREEN 0 且 `unavailable` 保真。
- **RED target failure**：ORACLE-POST-CONSUMER-001；断言 post 仍需 plan/tasks 或缺执行证据却称完成时失败。
- **GREEN oracle**：ORACLE-POST-CONSUMER-001；下游读取同一 Phase 集合，缺 E2E/执行/审查结果保持 unavailable，pre 保持可读。
- **Evidence**：`quality/tests/card07-post-consumer-red.json`、`quality/tests/card07-post-consumer-green.json`；正式 capture 前 planned。
- **RED evidence**：尚无本卡正式 final-analyze 与下游联合 RED 原件；局部子切片或计划路径不足以证明完整目标，待验证。
- **Coverage limit**：当前读回不能证明未来 build-code 已把 CARD-07 全部行为实现，也不证明 CARD-10 弱模型执行。
- **STOP / recovery**：若任何 post 活跃消费者依然无 cohort 分支先修此 Task；若需重新定义 CARD-05/10 的产品职责回决策 owner，不在本卡偷做。
- **test change request**：none；合法修改须登记旧/新 assertion、原因、原 RED 与独立审查 ref。
- **Done**：post/pre 下游同集合与 unavailable 负例通过；未来执行证据缺口显式交 P3/后续 stage。

### T006 — 原始 CARD-02 要求与当前真实 task 的对照收口

- **Source / FR / AC**：R-030、R-031、R-035、U-005、U-007、U-010；FR-49、FR-50、FR-51、FR-52；AC-49、AC-50、AC-51、AC-52。归档 CARD-02 的 clean archive criterion 只作来源审计，不冒充本 Phase 当前 AC。结果是物理形态、内容细度、来源分母与消费者四者分别报告，不再一条绿命令均摊。
- **Files / symbols**：`tests/acceptance/card-02-current.mjs`（symbol: deriveCard02Entries/readCard02PostPhaseEvidence）、`tests/acceptance/card-02-current.test.mjs`（symbol: CARD-02 current acceptance producer）、`tests/contract/post-phase-contract.test.mjs`（symbol: empty-shell negative）、`tests/contract/post-phase-official-handler.test.mjs`（symbol: official post fixture）、`docs/standard-workflow.md`（symbol: post material contract）、`CONTEXT.md`（symbol: material authority）；任务研究与 spec/Phase 原件作为证据读取，不新增第二验收 ledger。
- **Action**：从母 PRD R-002/AC-07/08/09 逐字反查当前 spec 四件翻译、每个 Phase 独立且可执行的差异/自身验收、纯索引与无 active plan/tasks；按 D-035 的用户确认清点 make-decision 的 active writer/reader/validator/completion，移除过期 aggregate consumer 与 outline completion dependency，保留历史字段只读；把 CARD-02 原 22 AC 的不相干测试错绑降为 incomplete，把 79 来源的虚假“已实现”逐项纠偏；只在有同义 oracle/真实证据时提升状态，区别 deferred、unavailable、unknown 与 achieved。
- **Inputs**：T001～T005 结果、母 PRD/归档只读原件、当前 CARD-07 spec/P1～P3/index、canonical review/test/confirmation facts；没有的原件显式列缺口。
- **Outputs / failure**：输出逐来源/逐 AC 核对表与当前材料质量对照；若只有模板标题、错误 AC 绑点、旧 attempt 或绿 exit，则该项 incomplete，不改写为 completed。
- **Boundary / DO NOT TOUCH**：不修改 CARD-02 归档、CARD-10 真弱模型执行事实、历史 provider output 或用户确认；无用户实际答复不发布最终确认。
- **Dependency**：`T001`、`T002`、`T003`、`T004`、`T005`；五条链都能回读后才可作最终当前范围声明。
- **Test tier / skill**：feature / backend-testing；原始需求验收 producer 加人工同义复核。
- **Scenario / fixture or service**：真实 CARD-07 三独立 Phase、仅一行 Tasks 反例、删原始来源、错绑 AC-FLOW-003、缺审查结果/缺确认，pre/history 只读对照。
- **Prewritten test**：`card-02-current.test.mjs` 已锁 DOC-003/004 的结构子断言不升完整 achieved；`stage-completion.test.mjs` 预写 current pre/post 无 outline/aggregate completion 前置；`stage-skill-consumer-contract.test.mjs` 预写 manifest 无 aggregate consumer/input/result。`post-phase-contract.test.mjs` 写一行 Task/缺源拒绝；`post-phase-official-handler.test.mjs` 写当前真实材料读回。原 22 AC 逐项复核，缺的目标断言待写、待验证。
- **Observable seam**：producer=母 PRD 原始 AC、当前 decision/spec/Phase 与 canonical test/review/confirm facts；artifact=CARD-02 逐 AC 状态表、当前 decision-log 与技能清单；reader=`deriveCard02Entries`/`readCard02PostPhaseEvidence`/`deriveStageCompletion`/`loadStageSkillManifest`；consumer=本 task 交付判断与 CARD-04 后续适配。分母为原 22 AC 与当前原话，不以测试名/模板字段自证；缺同义证据保持 incomplete/deferred/unavailable。
- **RED/GREEN gate_cmd**：`npx --no-install vitest run tests/acceptance/card-02-current.test.mjs tests/contract/post-phase-contract.test.mjs tests/contract/post-phase-official-handler.test.mjs --poolOptions.forks.singleFork --no-fileParallelism --reporter=dot`。
- **expected_exit**：RED nonzero 是来源错绑/空壳被接纳的目标断言；GREEN 0 仅证明当前目标断言，逐 AC 另读状态。
- **RED target failure**：ORACLE-CARD02-ORIGINAL-001；断言只生成文件、漏来源或错绑 AC 仍被标 achieved 时失败。
- **GREEN oracle**：ORACLE-CARD02-ORIGINAL-001；逐项定位母 PRD→FR/AC→Phase Task→正负证据，真实缺失项保持 incomplete。
- **Evidence**：`quality/tests/card07-card02-original-red.json`、`quality/tests/card07-card02-original-green.json` 与 `research/phase-detail-loss-audit.md`；正式 capture 前 planned。
- **RED evidence**：未见逐 22 AC 对应的本卡正式 RED 捕获；研究对照与计划路径不是 failing assertion/raw output，待验证。
- **Coverage limit**：本卡不证明 CARD-05/10 实验或所有历史任务迁移；原 22 AC 无充分同义证据时不以本 Task 自述填绿。
- **STOP / recovery**：若某项需用户新选方向或修改母 PRD，停在该来源并回 decision owner；其余安全核对继续，保留旧错误结论供审计。
- **test change request**：none；合法修改须登记旧/新 assertion、原因、原 RED 与独立审查 ref。
- **Done**：AC-07/08/09 当前任务样本、内容细度、目标负例、pre/post 读回及未完成事实逐项披露；正式 review/确认缺失不称 build-plan 完成。

## L2 — Removable reference

CARD-02 归档 `plan.md` 的全局设计栏目与 `tasks.md` 的逐卡执行前字段可作迁移对照，不应把执行后 `status/completed_at/evidence_refs` 回填复制到本 Phase。本段可在独立读者确认只凭 L0/L1 与 spec 即可定位首个编辑、目标 RED/GREEN 和恢复时删除；删除后上述合同不变。
