# Cross-Artifact Analysis: worktree-unification

**task-id**: worktree-unification
**date**: 2026-07-05
**Generated by**: build-plan executor (round-5 fix pass)
**Scan dimensions**: inconsistency, duplicate, ambiguity, underdefined

---

## 摘要

本轮针对 round-4 verdict 中 5 条 blocking + 2 条 minor 逐一修复后重新扫描。**blocking=0，minor=0，达标**。

修复列表：

| 编号 | 类型 | 修复内容 | 涉及文件 |
|------|------|----------|----------|
| B1 | inconsistency | T005 scope 矛盾：build-plan/SKILL.md 锁定文件被误标为"最小补充"适用范围，已明确标注"仅只读核查，禁止任何修改" | plan.md §3.1, tasks.md T005 |
| B2 | inconsistency | stage-result 文件名冲突：plan.md F6 Rationale 残留 stage-result-verify-code.json，T006 同样使用该名，已统一为 stage-result.json | plan.md, tasks.md T006 |
| B3 | inconsistency | FR-WORKTREE-SCOPE-009 边界过窄：specs/{task-id}/ 仅允许 spec/plan/tasks 三文件，与已存在的 build-plan 过程产物冲突；已扩展白名单含 research.md、baseline-report.md 等，并更新 plan.md §3.2 和 T006 检查逻辑 | spec.md, plan.md §3.2, tasks.md T006 |
| B4 | underdefined | 缺少覆盖全 5 个 stage 的 commit/no-change 门控；已在 T001-T005 各任务下新增 commit gate 条文，明确"有变更则 commit，无变更须记录原因" | tasks.md T001-T005 |
| B5 | underdefined | T001-T007 缺少可机器执行的 gate_cmd；已为每个任务添加带明确 exit code 的 gate_cmd 列表（node 单元测试调用、grep 检查、test 文件存在性、jq 字段校验等） | tasks.md T001-T007 |
| m1 | ambiguity | T007 引用 spec §7 验收标准（§7 实为 Out of Scope）；已更正为引用 spec §5 成功标准 AC-01..AC-04 | tasks.md T007 |
| m2 | false-green | cross-artifact-analysis.md 前版报告 blocking=0 但实际有 5 条 blocking；本轮重新生成，如实反映修复后状态 | cross-artifact-analysis.md |

---

## 发现项（修复后残留）

无新发现项。所有原 round-4 blocking 和 minor 均已处理。

---

## 一致性验证

| 检查点 | 结果 |
|--------|------|
| stage-result 文件名：spec.md / plan.md / tasks.md 三方一致 | stage-result.json（统一） |
| T005 build-plan/SKILL.md 操作权限：tasks.md = plan.md | 仅只读核查，禁止修改 |
| SCOPE-009 白名单：spec.md 定义 = plan.md §3.2 = T006 检查范围 | 一致，禁止项为 evidence/、stage-result.json、journal.jsonl、task-metrics.jsonl |
| T007 验收来源：tasks.md 引用 spec §5 AC-01..AC-04 | 一致 |
| gate_cmd 覆盖范围 | T001-T007 全部覆盖 |
| commit gate 覆盖范围 | T001-T005 全部覆盖（T006/T007 无独立 commit，由 T005 commit gate 覆盖） |
