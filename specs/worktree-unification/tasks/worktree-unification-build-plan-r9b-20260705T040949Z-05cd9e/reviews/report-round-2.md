# 审查报告 — worktree-unification-build-plan-r9b-20260705T040949Z-05cd9e (round 2)

- verdict: revise_required
- provenance: single-context

## Summary

Round 2 closure failed. Prior path-contract fixes for B3/B4/B5 and forbidden build-spec wording are partly closed, but executable blockers remain: build-spec/build-plan scope contradicts spec requirements, parser gates target the wrong API, normalization is inconsistent, durable tests still assert old fallback behavior, hard FR-WORKTREE-COMMIT-004 is advisory, 3rd-review is not fail-closed, runtime artifact checks run before producers, and several gates can false-green/false-red.

## Findings

- [blocking] 位置: specs/worktree-unification/plan.md:215 | 问题: spec.md requires build-spec/build-plan to read worktree.json, use target_repo_root/worktree_root, and fail-loud when missing, but plan.md and tasks.md forbid modifying workflows/build-spec/SKILL.md and workflows/build-plan/SKILL.md. This is still the same executable-scope conflict found in the prior r9b review. | 建议: Choose one path: allow tightly scoped edits to workflows/build-spec/SKILL.md and workflows/build-plan/SKILL.md with exact tasks/gates, or downgrade FR-WORKTREE-SCOPE-008 to read-only audit and remove the MUST/fail-loud consumer obligations from spec/data-contracts.
- [blocking] 位置: specs/worktree-unification/data-contracts.md:15 | 问题: Contract 1 lists build-code and verify-code as worktree.json consumers, but spec.md makes build-spec and build-plan read-only consumers for target_repo_root/worktree_root routing. Implementers can satisfy the contract while skipping two required stages. | 建议: Add build-spec and build-plan to Contract 1 as read-only consumers, or revise spec.md to remove their worktree.json consumer obligation.
- [blocking] 位置: specs/worktree-unification/tasks.md:15 | 问题: T001 gate_cmd calls require('./core/task-dir-parser.mjs').then(...), but core/task-dir-parser.mjs exports synchronous parseTaskDir(). The gate tests the wrong API shape and can fail or pass for reasons unrelated to FR-WORKTREE-ENVVAR-003. | 建议: Rewrite parser gates to use the real API, for example node --input-type=module with import { parseTaskDir } from './core/task-dir-parser.mjs'; then call parseTaskDir() synchronously and assert exact return value/exit behavior.
- [blocking] 位置: specs/worktree-unification/tasks.md:35 | 问题: task_dir normalization is inconsistent. spec.md says yaml values ending in /tasks require migration or env override; tasks.md requires automatic trimming and calls normalizeTaskTrackingRoot(); data-contracts.md does not define trimming. The current repo config already ends in /tasks, so this affects the main fallback path. | 建议: Pick one canonical rule and update spec.md, plan.md, data-contracts.md, and tasks.md. Recommended: yaml fallback trims exactly one trailing /tasks or /tasks/ to task_tracking_root; WORKFLOWHUB_TASK_DIR must already be task_tracking_root; do not expose an unapproved helper unless the API is specified.
- [blocking] 位置: specs/worktree-unification/tasks.md:36 | 问题: T002 requires normalizeTaskTrackingRoot(), but plan.md only specifies parseTaskDir(), and core/task-dir-parser.mjs has no such export. This introduces a new public API solely to satisfy a gate, without FR/data-contract backing. | 建议: Either specify normalizeTaskTrackingRoot as an exported API in spec/plan/data-contracts and add it to T001, or remove the helper gate and test normalization through parseTaskDir().
- [blocking] 位置: core/__tests__/task-dir-parser.test.mjs:14 | 问题: Existing persistent tests still assert the old ~/Knowledge/workflowhub fallback, which directly conflicts with the new fail-loud requirement when env var and yaml are both missing. tasks.md only has ad hoc shell gates and does not require updating the durable test file. | 建议: Add a test-first T001 subtask to update core/__tests__/task-dir-parser.test.mjs and run npx vitest run core/__tests__/task-dir-parser.test.mjs. Cover env priority, empty env, yaml fallback, missing yaml, missing task_dir, relative path, ~ path, nonexistent path, non-directory, hardcoded fallback removal, and chosen /tasks normalization.
- [blocking] 位置: specs/worktree-unification/tasks.md:84 | 问题: FR-WORKTREE-COMMIT-004 is a hard in-scope requirement for every stage/phase, but T008 makes missing commit/no-change coverage advisory/follow-up and read-only. plan.md Verification Mapping also omits T008. | 建议: Either explicitly narrow FR-WORKTREE-COMMIT-004 out of this task with spec/design approval, or add implementation tasks plus blocking gates for every affected row in the commit coverage matrix. Missing coverage cannot be follow-up while the FR remains in scope.
- [blocking] 位置: specs/worktree-unification/data-contracts.md:128 | 问题: Contract 4 says every stage/phase must run git add + git commit, while spec.md says commits occur only when files changed, forbids empty stage markers, and requires no-change records otherwise. | 建议: Align Contract 4 with spec.md: commit only on actual file changes; otherwise write a no-change reason to stage-result/journal; explicitly forbid empty marker commits.
- [blocking] 位置: specs/worktree-unification/tasks.md:47 | 问题: T004 requires 3rd-review verdict=pass before irreversible actions, but does not explicitly remove or block the existing verify-code unavailable/not_executed review path that can proceed to merge/push/delete. | 建议: Add a T004 requirement and gate: 3rd-review unavailable/not_executed must block close, write needs_human=true, escalate_to_human, and prevent merge/push/branch deletion unless an executed verdict=pass exists.
- [blocking] 位置: workflows/verify-code/SKILL.md:204 | 问题: The current 3rd-review runner path is still a placeholder (/path/to/3rd-review/standalone.sh), while tasks.md only says to run independent 3rd-review and does not specify a real entrypoint, output schema, timeout, or unavailable semantics. | 建议: T004 must require a real runner discovery/entrypoint rule, input package, verdict schema, evidence directory, timeout, and fail-closed behavior. Placeholder commands must be forbidden in the final skill.
- [blocking] 位置: specs/worktree-unification/spec.md:203 | 问题: spec.md/tasks.md use stage-result.json as the runtime result file, but existing workflow SKILL files use per-stage names such as stage-result-verify-code.json and stage-result-build-code.json. The plan can create a second file contract or make downstream readers look in the wrong place. | 建议: Choose a single result-file contract. Recommended: align with existing per-stage filenames or explicitly introduce close-stage stage-result.json with migration/read rules, then update spec.md, plan.md, tasks.md, and data-contracts.md consistently.
- [blocking] 位置: specs/worktree-unification/tasks.md:72 | 问题: T006 requires ${WORKFLOWHUB_TASK_DIR}/tasks/worktree-unification/stage-result.json and evidence/ to exist, but those are verify-code close runtime outputs. Stage 3 only verifies/edits plans and does not run close. | 建议: Change T006 to statically verify verify-code/SKILL.md path rules and repo absence only. Move external artifact existence checks to verify-code close acceptance after close has produced them.
- [blocking] 位置: specs/worktree-unification/tasks.md:71 | 问题: The forbidden-artifact gate uses git show HEAD diff headers. It misses current working-tree files, untracked files, pre-existing tracked files outside the last commit, and directory-level evidence/. | 建议: Replace it with current-state gates: test ! -e specs/worktree-unification/evidence, test ! -f specs/worktree-unification/stage-result.json, journal.jsonl, task-metrics.jsonl, plus git ls-files and git ls-files --others --exclude-standard scans for forbidden paths.
- [blocking] 位置: specs/worktree-unification/tasks.md:77 | 问题: T007 says the acceptance checklist is for verify-code, but the dependency graph schedules it after T006, while T006 validates close/runtime outputs. The supposed consumer can run before the checklist exists. | 建议: Move T007 before verify-code/final close checks, or state it is post-run documentation and remove the claim that verify-code consumes it.
- [blocking] 位置: specs/worktree-unification/tasks.md:42 | 问题: Several gates are broad whole-file greps/counts. For example, banning worktree add can false-fail on prohibition text; counting circled numbers can pass outside the close section; grep for stage-result.json proves only a string exists, not the write path. | 建议: Replace broad text greps with section-bounded assertions and executable checks. Pure prose scans should be advisory, not gate_cmd pass/fail criteria.
- [blocking] 位置: specs/worktree-unification/tasks.md:95 | 问题: Stage 2 marks T002/T003/T004 parallel, but their commit gates all check git log --oneline | head -1. Parallel commits can make the latest commit belong to another task, causing false failures. | 建议: Change commit gates to search the relevant commit range for each expected prefix rather than checking only HEAD, or remove per-task latest-commit gates from parallel tasks and verify commit coverage in a final serialized gate.
- [important] 位置: specs/worktree-unification/spec.md:183 | 问题: close flow hardcodes main/origin without explicit preflight for clean target_repo_root, default branch/remote detection, upstream sync, or fully-qualified remote branch ref. This is an operational risk for merge/push/delete. | 建议: Add close preflight requirements: target_repo_root clean, default branch/remote confirmed, main/upstream relationship validated, and remote branch operations use refs/heads/workflowhub/{task-id}.
- [important] 位置: specs/worktree-unification/tasks.md:77 | 问题: T007 stores acceptance.md outside the repo and forbids specs/worktree-unification/checklists/acceptance.md, while spec.md explicitly whitelists repo checklists/ as build-plan process artifacts. This is a storage-boundary ambiguity. | 建议: Classify acceptance checklist consistently. If it is a spec/verification checklist, store it under specs/{task-id}/checklists/; if it is runtime evidence, keep it in task_tracking_root and remove repo checklist implications.
- [important] 位置: specs/worktree-unification/cross-artifact-analysis.md:6 | 问题: cross-artifact-analysis.md claims all round-9 issues are fixed, but the latest r9b review package and this review still find blocking issues. The package gives contradictory pass/fail evidence. | 建议: Regenerate the closure matrix from the current artifacts, or mark this file superseded by the latest review package.
- [important] 位置: specs/worktree-unification/plan-eng-review.md:5 | 问题: Top-level plan-eng-review.md reports an older review state while newer r9b review artifacts exist under specs/worktree-unification/tasks/.../reviews/. The authoritative review source is ambiguous. | 建议: Keep one current review pointer or add a superseded-by path to the top-level review file.
- [important] 位置: .gitignore:50 | 问题: Runtime run-manifest.json files are not ignored or clearly classified, while untracked review package manifests under specs/worktree-unification/tasks contain in_progress state. | 建议: Move run-manifest.json to task_tracking_root, ignore it, or define which manifests are commit-worthy and forbid in-progress manifests in repo specs.

## Checks

审查维度覆盖：方向、盲点、细节
- 维度[方向]：已覆盖
- 维度[盲点]：已覆盖
- 维度[细节]：已覆盖

## Required Revisions

降级理由：(未提供，需补充)
- 必须修复：spec.md requires build-spec/build-plan to read worktree.json, use target_repo_root/worktree_root, and fail-loud when missing, but plan.md and tasks.md forbid modifying workflows/build-spec/SKILL.md and workflows/build-plan/SKILL.md. This is still the same executable-scope conflict found in the prior r9b review.
- 必须修复：Contract 1 lists build-code and verify-code as worktree.json consumers, but spec.md makes build-spec and build-plan read-only consumers for target_repo_root/worktree_root routing. Implementers can satisfy the contract while skipping two required stages.
- 必须修复：T001 gate_cmd calls require('./core/task-dir-parser.mjs').then(...), but core/task-dir-parser.mjs exports synchronous parseTaskDir(). The gate tests the wrong API shape and can fail or pass for reasons unrelated to FR-WORKTREE-ENVVAR-003.
- 必须修复：task_dir normalization is inconsistent. spec.md says yaml values ending in /tasks require migration or env override; tasks.md requires automatic trimming and calls normalizeTaskTrackingRoot(); data-contracts.md does not define trimming. The current repo config already ends in /tasks, so this affects the main fallback path.
- 必须修复：T002 requires normalizeTaskTrackingRoot(), but plan.md only specifies parseTaskDir(), and core/task-dir-parser.mjs has no such export. This introduces a new public API solely to satisfy a gate, without FR/data-contract backing.
- 必须修复：Existing persistent tests still assert the old ~/Knowledge/workflowhub fallback, which directly conflicts with the new fail-loud requirement when env var and yaml are both missing. tasks.md only has ad hoc shell gates and does not require updating the durable test file.
- 必须修复：FR-WORKTREE-COMMIT-004 is a hard in-scope requirement for every stage/phase, but T008 makes missing commit/no-change coverage advisory/follow-up and read-only. plan.md Verification Mapping also omits T008.
- 必须修复：Contract 4 says every stage/phase must run git add + git commit, while spec.md says commits occur only when files changed, forbids empty stage markers, and requires no-change records otherwise.
- 必须修复：T004 requires 3rd-review verdict=pass before irreversible actions, but does not explicitly remove or block the existing verify-code unavailable/not_executed review path that can proceed to merge/push/delete.
- 必须修复：The current 3rd-review runner path is still a placeholder (/path/to/3rd-review/standalone.sh), while tasks.md only says to run independent 3rd-review and does not specify a real entrypoint, output schema, timeout, or unavailable semantics.
- 必须修复：spec.md/tasks.md use stage-result.json as the runtime result file, but existing workflow SKILL files use per-stage names such as stage-result-verify-code.json and stage-result-build-code.json. The plan can create a second file contract or make downstream readers look in the wrong place.
- 必须修复：T006 requires ${WORKFLOWHUB_TASK_DIR}/tasks/worktree-unification/stage-result.json and evidence/ to exist, but those are verify-code close runtime outputs. Stage 3 only verifies/edits plans and does not run close.
- 必须修复：The forbidden-artifact gate uses git show HEAD diff headers. It misses current working-tree files, untracked files, pre-existing tracked files outside the last commit, and directory-level evidence/.
- 必须修复：T007 says the acceptance checklist is for verify-code, but the dependency graph schedules it after T006, while T006 validates close/runtime outputs. The supposed consumer can run before the checklist exists.
- 必须修复：Several gates are broad whole-file greps/counts. For example, banning worktree add can false-fail on prohibition text; counting circled numbers can pass outside the close section; grep for stage-result.json proves only a string exists, not the write path.
- 必须修复：Stage 2 marks T002/T003/T004 parallel, but their commit gates all check git log --oneline | head -1. Parallel commits can make the latest commit belong to another task, causing false failures.

