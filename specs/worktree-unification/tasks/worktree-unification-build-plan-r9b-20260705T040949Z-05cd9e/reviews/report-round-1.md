# 审查报告 — worktree-unification-build-plan-r9b-20260705T040949Z-05cd9e (round 1)

- verdict: revise_required
- provenance: single-context

## Summary

Round 1 full review of wu-r9-input.md: prior B3/B4/B5/B9/B10/B11/B12/B13 fixes are partly closed, but current plan/tasks remain non-executable because parser gates use the wrong API shape, normalization semantics are split, parser tests are missing, commit coverage is advisory despite a hard FR, build-spec/build-plan scope conflicts with SCOPE-008, close review gating is not fail-closed, runtime artifact gates are ordered incorrectly, and several gates can fake green. Verdict: revise_required.

## Findings

- [blocking] 位置: specs/worktree-unification/tasks.md:15 | 问题: T001/T002 gate_cmd commands call .then(...) on require('./core/task-dir-parser.mjs'), but the current module returns a synchronous namespace object and parseTaskDir is synchronous. T002 also references normalizeTaskTrackingRoot without a corresponding implementation task or explicit export contract. | 建议: Rewrite parser gates to use the real API shape, for example node --input-type=module import { parseTaskDir } ... or require(...).parseTaskDir(...). If normalizeTaskTrackingRoot is required, add it explicitly to T001/spec/data-contracts as an exported helper; otherwise move normalization assertions into parser tests.
- [blocking] 位置: specs/worktree-unification/tasks.md:35 | 问题: Parser normalization semantics are inconsistent across artifacts. spec.md says yaml values ending in /tasks require migration or env override; tasks.md requires automatic trimming; data-contracts.md only says parser returns task_tracking_root and does not define trimming behavior. | 建议: Choose one canonical rule and state it in spec.md, plan.md, data-contracts.md, and T001/T002. Recommended: yaml fallback trims exactly one trailing /tasks or /tasks/ to task_tracking_root; WORKFLOWHUB_TASK_DIR must already be task_tracking_root; both paths are tested.
- [blocking] 位置: specs/worktree-unification/tasks.md:13 | 问题: The parser behavior change lacks a real test task. Existing core tests still cover the old hardcoded ~/Knowledge/workflowhub fallback, while tasks.md relies on ad hoc node commands. | 建议: Add a T001 test-first subtask to update core/__tests__/task-dir-parser.test.mjs and run npx vitest run core/__tests__/task-dir-parser.test.mjs. Cover env priority, empty env, yaml fallback, missing yaml, missing task_dir, relative path, ~ path, nonexistent path, non-directory, and trailing /tasks normalization.
- [blocking] 位置: specs/worktree-unification/tasks.md:84 | 问题: FR-WORKTREE-COMMIT-004 is a hard requirement for every stage/phase, but T008 only performs a read-only advisory audit and allows missing commit/no-change coverage to become follow-up. plan.md Verification Mapping also omits T008. | 建议: Either narrow FR-WORKTREE-COMMIT-004 out of this task with explicit spec/design approval, or add implementation tasks and blocking gates for every affected row in the commit coverage matrix. Missing coverage must not be follow-up when the FR remains in scope.
- [blocking] 位置: specs/worktree-unification/plan.md:215 | 问题: spec.md requires build-spec/build-plan to read worktree.json, use target_repo_root/worktree_root, and fail-loud when missing, but plan/tasks forbid modifying build-spec/build-plan and make missing fail-loud wording only a recorded conclusion. | 建议: Choose one: allow tightly scoped edits to workflows/build-spec/SKILL.md and workflows/build-plan/SKILL.md with exact gates, or downgrade FR-WORKTREE-SCOPE-008 to read-only audit only and remove MUST/fail-loud consumer obligations from spec/data-contracts.
- [blocking] 位置: specs/worktree-unification/data-contracts.md:15 | 问题: Contract 1 omits build-spec and build-plan as worktree.json consumers, while spec.md requires them to consume target_repo_root/worktree_root for path routing. | 建议: Add build-spec and build-plan as read-only consumers in data-contracts.md, or revise spec.md to remove their consumer obligation.
- [blocking] 位置: specs/worktree-unification/tasks.md:47 | 问题: The plan requires 3rd-review verdict=pass before irreversible actions, but current verify-code has an unavailable/not_executed review path that can proceed to merge. T004 does not explicitly remove or block that path. | 建议: T004 must explicitly require review unavailable/not_executed to block close and escalate_to_human, or provide an approved exception. Add a gate that rejects not_executed/proceed-to-merge semantics in verify-code/SKILL.md.
- [blocking] 位置: specs/worktree-unification/tasks.md:72 | 问题: T006 requires stage-result.json and evidence/ to already exist under task_tracking_root, but these are verify-code close outputs. T004 only edits verify-code/SKILL.md; it does not run close. | 建议: Change T006 to verify verify-code/SKILL.md path rules and repo absence only. Defer actual external artifact existence checks to verify-code close acceptance after close executes.
- [blocking] 位置: specs/worktree-unification/tasks.md:71 | 问题: The forbidden-file gate checks git show diff headers instead of current repo state, and only stage-result.json has a direct test ! -f check. Top-level evidence/, journal.jsonl, or task-metrics.jsonl can exist untracked or pre-existing and still false-green. | 建议: Replace with current-state checks: test ! -e specs/worktree-unification/evidence, test ! -f specs/worktree-unification/journal.jsonl, test ! -f specs/worktree-unification/task-metrics.jsonl, plus git ls-files and git ls-files --others --exclude-standard scans for forbidden paths.
- [blocking] 位置: specs/worktree-unification/tasks.md:77 | 问题: T007 says the acceptance checklist is for verify-code, but the dependency graph schedules it after T006, which validates verify-code close outputs. This places the checklist after the stage that needs to consume it. | 建议: Move T007 before final verify-code/close boundary checks, or clarify it is post-run documentation and remove the claim that verify-code consumes it.
- [blocking] 位置: specs/worktree-unification/data-contracts.md:128 | 问题: Contract 4 says every stage/phase must run git add + git commit, while spec.md says commits occur only when files changed, forbids empty stage markers, and requires no-change records otherwise. | 建议: Align Contract 4 with spec.md: commit only when a stage/phase produces file changes; otherwise record no-change reason in stage-result/journal; do not use --allow-empty as a stage marker.
- [blocking] 位置: specs/worktree-unification/tasks.md:42 | 问题: Several gates are broad text greps/counts that can pass or fail on unrelated prose. Example: whole-file worktree add grep can false-fail on prohibition text; circled-number counts can pass outside close section; grep for stage-result.json proves only the string exists, not the write path. | 建议: Scope text checks to exact sections and ordered anchors, and add executable checks where behavior is required. At minimum, replace broad whole-file greps with section-bounded assertions and classify purely advisory text scans as non-gates.
- [important] 位置: specs/worktree-unification/cross-artifact-analysis.md:6 | 问题: Closure documents claim all round-9 issues are fixed, but this review found current blocking issues. Related stale files include constitution-check.md and plan-eng-review.md with outdated checklist/review state. | 建议: Regenerate the closure matrix and constitution check from the current checklist, or mark stale review outputs superseded and keep only dated historical reports outside the current review package.
- [important] 位置: .gitignore:50 | 问题: Runtime run-manifest.json files are not ignored or clearly classified, while untracked run manifests under specs/worktree-unification/tasks contain in_progress state. specs tasks/reviews paths are otherwise allowed package paths. | 建议: Either move run-manifest.json to task_tracking_root, add a specific ignore rule, or define which review manifests are commit-worthy and exclude transient in_progress manifests.

## Checks

审查维度覆盖：方向、盲点、细节
- 维度[方向]：已覆盖
- 维度[盲点]：已覆盖
- 维度[细节]：已覆盖

## Required Revisions

降级理由：(未提供，需补充)
- 必须修复：T001/T002 gate_cmd commands call .then(...) on require('./core/task-dir-parser.mjs'), but the current module returns a synchronous namespace object and parseTaskDir is synchronous. T002 also references normalizeTaskTrackingRoot without a corresponding implementation task or explicit export contract.
- 必须修复：Parser normalization semantics are inconsistent across artifacts. spec.md says yaml values ending in /tasks require migration or env override; tasks.md requires automatic trimming; data-contracts.md only says parser returns task_tracking_root and does not define trimming behavior.
- 必须修复：The parser behavior change lacks a real test task. Existing core tests still cover the old hardcoded ~/Knowledge/workflowhub fallback, while tasks.md relies on ad hoc node commands.
- 必须修复：FR-WORKTREE-COMMIT-004 is a hard requirement for every stage/phase, but T008 only performs a read-only advisory audit and allows missing commit/no-change coverage to become follow-up. plan.md Verification Mapping also omits T008.
- 必须修复：spec.md requires build-spec/build-plan to read worktree.json, use target_repo_root/worktree_root, and fail-loud when missing, but plan/tasks forbid modifying build-spec/build-plan and make missing fail-loud wording only a recorded conclusion.
- 必须修复：Contract 1 omits build-spec and build-plan as worktree.json consumers, while spec.md requires them to consume target_repo_root/worktree_root for path routing.
- 必须修复：The plan requires 3rd-review verdict=pass before irreversible actions, but current verify-code has an unavailable/not_executed review path that can proceed to merge. T004 does not explicitly remove or block that path.
- 必须修复：T006 requires stage-result.json and evidence/ to already exist under task_tracking_root, but these are verify-code close outputs. T004 only edits verify-code/SKILL.md; it does not run close.
- 必须修复：The forbidden-file gate checks git show diff headers instead of current repo state, and only stage-result.json has a direct test ! -f check. Top-level evidence/, journal.jsonl, or task-metrics.jsonl can exist untracked or pre-existing and still false-green.
- 必须修复：T007 says the acceptance checklist is for verify-code, but the dependency graph schedules it after T006, which validates verify-code close outputs. This places the checklist after the stage that needs to consume it.
- 必须修复：Contract 4 says every stage/phase must run git add + git commit, while spec.md says commits occur only when files changed, forbids empty stage markers, and requires no-change records otherwise.
- 必须修复：Several gates are broad text greps/counts that can pass or fail on unrelated prose. Example: whole-file worktree add grep can false-fail on prohibition text; circled-number counts can pass outside close section; grep for stage-result.json proves only the string exists, not the write path.

